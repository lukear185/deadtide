# DEADTIDE 開発引き継ぎ(CLAUDE.md)

対戦型ブラウザゲーム『DEADTIDE』の開発リポジトリ。**単一HTML(index.html)で完結**。
この文書はチャット側(Opus=設計担当)からClaude Code(実装担当)への引き継ぎであり、**ここに書かれた設計が唯一の正**。
**まずこの1ファイルだけ読めばよい。** 何をいつ入れたかの経緯は `CHANGELOG.md` にあるが、**普段は読まなくてよい**
(毎回読むとコンテキストを食うため2026-07-25に分離した)。

## 絶対ルール
- 出力・コメント・報告はすべて日本語。
- 設計判断はチャット側(永井さん⇔Opus)の決定が最優先。**ここに無い機能を勝手に追加しない**。追加提案は実装せず永井さんに確認。
- 変更は「アンカー文字列の一意性をassertした置換パッチ」で行い、毎回 `node --check`・`node test_undef.js`・`node test_headless.js` を通すこと。
- ライセンス運用: NC素材禁止。外部素材は使っていない(全部プロシージャル描画)。
- index.htmlはGitHub Pagesにそのまま置いて動く状態を維持する。
- **公開URL: https://lukear185.github.io/deadtide/** (repo=github.com/lukear185/deadtide・masterへpushすると1〜2分で自動反映)。スマホ/複数人テストはこのURLで行う。

## 見た目(アート)の方針 ※2026-07-25 ユーザー決定
- 現状=全部プロシージャル描画(コードで図形を組む/スプライトに焼く)。ハリボテ感がある自覚あり。
- **本格化はユニット/ゾンビ/タワーのモデルをBlenderMCPで作って2Dスプライト書き出し→drawImageで差し替える方針**(既存のSPR/drawSpr構造をそのまま流用できる)。**ただし今はやらない=後回し**。当面はプロシージャルのまま機能・バランスを進める。
- 代替案としてKenney.nl等のCC0既製アセットもあり(商用即OK)。どちらで行くかは着手時にユーザー判断。

## ゲーム概要(2026-07-25 現行版)
終末ゾンビ×2Dタワーディフェンス+ユニット召喚。**3つのモード**:
- **🛡 ソロ作戦(PvE・ローグライト)**: 1拠点でWAVE20(STAGE_W)を守り切ればそのステージをクリア。負けても🧬研究ptが貯まり、🔬研究所で永久解放して次の周回が強くなる。**ステージ制**=①廃線ハイウェイ(ゾンビ)②沈んだ港(ゾンビ化した海の生き物11種)。前のステージをどれかの難易度でクリアすると次が開く。
- **🤝 協力(最大3人・PvE)**: 共有フィールド1枚+共有コア(50+20/人)。各自の出撃テント(BRANCH)から自分の部隊を出す。⚙️/解放/強化は各自持ち、タワーは共有スロット。
- **⚔ オンライン対戦(最大3拠点)**: 死潮の綱引き。相手盤面はミニ小窓+タップで全画面観戦。

## 確定済み設計
- **二重経済**: ⚙️スクラップ=建設/出撃/解放(ゾンビ撃破で入手)。**🔩強化ポイント=ウェーブ制圧で入手し、タワー個別強化と部隊レベルにのみ使う**(wavePts=4+w*1.6+w^2*0.16、初期12)。
- **建設スロット**: 通常22枠のうち**初期7枠**(SLOT0=7)のみ解放済み。🔒枠は⚙️200×1.3^nで解放(SLOT_ORDER順の価格、どの枠でも可)。**廃品工房は画面左上の専用エリア3枠**(初期1枠・⚙️450/1100で増設)。工房は専用枠にしか建たず、専用枠には工房しか建たない。
- **タワー20種**(基本10+研究所10)。強化は⚔攻撃/⏩連射/📡射程を各Lv5まで(🔩払い)。電撃塔だけ⏩連射の代わりに**⚡連鎖数**(敵が少なくても跳ね返って連鎖)。
- **兵科32種**(基本8+研究所24)。上限MAXU=40(協力は各20)。**部隊レベル**(🔩・最大10)で全兵科+10%/Lv。デッキ**長押しでレベルアップ**。※姿が変わる段階進化は**廃止済み**=レベルは数値強化だけ。
- **🎖編成(ロードアウト・ソロ専用)**: 7つの枠それぞれで、基本キャラか**派生キャラ33種**(基本7兵科×3段階+上級4種×3段階)のどちらを使うか選ぶ。**弾薬**(通常弾→ビーム→プラズマ弾→崩壊弾)もここで装備し、実弾タワーの威力に乗る。保存=`META.ld`/`META.amEq`。**協力/対戦では無効**。
- **作戦タイム20秒**(IV_SEC)+**ウェーブ開始から10秒は敵が出ない設置猶予**(WAVE_LEAD)。
- **中断と再開(ソロ専用)**: 作戦タイムのたびに自動保存(localStorage `dt_run`)+⏸メニューから中断。ソロ設定に「▶続きから」。保存するのは**ウェーブの区切りの状態**=中断した波は最初からやり直し。
- **🚩集結旗**: 部隊は旗まで前進して戦線を張る。射撃系は旗+70で待機。協力は各自の旗を色分け表示。
- **🏳ウェーブ放棄(ソロ)**: 確認ダイアログ→残敵ぶんのコアダメージを受けて即作戦タイム。工房の残り生産ぶんは精算、🔩は半減。
- **🔬研究所(ソロ専用メタ)**: 研究pt=WAVE×25+撃破×1.2×難易度係数。localStorage `dt_meta`。**6カテゴリ**=🆕新しい種類(タワー/兵科)・🧬派生キャラと弾薬・🎯砲撃の種類・📈系統強化(型ごとに各Lv3。研究所で解放した新型にも乗る)・🚀開始時のブースト・💰収入と効率。
- **砲撃5種**: 🎯標準/🔫機関銃掃射/🛩絨毯爆撃/❄凍結爆弾/🔥ナパーム(チャージ秒数も別)。**初期は🎯のみ**で残り4種は研究所で解放。ソロ設定画面で装備を選択。**即着弾ではなく飛来物が飛んでくる**(標準=ロケット/絨毯=爆撃機/凍結・ナパーム=落下爆弾/機関銃掃射だけ飛来物なしで即発動)。
- 難易度6段階(D5表: 新兵/兵長/古参/鬼軍曹/悪夢/**🌑ナイトメア**)+PvE序盤ウォームアップ(W1-4)。
- **🌑ナイトメア(最難関)**: 悪夢クリアで解禁(META.nmOK・localStorage永続)。**敵の生態系が完全に別プール**=獣ゾンビ7種のみ出現(巨鼠/変異猪/肉蜘蛛(すり抜け)/吸血蝙蝠(飛行・すり抜け)/腐肉鴉(周囲加速)/屍熊(3体薙ぎ払い)/**肉塊の主**=専用ボス・7体薙ぎ払い)。空は終始「血月の夜」、BGMも専用。研究pt倍率2.1。
- 中盤(W8〜)・終盤(W14〜)に段階的な強化倍率。ゾンビの出現は重み(w)制。
- 綱引き(対戦のみ): 押し=45×1.15^n で7%移動 / 大押し流し=15% / 下限8% / 0.45%/秒で均等化 / W8以降は波が加速。
- 勝利条件: コアHP0で脱落。対戦は最後の生き残り、PvE/協力はWAVE20到達。
- **最終ボス**: WAVE20専用のボスが各ステージにいる(①屍山の王 ②深淵の母)。5波ごとの通常ボスとは別物。🌑ナイトメアぶんは未作成。
- **新登場の紹介モーダル**: ソロで、新しく解放したタワー/兵科と**初めて出るゾンビ**をウェーブ開始直後に画面中央で説明する(表示中はゲーム停止)。

## 実装状況(2026-07-25 時点)

**ゲーム側の機能はひととおり全部入っている。** 実機テスト(第20弾までの7項目)も全部OK確認済み。
何をいつ入れたかの経緯は **`CHANGELOG.md`**(第1弾〜第21弾)にある。**普段は読まなくてよい**。
「なぜこうなっているのか分からない」「昔の判断を蒸し返しそう」という時だけ開くこと。

### 残っている宿題(着手はユーザー指示待ち)
1. **🌑ナイトメア(獣プール)の最終ボスがまだ無い**。他2ステージにはWAVE20専用ボスがある(屍山の王/深淵の母)。
2. **対戦の死潮ゲージ(#tidebar)が右上の道路と被る可能性**。対戦画面を詰める時に一緒に直す。
3. **アートのBlender移行**。今は全部プロシージャル描画。着手はユーザーがGoを出してから(→冒頭「見た目の方針」)。
4. バランスの微調整(実機の体感しだい)。

## ⚠作業の時に必ず守ること(過去に実際にやらかしたこと)

> ここは**同じ失敗を繰り返さないための決まり**。新しい落とし穴を踏んだら**必ずここに1行足す**こと。

### 検査
- **変更のたびに `node --check` → `node test_undef.js` → `node test_headless.js` を通す**。
  数値(hp/atk等)を触ったら `node test_balance.js`、画面の寸法を触ったら `node test_layout.js`。
- **見た目を直したら必ず `node test_shot.js` で撮って自分の目で見てから報告する**。数値の確認だけで不具合を3回見逃した。
- **一括置換(sed/正規表現)をしたら、その行が実際に通る経路を走らせる**。`node --check` は文法しか見ないので通ってしまう。
  `test_undef.js` がこの種の事故を静的に捕まえるので、置換後は必ず流すこと。
- **rAF(毎フレーム処理)の中の例外は try/catch に飲まれてテストに出ない**。怪しい処理は直接呼んで確かめる。
- **日本語以外の文字が紛れ込む事故**が数回あった(точно/один/강화)。書いたら見直すこと。

### 数値・バランス
- **`UNITS` の数値がそのままゲーム内の値**(一括倍率は廃止済み)。強さの調整は hp/atk を直接いじる。
- **壁役(big/shd/gld/tnk/titan)は戦力カーブの対象外**。攻撃力ゼロの壁は戦力式で正しく測れないため、
  `test_balance.js` は壁どうしのHPの並びだけを見る。壁の判定は**IDで固定**する(HPの閾値でやると強化した瞬間に評価が跳ねる)。
- 火炎系**タワー**は装甲に特効1.9倍。**火炎系の兵科は据え置き**(兵科は戦力カーブで管理しているため)。

### 画面・レイアウト
- **CSSの順番の罠**: `@media (pointer:coarse)` のスマホ用指定は、**対象の定義より後ろ**に書かないと効かない(同じ強さの指定は後ろが勝つ)。
  デッキ・操作バー系は `#deck .dk:disabled` の直後の第2ブロックへ書く。上のブロックにはHUD系だけ。
- **操作バーの幅にもう余裕が無い**(実測 中身730 vs 使える幅734)。**兵科を増やす時は必ず幅を再計算**する。しないと横スクロールになる。
- 地面の裁ち落とし: 時間帯の色・夜の陰・ビネットは**マップ矩形ではなく画面の可視範囲(VL/VT/VR/VB)**に掛ける。
  マップ矩形に掛けると四角い縁が出て「盤が浮いて」見える。
- **建設マスを動かす時の除外条件**: 道路の中心線から95 / 拠点150 / 工房枠135 / 協力の出撃テント(BRANCH)120 /
  コアHPバーの位置(x<270・y400〜520) / マス同士110 / 画面内(x75〜1560・y205〜730)。
- **建設マスの数は全ステージ22で固定**(ECO_BASEが動くと保存や配列の形が壊れる)。工房3枠とBRANCHは共通。
- 研究所の項目は**値段順に並べ替えない**。買うと値段が上がって順番が入れ替わり、押した項目が別の場所へ飛ぶ。

### テストを書く時
- `test_headless.js` が**建てる枠は AI_ORDER の先頭(初期解放ぶん)から選ぶ**。解放されていない枠に建てても無視される。
- 紹介モーダルはOK待ちで止まるので、`frames()` の中で `PAUSED && INTROQ` なら `introNext()` で自動送りする。
- **ヘッドレスのNode環境には localStorage が無い**ので、中断/再開などの保存系は検証されない(実ブラウザで往復確認する)。
- ヘッドレスChromeでは `(pointer:coarse)` が効かずPC寸法で描画される。`test_shot.js` はスマホ用CSSを強制適用して回避している。

### 設計として決まっていること(蒸し返さない)
- **部隊の段階進化(Lv3/5/10で姿が変わる)は廃止済み**。部隊レベルは数値強化だけ(+10%/Lv)。
- **協力/対戦では編成(ロードアウト)は無効**。素の兵科・通常弾で統一する。
- 協力/対戦は**必ずステージ1**に戻す(地形の食い違いを防ぐため)。

## コードスキーマ(既存に合わせること)
- キャンプ: `C.units[] {eid,ui,own,am,evo,d,hp,mhp,cd,hitT,fireT,ph,px,py,dr,back,eng}` / `C.ucd[]` / `C.uUn` / `C.uLv`(部隊Lv) / `C.up`(🔩) / `C.ecoN` / `C.slk[]`(スロット解放) / `C.ps[]`(パーティクル)
- タワー: `C.towers[si] {ti,us:{d,r,g,c},cd,ang,jamT,fireT,spent,own}` — usが個別強化Lv(cは電撃の連鎖)
- UNITS: `{id,n,cost,cd,hp,atk,rate,rng,sp,type,aoe?,burn?,multi?,c,dsc}` type: melee/bullet/fire/pierce/blast/frost/heal
- 座標系: 論理マップ1600×760、PATH折れ線、d=経路距離(0=侵入口、PLEN=コア)。ユニットはd減少方向へ前進。描画はSC/OX/OYでcontain-fit。
- 通信: ホスト権威。スナップショットに fq(演出) pq(粒) us/es(実体) sk(スロット) up/ec/ul などを同梱。コマンドは `{t:'c',a:'bd|up|sl|st|buy|rd|ps|un|fg',j:席番号}`。
- 演出: addFx(C,kind,…) kind: tr/zap/ring/boom/boomL/spark/splat/blood/txt/dmg/leak。粒は addP(C,n,x,y,opt)。
- 描画最適化: bakeU/bakeZ→SPRキャッシュ(4コマ)、drawSpr()で描画。特殊状態のみ drawUnit/drawZombie に直接フォールバック。

## テスト手順(Node.js=`C:\Program Files\nodejs\node.exe`)
毎回流すもの:
```
node --check <(index.htmlの<script>部を抽出)   # 実際は sed で抽出してから
node test_undef.js               # 未定義の名前(一括置換事故)を静的に検出。FAILなら終了コード1
node test_headless.js            # ./index.html を実走(PvE/協力/対戦+砲撃5種+ステージ2+最終ボス)
```
必要な時に流すもの:
```
node test_balance.js             # 兵科の強さが解放順に上がっているか(数値を触ったら必ず)
node test_layout.js              # マス/道路がHUDや操作バーと重なっていないか(寸法を触ったら必ず)
node test_shot.js out.png 852 393        # 実画面のスクショ(スマホ用CSSを強制適用)
node test_shot.js out.png 852 393 st2    # ステージ2を撮る / pc=PC表示 / w30000=時間を進める
```
合格基準: 試合が over=true / シェア合計100±2 / FAILが出ない。
※ ヘッドレスはDOM/Canvasをスタブしているため見た目は検証できない。**見た目は`test_shot.js`で撮って目視**し、最終確認は永井さんが実機で行う(画面下部に赤いエラー帯が出る仕組みあり=window.onerror)。

## チューニングノブ(数値調整はここから)
押しコスト45(×1.15) / 1押し7% / 大押し15% / 下限8% / ドリフト0.45%毎秒 / 潮位上昇+20%量+25%強 / 波成長 1+.16w, WAVE10以降×1.17^n(PvEは1.11) / ゾンビ近接√mulスケール / UNITSのhp・atk・コスト・CD / UNITP解放価格 / MAXU=40(協力は各20) / IV_SEC=20 / WAVE_LEAD=10 / STAGE_W=20。

## 既知の注意点
- PeerJSオンラインはβ: 公開ページ同士のみ、NATで失敗あり、ホスト切断で終了(切断者はCPU引継)。クライアント側はダメージ数字等を簡略化。
- 巨大な一括書き込みは避け、小さなパッチの積み重ねで(アンカーassert必須)。
- 変異体送り込みの送付先は購入時に相手の inc.sent に積み、buildTide()がpoolへ挿入する方式(spec.tgt固定)。
