---
tags: [deadtide, handover]
aliases: [引き継ぎ]
---
# DEADTIDE 開発引き継ぎ(CLAUDE.md)

対戦型ブラウザゲーム『DEADTIDE』。**単一HTML(index.html)で完結**。ここに書かれた設計が唯一の正。
**この1枚だけ毎回全部読む。** 他は必要な時だけ開く。

| ノート | 中身 | いつ読む |
|---|---|---|
| **CLAUDE.md**(これ) | ルール・進め方・検査・いまの状態 | **毎回・最初に全部** |
| [[TODO|TODO.md]] | 止めてあるもの・実機で未確認のもの | 次に何をやるか決める時 |
| [[DESIGN|DESIGN.md]] | 確定済み設計の明細(モード/タワー/兵科/研究所/ガチャ/冒険/コードスキーマ/数値のノブ) | 仕様を確かめる時・設計を変える時 |
| [[NOTES|NOTES.md]] | 過去にやらかしたこと・撮影オプション・DEVの底上げ | ⭐**絵か数値を触る前に必ず** |
| [[CHANGELOG|CHANGELOG.md]] | 経緯 | 昔の判断を蒸し返しそうな時だけ |
| [[HOME|HOME.md]] | 人間向けの入口 | Claude Code は読まなくてよい |

リポジトリの根っこが Obsidian の保管庫。⚠`.obsidian/` は git に入れない(除外済み)。

## 絶対ルール
- 出力・コメント・報告はすべて日本語。**報告は端的に**(経緯・過程は書かない。結果と重要なことだけ)。
- ⭐**着手のルール(最重要)**
  1. **「どう?」「〜したい」「〜できない?」と聞かれたら、答えるだけ。勝手に作業を始めない**。
     感想・評価・可否を聞かれているのであって、着手の指示ではない。⚠何度もやらかしている。
  2. **指示された事はすぐやってよい**。いちいち確認を挟まない。
  3. **指示されていない事をやるなら、着手前に「〜をやります、いいですか」と聞く**。思い付き自体は歓迎。
- 設計判断はチャット側(永井さん⇔Opus)の決定が最優先。**ここに無い機能を勝手に追加しない**。
- 変更は「アンカー文字列の一意性をassertした置換パッチ」で行う。
- ライセンス: NC素材禁止。**絵は全部プロシージャル描画**。効果音だけ外部素材(Sonniss #GameAudioGDC・商用可)。
  生WAVはリポジトリに入れない。加工済みMP3を base64 で埋め込んである。
- **公開URL: https://lukear185.github.io/deadtide/** (repo=github.com/lukear185/deadtide・masterへpushで1〜2分で反映)。
- **作業が一区切りしたら毎回 master へ push してよい**(許可済み・毎回聞かなくてよい)。
  検査を通してコミットしたら、push して公開URLを渡すところまでやる。
- ⚠**新しく「珍しいもの」を足したら、同じ回に `?dev=1` の底上げも足す**(でないと実機で一度も見られない)。
  底上げの一覧は [[NOTES]]。
- ⚠**バックグラウンドに回った処理を放置しない**。区切りごとに見て、不要なら止める。

## 蒸し返さない決定
- **難易度**: 悪夢/🌑ナイトメアは**研究所の永久解放を積んで初めて勝てる**難易度。素で勝てないのは設計どおり。
  ⭐`test_headless.js` の PvE は負けて当たり前。**その結果を見て難易度を下げない**。見るのは wave の推移だけ。
- **遊びの尺**: 「息抜きゲーム」=**Yadochoの合間に作る**という意味で、短時間で遊べるという意味ではない。
  **1試合が長いことは問題ではない**。プレイ時間を理由に難易度・尺・テンポを削らない。困るのは決着しない時だけ。
- **見た目**: 方針は**「②コードの絵を磨く」で確定**(外部素材もBlenderも使わない)。単一HTMLは当面守る。
  外部フォントは埋め込まない。⚠容量の話を持ち出さない(公開する段で `sw.js` と一緒に直す)。
  ❌捨てたもの=**ドット絵 / 輪郭の2度描き / Blender(2回)**。効く3つ(大きさ・色の区分け・ハリボテ感)は [[NOTES]]。

## いまの状態(2026-08-01)
- ゲームの機能はひととおり全部入っている。⚠まだ手を入れていない絵=**背景/道路/建設マスの意匠・演出(fx)・冒険(RPG)側**。
- ⭐**見た目の作り込みの途中**(ユーザー指示「全力でプロ顔負けの完璧な見た目に」)。進捗:
  **①土台=100%**(兵科32種の骨格・武器の縮尺は共通部品なので全部に効いている)
  **②兵科30種=100%・全部「専用の体」**(2026-08-01)。表 `UBODY[id]` に1体ずつ描画関数を登録してある。
    道具は `bLeg`/`bArm`/`bTorso`/`bNeck`/`bFace`(⚠ここに兵科ごとの分岐を書かない)。詳しくは [[NOTES]]。
  **③英雄18人=100%・全部「専用の体」**(2026-08-01)。表 `HBODY[id]`。ゾンビはウォーカーだけ済み
  ⭐**攻撃の見せ方**: 溜めは体の傾き(`uChgUp`)+**装備そのものの動き**(`bHold`)で
    兵科21種と英雄18人に入っている(2026-08-01)。掟は [[NOTES]]。
  ⚠磨く時は必ず `node test_shot.js out.png 1200 740 "pose=u:<id>"` で大写しにして撮る。
  ⚠⚠**絵を触る前に [[NOTES]] の「キャラの絵」の節を読む**(骨格の割り付け・攻撃モーション・
    黒フチに食われる大きさ・★4の光・`shade()`の落とし穴 が全部そこにある)。
- ⭐**🧪検証場**=DEV(`?dev=1`)のタイトルから直に入る一本道。**押した時だけ出る**
  (味方=下のデッキ/敵と🦸英雄=上の帯)。撮るなら `"arena=bat:walk+arn=16"`。
- ✅**🚌ゾンビバス(バスくん)**=タイトル専用の巨大ゆるキャラ(2026-08-01)。🐻🌈虹の熊は廃止。
  ⚠**ZOMBIES に足していない**(図鑑・波・冒険に出さない)=絵は行進の描画側 `drawBusKun`。
- ⭐**次にやることは決まっていない**=[[TODO]]の候補(①ゾンビの `ZW` を明るく(表ごと直すと12種に効く)
  ②背景/道路/建設マス・演出(fx)・冒険)から**ユーザーの指示待ち**。勝手に始めない。

## ⚡ 渡し方
- **実機テストのURLは本文にそのまま書く**(タップで開ける)。コマンドやコードブロックにしない。
- **「引き継ぎ」と言われたら、引き継ぎ文を三連バッククォートのコードブロック(言語指定なし)で出す**。
  ⚠`bash` などを付けるとRunボタンになってコピーできない。
  ⭐**引き継ぎ文は短くする**(2026-07-30ユーザー指示)。状態は全部この CLAUDE.md と [[TODO]] にあるので、
  **中身は「これは何 / 置き場所とURL / まず CLAUDE.md を読む / 次にやること1〜3行 / 実機で見てほしい所」だけ**。
  ⚠**やったことの一覧・教訓・コミット履歴を貼らない**(全部この保管庫に書いてある)。

## ⭐引き継ぎのたびにやること(棚卸し・2026-07-30ユーザー指示)
1. **CLAUDE.md から「続きをやるのに毎回は要らないもの」を追い出す**。⚠**消すのではなく移す**:
   済んだ作業の明細 → [[CHANGELOG]] / 道具と落とし穴 → [[NOTES]] / 確定した仕様 → [[DESIGN]] /
   止めてあるもの → [[TODO]] / どれにも当てはまらないなら**新しいmdを作る**。
2. 目安=**CLAUDE.md は100行前後**。超えたら削る所を探す合図。
3. 追い出したら [[HOME]] の見取り図も直す。

## テスト手順(Node.js=`C:\Program Files\nodejs\node.exe`)
毎回流す:
```
node test_undef.js      # 先頭で文法検査もする。FAILなら終了コード1
node test_headless.js   # 実走(PvE/協力/対戦+砲撃5種+ステージ2+🌑ナイトメア+最終ボス)
```
必要な時:
```
node test_balance.js    # 数値(hp/atk)を触ったら必ず
node test_layout.js     # 画面の寸法を触ったら必ず
node test_shot.js out.png 852 393 [オプション]   # ⭐見た目は必ず撮って目視。オプション一覧は NOTES.md
```
合格基準: 試合が over=true / シェア合計100±2 / FAILが出ない。
※ヘッドレスはDOM/Canvasをスタブしているので**見た目は検証できない**。最終確認は永井さんが実機で。
