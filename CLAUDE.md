# DEADTIDE 開発引き継ぎ(CLAUDE.md)

対戦型ブラウザゲーム『DEADTIDE』の開発リポジトリ。**単一HTML(index.html)で完結**。
この文書はチャット側(Opus=設計担当)からClaude Code(実装担当)への引き継ぎであり、**ここに書かれた設計が唯一の正**。

## 絶対ルール
- 出力・コメント・報告はすべて日本語。
- 設計判断はチャット側(永井さん⇔Opus)の決定が最優先。**ここに無い機能を勝手に追加しない**。追加提案は実装せず永井さんに確認。
- 変更は「アンカー文字列の一意性をassertした置換パッチ」で行い、毎回 `node --check` と `node test_headless.js` を通すこと。
- ライセンス運用: NC素材禁止。外部素材は使っていない(全部プロシージャル描画)。
- index.htmlはGitHub Pagesにそのまま置いて動く状態を維持する。
- **公開URL: https://lukear185.github.io/deadtide/** (repo=github.com/lukear185/deadtide・masterへpushすると1〜2分で自動反映)。スマホ/複数人テストはこのURLで行う。

## ゲーム概要(v4系・開発中)
終末ゾンビ×対戦型2Dタワーディフェンス+ユニット召喚。1〜3拠点(ソロはCPU、オンラインはPeerJS P2Pβ)。
**核となる独自システム=死潮の綱引き**: ゾンビの大群は全体で一つだけ存在し、上部ゲージの「取り分(シェア)」に応じて各拠点へ流れる。⚙️を払って取り分を相手へ押し流せる。受けた側は撃破報酬を総取り(耐えれば潮は金脈)。5波毎のボスは押し負けている拠点へ向かう。

## 確定済み設計(変更禁止)
- 経済: ⚙️スクラップ1本(建設・召喚・押し・カード全部同じ財布)。初期250(※300への増額は未決定、バランス次第で永井さんに確認)。
- 綱引き: 押し=45×1.15^n で7%移動 / 大押し流し=15%移動 / 取り分下限8% / 毎秒0.6%ずつ均等へドリフト / 潮位上昇=次波全体を量+20%・強さ+25% / 受けた側が撃破⚙️総取り / ボスはシェア最大の拠点へ / 脱落時はシェア再分配(normShares)。
- タワー8種は**支援役として現状維持**(rifle/shot/net/scrap工房/flame/tesla/mortar/sniper、Lv1-3、売却60%)。
- **主役はユニット(生存者部隊)**。8兵科(UNITS配列: バット/猟犬/ガンナー/盾持ち/火炎瓶/チェーンソー/スナイパー/ロケラン)。購入解放式(UNITP、作戦タイムのカード)。上限MAXU=20。ウェーブを跨いで持ち越し。
- **案1『集結旗』型を採用(永井さん承認済み・未実装)**: 部隊はシェルターから出撃し、プレイヤーが置く🚩集結旗(flagD)まで前進してそこで戦線を張る。旗はいつでも置き直し可。
- 妨害カード: 大押し流し/潮位上昇/変異体送り込み/妨害工作(タワー20秒封印)。旧「感染増強」は廃止済み。
- 勝利条件: コアHP50、リークで減少、0で脱落、最後の生き残りが勝ち。
- エアストライク: 30秒チャージ、🎯→地点タップ。

## 実装状況
### 完了(index.htmlに実装済み・テスト済み)
- 綱引き一式(tide.pool/shares/tideStep/pushTide/big/tide カード/ゲージUI/押すボタン/通信同期)
- タワー・ゾンビ7種・マップ描画・夜の帳・エアストライク・作戦タイム・CPU AI(建設/押し/カード)・PeerJSオンラインβ・リザルト
- ユニットの**シム層**: UNITSデータ、deployUnit()、dmgU()、ゾンビの近接攻撃(atk/ar/acd、足止め窓 gap∈[-6,36])、ユニットの攻撃(type: melee/bullet/fire/pierce/blast、aoe/burn/multi対応)、drawUnit()、renderCampでの描画+HPバー、sfx.dep
- ヘッドレステスト(test_headless.js)グリーン: 1v1と三つ巴が完走、シェア合計100維持、ユニット出撃・交戦・死亡確認済み

### 未実装(これが残タスク。上から順に)
**なし。旧残タスク1〜8はすべて実装済み(2026-07-24、下記参照)。**

### 2026-07-24 実装分(Claude Code・各タスク毎に個別コミット済み)
1. **集結旗**: flagD(初期PLEN*.55)/projPath(x,y)/ユニットはflagDへ双方向移動(射撃系rng>60はflagD+70待機、後退時はu.backで向き反転)/旗描画(緑ペナント)+照準プレビュー/🚩ボタン+FLAGAIMモード(🎯AIMと相互排他)/CPUは.45+ag*.06、コア半分以下で.65に後退。
2. **足止め上限**: 毎tick u.eng=0→eng<3のみ足止め対象、確保でeng++、溢れは素通り。
3. **出撃デッキUI**: #deck(下中央)8ボタン=UTHUMBSサムネ+コスト+CD(conic-gradient)+未解放🔒。actDeploy(ui)、client楽観更新。
4. **新部隊解放カード**: kind 'uun'(priceOf/doPurchase/ivRender/actBuyラベル/CPU plan重み2.8)。次兵科名+説明表示。
5. **CPU出撃**: cpuTickで盾1+近接2+射撃2維持(+wave/5で増員)、scrap×res余裕時のみ、タワー建設より後・強化より先。
6. **通信**: packLaneに us/fd/uu/dp、normMeに uUn+ucd保持、clientCampにユニットeidマップ(um)補間+旗同期、hostDataに 'un'/'fg'(clamp済)、meV既定uUn:2、client側ucdは自前減算。
7. **文言**: WAVE1 toastに部隊/旗の2本追加、あそびかたに部隊・旗・足止め上限の段落、リザルトに「部隊出撃」(dep/dp)。
8. **バランス確認**: テストに旗ランダム移動+uun購入を追加、決着分数と回収⚙️/uUnを診断出力、10分超はWARN。3回実行で2試合完走・全て10分以内・ユニット機能(dep>0・交戦・死亡)を確認。ブラウザ実起動で出撃/旗移動の操作も確認済み。

### 2026-07-24 大改修(永井さんのチャット直接指示=最優先の設計変更として実装)
> ⚠この節は「ゲーム概要(v4系)」より新しい。矛盾したらこちらが正。
1. **ソロ=CPU対戦廃止→ステージ制PvE**。1拠点でWAVE20(STAGE_W)を守り切ればステージ1クリア。まずステージ1のみ。専用リザルト/チュートリアル/対人UI非表示。
2. **対戦(オンライン)=敵盤面の可視化**。ミニパネルに他プレイヤー2人のライブ小窓(簡略ドット)+タップでフル観戦(SPEC)。もう一度タップで戻る。
3. **兵科8→12種**。追加=擲弾兵(blast)/冷凍兵(frost=範囲slow)/医療兵(heal=新メカニクス)/ミニガン(高速連射)。drawUnitはid分岐。UNITP12段。
4. **協力プレイ(最大3人Co-op PvE)**。共有フィールド1枚+共有コア(50+20/人)。各自の出撃ポイント(P1〜P3テント+短い合流レーン=BRANCH)から自分のユニットを出撃。⚙️/解放/火力増強/旗(色分け)=各自。タワー=共有スロット(建てた人の⚙️・売却は本人のみ)。撃破⚙️は全員分配(COOP_PAY)。ロビーでモード選択(⚔対戦/🤝協力)。
5. **難易度5段階(D5表)**=新兵/兵長/古参/鬼軍曹/悪夢。HP/物量/速度/ボス/初期⚙️/報酬倍率を個別調整。ソロ/協力共通。協力は人数スケール(量1+.4/人・強さ1+.08/人)。
6. **演出強化パス1**=マズルフラッシュ+リコイル/ゾンビ突進モーション/血しぶき粒子/凍結❄表示/ボス入場演出/旗ポップ/回復フロート/新SFX(heal/frost)。※美的作業=永井さんが理想と言うまで反復対象。
7. **スマホ3台**=viewport/セーフエリア/タッチサイズ/小画面調整は済み。**3台実機のPeerJS接続確認は未**(GitHub Pages等へ置いて確認する)。

### ⚠実装後の観察(要・永井さん裁定。勝手に直していない)
- **CPUが新部隊解放をほぼ買えない**: CPUはウェーブ中に⚙️を使い切り、作戦タイム到達時の所持が23〜55⚙️程度(診断ログ実測)。uupCost最低150に届かず、CPUのuUnは2のまま。対策案=cpuTickに「wave3以降は⚙️150+αを残す貯金ロジック」(小変更)。入れるかは判断待ち。
- **三つ巴のguard上限30000はギリギリ**: 変更前コードでも28000〜29000フレームの試合があり、稀に30000超で偽FAILする(乱数依存・タスク1実装時に変更前後3回ずつ実走して確認)。CPU出撃実装後は12000〜17000に短縮され当面問題ないが、将来のバランス変更で再発しうる。対策案=乱数シード化 or 上限35000。判断待ち。

## コードスキーマ(既存に合わせること)
- キャンプ: `C.units[] {eid,ui,d,hp,mhp,cd,hitT,ph,px,py,dr}` / `C.ucd[8]`(召喚CD) / `C.uUn`(解放数、初期2) / `C.dep`(出撃数)
- UNITS: `{id,n,cost,cd,hp,atk,rate,rng,sp,type,aoe?,burn?,multi?,c,dsc}` type: melee/bullet/fire/pierce/blast(fire/blast/pierceは装甲貫通)
- ゾンビ近接: `z.atk`(zSpecで√mulスケール済み)、`z.ar`(攻撃間隔)、`z.acd`(内部CD)。足止め判定は d 距離ベース。
- 座標系: 論理マップ1600×760、PATH折れ線、d=経路距離(0=侵入口、PLEN=コア)。ユニットはd減少方向へ前進。描画はSC/OX/OYでcontain-fit。
- 通信: ホスト権威。クライアントは楽観更新+100msスナップショットで上書き。コマンドは `{t:'c',a:'bd|up|sl|st|buy|rd|ps|(新設)un|fg',...}`。
- 演出: addFx(C,kind,…) kind: tr/zap/ring/boom/boomL/splat/txt/dmg/leak。

## テスト手順
```
node --check <(index.htmlの<script>部を抽出)   # 実際は sed で抽出してから
node test_headless.js            # ./index.html を実走(2試合+シェア整合)
node test_headless.js dt.html    # 任意ファイル指定
```
合格基準: 2試合とも over=true / シェア合計100±2 / FAILが出ない / 三つ巴が30000フレーム(実質16分)以内に決着。
※ ヘッドレスはDOM/Canvasをスタブしているため見た目は検証できない。**実ブラウザ確認は永井さんが行う**(画面下部に赤いエラー帯が出る仕組みあり=window.onerror)。

## チューニングノブ(数値調整はここから)
押しコスト45(×1.15) / 1押し7% / 大押し15% / 下限8% / ドリフト0.6%毎秒 / 潮位上昇+20%量+25%強 / 波成長 1+.16w, WAVE10以降×1.17^n / ゾンビ近接√mulスケール / UNITSコスト・CD / UNITP解放価格 / MAXU=20。

## 既知の注意点
- PeerJSオンラインはβ: 公開ページ同士のみ、NATで失敗あり、ホスト切断で終了(切断者はCPU引継)。クライアント側はダメージ数字等を簡略化。
- 巨大な一括書き込みは避け、小さなパッチの積み重ねで(アンカーassert必須)。
- 変異体送り込みの送付先は購入時に相手の inc.sent に積み、buildTide()がpoolへ挿入する方式(spec.tgt固定)。
