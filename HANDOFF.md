---
tags: [deadtide, handoff]
aliases: [引き継ぎ文, つづきから]
---
# つづきから(HANDOFF.md)

> ⭐**新しいセッションはこの1枚から始める**。⚠ここは**入口と直近の差分だけ**。
> **状態の正は [[CLAUDE]]**、やることは [[TODO]]、設計は [[DESIGN]]、落とし穴は [[NOTES]](索引)。
> ⚠**同じことを2か所に書かない**=ここが厚くなったら [[CLAUDE]] か [[TODO]] へ移す。

## 🚀 まずやること(この順で)
1. **[[CLAUDE|CLAUDE.md]] を全部読む**。ルール・いまの状態・検査はそこに全部ある。
2. 触る物が決まったら **[[NOTES]] の索引から、合う分冊だけ**開く(全部読まない)。
3. 次に何をやるかは下の「⏭次にやること」。仕様を確かめる時だけ [[DESIGN]]。

## 📦 これは何 / 置き場所
- 息抜き用ブラウザゲーム『DEADTIDE』(2Dタワーディフェンス+ユニット召喚)。**単一 index.html で完結**。
- ⚠**Yadocho(本命のUE5ゲーム)とは無関係の別プロジェクト**。⚠**親フォルダの `Claude Games` や
  `Game` で開かない**(`Game` は Yadocho 側)。作業ディレクトリ=`…\Claude Games\DEADTIDE`
- 公開URL: https://lukear185.github.io/deadtide/ ／ 音 …/?sfx=1
  ⚠⚠**実機テストに渡すURLは必ず …/?dev=1**(素のURLだと2面もナイトメアも開かない)。

## 🕘 直近でやったこと(2026-08-05〜06(179)〜(186))
> ⭐**前半は「熱さ潰し」、後半は「進行の作り替えを決めた」回**。細かい経緯は [[CHANGELOG]]。

- 🎬**幕を「クリアしたその場」で流すようにした**=成績表のボタンが**「▶ 次へ」1つ**になり、押すと幕。
  幕は**画面の8割**に拡大。閉じたら成績表に戻る。
- 🏆⚠**横画面の成績表が壊れていたのを直した**=横画面用CSSが**素の指定より前**にあり後ろ勝ちで死んでいた
  (見出し60pxのまま/表が上下とも画面の外)。⭐**@mediaは必ず素の指定より後ろへ**。
- ⏩**全ステージに倍速ボタン**(×1→×2→×3→×1)。⚠ソロPvEだけ(協力/対戦/チュートリアルでは出さない)。
- 🎓**チュートリアルの🏗支援の段で詰むのを直した**=`sc0` の決め打ちが値上げに追従していなかった。
  検査 `checkTut` に「その段で持たせる⚙️ < やらせる買い物」で落ちる柵を足した。
- 🛠**テストモードをホーム画面に置けるようにした**=**…/dev.html** から追加すると別アイコン「DT TEST」で
  `?dev=1` のまま起動する(本番の manifest は `start_url:"."` なので落ちていた)。
- 🔥⭐⭐**熱さを3つ潰した**(**絵は1画素も変えずに**)=①ラジアルグラデを単位の円で作って使い回す
  ②撃たれた敵の白光り ③倒れた死体の白光り を**溜めて最後に1回**。
  📌12枠MAX+ボスで **グラデ46.5枚→0 / 合成11.3→5.5回**。
- ⚔**兵科30種を測って⚔ヴァルキリーだけ潰した**(剣の光で1体3.7回=20体74.5回 → 実戦上限5体で20.7→6.4)。
- 🛠**道具の穴を2つ直した**=①測る場面が足りない(12枠MAX+ボス/兵科の場面を `tool_paint` に追加)
  ②合成の数え方(`restore` を数え落としていた)。⚠**軸が足りないと山は見つからない**。

## ⏭ 次にやること
⭐⭐**次のセッションは [[TODO]] の先頭「進行の作り替え」から着手する**(ユーザー「次のセッションから作業開始」)。
1. ⚡⭐**進行の作り替え**(2026-08-06ユーザー決定)=**設計は [[DESIGN]] の「🎓進行の作り替え」に全部ある**。
   要点だけ: **解放はステージクリアで研究所の棚に並ぶ→🧬で買う** / **悪夢・NMを下げる**(⚠「研究所を
   積んで初めて勝てる」の決定は**撤回**) / **研究pを減らす** / **解放順は属性順**(①火炎②電撃③実弾…) /
   **兵科にも属性を効かせる** / **材質3種を追加**(6〜10面は弱点違いの敵を混ぜる) / **10ステージぶんに配る**。
   ⭐**A→B→C→D→E の順に、区切りごとにコミット**。
2. 🔥**熱さ・重さの続き**=重テスラの稲妻を1本にまとめる(**描く回数632→470目標**)/
   道具に「描く回数」の軸 / 地面+ビネットを1枚に(**塗りの75%がここ**)。
3. 🚌**開拓便の🌊高潮を改善** / 🎬**幕に「鳥系ゾンビのいそうな場所」のカットを足す**。
4. ⚠**実機で見てもらう溜まり**は [[TODO]] の末尾(バランスの数字はずっとこれ待ちだったが、
   **2026-08-06にユーザー実機の判断が出た**ので1が動かせるようになった)。

## 🧪 検査
`node tool_release.js` 1本で**7本**まとめて流れる(約40秒)。⚠**絵は見ていない**ので撮って目視する。
⏱**「重い」=`tool_bench.js`** / 🔥**「熱い」=`tool_paint.js`**(⚠**別物**)。明細は [[CLAUDE]] の「テスト手順」。

## ✍ この1枚の更新のしかた
- ⭐**「引き継ぎ」と言われたら、この HANDOFF.md を最新化する**。渡す文は**2行だけ**にする:
  ```
  作業ディレクトリ: C:\Users\acecl\Documents\Claude Games\DEADTIDE
  HANDOFF.md を読んで続きから。
  ```
  ⚠**1行目の作業ディレクトリは必ず残す**(別のフォルダでセッションが開くのを防ぐため)。
- 書き換えるのは**「直近でやったこと」と「次にやること」の2節だけ**。
  ⚠**やったことの一覧・教訓・コミット履歴を貼らない**(全部 [[CHANGELOG]] と [[NOTES]] にある)。
- ⚠**この1枚は50〜70行**に保つ。超えたら [[CLAUDE]] か [[TODO]] へ移す。
- ⭐**同じ回に [[CLAUDE]] の棚卸しもやる**=**毎回は要らないものを他のmdへ移す**(消すのではなく移す)。
  ⚠目安=**CLAUDE.md は100〜120行**。⚠動かしたら [[HOME]] の見取り図も直す。
