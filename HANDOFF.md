---
tags: [deadtide, handoff]
aliases: [引き継ぎ文, つづきから]
---
# つづきから(HANDOFF.md)

> ⭐**新しいセッションはこの1枚から始める**。⚠ここは**入口と直近の差分だけ**。
> **状態の正は [[CLAUDE]]**、やることは [[TODO]]、設計は [[DESIGN]]、落とし穴は [[NOTES]](索引)。
> ⚠**同じことを2か所に書かない**=ここが厚くなったら [[CLAUDE]] か [[TODO]] へ移す。

## 🚀 まずやること(この順で)
1. **[[CLAUDE|CLAUDE.md]] を全部読む**。ルール・いまの状態・検査はそこに全部ある。
2. 触る物が決まったら **[[NOTES]] の索引から、合う分冊だけ**開く(全部読まない)。
3. 次に何をやるかは下の「⏭次にやること」。仕様を確かめる時だけ [[DESIGN]]。

## 📦 これは何 / 置き場所
- 息抜き用ブラウザゲーム『DEADTIDE』(2Dタワーディフェンス+ユニット召喚)。**単一 index.html で完結**。
- ⚠**Yadocho(本命のUE5ゲーム)とは無関係の別プロジェクト**。⚠**親フォルダの `Claude Games` や
  `Game` で開かない**(`Game` は Yadocho 側)。作業ディレクトリ=`…\Claude Games\DEADTIDE`
- 公開URL: https://lukear185.github.io/deadtide/ ／ 音 …/?sfx=1
  ⚠⚠**実機テストに渡すURLは必ず …/?dev=1**(素のURLだと2面もナイトメアも開かない)。

## 🕘 直近でやったこと(2026-08-06(192))
> ⭐**ユーザーが実機テストを休んでいる回**=こちら単体でできる作業を続けた。
- ⚔⭐⭐**派生90種の見分けを最後までやった**=🪖肩当て`vPaul`・🎭面頬`vMask`・⚔得物`vWep`
  ((191)の背中の布の次の一手)。⭐**兵科ごとの違いは系統(`VFAM`)4つ**=
  🛡重装は角板 / 🪓荒くれは棘と牙 / ⚡技術は鰭とバイザー / 🎯軽装は丸い。
  ⚠⚠**bat/shd/axe/valk/big は共通の道具を1つも使っていない**=素通りしたので手で差した。
  ⚠⚠**得物には共通の口が無い**=**1体ずつ「頭の座標」を渡して呼んだ(15か所)**。
  ⚠**肩当てを一度でかくしすぎて「胸当て」になった**(撮って直した)。掟は [[NOTES_絵]]。
- 🔥**先に柵を立ててから描いた**=`tool_paint.js` に「⚔派生段3」の場面を3つ(系統ごと)。
  実測=**描く回数 最大722回/コマ(柵1400)・塗り 5.12倍(柵6.5)**=1つの図形で言い切ったので増えていない。
- 🖼`tool_sheet.js` に **`--pick=名前,名前`**(数種だけ大きく撮る)。⚠90種を1枚にすると小さい差は潰れる。
- 🎨⭐**外の画面をひととおり掃除した**(⭐掟は全部 [[NOTES_UI]] の末尾に足した)=
  💎**英雄図鑑の札に姿**(未所持は影)/ **塗るのは10連だけ**/ 🏋**鍛錬所のカードに姿**(左に置く)/
  🎯**砲撃名の切れ**(バッジを右下へ)/ 🔤**⚔が✕に見えていた**のを「攻」に/
  🐞**研究所のカードが1列だった**(`float` の下の grid は幅が縮む)/
  🔤**文字が10.5px未満 94→7件**(全部1行のCSSだった)/ 👆**✕の押せる範囲を44pxに**(見た目は不変)。
- 🛠`test_shot.js` に **`hopt`(⚙オプション)/`hmsn`(🎯任務)**=撮る口が無くて一度も見ていなかった。

> 📌**それ以前の回((187)〜(191))は [[CHANGELOG]] に全部ある**。

## 🧪 実機テスト(2026-08-06(191)ユーザー指示で2枚に分けた)
⭐**[[TEST|TEST.md]]=これから見る表**(ユーザーが `⬜`→`✅`/`❌`)/ ✅**[[TEST_OK]]=OKと言われた物の置き場**。
⚠**見てほしい物が増えたらその回に [[TEST]] の A へ1行**(明細は [[TODO]])。⚠**OKの行は [[TEST_OK]] へ移す**。

## ⏭ 次にやること
⚠⚠⭐**2026-08-06(192)ユーザー決定=「実機テストが済むまで小さい物だけ」**。
  ⭐**下の1・2は実機テストが終わるまで着手しない**(聞いたうえでこう決まった)。
0. ⭐**小さい物を拾い続ける**=`node test_view.js --all` の残り(押す所44px未満55件・文字7件)/
   撮って見つかる切れ・重なり。⭐**件数の多い所から数えると直す所は1行のことが多い**([[NOTES_UI]])。
1. 🏰**塔の描く回数**(約50回/台=12台で500〜600回)=残る一番大きい山。⚠**焼いて貼る**方向は大仕事。
2. 🧟**⭐⭐アイデアが1つ溜まっている**=「死体が積もる→敵が食って育つ」(ユーザー発案・**本命**)。
   ⚠**未決2つ**(食う役は新規か既存か / ①廃線から始めるか)。明細は [[TODO]] の置き場。

⚠**判断待ちで止まっているもの**=⭐**明細は [[TEST]] の F の節**(🔊`sonic`の素材待ち /
🎨rustとlife2の色覚 / 👊必殺の揺れ)。⚠⚠**「道をやめる」の話は取り下げずみ**=**道は1本のまま**。

## 🧪 検査
`node tool_release.js` 1本で**11本**まとめて流れる(約50秒)。⚠**絵は見ていない**ので撮って目視する。
⭐**道具は29本ある**=一覧と使い分けは [[TOOLS]] の表・明細は [[NOTES_道具]]。手順は [[CLAUDE]]。

## ✍ この1枚の更新のしかた
- ⭐**「引き継ぎ」と言われたら、この HANDOFF.md を最新化する**。渡す文は**2行だけ**にする:
  ```
  作業ディレクトリ: C:\Users\acecl\Documents\Claude Games\DEADTIDE
  HANDOFF.md を読んで続きから。
  ```
  ⚠**1行目の作業ディレクトリは必ず残す**(別のフォルダでセッションが開くのを防ぐため)。
- 書き換えるのは**「直近でやったこと」と「次にやること」の2節だけ**。
  ⚠**やったことの一覧・教訓・コミット履歴を貼らない**(全部 [[CHANGELOG]] と [[NOTES]] にある)。
- ⚠**この1枚は50〜70行**に保つ。超えたら [[CLAUDE]] か [[TODO]] へ移す。
- ⭐**同じ回に [[CLAUDE]] の棚卸しもやる**=**毎回は要らないものを他のmdへ移す**(消すのではなく移す)。
  ⚠目安=**CLAUDE.md は100〜120行**。⚠動かしたら [[HOME]] の見取り図も直す。
