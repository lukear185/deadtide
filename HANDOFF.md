---
tags: [deadtide, handoff]
aliases: [引き継ぎ文, つづきから]
---
# つづきから(HANDOFF.md)

> ⭐**新しいセッションはこの1枚から始める**。⚠ここは**入口と直近の差分だけ**。
> **状態の正は [[CLAUDE]]**、やることは [[TODO]]、設計は [[DESIGN]]、落とし穴は [[NOTES]](索引)。
> ⚠**同じことを2か所に書かない**=ここが厚くなったら [[CLAUDE]] か [[TODO]] へ移す。

## 🚀 まずやること(この順で)
1. **[[CLAUDE|CLAUDE.md]] を全部読む**(約110行)。ルール・いまの状態・検査はそこに全部ある。
2. 触る物が決まったら **[[NOTES]] の索引から、合う分冊だけ**開く(全部読まない)。
3. 仕様を確かめる時だけ [[DESIGN]]。次に何をやるかは [[TODO]] の先頭。

## 📦 これは何 / 置き場所
- 息抜き用ブラウザゲーム『DEADTIDE』(2Dタワーディフェンス+ユニット召喚)。**単一 index.html で完結**。
- ⚠**Yadocho(本命のUE5ゲーム)とは無関係の別プロジェクト**。
  ⚠**親フォルダの `Claude Games` や `Game` で開かない**(`Game` は Yadocho 側)。
- 作業ディレクトリ: `C:\Users\acecl\Documents\Claude Games\DEADTIDE`
- 公開URL: https://lukear185.github.io/deadtide/ ／ 音 …/?sfx=1
  ⚠⚠**実機テストに渡すURLは必ず …/?dev=1**(素のURLだと2面もナイトメアも開かず、新しく作った物へ行けない)。

## 🕘 直近でやったこと(2026-08-03(104)〜(123)・⚠**実機で見てもらったのはチケット周りだけ**)
- 🎫💫⭐⭐**配りもの2種が動いている**=①**★5確定チケット**(1人1回・`GIFT`)
  ②**💫幸運チケット**(1/3/5/10日目のログイン)。⚠**明細と止め方は [[CLAUDE]] の「🎫いま配っているもの」**。
  ⚠**配り直しは `LUCK_GEN` を上げるだけ**(いま3)。
- 🏋**鍛錬所を「普通の強化画面」として開けた**(🔧でLv+1・上限50)。⚠**遊びの中身は白紙のまま**([[TODO]])。
- 👁⭐⭐**`test_view.js` を作った**=**本物のブラウザで座標を実測する検査**(はみ出し/重なり/44px/10.5px)。
  **ゲートの6本目**。⚠`test_layout.js` はDOMがスタブで**はみ出しを1件も見つけられない**。
- 🚌**拠点開拓**=⚡**スコアの段位報酬**(1000/3000/5000/10000で💎)と**専用の窓**/ニトロは押した時だけ消費/
  車体の大きさ3段(1.17/1.33/1.50)/**読み込み 538ms→205ms**/走行音の鳴りっぱなしを修正。
- 🦸**英雄まわり**=必殺技のデモを**「必殺技だけを見せる舞台」に作り直した**/英雄ステータスの一覧を削除/
  **←戻る**を追加/近接の英雄は旗の少し手前に立つ。
- 🛠**支援枠は🏥野戦病院だけ**(電波塔と物資投下所を外した)。野戦病院は**英雄も割合で回復**。

## ⏭ 次にやること
1. ⚠⚠**実機で見てもらう**(大量に溜まっている)=①🏠ホームと🗺マップ ②ルピナ ③②のギミック
   ④必殺技の音(🎛の★仮7本・暁の王5案は未決) ⑤▶必殺技のデモ ⑥🎓チュートリアル ⑦🚌開拓便。
   ⚠**バランスの数字の詰めはこれ待ち**。⚠URLは必ず `?dev=1`(配りものを見る時だけ素のURL)。
2. 🎨⭐⭐**残り21人の顔を画像にする**=いま画像なのは**ルピナだけ**。
   `ui_src/faces/<英雄id>.png` に置いて2コマンド流すだけ(**合わせは自動**)。
   ⚠絵の条件=**顔と髪型だけ・顎まで(首は入れない)**。
3. 🏋⭐**鍛錬所の「遊びの中身」を決める**=白紙。保留した案「⚔実戦稽古」は [[TODO]] に全部ある。
   ⭐**英雄を2人連れて行く**話と**🦸ヒーローモード(2つ目のゲーム)**の構想も [[TODO]] に置いてある。
4. 🖼**アイコンのシートG〜L**=ユーザーが生成したら `ui_src/` へ→切り出し→埋め込み。
5. ⬜進化の中段17種の絵 / ⬜⚔装備の⑥段目 / ❄ボスのインベーダー化は**凍結**。

## 🧪 検査(毎回流す)
```
node tool_release.js
```
⚠これ1本で**6本**まとめて流れる(約40秒)。⚠**絵は見ていない**ので `test_shot.js` で撮って目視する。
詳しい手順とオプションは [[CLAUDE]] の「テスト手順」と [[NOTES_道具]]。

## ✍ この1枚の更新のしかた
- ⭐**「引き継ぎ」と言われたら、この HANDOFF.md を最新化する**。渡す文は**2行だけ**にする:
  ```
  作業ディレクトリ: C:\Users\acecl\Documents\Claude Games\DEADTIDE
  HANDOFF.md を読んで続きから。
  ```
  ⚠**1行目の作業ディレクトリは必ず残す**(別のフォルダでセッションが開くのを防ぐため)。
- 書き換えるのは**「直近でやったこと」と「次にやること」の2節だけ**。
  ⚠**やったことの一覧・教訓・コミット履歴を貼らない**(全部 [[CHANGELOG]] と [[NOTES]] にある)。
- ⚠**この1枚は50〜65行**に保つ。超えたら [[CLAUDE]] か [[TODO]] へ移す。
- ⭐**同じ回に [[CLAUDE]] の棚卸しもやる**(2026-07-30ユーザー指示)=
  **「続きをやるのに毎回は要らないもの」を他のmdへ移す**。⚠**消すのではなく移す**:
  済んだ作業の明細 → [[CHANGELOG]] / 道具と落とし穴 → [[NOTES]] の分冊 /
  確定した仕様 → [[DESIGN]] / 止めてあるもの → [[TODO]] / どれにも当てはまらないなら**新しいmdを作る**。
  ⚠目安=**CLAUDE.md は100〜120行**。⚠動かしたら [[HOME]] の見取り図も直す。
