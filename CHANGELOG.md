# DEADTIDE 作業履歴(CHANGELOG)

『DEADTIDE』に何をいつ入れたかの記録。**設計の正は `CLAUDE.md` の方**で、こちらは経緯だけ。
普段は読まなくてよい。「なぜこうなっているのか」を知りたい時、
過去の判断を蒸し返しそうな時だけ開くこと。新しい作業を足す時は一番下に追記する。

## 実装状況
### 完了(index.htmlに実装済み・テスト済み)
- 綱引き一式(tide.pool/shares/tideStep/pushTide/big/tide カード/ゲージUI/押すボタン/通信同期)
- タワー・ゾンビ7種・マップ描画・夜の帳・エアストライク・作戦タイム・CPU AI(建設/押し/カード)・PeerJSオンラインβ・リザルト
- ユニットの**シム層**: UNITSデータ、deployUnit()、dmgU()、ゾンビの近接攻撃(atk/ar/acd、足止め窓 gap∈[-6,36])、ユニットの攻撃(type: melee/bullet/fire/pierce/blast、aoe/burn/multi対応)、drawUnit()、renderCampでの描画+HPバー、sfx.dep
- ヘッドレステスト(test_headless.js)グリーン: 1v1と三つ巴が完走、シェア合計100維持、ユニット出撃・交戦・死亡確認済み

### 未実装(これが残タスク。上から順に)
**なし。旧残タスク1〜8はすべて実装済み(2026-07-24、下記参照)。**

### 2026-07-24 実装分(Claude Code・各タスク毎に個別コミット済み)
1. **集結旗**: flagD(初期PLEN*.55)/projPath(x,y)/ユニットはflagDへ双方向移動(射撃系rng>60はflagD+70待機、後退時はu.backで向き反転)/旗描画(緑ペナント)+照準プレビュー/🚩ボタン+FLAGAIMモード(🎯AIMと相互排他)/CPUは.45+ag*.06、コア半分以下で.65に後退。
2. **足止め上限**: 毎tick u.eng=0→eng<3のみ足止め対象、確保でeng++、溢れは素通り。
3. **出撃デッキUI**: #deck(下中央)8ボタン=UTHUMBSサムネ+コスト+CD(conic-gradient)+未解放🔒。actDeploy(ui)、client楽観更新。
4. **新部隊解放カード**: kind 'uun'(priceOf/doPurchase/ivRender/actBuyラベル/CPU plan重み2.8)。次兵科名+説明表示。
5. **CPU出撃**: cpuTickで盾1+近接2+射撃2維持(+wave/5で増員)、scrap×res余裕時のみ、タワー建設より後・強化より先。
6. **通信**: packLaneに us/fd/uu/dp、normMeに uUn+ucd保持、clientCampにユニットeidマップ(um)補間+旗同期、hostDataに 'un'/'fg'(clamp済)、meV既定uUn:2、client側ucdは自前減算。
7. **文言**: WAVE1 toastに部隊/旗の2本追加、あそびかたに部隊・旗・足止め上限の段落、リザルトに「部隊出撃」(dep/dp)。
8. **バランス確認**: テストに旗ランダム移動+uun購入を追加、決着分数と回収⚙️/uUnを診断出力、10分超はWARN。3回実行で2試合完走・全て10分以内・ユニット機能(dep>0・交戦・死亡)を確認。ブラウザ実起動で出撃/旗移動の操作も確認済み。

### 2026-07-24 大改修(永井さんのチャット直接指示=最優先の設計変更として実装)
> ⚠この節は「ゲーム概要(v4系)」より新しい。矛盾したらこちらが正。
1. **ソロ=CPU対戦廃止→ステージ制PvE**。1拠点でWAVE20(STAGE_W)を守り切ればステージ1クリア。まずステージ1のみ。専用リザルト/チュートリアル/対人UI非表示。
2. **対戦(オンライン)=敵盤面の可視化**。ミニパネルに他プレイヤー2人のライブ小窓(簡略ドット)+タップでフル観戦(SPEC)。もう一度タップで戻る。
3. **兵科8→12種**。追加=擲弾兵(blast)/冷凍兵(frost=範囲slow)/医療兵(heal=新メカニクス)/ミニガン(高速連射)。drawUnitはid分岐。UNITP12段。
4. **協力プレイ(最大3人Co-op PvE)**。共有フィールド1枚+共有コア(50+20/人)。各自の出撃ポイント(P1〜P3テント+短い合流レーン=BRANCH)から自分のユニットを出撃。⚙️/解放/火力増強/旗(色分け)=各自。タワー=共有スロット(建てた人の⚙️・売却は本人のみ)。撃破⚙️は全員分配(COOP_PAY)。ロビーでモード選択(⚔対戦/🤝協力)。
5. **難易度5段階(D5表)**=新兵/兵長/古参/鬼軍曹/悪夢。HP/物量/速度/ボス/初期⚙️/報酬倍率を個別調整。ソロ/協力共通。協力は人数スケール(量1+.4/人・強さ1+.08/人)。
6. **演出強化パス1**=マズルフラッシュ+リコイル/ゾンビ突進モーション/血しぶき粒子/凍結❄表示/ボス入場演出/旗ポップ/回復フロート/新SFX(heal/frost)。※美的作業=永井さんが理想と言うまで反復対象。
7. **スマホ3台**=viewport/セーフエリア/タッチサイズ/小画面調整は済み。**3台実機のPeerJS接続確認は未**(GitHub Pages等へ置いて確認する)。
8. **永久強化=研究所(ソロ限定ローグライト・真ロック式)**: ソロの勝敗で研究pt獲得(WAVE×10+撃破×0.5×難易度係数)→タイトル→ソロ設定の🔬研究所でタワー枠/兵科枠/新砲撃(絨毯・凍結・ナパーム)/初期⚙️/コアHPを永久解放。未解放はゲーム内解放カードに出ない(初期=タワー3種/兵科4種/🎯のみ)。装備砲撃はソロ設定画面で選択。localStorage(dt_meta)永続。
9. **部隊の段階進化**: 部隊Lv3=強化装備/Lv5=進化(犬→ドラゴン)/Lv10=最終形態(金色ドラゴン+オーラ)。出撃コストはLvに比例。
10. **その他確定**: 建設マス22・部隊上限40(協力各20)・ウェーブ放棄🏳(ソロ)・演出バランス弱中強・WAVE3まで解放ロック・タレット個別強化(攻/連射/射程)。

### 2026-07-24 最終バランス調整(永井さん指示「全体的に見て最終調整」で実施)
計測方法=チート無しの「堅実プレイ」ボット(scratchpadのbalance_probe.js相当)で全難易度実走。
- **PvE後半緩和**: W10以降の成長率をPvEのみ1.17→1.11・物量基礎をPvEのみ(6.5+w*1.6)に。新兵hp.68/cnt.8/pay1.2、兵長hp.84/cnt.92/pay1.08に緩和。
- **確定カーブ(堅実プレイbot実測)**: 新兵=クリア(残コア27)/兵長=W19/古参=W17/鬼軍曹=W11/悪夢=W5。botは工房不使用の控えめプレイ=人間は+2〜4波上振れ想定。狙い=新兵は誰でも・古参は上手いプレイでクリア・悪夢は極限。
- **対戦の決着促進**: W8以降に追加成長×1.1^n(対戦のみ)・取り分の自動均等化0.6→0.45%/秒(押した優位が残る)。人間同士の試合時間を締める目的。
- **CPU貯金(対戦の空き枠CPU/切断引き継ぎ用のみ)**: 新部隊解放費を貯金してから出撃に使う=CPUもuUn3〜5まで解放するように。※CPU戦モード自体は廃止済み(ソロ=PvE)なので影響は対戦の空き枠埋めだけ。
- 既知: 対戦テスト(CPU代理)は13分前後。人間は押し合い/カードで加速するため実測は実機待ち。三つ巴guard上限30000の偽FAILリスクは残置(乱数依存・必要ならシード化)。

### 2026-07-24 追加改修(第2弾・チャット直接指示)
1. **強化ポイント🔩の独立**: ウェーブ制圧で入手。タワー個別強化(stCost)と部隊レベル(prUup)は🔩払いに変更。⚙️は建設/出撃/解放専用。放棄時は🔩半減+工房の残り生産を精算(ecoCompensate)。
2. **スロット解放制**: 通常22枠は初期6枠(SLOT0)・prSlot=200×1.3^n。廃品工房は左上ECO_BASE〜の3枠(prEco=450/1100)。CPUも空き枠が無ければ解放を買う。
3. **種類2倍**: タワー20種/兵科30種。基本はタワー10・兵科6のみで、残りは研究所解放(8割メタ化)。基本兵科は価格順に自動ソート。
4. **時間帯表現**: 「暗くなって見づらい」を廃し、**画面の縁と空を朝→昼→夕→宵→夜に色変化**(TOD_STOPS/todCol、CSS背景も追従)。フィールドの暗転は最小(1→20波で約21%)。太陽/月/星も追加。
5. **見た目オーバーホール(3段)**: ①動的ライティング(闇を光で削る方式)+地面/道路の多層質感 ②統一パーティクル基盤(火花/煙/薬莢/血だまり/土埃/残り火、FXLVとモバイルで自動間引き) ③**スプライト焼き込み**(歩行4コマをキャラ実寸のキャンバスへ焼く。ゾンビ110体で3.2倍速・約3.8MB)+全キャラの接地影。
6. **UI/操作**: デッキに兵科名表示・長押しで部隊強化・🏳放棄は確認ダイアログ・マルチ中も⏸でタイトル復帰・協力ではミニ盤面を非表示・研究所のスクロール位置維持。
7. **マルチ修復**: 参加側に演出/パーティクルが出ない問題(ホストから配信)・3人目の操作が無視される問題(コマンドに席番号jを付与しconnを再紐付け)・協力の黒画面フリーズ(minisUpdのnull参照+rAFのtry/catch化)。
8. **iOS対策**: 連打でタップ不能になる問題(ダブルタップズーム抑止・touch-action個別指定)。PWA化(manifest/SW/自作アイコン)で全画面+横画面。

### 2026-07-25 大型アップデート(編成システム・チャット直接指示)
1. **🎖編成(ロードアウト)**: ソロ設定画面に「🎖編成」ボタン。作戦タイムで解放できる**7つの枠**それぞれについて、基本キャラか派生キャラかを選ぶ。弾薬もここで装備。保存=`META.ld`(枠→派生id)/`META.amEq`。**協力/対戦では無効**(素の兵科・通常弾で統一)。
2. **派生キャラ33種**: 基本7兵科×3段階(例 猟犬→狼→ケルベロス→フェンリル)＝21種。**+2026-07-25: 研究所限定の上級4種(装甲車/メカスーツ/タイタン/ヴァルキリー)にも姿が変わる3段階を追加=12種**(FVBASE・adv:1)。編成画面の「🔬上級ユニット」欄で切替、drawUnitはvtで肩キャノン/第2砲/冠/翼増を描き分け。実体は`UVAR`の倍率テーブル+`mkVar()`で生成する派生定義(`U.base`に元id、`U.vid`/`U.vt`で見た目分岐)。コストは微増(×1.2/1.45/1.75)。研究所で1体ずつ解放(LAB_UV=90×1.13^k・全21)。見た目=犬系は専用描画、人型は段階ごとに鉢巻→兜+肩当て→マント+角。
3. **弾薬アップグレード**: 通常弾→ビーム→プラズマ弾→崩壊弾(`AMMO`)。**実弾(bullet)タワーの弾に反映**=威力×1.18/1.42/1.75、プラズマ以上は装甲貫通、崩壊弾は着弾点に小範囲(45%)。研究所で240/460/760。
4. **新兵科『鎖使い』(基本4番目・⚙️105)**: `hook:1`。**すり抜け(noblock)の敵を唯一足止めできる**。派生=捕縛兵/大網使い/鎖鬼。
5. **全兵科のバランス**: HPを一律2倍・**射程100以上(遠距離)の攻撃力を1.5倍**。基本兵科は6→7種、総数31種(基本7+研究所24)。UNITP解放価格表は自動延長。
6. 実装メモ: `UBASE`=素の定義配列(不変)。`applyLoadout(on)`が`UNITS[i]`を差し替え+`SPR.u`クリア+`makeThumbs()`再生成。ソロ開始時のみ`on=true`。

### 2026-07-25 第2弾(チャット直接指示)
1. **部隊の段階進化を廃止**: Lv3/5/10の姿変化(犬→ドラゴン→金竜)を全削除。部隊レベルは**数値強化のみ**(+10%/Lv)。`u.evo`/`UTHUMBS_S`/`DECKEVO`/bakeUのevoキーを撤去。
2. **マルチの開始通知を保証**: ホストは`P.st0`を保持し、**受領確認(`{t:'ok'}`)が来るまでスナップショットに`st0`を同梱**。クライアントは`st`未受信でも`sn.st0`から`clientStart()`で復帰。コマンド受信でも`stAck`を立てる。クライアント側に**4秒以上更新が来ないと警告トースト**+再ack。3人目が開始通知を取りこぼして操作不能になる事故への対策。
3. **フィールド拡大**: スマホのHUD/デッキを小型化(パネルpadding・フォント・バー幅・カード48px)。fitCanvasに**3つ目の制約**(マップ全体がボタン帯より上に収まる)を追加し、SKYHクロップ時に下へはみ出すバグを修正。表示倍率 0.394→0.430(面積+19%)。**HUD/デッキ/砲撃ボタンと全25スロットの重なりゼロを実測で確認**。
4. **出撃コストの視認性**: `.dc`が`--paper`(淡色)×`--pane`(淡色)で読めなかった → **カード下端の黒帯に金文字(11px)**へ。
5. **発砲光と時間帯の分離**: 光は「闇をくり抜く」方式のみだったため夜色と同色に見えていた → くり抜き位置を`glow[]`に控え、**加算合成で暖色(電撃だけ青白)を重ねる**。昼夜どちらでも「撃った光」と分かる。
6. **研究所に序盤・中盤系を6種追加**(いずれも1波目から効く): 🔩開始強化pt+6/🤝開始部隊枠+1/🧱建設スロット初期+1/💰撃破報酬+6%/🏭工房産出+12%/⏱出撃CD-6%。研究所の項目は**価格の安い順に並べ替え**て序盤に届くものを上に出す。META: up0/uu0/sl0/py0/ec0/cd0。効果はソロのみ(`soloMeta()`ゲート=metaPay/metaCd/metaEco)。

### 2026-07-25 第3弾(チャット直接指示)
1. **廃品工房つき開始**: 研究所`🏭 廃品工房を建てた状態で開始`(META.ec1・220pt)。startSoloで`me.towers[ECO_BASE]`に工房を直接配置。※工房は元から**タップで個別強化可能**(twStats=`['d','r']`=⚙️産出/⏩サイクル、stCost払い)=既存仕様を確認。
2. **系統強化(研究所)**: タワーは型ごと(TLINE: **bullet**/eco/fire/net/blast/elec=全6型網羅)、兵科は型ごと(ULINE: melee/bullet/pierce/blast/fire/frost/heal=全7型網羅)に各Lv3。⚠当初bullet型タワー(最多。base+研究所解放のrail/gat/fort等)を入れ忘れ→2026-07-25追加。**研究所で解放した新型タワー/新兵科にもそのまま乗る**(型判定なので)。META.tl{}/ul{}、tlv()/ulv()はsoloMetaゲート。効果=タワー威力+10%/段(+型別: eco産出+15%/fire燃焼+25%/net減速+20%/blast範囲+12%/elec連鎖+1)、兵科HP+8〜10%・攻撃+12%/段。価格LAB_TL/LAB_UL=140/110×1.55^lv。
3. **新登場の紹介モーダル(md-intro)**: ソロで新解放タワー/兵科・**初登場のゾンビ**をウェーブ開始直後に画面中央で説明(絵+名前+ステータス+特性)。表示中`PAUSED=true`でゲーム停止、OKで次→全部閉じると再開。解放は`G.introQ`にpush(doPurchase unlock/uun)、敵は`G.seenZ`で初出だけ。⚠ヘッドレステストはOK待ちで固まるので**frames()内でPAUSED&&INTROQなら即introNext()**で自動送り。
4. **終盤の敵: 数減・質増**: buildTideのnにW12〜taper(-0.5/wave)・下限8、増加係数.03→.016、lateX 1.035→1.055。1体HPは据置き成長だが**bt(報酬)にW12〜+5%/wave**。画面が埋まりにくく、撃破1体あたりの旨味を上げた。
5. **ボスの削り報酬**: dmgZでボスに、削ったHP割合×(bt×pay×0.6)を**先払い**(z.bpaid累積)。倒しきれなくても削った分は入る。killZのとどめ報酬はbt×pay−bpaidで二重取り防止(協力も同様)。

### 2026-07-25 第4弾(チャット直接指示)
1. **射程強化のプレビュー**: タワーパネルを開くと、現在の射程(オレンジ点線)に加え**強化後の射程を薄緑の輪+塗りで表示**(renderCamp SEL>=0)。📡射程ボタンにも「現在→強化後」の数値(例 832→917)を併記。
2. **射程強化の拡大率アップ**: twRngMの1段あたり .08→**.13**(RNG_STEP)。twRngMlv(g)で任意Lvの倍率を計算(プレビュー用)。

### 2026-07-25 第5弾(画面レイアウト最適化・スマホ実機の指摘「両サイドと下部が空いて盤が浮いて見える/兵科名が読めない」)
1. **下の操作バーに集約(#abar)**: 左下に浮いていた🚩集結旗・🎯航空支援と、右下の🌊押すを**出撃デッキと同じ一列**に入れた(`#abar`=画面下端のflex行)。フィールドの上に浮くボタンがゼロになり、そのぶんマップを下まで広げられる。ヒント文とpushtgtはバーの上に固定。`.spec #abar`で観戦時は非表示。
2. **フィット計算の作り直し(fitCanvas)**: 旧「マップ全体(空込み)がボタン帯の上に収まること」を撤廃。**空(SKYH=108)は上へはみ出してよい・ただし地平線の帯(SKYKEEP=60)は必ず残す**方式に変更し、地面を最大まで拡大。上のHUD用の予約(hudTopPx)は廃止(空がHUDの裏に入るため不要)。対戦のミニ盤面ぶんは`minisResPx()`で右に余白を取り重なりを回避。帯の実測は`barBandPx()`(#abarを測る)。
3. **地面の裁ち落とし(bleed)**: renderCampで画面に映る論理範囲(VL/VR/VT/VB)を計算し、**荒野・空・地平線の影を画面の端まで描く**。額縁色の余白が消えて「盤が浮いている」見え方を解消。廃墟のシルエットも左右に4棟追加。
4. **工房エリアの看板を枠の下へ**: マップ上端が上部HUDの裏に入るため、`ey0-53`→`ey0+63`。
5. **デッキのカード拡大+兵科名を専用の黒帯へ**: カード58×60(PC 60×62)・アイコン26。**兵科名は下から2段目の黒帯に白文字12px**、その下がコストの黒帯(金文字11px)。淡色カードに黒文字だと潰れて読めなかったため帯方式に変更。
6. **⚠CSSの順番の罠(実機で名前が見えなかった真因)**: `@media (pointer:coarse)`のスマホ用指定を**`#deck`の定義より前**に書いていたため、同じ強さ(#id+.class)の後ろの指定に負けて**全部無効**だった(カードもボタンもPC用サイズのまま動いていた)。デッキ/操作バー系のスマホ指定は**`#deck .dk:disabled`の直後の第2ブロック**へ移動。上のブロックにはHUD系(=定義がそれより前にあるもの)だけを残す。
7. **⚠裁ち落としの継ぎ目(実機で余白に見えた真因)**: 地面は端まで描いていたが、その後の**時間帯の色被り・夜の陰・ビネットが`0,0,MW,MH`のマップ矩形にだけ**掛かっていたため、マップの四角い縁がくっきり出て「盤が浮いて」見えていた。3つとも画面の可視範囲(VL/VT/VR/VB)へ拡張。地面の模様(DOTS/PATCHES/TUFTS)も撒く範囲を画面外まで広げ数を増量。
8. **効果(iPhone横 852×393・セーフエリア左右59/下21で計算)**: 表示倍率 0.409→**0.4637**(+13%)・フィールド 654×311→**742×352(面積+28%)**・左右の余白は各55pxだが地面が続くので縁は見えない。SKYKEEP=36。全25スロット・コア・侵入口・道路がHUD/操作バーと**重なりゼロ**。
9. **`test_layout.js`を追加**: 端末サイズを与えてfitCanvasを実走し、スロット/コア/道路とUIの重なりを自動判定する簡易チェック(ゲーム本体からは参照しない開発用)。⚠測定関数は起動400msは既定値を返すので、テスト側で`NOW`を進めてから呼ぶこと。

### 2026-07-25 第6弾(建設マスの整列・ユーザー指摘「まばらに見える。整列させて道路の近くに」)
1. **SLOTS(通常22枠)を全面的に置き直し**: 道路の中心線から**きっかり95**の位置に、**道なりの順**で並べる(道路半幅49+マス半28=77 → 18px空く)。結果、各直線区間ごとに「道路に沿った列」ができる。実測=道路までの距離 最短95/最遠112、マス同士の最短110。
2. **配置の除外条件**(次に動かす時もこれを守る): 拠点150 / 工房枠135 / 協力の出撃テント(BRANCH y=742)120 / コアHPバーの表示位置(x<270・y400〜520) / マス同士110 / 画面内(x75〜1560・y205〜730=上部HUDと下部バーを避ける)。
3. **AI_ORDER(=SLOT_ORDER)を作り直し**: 先頭6つ(初期解放)を道なりに散らして、最初から全域を守れるように。残りはCPUの建設優先順。
4. **⚠test_headless.jsの初期建設スロットも更新**: 旧`2,1,5`は初期解放から外れたため`2,8,12`へ。**解放されていない枠に建てても無視される**ので、テストが建てる枠はAI_ORDERの先頭6つから選ぶこと。
5. **参考画像**: ユーザーから「フィールドの感じはこれ」とAI生成のモック(砂漠の実写調・盤面が画面いっぱい)を受領。**見た目の本格化は後回し**で、**画面いっぱいに地面が続く構図だけ**が今回の目標。

### 2026-07-25 第7弾(右上の道路と上のHUDの重なりを解消)
1. **🏳放棄と🔊ミュートを上のHUDから下の操作バーへ移動**: 上のHUDが横に長く、右上の道路(PATH y=150・x1100〜1660)の上端(論理y=101→画面の上のほう)に被っていた。上に残すのは**WAVE/コア/⚙️/🔩/⏸まで**。放棄の文字は`🏳 放棄 -n`→`🏳-n`に短縮。
2. **スマホのHUDをさらに小型化**: gap5→4・panel padding7→6・進行バー52→42・コアバー46→38。上のHUDの右端が約486pxに収まり、道路の左端(画面x約541)まで**55pxの余裕**ができた。
3. **判定を`test_layout.js`に追加**: 道路(半幅49+路肩=52)が上のHUDと下の操作バーに潜っていないかを経路10刻みでサンプルして判定。
4. ⚠**残件**: 対戦の死潮ゲージ(#tidebar・画面上中央)は同じ帯にあるため右上の道路と被る可能性がある。対戦の画面を詰める時に一緒に直すこと。⏩早送りボタン(自分が陥落した時だけ出る)も出ると数px被る。

### 2026-07-25 第8弾(デッキの絵が豆粒問題+初期解放マスの調整)
1. **サムネを「絵のある範囲」に合わせて焼き直す(`bakeUThumb`)**: 従来は88px枠の中に小さく描いていたため、カードで26pxに縮めると人が豆粒で姿が分からなかった。**大きめ(180px)の下絵に描く→`getImageData`でインクの外接矩形を測る→枠いっぱいに縮小して88pxへ焼く**方式に変更。全兵科・派生に自動で効く(将来追加分も)。⚠測れない環境(ヘッドレステスト)はtry/catchで素通りする。
2. **カードとアイコンを拡大**: スマホ 58×62(アイコン30)・PC 60×66(アイコン32)。
3. **初期解放マスを6→7へ(ユーザー指示)**: 追加=**1(1370,245 右上・入口の道と中央の道を同時に見る良位置)**と**4(1005,320 縦道沿い・手薄だったS2区間をカバー)**。除外=**21(530,640 左下の終盤)**→最初の購入対象(AI_ORDERの8番目)に。`SLOT0=7`。
4. 初期7枠=2/1/4/8/12/16/19。テストが建てる2・8・12は初期解放のままなので変更不要。

### 2026-07-25 第9弾(「姿が見えない」の真因+実画面スクショ環境の獲得)
1. **★`test_shot.js`を追加=見た目を実際に確認できるようになった**: ヘッドレスChrome(無ければEdge)でindex.htmlを実起動し、ソロを自動開始してPNGを撮る。`node test_shot.js out.png 852 393`。一時HTMLはOSのテンポラリに作るのでリポジトリは汚れない。**以後、見た目の修正は必ずこれで撮って目視してから報告すること**(数値だけの確認では今回の不具合を3回見逃した)。⚠ヘッドレスでは`(pointer:coarse)`が効かず**PC用のCSSで描画される**(カードは62×78)。スマホ実機は58×72。
2. **「姿が見えない」の真因は2つ**: ①**未解放カードの🔒が全面に被さっていた**(`.lk{inset:0;background:rgba(0,0,0,.55)}`+中央に大きな錠前)。7枚中5枚が未解放なので、ほとんどのカードで兵科の絵が見えない状態だった。②絵の欄が32pxしかなく、カード幅60pxの中で小さかった。※サムネの焼き直し(第8弾)自体は正しく動いていた。
3. **修正**: 🔒は**右上の小さなバッジ**にして絵は隠さない+未解放カードは`.lkon`で**絵を灰色**にして区別。サムネを**横長104×78**にして(犬は横幅・人型は高さいっぱいになる)、カードの絵の欄を**52×39(PC 56×42)**へ拡大。カードは58×72(PC 62×78)。
4. **代償**: 操作バーが高くなった分フィールドは 737×350 → **714×339(-3%)**。ユーザー了承済み(「ユニットのボタンの枠広げてもいい」)。

### 2026-07-25 第10弾(諦めるボタンの文字+HUDの伸縮を止める)
1. **「諦める」を文字で表示**: 操作バーのボタンを`🏳 諦める -n`に。用語も**放棄→諦める**へ統一(確認ダイアログの見出し/はいボタン/あそびかた/バナー/強化ptのトースト)。※コード内の変数名(ffwave/giveUpDmg)とこの文書の見出しは従来どおり。
2. **WAVEパネルの幅を固定**: 敵の数が1桁↔2桁、💀(5の倍数ウェーブ)の有無で幅が変わりHUDが伸び縮みしていた → `#wavebadge`に**最長表示ぶんのmin-width**(スマホ154px/PC 192px)+`tabular-nums`+左寄せ。**5パターン(1桁/2桁/💀つき/敵表示なし)すべてで幅216px・HUD右端455pxで一定**なのを実測で確認。
3. **`test_shot.js`を改良=スマホ用CSSを強制適用**: ヘッドレスChromeでは`@media (pointer:coarse)`が効かずPC寸法で撮れてしまうため、そのブロックを条件なしCSSとして後ろに足すようにした(`pc`引数でPC表示も撮れる)。**これで実機と同じ寸法の絵を撮って確認できる**。
4. 実測値(スマホ852×393・セーフエリア左右59/下21): HUD右端514 / 道路の左端537(27px余裕) / 操作バーの中身711 vs 使える幅734。

### 2026-07-25 第11弾(初期ユニットが強すぎる=基本7種だけ下方修正)
1. **基本兵科のうち5種**: HP倍率 2倍→**1.5倍**(実質-25%)、攻撃力**-12%**。⚠**最初から持っている猟犬とバットの2体は元の強さのまま**(一度下げたが「戻して」との指示で復帰)。研究所で解放する上位兵科・派生・上級4種も**据え置き**=解放して強くなる実感を残す。実装は加工ループの`const bas=i<BASE_U&&['hnd','bat'].indexOf(UNITS[i].id)<0`。
2. **現在の基本7種**(出撃コストは変更なし): 猟犬HP156/攻14・バット312/22(=据え置き)、斥候135/25・鎖使い414/27・ガンナー153/32・斧使い378/46・盾持ち792/8(=下方修正済み)。
3. **効果**: ボット計測ではウェーブ到達は15〜16でほぼ変化なし。**代わりに出撃回数が111→186に増えた**=部隊が溶けやすくなったぶん⚙️の消費が増える形。壁として置きっぱなしにできなくなった。
4. ⚠実機の体感が「まだ強い/下げすぎ」なら、この係数(1.5と0.88)だけを動かせばよい。

### 2026-07-25 第12弾(全31兵科を「解放順に強くなる」よう総調整)
1. **★一括倍率を廃止=UNITSの数値がそのままゲーム内の値**: `UBUF`/ティア補正/HP2倍/遠距離1.5倍/コスト÷2.5の加工ループを削除し、**最終値を定義側に焼き込んだ**。以後、強さの調整は`UNITS`のhp/atkを直接いじる。⚠`uTier`と`TSCALE`(見た目の大きさ・デッキ枠の色)はループとは別物なので残してある。
2. **戦力の定義**: `(実DPS)^0.6 × (HP)^0.4 × 役割ボーナス`。実DPS=攻撃力÷間隔×(複数同時は割引 1+(multi-1)*0.6)×(範囲 1+aoe/220)+燃焼×0.8。役割ボーナス=**凍結2.2/回復2.0/壁(shd,gld,tnk,titan)1.45**(数値に出ない価値の補正)。⚠壁の判定はHPやDPSの閾値でやると強化した瞬間に評価が跳ねるので**IDで固定**する。
3. **目標曲線**: 猟犬(0)とバット(1)は据え置き、**バットから末尾まで1段ごとに約+7%の等比**。全31種が目標±1%に収まり、**逆転ゼロ**を`test_balance.js`で確認。
4. **主な変化**: 大きく上がった=火炎瓶/擲弾兵/冷凍兵/スナイパー/迫撃兵/ミニガン/ロケラン/レール狙撃/絶対零度(解放費に対して弱すぎた)。下がった=**チェーンソー(-36%)**と**タイタン(-33%)**(順番の割に強すぎた)。据え置き=猟犬/バット/短機関銃/長槍兵/隊長/ヴァルキリー。
5. **`test_balance.js`を追加**: 現在の戦力・目標・ずれ%を一覧し、逆転があれば警告する。**数値を触ったら必ず流す**。
6. 派生兵科(UVAR)は基本兵科への倍率なので自動追従。出撃コスト・解放費(UNITP)は変更なし。

### 2026-07-25 第13弾(★ステージ2「沈んだ港」を新規追加=ステージ制の土台)
1. **ステージ表(STAGES)を新設**: 1ステージ=`{n,sub,zs,path,slots,order,pal}`。`loadStage(i)`が経路(PATH/SEG/PLEN)・建設マス(SLOTS)・解放順(AI_ORDER/SLOT_ORDER)・拠点(CORE)・地面の模様(makeScenery)を差し替える。⚠**建設マスの数は全ステージ22で固定**(ECO_BASEが動くと保存や配列の形が壊れる)。廃品工房の3枠(ECO_SLOTS)と協力の出撃テント(BRANCH)は共通。
2. **ステージ2「沈んだ港」**: 死潮が引いた後の港。道は右下から入って上へ回り込み、拠点は左下。地面/廃墟/草の色を青灰色に差し替え(`pal`)。マス22個は道路から95で整列(test_layoutで両ステージとも重なりゼロを確認)。
3. **敵=ゾンビ化した海の生き物11種(`st:2`)**: 腐れガニ/跳ねイワシ/甲殻ロブスター/膨張フグ(分裂)/鳴き貝(加速オーラ)/咬みウツボ/這いヒトデ(すり抜け)/溶けクラゲ(すり抜け+装甲)/深海アンコウ/影サメ/**触腕の主(ボス)**。役割はステージ1のゾンビと1対1で対応させ、強さもほぼ同値=難易度カーブを引き継ぐ。drawZombieに専用の描画を追加。
4. **敵プールの分岐**: `ナイトメア=獣(nm) / ステージ2=海(st:2) / それ以外=ゾンビ`の完全別プール。ボスも`ST2_BOSS_ZI`で分岐。⚠**分裂(split)で出る小型が常にウォーカー固定だったバグを修正**=同じ生態系の最弱個体を出すようにした(ステージ2にゾンビが混ざっていた)。
5. **ステージ選択UI**: ソロ設定画面の一番上に追加。**前のステージをどれかの難易度でクリアすると次が解放**(`META.sclr[]`)。未解放は🔒で押せない。選択は`META.stg`に保存。協力/対戦は`hostStart`/`clientStart`で必ずステージ1に戻す(地形の食い違い防止)。
6. **テスト**: `test_headless.js`に**ステージ2の疎通確認**(海の敵しか出ないこと・1体も出ないなら失敗)を追加。`test_layout.js`は全ステージのマスと道路をUIと突き合わせる。`test_shot.js`は`st2`引数でステージ2を撮れる(例 `node test_shot.js s.png 852 393 st2`)。

### 2026-07-25 第14弾(作戦タイム延長+設置猶予+中断/再開+海の敵に腐敗表現)
1. **作戦タイム15秒→20秒**(`IV_SEC`)。
2. **ウェーブ開始から10秒は敵が出ない設置猶予**(`WAVE_LEAD`)。buildTideで`T.lead=10`+最初の出現を10秒後ろ倒し。HUDのWAVE欄に**🔨残り秒**を表示(敵が出始めると🧟表示に戻る)。※1ウェーブあたり約15秒延びるので、通し時間は20波で+5分ほど。
3. **★中断と再開(ソロ専用)**: `localStorage['dt_run']`に保存。**作戦タイムのたびに自動保存**+⏸メニューの`💾 中断してタイトルへ`でその場保存。ソロ設定画面に`▶ 続きから — <ステージ> WAVE n / <難易度>`が出る。保存するのは「ウェーブの区切りの状態」=中断したウェーブは最初からやり直し(敵の位置は保存しない)。復元するもの=ステージ/難易度/波/コア/⚙️/🔩/解放タワー数/部隊枠/部隊Lv/工房枠/マス解放/**建てたタワーと個別強化**/装備砲撃/旗の位置。決着(awardMeta)と`やめる`で記録を消す。⚠`防衛開始`では消さない(押し間違い対策)。
4. **海の敵に腐敗表現**(「ゾンビ感が無い」の指摘): 共通ヘルパー`rotMark()`で**黒ずんだ腐れ+露出した骨(白)+したたる粘液**を全11種に重ねた。生き物らしい明るい色6種を腐敗色へ寄せた(腐れガニ/跳ねイワシ/甲殻ロブスター/膨張フグ/鳴き貝/這いヒトデ)。
5. 検証: 中断→再開は実ブラウザで往復テスト済み(scratchpad/resumetest.js相当)。⚠ヘッドレステストのNode環境にはlocalStorageが無いので保存系は検証されない。

### 2026-07-25 第15弾(各ステージに最終ボスを追加)
1. **最終ウェーブ(WAVE20)だけに出る専用ボス**を`fin:1`で定義。5波ごとの通常ボスとは別物。
   - ステージ1 **屍山の王**(hp2600/報酬900/薙ぎ払い7体/装甲/**周囲を加速させるオーラ**/sc2.6)=融合した屍の山。3つの顔と鉄筋の冠。
   - ステージ2 **深淵の母**(hp2600/報酬900/薙ぎ払い6体/装甲/**倒すと仔が3体這い出る(split)**/sc2.6)=光る卵嚢と大顎。
2. 選び方は`buildTide`: `wave%5===0`のとき、**PvEかつwave>=STAGE_Wなら`FIN_ZI`/`FIN2_ZI`**、それ以外は従来のボス。⚠ナイトメアは従来どおり肉塊の主(獣プールの最終ボスは未作成)。
3. `mw:20`にしてあるので通常の雑魚プールには絶対に出ない。紹介モーダル(初出の敵)には自動で載る。
4. `test_headless.js`に**WAVE15とWAVE20のボスが別物であること**の検査を追加。

### 2026-07-25 第16弾(火炎系タワーを装甲特効に)
1. **fire型タワーは装甲持ちに`FIRE_VS_ARMOR=1.9`倍**(直撃も燃焼も)。燃焼時間も装甲相手だけ+1秒。従来は「装甲を無視する(0.45倍にならない)」だけだった。
2. 対象=**火炎放射塔**と**酸噴射器**(fire型タワー2種)。説明文も「装甲に特効(約1.9倍)」へ。⚠**火炎系の兵科(火炎瓶/チェーンソー/火炎放射兵)は据え置き**=兵科は戦力カーブ(第12弾)で管理しているため。必要なら同じ規則を入れる。
3. 目安(強化なし): 火炎放射塔の装甲持ちへの秒間ダメージ 約71→約135。同じ相手にライフル櫓は約18(装甲で0.45倍)なので、**装甲相手の専門家**という性格がはっきりした。装甲持ち=アーマード/スライム状体/ブルート/重装兵/ボス全般/海のロブスター・クラゲ・アンコウ。

### 2026-07-25 第17弾(基本兵科に壁役「巨漢」を追加=基本7→8種)
1. **巨漢**(id`big`・出撃26・cd7): HP900/攻11/間隔1.2/射程40/足44。**バット(24)の次・斥候(28)の前**に入るようcostで位置決め(⚠一度「猟犬とバットの間」に置いたが指示変更で移動。開始2枠が猟犬+巨漢になってしまうのも解消)。役割は純粋な壁=火力はほぼ無い代わりに硬い(バットの約3倍のHP)。見た目は一回り大きい体格+太い腕と拳(武器なし)。
2. **派生3段階**: 用心棒(HP1305)→人間戦車(HP1800)→不動の巨神(HP2520)。倍率は盾持ち系と同じ1.45/2/2.8。
3. **BASE_U 7→8**。VBASEに`big`を追加(編成画面の枠も8つに)。UNITPの解放費は位置ぶんずれる。
4. 開始2枠は従来どおり**猟犬+バット**。巨漢は最初の解放対象(160⚙️)。
5. ⚠**操作バーの幅**: 基本8種になったのでスマホでカード54px・gap3px・丸ボタン52pxへ縮めた(実測 中身730 vs 使える幅734)。**これ以上兵科を増やすと横スクロールになる**ので、増やす時は幅を再計算すること。
6. `test_balance.js`のTANK判定に`big`を追加。戦力は猟犬56→**巨漢72**→バット79で逆転なし。

### 2026-07-25 第18弾(砲撃を即着弾→ロケットが飛んでくる演出に)
1. **`airstrike()`は「発射」だけを行い、着弾処理は`airstrikeHit()`へ分離**。発射時に`C.shells`へロケット弾(`rk:1,stk,wave,byMe`)を1発積み、**画面外(目標の左上640×500)から`STK_FLY=0.85`秒かけて飛ぶ**。着弾した瞬間にshellの更新ループが`airstrikeHit`を呼ぶ。
2. **見た目**: 白い機体+オレンジの弾頭+噴射炎、後ろに太い煙の尾。着弾予定地には**黄色の照準(近づくほど小さくなる)**。既存のshell(擲弾タワー)は今までどおり放物線で飛ぶ(`rk`が無いものは弧を描く)。
3. **遊びへの影響**: 0.85秒ぶん敵が動くので**置き撃ち(未来位置を狙う)が必要**になった。チャージは発射した瞬間に消費する。機関銃掃射も同じ0.85秒後に開始。
4. マルチ: shellsは既にスナップショットに座標だけ載っているので、参加側では丸い点として飛んで見える(ロケットの絵は host のみ)。

### 2026-07-25 第19弾(砲撃の飛来物を種類別に+研究所のカテゴリ分け+壁役の作り直し)
1. **砲撃の飛来物を種類ごとに変えた**(`STK_DELIV`): 標準=**ロケット**(斜め上から煙の尾)/絨毯爆撃=**爆撃機が水平に横切る**/凍結・ナパーム=**真上から落ちる爆弾**(色は種類別)/機関銃掃射=**飛来物なし=即発動**(拠点から撃つ設定のため)。描画は`sh.rk`で分岐。
2. **研究所をカテゴリ分け**(`LAB_CATS`): 🆕新しい種類を解放 / 🧬派生キャラ・弾薬 / 🎯砲撃の種類 / 📈系統強化 / 🚀開始時のブースト / 💰収入と効率。見出しには「それが何なのか」の一行説明つき。
3. **⚠値段順の並べ替えを廃止**: 買うと値段が上がって順番が入れ替わり、押した項目が別の場所へ飛んでいた。カテゴリ順+定義順の固定に。スクロール位置の維持は実測で確認(520→520)。
4. **壁役を作り直し(ユーザー指示)**: 巨漢 900→**450**(半分)、盾持ち 1235→**4500**・攻撃12→**3**(攻撃力は皆無・ひたすら硬い)。これに合わせて後続の壁も引き上げ: 重装盾2400→**6500**/装甲車3025→**8500**/タイタン3115→**10500**。
5. **⚠壁役は戦力カーブ(第12弾)の対象外にした**: 戦力式はDPS重視なので、攻撃力ゼロの壁を正しく測れない(盾持ちは-31%と出る)。`test_balance.js`は壁役(big/shd/gld/tnk/titan)を順番検査から外し、**壁どうしでHPが解放順に増えているか**だけを見る。

### 2026-07-25 第20弾(機関銃掃射が動かないバグ修正+砲撃の実行テスト追加)
1. **バグ**: 機関銃掃射を撃つと`ReferenceError: ifisMinesfx is not defined`で何も起きなかった。原因は**第18弾(コミット382f33c)**で`(C.isMe||F.isMe)`→`isMine`をスクリプト一括置換した際に**括弧ごと置き換えてしまい**`if(C.isMe||F.isMe)sfx.shotgun();`が`ifisMinesfx.shotgun();`になっていたこと(凍結爆弾も同じ)。`if(isMine)sfx...`へ修正。※当初この文書は「第19弾で」と書いていたが誤り(第21弾の調査で確定)。
2. **⚠教訓**: `node --check`は通ってしまう(未定義変数は実行時エラー)。**一括置換したら必ず「その行が実行される経路」を走らせる**こと。
3. **`test_headless.js`に砲撃の実行テストを追加**: 5種すべてについて`airstrike()`の発射と`airstrikeHit()`の着弾を直接呼び、例外が出ないか確かめる。⚠**rAFの中の例外はtry/catchに飲まれるので、着弾処理は直接呼ぶ**。バグを再現したファイルで実際に落ちること(FAILになること)も確認済み。

### 2026-07-25 第21弾(一括置換事故の総点検+`test_undef.js`を追加)
1. **置換事故の残りは無いことを確定**: 事故を起こした置換の親コミット(`382f33c^`)を調べたところ`(C.isMe||F.isMe)`は**全部で3箇所**しか無く、うち2箇所が壊れて第20弾で修正済み・残り1箇所(`if(isMine&&stk!=='frost')sfx.boom()`)は元から正しい。**この置換による未修正の壊れは残っていない**。
2. **⚠この文書の記録ミスを訂正**: 事故を起こした置換は第19弾ではなく**第18弾(382f33c)**だった。
3. **★`test_undef.js`を追加=実行しなくても未定義の名前を見つける検査**: `<script>`からコメント・文字列・正規表現を取り除き、変数として使われている名前を全部拾って、どこにも宣言が無い物を報告する。`node --check`は文法しか見ず、`test_headless.js`は「その行を通る経路」を走らせないと気づけない。**この検査は一度も実行されない分岐の中まで全部見る**ので、今回のような事故を出た瞬間に捕まえられる。
   - 使い方: `node test_undef.js`(FAILなら終了コード1)。
   - 判定: **予約語がくっついた名前**(`ifXxx`等)と**宣言が無く1回しか出ない名前**はFAIL。宣言が見つからないが複数回出る物は⚠として出すだけ(誤検出が多いため)。
   - **壊れていた版(382f33c)を食わせると`ifisMinesfx`を検出してFAILすることを確認済み**。現行のindex.htmlは⚠も含めてゼロ。
   - 誤検出だと現物を見て確認できた場合だけ`ALLOW`に足す(安易に足さない)。
   - 実装メモ: テンプレート文字列は入れ子(`` `...${x?`...`:''}...` ``)に対応し、`${}`の中はコードとして残す。クォートを含む物は正規表現と見なさない(誤判定しても文字列の判定がズレないため)。数値の`1e9`から`e9`を拾わないよう数字の直後は除外。
4. **変更のたびに流す検査は4つ**: `node --check` → `node test_undef.js` → `node test_headless.js` →(見た目を触ったら)`node test_shot.js`。数値を触ったら`test_balance.js`、画面寸法を触ったら`test_layout.js`。
5. 今回index.htmlは**一切変更していない**(検査ツールの追加と文書の訂正のみ)。既存テストは全て通る(headless/balance/layout)。

### 2026-07-25 第22弾(実機テスト全部OK+文書の分割)
1. **第20弾までの実機テスト7項目が全部OK**(ユーザー確認済み)。機関銃掃射・凍結爆弾の修正、砲撃4種の飛来演出、研究所のカテゴリ分けとスクロール維持、盾持ち/巨漢のHP、ステージ2の手応え、最終ボス2体と設置猶予10秒、中断→再開の往復——**すべて実機で通った**。
2. **CLAUDE.mdからこのCHANGELOG.mdを分離**(24,024字→約7,000字)。理由=作業のたびに第N弾が増え、その全部を毎セッションの最初に読んでいたためコンテキストを圧迫していた。
   - CLAUDE.mdに残したもの=**今の仕様・絶対ルール・コードスキーマ・テスト手順・チューニングノブ**、そして新設した**「⚠作業の時に必ず守ること」**(過去の弾ログに埋もれていた落とし穴・決まりごとを全部そこへ集約)。
   - CHANGELOG.mdへ移したもの=第1弾〜第21弾の経緯。**普段は読まない**。
   - **⚠今後、新しい落とし穴を踏んだらCLAUDE.mdの「必ず守ること」に1行足す**こと。経緯はこちらに書く。
3. **ついでに古くなっていた仕様の記述を現物に合わせて直した**: 建設スロット初期6→**7**(SLOT0=7)/兵科30種(基本6)→**32種(基本8)**/航空支援4種→**砲撃5種**(初期は🎯のみ)/部隊の段階進化は**廃止済み**と明記/MAXU=20→**40**/ドリフト0.6→**0.45%毎秒**。ステージ制・編成・弾薬・作戦タイム20秒・設置猶予10秒・中断再開・最終ボス・紹介モーダルを「確定済み設計」へ追記。

### 2026-07-25 第23弾(🌑ナイトメアの最終ボス+対戦のHUD事故修正+ステージ2の終盤が軽すぎた件)
1. **🌑ナイトメアにWAVE20専用の最終ボス「原初の獣」を追加**(id=fBeast)。これで3つのプール全部に最終ボスが揃った。
   - すべての獣の源。**装甲+周囲加速(aura)+分裂3(倒すと巨鼠が這い出る)+8体薙ぎ払い**。HP3000/速さ44/攻撃94。
   - 姿=背から仔獣の頭が3つ生え、腹に仔をぶら下げた四つ足の巨獣。角2本と巨大な顎。
   - `FIN_ZI`の条件に`!z.nm`を足し、`FINNM_ZI`を新設。ボス選択は `nmOn?(fin?FINNM_ZI:NM_BOSS_ZI):…`。
   - **⚠この作業で見つけた罠**: ゾンビはスプライトに焼かれる(bakeSet)ため、描ける局所座標は`|x|≦54・y≧-105`まで。
     最初は顎をx=94まで伸ばして**無言で切り落とされた**(画面では顎だけ消える)。CLAUDE.mdに追記済み。
   - `test_headless.js`に**ナイトメアの実走テスト**(獣以外が混ざらないか/最終ボスが出るか/描画で例外が出ないか)を追加。
2. **対戦の死潮ゲージ(#tidebar)を上のHUDの真下・左寄せへ移動**。
   - **元の実装は画面中央上に幅420pxの帯を置いており、右上の道路と侵入口だけでなく、
     コアHP・⚙️・🔩・⏸ の4パネルを丸ごと覆い隠していた**(z-indexが同じでDOM順が後ろ=上に乗る)。
     スマホ実測で topbar 0〜455 に対し tidebar 230〜622。**対戦中は自分のコアHPが見えない状態だった**。
   - 直し方: 絶対配置の中央寄せをやめ、`top=HUDの下`・`left=左端`・幅min(230px,27vw)の1行に。
     ラベルは「🌊 死潮の取り分 — あなた33%」→**「🌊あなた33%」**に短縮してバーの左へ横並び。
   - ミニ盤面(.mini)を詰める指定を`max-width:760px`→**900px**に(852pxのスマホにも効くように)。
     マップの右余白(minisResPx)が減ったぶん**フィールドが横に約40px広がり、倍率が高さ頭打ち=最大**になった。
   - `test_layout.js`に**対戦のセクション**を追加(死潮ゲージ×HUD/ミニ盤面/道路。参考値としてマス・侵入路も表示)。
   - `test_shot.js`に`vs`(対戦を撮る)と`z=id,id`(指定した敵だけ経路に並べて撮る)を追加。
   - **残った件**: ミニ盤面が右上の侵入路(マップ矩形の外)に掛かる。侵入口はmap x≒1671でMW=1600の外にあり、
     minisResPxはマップ矩形しか避けていないため。完全に避けるにはマップを6〜12%縮めることになるので保留。
3. **ステージ2の終盤が、ステージ1より4割も軽かったのを修正**(数値上の破綻・実機では気づきにくい)。
   - 原因: `buildTide`はW4以降、最大40%の確率で**プールの最後の個体**を引く。ステージ1の末尾は重装兵(HP680)だが、
     ステージ2にはレイス/重装兵の相当種が無く**末尾が影サメ(HP195)**だった。W20総HPが ①246k に対し ②149k。
   - 対策: **亡霊エイ**(sRay=レイス相当・すり抜け+周囲加速)と**巨大ウミガメ**(sTurt=重装兵相当・装甲)を追加。
     8回平均で 悪夢W20総HP ①383k ②373k とほぼ同等になった。
   - **⚠プールに敵を足す時は必ず末尾の重さを揃える**(CLAUDE.mdに追記済み)。
4. 検査は全部通っている: `node --check` / `test_undef.js`(未定義ゼロ) / `test_headless.js`(最終ボス3体とも確認) /
   `test_balance.js`(逆転なし) / `test_layout.js`(死潮ゲージ×道路・×HUD ともOK)。見た目は`test_shot.js`で目視済み。

### 2026-07-25 第24弾(第23弾の実機テストOK+設置猶予を10→5秒)
1. 第23弾の実機テスト(ナイトメアの最終ボス・対戦のHUD・ステージ2の終盤)は**全部OK**(ユーザー確認済み)。
2. **ウェーブ開始の設置猶予(WAVE_LEAD)を10秒→5秒**にした(ユーザー指示)。作戦タイム20秒は据え置き。
   10秒は待たされ過ぎで、作戦タイムで組み終えているぶんダレるため。表示は上のHUDの🔨カウントダウンがそのまま短くなる。

### 2026-07-25 第25弾(スナイパー塔と冷却塔のテコ入れ)
1. **スナイパー塔**: 威力175→**875**(×5)・間隔2.4→**4.8秒**(×2遅く)。ユーザー指示。
   - 狙いは「雑魚を掃除する塔」から「最大HPの敵を一撃で抜く塔」への役割変更。単体DPSは73→182に上がるが、
     **1発あたり4.8秒なので雑魚の処理速度は逆に半分になる**(1体/2.4秒 → 1体/4.8秒)。ボス・重装向けの塔になった。
   - ⚠レールガン(研究所・cost680)との比較: 素のDPSはスナイパー182>レールガン103で逆転するが、
     レールガンは**装甲貫通**があるため装甲相手では103>82で勝つ(スナイパーは装甲に×.45)。射程も900>660。
     完全な逆転ではないが**要観察**。実機で「レールガンを取る意味が無い」と感じたら上げる。
2. **冷却塔を「完全凍結」に変更**(ユーザー指示)。間隔1.4→**6秒**とかなり遅く。
   - 新フィールド`frz`(秒)。`z.frzT>0`の間は**移動速度0かつ攻撃不可**(`z.acd`を減らさない)。凍結が解けたあとは
     `slowT`(4.5秒)の減速が残る=「解けかけ」の表現。
   - **⚠永久ロック防止**: 凍結時に`z.frzCd=frz×2`を立て、これが残っている間は再凍結しない。
     これが無いと⏩連射Lv5(間隔×.55)+研究所の🕸強化(凍結+20%×3段)で**凍結時間>再発射間隔**になり、
     範囲内の敵を永久に止められてしまう(6×.55=3.3秒 < 2.6×1.6=4.16秒)。frzCdにより稼働率は最大50%。
   - 見た目: 氷漬けのブロック(半透明の水色+上に氷柱)で囲む。止まっていることが一目で分かる。
   - 通信: スナップショットの`es`に9番目の要素(凍結中か)を追加。クライアント側も氷漬けで描画される。
   - `test_headless.js`に**checkCryo()**を追加(凍る/凍結中に1mmも動かない/解凍直後に再凍結しない を直接検証)。
     ⚠campStepはrAFの中なので、例外もロジック抜けも通しプレイのテストには出ない。直接呼んで数字で見ている。
   - `test_shot.js`に`frz`オプション(z=で並べた敵を1体おきに凍らせて見比べる)を追加。
3. **タワー定義の`slow:`は死んだ値**だと判明(拘束ネット砲の`slow:.45`は参照されておらず、減速率は一律.55)。CLAUDE.mdに明記。

### 2026-07-25 第26弾(レールガンをビーム砲=線状の範囲ダメージに)
1. **レールガンをビーム砲に変更**(ユーザー指示「ビーム砲で範囲ダメージ」)。
   - 新フィールド`beam`(ビームの半幅26)。狙った敵の**方向**へ射程900いっぱいまで線を伸ばし、
     `segDist`(点と線分の距離)で線上の敵を全部拾って**全員に満額ダメージ**(装甲貫通あり)。
   - **`type`は`bullet`のまま**にした。ここを`'beam'`のような新typeにすると
     ①弾薬(AMMO)が乗らなくなる ②研究所の🔫銃器改良(`tlv('bullet')`)が効かなくなる、の2つを失うため。
     撃ち方だけ`if(T.beam)`で分岐している。
   - 📡射程強化(`us.g`)でビームが太くなる(+6%/段)。
   - 演出: 新しいfx種別`beam`。太い紫の帯+白い芯が0.3秒で走って消える(電撃の`zap`と同じ多重ストローク方式)。
     `addFx`経由なので対戦のクライアント側にもそのまま流れる。
   - `test_headless.js`に**checkBeam()**を追加(線上に3体並べて1発で全部削れることを確認)。
   - `test_shot.js`に`t=<タワーid>`を追加(そのタワーを建てて撃たせ続ける=発射エフェクトを撮れる)。
2. **役割の住み分け**(第25弾のスナイパーとの比較):
   - スナイパー塔 = 単体875/4.8秒 → **1体に強い**(対ボス)。DPS182。
   - レールガン = 310/3秒だが線上を全部 → **並んでいる敵に強い**。1体なら103、3体巻き込めば310。
   - 装甲相手はどちらも貫通の有無で差が出る(スナイパーは装甲に×.45、レールガンは貫通で満額)。

### 2026-07-25 第27弾(重テスラを「ゆっくり伝わって加速する連鎖」に)
1. **ユーザー指摘「重テスラってほぼテスラと同じじゃね？」は正しかった**。
   テスラコイル(⚙️340)に対し重テスラ(⚙️700・🧬622pt)は 威力1.5倍/連鎖4→7 だけで、**新しい挙動がゼロ**。
   研究所の目玉なのに「金を払った上位互換」でしかなかった。
2. **ユーザー案を採用: 連鎖の速度を遅くし、跳ねるたびに威力が加速度的に増える**。
   - 新フィールド`arcT`(1跳ねの秒数=0.1)・`grow`(1.45倍/跳ね)・`gmax`(5倍で頭打ち)。
   - 連鎖の実体は`C.arcs[]`。campStepの中で1跳ねずつ進める(砲弾`C.shells`と同じ作り)。
     `newCamp`に`arcs:[]`を追加、`resetCampWave`でクリア(前の波の連鎖が残ると居ない敵を起点に跳ね続ける)。
   - 基礎威力は 68→**15**へ下げた(定義値。ゲーム内は0.5625倍で8.4)。1発の合計は連鎖7で約185=従来の173とほぼ同じだが、
     **配分が「最初は8、最後は42」に変わる**。群れが長いほど後半が重い塔になった。
   - ⚠`gmax`が無いと⚡連鎖数Lv5(+5)+研究所⚡(+3)で15跳ね→1.45^14=約300倍となり青天井。必ず上限で止めること。
   - 見た目: 跳ねるほど電撃が太くなる(`zap`のfxに`wm`=太さ倍率を追加)。
   - `test_headless.js`に**checkCoil()**を追加(1フレームで終わらない=遅延している/4体以上に伸びる/最大が最小の2.5倍以上)。
3. **⚠時間を溶かした反省**: 上の実装後、`test_shot.js`で連鎖のスクショを撮ろうとしたが何度撮っても電撃が写らず、
   「ブラウザだけで壊れている」と疑って長時間デバッグした。**実際は壊れていなかった**。
   ヘッドレスChromeの仮想時間は`setInterval`を仕掛けると進みが鈍り、撮影時にはfx(寿命0.5秒)が消えていただけ。
   最終的に`--dump-dom`で`C.fx`の中身を吐かせ、**zap=4本・太さ1.74→2.20→2.40(上限)・被弾42/42/37/26/18/12/8**と
   数字で確認して決着した。CLAUDE.mdに「写らない=壊れている と早合点しない」と手順を追記。
4. `test_shot.js`の`t=`は**建てる瞬間だけ`unlocked`を上げて戻す**ように修正(上げっぱなしだと画面が止まる)。
