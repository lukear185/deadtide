---
tags: [deadtide, history]
aliases: [経緯]
---
# DEADTIDE 作業履歴(CHANGELOG)

見取り図=[[HOME]] ／ ルールと進め方=[[CLAUDE]] ／ 設計の明細=[[DESIGN]]

『DEADTIDE』に何をいつ入れたかの記録。**設計の正は [[CLAUDE]] の方**で、こちらは経緯だけ。
普段は読まなくてよい。「なぜこうなっているのか」を知りたい時、
過去の判断を蒸し返しそうな時だけ開くこと。新しい作業を足す時は一番下に追記する。

## 実装状況
### 完了(index.htmlに実装済み・テスト済み)
- 綱引き一式(tide.pool/shares/tideStep/pushTide/big/tide カード/ゲージUI/押すボタン/通信同期)
- タワー・ゾンビ7種・マップ描画・夜の帳・エアストライク・作戦タイム・CPU AI(建設/押し/カード)・PeerJSオンラインβ・リザルト
- ユニットの**シム層**: UNITSデータ、deployUnit()、dmgU()、ゾンビの近接攻撃(atk/ar/acd、足止め窓 gap∈[-6,36])、ユニットの攻撃(type: melee/bullet/fire/pierce/blast、aoe/burn/multi対応)、drawUnit()、renderCampでの描画+HPバー、sfx.dep
- ヘッドレステスト(test_headless.js)グリーン: 1v1と三つ巴が完走、シェア合計100維持、ユニット出撃・交戦・死亡確認済み

### 未実装(これが残タスク。上から順に)
**なし。旧残タスク1〜8はすべて実装済み(2026-07-24、下記参照)。**

### 2026-07-24 実装分(Claude Code・各タスク毎に個別コミット済み)
1. **集結旗**: flagD(初期PLEN*.55)/projPath(x,y)/ユニットはflagDへ双方向移動(射撃系rng>60はflagD+70待機、後退時はu.backで向き反転)/旗描画(緑ペナント)+照準プレビュー/🚩ボタン+FLAGAIMモード(🎯AIMと相互排他)/CPUは.45+ag*.06、コア半分以下で.65に後退。
2. **足止め上限**: 毎tick u.eng=0→eng<3のみ足止め対象、確保でeng++、溢れは素通り。
3. **出撃デッキUI**: #deck(下中央)8ボタン=UTHUMBSサムネ+コスト+CD(conic-gradient)+未解放🔒。actDeploy(ui)、client楽観更新。
4. **新部隊解放カード**: kind 'uun'(priceOf/doPurchase/ivRender/actBuyラベル/CPU plan重み2.8)。次兵科名+説明表示。
5. **CPU出撃**: cpuTickで盾1+近接2+射撃2維持(+wave/5で増員)、scrap×res余裕時のみ、タワー建設より後・強化より先。
6. **通信**: packLaneに us/fd/uu/dp、normMeに uUn+ucd保持、clientCampにユニットeidマップ(um)補間+旗同期、hostDataに 'un'/'fg'(clamp済)、meV既定uUn:2、client側ucdは自前減算。
7. **文言**: WAVE1 toastに部隊/旗の2本追加、あそびかたに部隊・旗・足止め上限の段落、リザルトに「部隊出撃」(dep/dp)。
8. **バランス確認**: テストに旗ランダム移動+uun購入を追加、決着分数と回収⚙️/uUnを診断出力、10分超はWARN。3回実行で2試合完走・全て10分以内・ユニット機能(dep>0・交戦・死亡)を確認。ブラウザ実起動で出撃/旗移動の操作も確認済み。

### 2026-07-24 大改修(永井さんのチャット直接指示=最優先の設計変更として実装)
> ⚠この節は「ゲーム概要(v4系)」より新しい。矛盾したらこちらが正。
1. **ソロ=CPU対戦廃止→ステージ制PvE**。1拠点でWAVE20(STAGE_W)を守り切ればステージ1クリア。まずステージ1のみ。専用リザルト/チュートリアル/対人UI非表示。
2. **対戦(オンライン)=敵盤面の可視化**。ミニパネルに他プレイヤー2人のライブ小窓(簡略ドット)+タップでフル観戦(SPEC)。もう一度タップで戻る。
3. **兵科8→12種**。追加=擲弾兵(blast)/冷凍兵(frost=範囲slow)/医療兵(heal=新メカニクス)/ミニガン(高速連射)。drawUnitはid分岐。UNITP12段。
4. **協力プレイ(最大3人Co-op PvE)**。共有フィールド1枚+共有コア(50+20/人)。各自の出撃ポイント(P1〜P3テント+短い合流レーン=BRANCH)から自分のユニットを出撃。⚙️/解放/火力増強/旗(色分け)=各自。タワー=共有スロット(建てた人の⚙️・売却は本人のみ)。撃破⚙️は全員分配(COOP_PAY)。ロビーでモード選択(⚔対戦/🤝協力)。
5. **難易度5段階(D5表)**=新兵/兵長/古参/鬼軍曹/悪夢。HP/物量/速度/ボス/初期⚙️/報酬倍率を個別調整。ソロ/協力共通。協力は人数スケール(量1+.4/人・強さ1+.08/人)。
6. **演出強化パス1**=マズルフラッシュ+リコイル/ゾンビ突進モーション/血しぶき粒子/凍結❄表示/ボス入場演出/旗ポップ/回復フロート/新SFX(heal/frost)。※美的作業=永井さんが理想と言うまで反復対象。
7. **スマホ3台**=viewport/セーフエリア/タッチサイズ/小画面調整は済み。**3台実機のPeerJS接続確認は未**(GitHub Pages等へ置いて確認する)。
8. **永久強化=研究所(ソロ限定ローグライト・真ロック式)**: ソロの勝敗で研究pt獲得(WAVE×10+撃破×0.5×難易度係数)→タイトル→ソロ設定の🔬研究所でタワー枠/兵科枠/新砲撃(絨毯・凍結・ナパーム)/初期⚙️/コアHPを永久解放。未解放はゲーム内解放カードに出ない(初期=タワー3種/兵科4種/🎯のみ)。装備砲撃はソロ設定画面で選択。localStorage(dt_meta)永続。
9. **部隊の段階進化**: 部隊Lv3=強化装備/Lv5=進化(犬→ドラゴン)/Lv10=最終形態(金色ドラゴン+オーラ)。出撃コストはLvに比例。
10. **その他確定**: 建設マス22・部隊上限40(協力各20)・ウェーブ放棄🏳(ソロ)・演出バランス弱中強・WAVE3まで解放ロック・タレット個別強化(攻/連射/射程)。

### 2026-07-24 最終バランス調整(永井さん指示「全体的に見て最終調整」で実施)
計測方法=チート無しの「堅実プレイ」ボット(scratchpadのbalance_probe.js相当)で全難易度実走。
- **PvE後半緩和**: W10以降の成長率をPvEのみ1.17→1.11・物量基礎をPvEのみ(6.5+w*1.6)に。新兵hp.68/cnt.8/pay1.2、兵長hp.84/cnt.92/pay1.08に緩和。
- **確定カーブ(堅実プレイbot実測)**: 新兵=クリア(残コア27)/兵長=W19/古参=W17/鬼軍曹=W11/悪夢=W5。botは工房不使用の控えめプレイ=人間は+2〜4波上振れ想定。狙い=新兵は誰でも・古参は上手いプレイでクリア・悪夢は極限。
- **対戦の決着促進**: W8以降に追加成長×1.1^n(対戦のみ)・取り分の自動均等化0.6→0.45%/秒(押した優位が残る)。人間同士の試合時間を締める目的。
- **CPU貯金(対戦の空き枠CPU/切断引き継ぎ用のみ)**: 新部隊解放費を貯金してから出撃に使う=CPUもuUn3〜5まで解放するように。※CPU戦モード自体は廃止済み(ソロ=PvE)なので影響は対戦の空き枠埋めだけ。
- 既知: 対戦テスト(CPU代理)は13分前後。人間は押し合い/カードで加速するため実測は実機待ち。三つ巴guard上限30000の偽FAILリスクは残置(乱数依存・必要ならシード化)。

### 2026-07-24 追加改修(第2弾・チャット直接指示)
1. **強化ポイント🔩の独立**: ウェーブ制圧で入手。タワー個別強化(stCost)と部隊レベル(prUup)は🔩払いに変更。⚙️は建設/出撃/解放専用。放棄時は🔩半減+工房の残り生産を精算(ecoCompensate)。
2. **スロット解放制**: 通常22枠は初期6枠(SLOT0)・prSlot=200×1.3^n。廃品工房は左上ECO_BASE〜の3枠(prEco=450/1100)。CPUも空き枠が無ければ解放を買う。
3. **種類2倍**: タワー20種/兵科30種。基本はタワー10・兵科6のみで、残りは研究所解放(8割メタ化)。基本兵科は価格順に自動ソート。
4. **時間帯表現**: 「暗くなって見づらい」を廃し、**画面の縁と空を朝→昼→夕→宵→夜に色変化**(TOD_STOPS/todCol、CSS背景も追従)。フィールドの暗転は最小(1→20波で約21%)。太陽/月/星も追加。
5. **見た目オーバーホール(3段)**: ①動的ライティング(闇を光で削る方式)+地面/道路の多層質感 ②統一パーティクル基盤(火花/煙/薬莢/血だまり/土埃/残り火、FXLVとモバイルで自動間引き) ③**スプライト焼き込み**(歩行4コマをキャラ実寸のキャンバスへ焼く。ゾンビ110体で3.2倍速・約3.8MB)+全キャラの接地影。
6. **UI/操作**: デッキに兵科名表示・長押しで部隊強化・🏳放棄は確認ダイアログ・マルチ中も⏸でタイトル復帰・協力ではミニ盤面を非表示・研究所のスクロール位置維持。
7. **マルチ修復**: 参加側に演出/パーティクルが出ない問題(ホストから配信)・3人目の操作が無視される問題(コマンドに席番号jを付与しconnを再紐付け)・協力の黒画面フリーズ(minisUpdのnull参照+rAFのtry/catch化)。
8. **iOS対策**: 連打でタップ不能になる問題(ダブルタップズーム抑止・touch-action個別指定)。PWA化(manifest/SW/自作アイコン)で全画面+横画面。

### 2026-07-25 大型アップデート(編成システム・チャット直接指示)
1. **🎖編成(ロードアウト)**: ソロ設定画面に「🎖編成」ボタン。作戦タイムで解放できる**7つの枠**それぞれについて、基本キャラか派生キャラかを選ぶ。弾薬もここで装備。保存=`META.ld`(枠→派生id)/`META.amEq`。**協力/対戦では無効**(素の兵科・通常弾で統一)。
2. **派生キャラ33種**: 基本7兵科×3段階(例 猟犬→狼→ケルベロス→フェンリル)＝21種。**+2026-07-25: 研究所限定の上級4種(装甲車/メカスーツ/タイタン/ヴァルキリー)にも姿が変わる3段階を追加=12種**(FVBASE・adv:1)。編成画面の「🔬上級ユニット」欄で切替、drawUnitはvtで肩キャノン/第2砲/冠/翼増を描き分け。実体は`UVAR`の倍率テーブル+`mkVar()`で生成する派生定義(`U.base`に元id、`U.vid`/`U.vt`で見た目分岐)。コストは微増(×1.2/1.45/1.75)。研究所で1体ずつ解放(LAB_UV=90×1.13^k・全21)。見た目=犬系は専用描画、人型は段階ごとに鉢巻→兜+肩当て→マント+角。
3. **弾薬アップグレード**: 通常弾→ビーム→プラズマ弾→崩壊弾(`AMMO`)。**実弾(bullet)タワーの弾に反映**=威力×1.18/1.42/1.75、プラズマ以上は装甲貫通、崩壊弾は着弾点に小範囲(45%)。研究所で240/460/760。
4. **新兵科『鎖使い』(基本4番目・⚙️105)**: `hook:1`。**すり抜け(noblock)の敵を唯一足止めできる**。派生=捕縛兵/大網使い/鎖鬼。
5. **全兵科のバランス**: HPを一律2倍・**射程100以上(遠距離)の攻撃力を1.5倍**。基本兵科は6→7種、総数31種(基本7+研究所24)。UNITP解放価格表は自動延長。
6. 実装メモ: `UBASE`=素の定義配列(不変)。`applyLoadout(on)`が`UNITS[i]`を差し替え+`SPR.u`クリア+`makeThumbs()`再生成。ソロ開始時のみ`on=true`。

### 2026-07-25 第2弾(チャット直接指示)
1. **部隊の段階進化を廃止**: Lv3/5/10の姿変化(犬→ドラゴン→金竜)を全削除。部隊レベルは**数値強化のみ**(+10%/Lv)。`u.evo`/`UTHUMBS_S`/`DECKEVO`/bakeUのevoキーを撤去。
2. **マルチの開始通知を保証**: ホストは`P.st0`を保持し、**受領確認(`{t:'ok'}`)が来るまでスナップショットに`st0`を同梱**。クライアントは`st`未受信でも`sn.st0`から`clientStart()`で復帰。コマンド受信でも`stAck`を立てる。クライアント側に**4秒以上更新が来ないと警告トースト**+再ack。3人目が開始通知を取りこぼして操作不能になる事故への対策。
3. **フィールド拡大**: スマホのHUD/デッキを小型化(パネルpadding・フォント・バー幅・カード48px)。fitCanvasに**3つ目の制約**(マップ全体がボタン帯より上に収まる)を追加し、SKYHクロップ時に下へはみ出すバグを修正。表示倍率 0.394→0.430(面積+19%)。**HUD/デッキ/砲撃ボタンと全25スロットの重なりゼロを実測で確認**。
4. **出撃コストの視認性**: `.dc`が`--paper`(淡色)×`--pane`(淡色)で読めなかった → **カード下端の黒帯に金文字(11px)**へ。
5. **発砲光と時間帯の分離**: 光は「闇をくり抜く」方式のみだったため夜色と同色に見えていた → くり抜き位置を`glow[]`に控え、**加算合成で暖色(電撃だけ青白)を重ねる**。昼夜どちらでも「撃った光」と分かる。
6. **研究所に序盤・中盤系を6種追加**(いずれも1波目から効く): 🔩開始強化pt+6/🤝開始部隊枠+1/🧱建設スロット初期+1/💰撃破報酬+6%/🏭工房産出+12%/⏱出撃CD-6%。研究所の項目は**価格の安い順に並べ替え**て序盤に届くものを上に出す。META: up0/uu0/sl0/py0/ec0/cd0。効果はソロのみ(`soloMeta()`ゲート=metaPay/metaCd/metaEco)。

### 2026-07-25 第3弾(チャット直接指示)
1. **廃品工房つき開始**: 研究所`🏭 廃品工房を建てた状態で開始`(META.ec1・220pt)。startSoloで`me.towers[ECO_BASE]`に工房を直接配置。※工房は元から**タップで個別強化可能**(twStats=`['d','r']`=⚙️産出/⏩サイクル、stCost払い)=既存仕様を確認。
2. **系統強化(研究所)**: タワーは型ごと(TLINE: **bullet**/eco/fire/net/blast/elec=全6型網羅)、兵科は型ごと(ULINE: melee/bullet/pierce/blast/fire/frost/heal=全7型網羅)に各Lv3。⚠当初bullet型タワー(最多。base+研究所解放のrail/gat/fort等)を入れ忘れ→2026-07-25追加。**研究所で解放した新型タワー/新兵科にもそのまま乗る**(型判定なので)。META.tl{}/ul{}、tlv()/ulv()はsoloMetaゲート。効果=タワー威力+10%/段(+型別: eco産出+15%/fire燃焼+25%/net減速+20%/blast範囲+12%/elec連鎖+1)、兵科HP+8〜10%・攻撃+12%/段。価格LAB_TL/LAB_UL=140/110×1.55^lv。
3. **新登場の紹介モーダル(md-intro)**: ソロで新解放タワー/兵科・**初登場のゾンビ**をウェーブ開始直後に画面中央で説明(絵+名前+ステータス+特性)。表示中`PAUSED=true`でゲーム停止、OKで次→全部閉じると再開。解放は`G.introQ`にpush(doPurchase unlock/uun)、敵は`G.seenZ`で初出だけ。⚠ヘッドレステストはOK待ちで固まるので**frames()内でPAUSED&&INTROQなら即introNext()**で自動送り。
4. **終盤の敵: 数減・質増**: buildTideのnにW12〜taper(-0.5/wave)・下限8、増加係数.03→.016、lateX 1.035→1.055。1体HPは据置き成長だが**bt(報酬)にW12〜+5%/wave**。画面が埋まりにくく、撃破1体あたりの旨味を上げた。
5. **ボスの削り報酬**: dmgZでボスに、削ったHP割合×(bt×pay×0.6)を**先払い**(z.bpaid累積)。倒しきれなくても削った分は入る。killZのとどめ報酬はbt×pay−bpaidで二重取り防止(協力も同様)。

### 2026-07-25 第4弾(チャット直接指示)
1. **射程強化のプレビュー**: タワーパネルを開くと、現在の射程(オレンジ点線)に加え**強化後の射程を薄緑の輪+塗りで表示**(renderCamp SEL>=0)。📡射程ボタンにも「現在→強化後」の数値(例 832→917)を併記。
2. **射程強化の拡大率アップ**: twRngMの1段あたり .08→**.13**(RNG_STEP)。twRngMlv(g)で任意Lvの倍率を計算(プレビュー用)。

### 2026-07-25 第5弾(画面レイアウト最適化・スマホ実機の指摘「両サイドと下部が空いて盤が浮いて見える/兵科名が読めない」)
1. **下の操作バーに集約(#abar)**: 左下に浮いていた🚩集結旗・🎯航空支援と、右下の🌊押すを**出撃デッキと同じ一列**に入れた(`#abar`=画面下端のflex行)。フィールドの上に浮くボタンがゼロになり、そのぶんマップを下まで広げられる。ヒント文とpushtgtはバーの上に固定。`.spec #abar`で観戦時は非表示。
2. **フィット計算の作り直し(fitCanvas)**: 旧「マップ全体(空込み)がボタン帯の上に収まること」を撤廃。**空(SKYH=108)は上へはみ出してよい・ただし地平線の帯(SKYKEEP=60)は必ず残す**方式に変更し、地面を最大まで拡大。上のHUD用の予約(hudTopPx)は廃止(空がHUDの裏に入るため不要)。対戦のミニ盤面ぶんは`minisResPx()`で右に余白を取り重なりを回避。帯の実測は`barBandPx()`(#abarを測る)。
3. **地面の裁ち落とし(bleed)**: renderCampで画面に映る論理範囲(VL/VR/VT/VB)を計算し、**荒野・空・地平線の影を画面の端まで描く**。額縁色の余白が消えて「盤が浮いている」見え方を解消。廃墟のシルエットも左右に4棟追加。
4. **工房エリアの看板を枠の下へ**: マップ上端が上部HUDの裏に入るため、`ey0-53`→`ey0+63`。
5. **デッキのカード拡大+兵科名を専用の黒帯へ**: カード58×60(PC 60×62)・アイコン26。**兵科名は下から2段目の黒帯に白文字12px**、その下がコストの黒帯(金文字11px)。淡色カードに黒文字だと潰れて読めなかったため帯方式に変更。
6. **⚠CSSの順番の罠(実機で名前が見えなかった真因)**: `@media (pointer:coarse)`のスマホ用指定を**`#deck`の定義より前**に書いていたため、同じ強さ(#id+.class)の後ろの指定に負けて**全部無効**だった(カードもボタンもPC用サイズのまま動いていた)。デッキ/操作バー系のスマホ指定は**`#deck .dk:disabled`の直後の第2ブロック**へ移動。上のブロックにはHUD系(=定義がそれより前にあるもの)だけを残す。
7. **⚠裁ち落としの継ぎ目(実機で余白に見えた真因)**: 地面は端まで描いていたが、その後の**時間帯の色被り・夜の陰・ビネットが`0,0,MW,MH`のマップ矩形にだけ**掛かっていたため、マップの四角い縁がくっきり出て「盤が浮いて」見えていた。3つとも画面の可視範囲(VL/VT/VR/VB)へ拡張。地面の模様(DOTS/PATCHES/TUFTS)も撒く範囲を画面外まで広げ数を増量。
8. **効果(iPhone横 852×393・セーフエリア左右59/下21で計算)**: 表示倍率 0.409→**0.4637**(+13%)・フィールド 654×311→**742×352(面積+28%)**・左右の余白は各55pxだが地面が続くので縁は見えない。SKYKEEP=36。全25スロット・コア・侵入口・道路がHUD/操作バーと**重なりゼロ**。
9. **`test_layout.js`を追加**: 端末サイズを与えてfitCanvasを実走し、スロット/コア/道路とUIの重なりを自動判定する簡易チェック(ゲーム本体からは参照しない開発用)。⚠測定関数は起動400msは既定値を返すので、テスト側で`NOW`を進めてから呼ぶこと。

### 2026-07-25 第6弾(建設マスの整列・ユーザー指摘「まばらに見える。整列させて道路の近くに」)
1. **SLOTS(通常22枠)を全面的に置き直し**: 道路の中心線から**きっかり95**の位置に、**道なりの順**で並べる(道路半幅49+マス半28=77 → 18px空く)。結果、各直線区間ごとに「道路に沿った列」ができる。実測=道路までの距離 最短95/最遠112、マス同士の最短110。
2. **配置の除外条件**(次に動かす時もこれを守る): 拠点150 / 工房枠135 / 協力の出撃テント(BRANCH y=742)120 / コアHPバーの表示位置(x<270・y400〜520) / マス同士110 / 画面内(x75〜1560・y205〜730=上部HUDと下部バーを避ける)。
3. **AI_ORDER(=SLOT_ORDER)を作り直し**: 先頭6つ(初期解放)を道なりに散らして、最初から全域を守れるように。残りはCPUの建設優先順。
4. **⚠test_headless.jsの初期建設スロットも更新**: 旧`2,1,5`は初期解放から外れたため`2,8,12`へ。**解放されていない枠に建てても無視される**ので、テストが建てる枠はAI_ORDERの先頭6つから選ぶこと。
5. **参考画像**: ユーザーから「フィールドの感じはこれ」とAI生成のモック(砂漠の実写調・盤面が画面いっぱい)を受領。**見た目の本格化は後回し**で、**画面いっぱいに地面が続く構図だけ**が今回の目標。

### 2026-07-25 第7弾(右上の道路と上のHUDの重なりを解消)
1. **🏳放棄と🔊ミュートを上のHUDから下の操作バーへ移動**: 上のHUDが横に長く、右上の道路(PATH y=150・x1100〜1660)の上端(論理y=101→画面の上のほう)に被っていた。上に残すのは**WAVE/コア/⚙️/🔩/⏸まで**。放棄の文字は`🏳 放棄 -n`→`🏳-n`に短縮。
2. **スマホのHUDをさらに小型化**: gap5→4・panel padding7→6・進行バー52→42・コアバー46→38。上のHUDの右端が約486pxに収まり、道路の左端(画面x約541)まで**55pxの余裕**ができた。
3. **判定を`test_layout.js`に追加**: 道路(半幅49+路肩=52)が上のHUDと下の操作バーに潜っていないかを経路10刻みでサンプルして判定。
4. ⚠**残件**: 対戦の死潮ゲージ(#tidebar・画面上中央)は同じ帯にあるため右上の道路と被る可能性がある。対戦の画面を詰める時に一緒に直すこと。⏩早送りボタン(自分が陥落した時だけ出る)も出ると数px被る。

### 2026-07-25 第8弾(デッキの絵が豆粒問題+初期解放マスの調整)
1. **サムネを「絵のある範囲」に合わせて焼き直す(`bakeUThumb`)**: 従来は88px枠の中に小さく描いていたため、カードで26pxに縮めると人が豆粒で姿が分からなかった。**大きめ(180px)の下絵に描く→`getImageData`でインクの外接矩形を測る→枠いっぱいに縮小して88pxへ焼く**方式に変更。全兵科・派生に自動で効く(将来追加分も)。⚠測れない環境(ヘッドレステスト)はtry/catchで素通りする。
2. **カードとアイコンを拡大**: スマホ 58×62(アイコン30)・PC 60×66(アイコン32)。
3. **初期解放マスを6→7へ(ユーザー指示)**: 追加=**1(1370,245 右上・入口の道と中央の道を同時に見る良位置)**と**4(1005,320 縦道沿い・手薄だったS2区間をカバー)**。除外=**21(530,640 左下の終盤)**→最初の購入対象(AI_ORDERの8番目)に。`SLOT0=7`。
4. 初期7枠=2/1/4/8/12/16/19。テストが建てる2・8・12は初期解放のままなので変更不要。

### 2026-07-25 第9弾(「姿が見えない」の真因+実画面スクショ環境の獲得)
1. **★`test_shot.js`を追加=見た目を実際に確認できるようになった**: ヘッドレスChrome(無ければEdge)でindex.htmlを実起動し、ソロを自動開始してPNGを撮る。`node test_shot.js out.png 852 393`。一時HTMLはOSのテンポラリに作るのでリポジトリは汚れない。**以後、見た目の修正は必ずこれで撮って目視してから報告すること**(数値だけの確認では今回の不具合を3回見逃した)。⚠ヘッドレスでは`(pointer:coarse)`が効かず**PC用のCSSで描画される**(カードは62×78)。スマホ実機は58×72。
2. **「姿が見えない」の真因は2つ**: ①**未解放カードの🔒が全面に被さっていた**(`.lk{inset:0;background:rgba(0,0,0,.55)}`+中央に大きな錠前)。7枚中5枚が未解放なので、ほとんどのカードで兵科の絵が見えない状態だった。②絵の欄が32pxしかなく、カード幅60pxの中で小さかった。※サムネの焼き直し(第8弾)自体は正しく動いていた。
3. **修正**: 🔒は**右上の小さなバッジ**にして絵は隠さない+未解放カードは`.lkon`で**絵を灰色**にして区別。サムネを**横長104×78**にして(犬は横幅・人型は高さいっぱいになる)、カードの絵の欄を**52×39(PC 56×42)**へ拡大。カードは58×72(PC 62×78)。
4. **代償**: 操作バーが高くなった分フィールドは 737×350 → **714×339(-3%)**。ユーザー了承済み(「ユニットのボタンの枠広げてもいい」)。

### 2026-07-25 第10弾(諦めるボタンの文字+HUDの伸縮を止める)
1. **「諦める」を文字で表示**: 操作バーのボタンを`🏳 諦める -n`に。用語も**放棄→諦める**へ統一(確認ダイアログの見出し/はいボタン/あそびかた/バナー/強化ptのトースト)。※コード内の変数名(ffwave/giveUpDmg)とこの文書の見出しは従来どおり。
2. **WAVEパネルの幅を固定**: 敵の数が1桁↔2桁、💀(5の倍数ウェーブ)の有無で幅が変わりHUDが伸び縮みしていた → `#wavebadge`に**最長表示ぶんのmin-width**(スマホ154px/PC 192px)+`tabular-nums`+左寄せ。**5パターン(1桁/2桁/💀つき/敵表示なし)すべてで幅216px・HUD右端455pxで一定**なのを実測で確認。
3. **`test_shot.js`を改良=スマホ用CSSを強制適用**: ヘッドレスChromeでは`@media (pointer:coarse)`が効かずPC寸法で撮れてしまうため、そのブロックを条件なしCSSとして後ろに足すようにした(`pc`引数でPC表示も撮れる)。**これで実機と同じ寸法の絵を撮って確認できる**。
4. 実測値(スマホ852×393・セーフエリア左右59/下21): HUD右端514 / 道路の左端537(27px余裕) / 操作バーの中身711 vs 使える幅734。

### 2026-07-25 第11弾(初期ユニットが強すぎる=基本7種だけ下方修正)
1. **基本兵科のうち5種**: HP倍率 2倍→**1.5倍**(実質-25%)、攻撃力**-12%**。⚠**最初から持っている猟犬とバットの2体は元の強さのまま**(一度下げたが「戻して」との指示で復帰)。研究所で解放する上位兵科・派生・上級4種も**据え置き**=解放して強くなる実感を残す。実装は加工ループの`const bas=i<BASE_U&&['hnd','bat'].indexOf(UNITS[i].id)<0`。
2. **現在の基本7種**(出撃コストは変更なし): 猟犬HP156/攻14・バット312/22(=据え置き)、斥候135/25・鎖使い414/27・ガンナー153/32・斧使い378/46・盾持ち792/8(=下方修正済み)。
3. **効果**: ボット計測ではウェーブ到達は15〜16でほぼ変化なし。**代わりに出撃回数が111→186に増えた**=部隊が溶けやすくなったぶん⚙️の消費が増える形。壁として置きっぱなしにできなくなった。
4. ⚠実機の体感が「まだ強い/下げすぎ」なら、この係数(1.5と0.88)だけを動かせばよい。

### 2026-07-25 第12弾(全31兵科を「解放順に強くなる」よう総調整)
1. **★一括倍率を廃止=UNITSの数値がそのままゲーム内の値**: `UBUF`/ティア補正/HP2倍/遠距離1.5倍/コスト÷2.5の加工ループを削除し、**最終値を定義側に焼き込んだ**。以後、強さの調整は`UNITS`のhp/atkを直接いじる。⚠`uTier`と`TSCALE`(見た目の大きさ・デッキ枠の色)はループとは別物なので残してある。
2. **戦力の定義**: `(実DPS)^0.6 × (HP)^0.4 × 役割ボーナス`。実DPS=攻撃力÷間隔×(複数同時は割引 1+(multi-1)*0.6)×(範囲 1+aoe/220)+燃焼×0.8。役割ボーナス=**凍結2.2/回復2.0/壁(shd,gld,tnk,titan)1.45**(数値に出ない価値の補正)。⚠壁の判定はHPやDPSの閾値でやると強化した瞬間に評価が跳ねるので**IDで固定**する。
3. **目標曲線**: 猟犬(0)とバット(1)は据え置き、**バットから末尾まで1段ごとに約+7%の等比**。全31種が目標±1%に収まり、**逆転ゼロ**を`test_balance.js`で確認。
4. **主な変化**: 大きく上がった=火炎瓶/擲弾兵/冷凍兵/スナイパー/迫撃兵/ミニガン/ロケラン/レール狙撃/絶対零度(解放費に対して弱すぎた)。下がった=**チェーンソー(-36%)**と**タイタン(-33%)**(順番の割に強すぎた)。据え置き=猟犬/バット/短機関銃/長槍兵/隊長/ヴァルキリー。
5. **`test_balance.js`を追加**: 現在の戦力・目標・ずれ%を一覧し、逆転があれば警告する。**数値を触ったら必ず流す**。
6. 派生兵科(UVAR)は基本兵科への倍率なので自動追従。出撃コスト・解放費(UNITP)は変更なし。

### 2026-07-25 第13弾(★ステージ2「沈んだ港」を新規追加=ステージ制の土台)
1. **ステージ表(STAGES)を新設**: 1ステージ=`{n,sub,zs,path,slots,order,pal}`。`loadStage(i)`が経路(PATH/SEG/PLEN)・建設マス(SLOTS)・解放順(AI_ORDER/SLOT_ORDER)・拠点(CORE)・地面の模様(makeScenery)を差し替える。⚠**建設マスの数は全ステージ22で固定**(ECO_BASEが動くと保存や配列の形が壊れる)。廃品工房の3枠(ECO_SLOTS)と協力の出撃テント(BRANCH)は共通。
2. **ステージ2「沈んだ港」**: 死潮が引いた後の港。道は右下から入って上へ回り込み、拠点は左下。地面/廃墟/草の色を青灰色に差し替え(`pal`)。マス22個は道路から95で整列(test_layoutで両ステージとも重なりゼロを確認)。
3. **敵=ゾンビ化した海の生き物11種(`st:2`)**: 腐れガニ/跳ねイワシ/甲殻ロブスター/膨張フグ(分裂)/鳴き貝(加速オーラ)/咬みウツボ/這いヒトデ(すり抜け)/溶けクラゲ(すり抜け+装甲)/深海アンコウ/影サメ/**触腕の主(ボス)**。役割はステージ1のゾンビと1対1で対応させ、強さもほぼ同値=難易度カーブを引き継ぐ。drawZombieに専用の描画を追加。
4. **敵プールの分岐**: `ナイトメア=獣(nm) / ステージ2=海(st:2) / それ以外=ゾンビ`の完全別プール。ボスも`ST2_BOSS_ZI`で分岐。⚠**分裂(split)で出る小型が常にウォーカー固定だったバグを修正**=同じ生態系の最弱個体を出すようにした(ステージ2にゾンビが混ざっていた)。
5. **ステージ選択UI**: ソロ設定画面の一番上に追加。**前のステージをどれかの難易度でクリアすると次が解放**(`META.sclr[]`)。未解放は🔒で押せない。選択は`META.stg`に保存。協力/対戦は`hostStart`/`clientStart`で必ずステージ1に戻す(地形の食い違い防止)。
6. **テスト**: `test_headless.js`に**ステージ2の疎通確認**(海の敵しか出ないこと・1体も出ないなら失敗)を追加。`test_layout.js`は全ステージのマスと道路をUIと突き合わせる。`test_shot.js`は`st2`引数でステージ2を撮れる(例 `node test_shot.js s.png 852 393 st2`)。

### 2026-07-25 第14弾(作戦タイム延長+設置猶予+中断/再開+海の敵に腐敗表現)
1. **作戦タイム15秒→20秒**(`IV_SEC`)。
2. **ウェーブ開始から10秒は敵が出ない設置猶予**(`WAVE_LEAD`)。buildTideで`T.lead=10`+最初の出現を10秒後ろ倒し。HUDのWAVE欄に**🔨残り秒**を表示(敵が出始めると🧟表示に戻る)。※1ウェーブあたり約15秒延びるので、通し時間は20波で+5分ほど。
3. **★中断と再開(ソロ専用)**: `localStorage['dt_run']`に保存。**作戦タイムのたびに自動保存**+⏸メニューの`💾 中断してタイトルへ`でその場保存。ソロ設定画面に`▶ 続きから — <ステージ> WAVE n / <難易度>`が出る。保存するのは「ウェーブの区切りの状態」=中断したウェーブは最初からやり直し(敵の位置は保存しない)。復元するもの=ステージ/難易度/波/コア/⚙️/🔩/解放タワー数/部隊枠/部隊Lv/工房枠/マス解放/**建てたタワーと個別強化**/装備砲撃/旗の位置。決着(awardMeta)と`やめる`で記録を消す。⚠`防衛開始`では消さない(押し間違い対策)。
4. **海の敵に腐敗表現**(「ゾンビ感が無い」の指摘): 共通ヘルパー`rotMark()`で**黒ずんだ腐れ+露出した骨(白)+したたる粘液**を全11種に重ねた。生き物らしい明るい色6種を腐敗色へ寄せた(腐れガニ/跳ねイワシ/甲殻ロブスター/膨張フグ/鳴き貝/這いヒトデ)。
5. 検証: 中断→再開は実ブラウザで往復テスト済み(scratchpad/resumetest.js相当)。⚠ヘッドレステストのNode環境にはlocalStorageが無いので保存系は検証されない。

### 2026-07-25 第15弾(各ステージに最終ボスを追加)
1. **最終ウェーブ(WAVE20)だけに出る専用ボス**を`fin:1`で定義。5波ごとの通常ボスとは別物。
   - ステージ1 **屍山の王**(hp2600/報酬900/薙ぎ払い7体/装甲/**周囲を加速させるオーラ**/sc2.6)=融合した屍の山。3つの顔と鉄筋の冠。
   - ステージ2 **深淵の母**(hp2600/報酬900/薙ぎ払い6体/装甲/**倒すと仔が3体這い出る(split)**/sc2.6)=光る卵嚢と大顎。
2. 選び方は`buildTide`: `wave%5===0`のとき、**PvEかつwave>=STAGE_Wなら`FIN_ZI`/`FIN2_ZI`**、それ以外は従来のボス。⚠ナイトメアは従来どおり肉塊の主(獣プールの最終ボスは未作成)。
3. `mw:20`にしてあるので通常の雑魚プールには絶対に出ない。紹介モーダル(初出の敵)には自動で載る。
4. `test_headless.js`に**WAVE15とWAVE20のボスが別物であること**の検査を追加。

### 2026-07-25 第16弾(火炎系タワーを装甲特効に)
1. **fire型タワーは装甲持ちに`FIRE_VS_ARMOR=1.9`倍**(直撃も燃焼も)。燃焼時間も装甲相手だけ+1秒。従来は「装甲を無視する(0.45倍にならない)」だけだった。
2. 対象=**火炎放射塔**と**酸噴射器**(fire型タワー2種)。説明文も「装甲に特効(約1.9倍)」へ。⚠**火炎系の兵科(火炎瓶/チェーンソー/火炎放射兵)は据え置き**=兵科は戦力カーブ(第12弾)で管理しているため。必要なら同じ規則を入れる。
3. 目安(強化なし): 火炎放射塔の装甲持ちへの秒間ダメージ 約71→約135。同じ相手にライフル櫓は約18(装甲で0.45倍)なので、**装甲相手の専門家**という性格がはっきりした。装甲持ち=アーマード/スライム状体/ブルート/重装兵/ボス全般/海のロブスター・クラゲ・アンコウ。

### 2026-07-25 第17弾(基本兵科に壁役「巨漢」を追加=基本7→8種)
1. **巨漢**(id`big`・出撃26・cd7): HP900/攻11/間隔1.2/射程40/足44。**バット(24)の次・斥候(28)の前**に入るようcostで位置決め(⚠一度「猟犬とバットの間」に置いたが指示変更で移動。開始2枠が猟犬+巨漢になってしまうのも解消)。役割は純粋な壁=火力はほぼ無い代わりに硬い(バットの約3倍のHP)。見た目は一回り大きい体格+太い腕と拳(武器なし)。
2. **派生3段階**: 用心棒(HP1305)→人間戦車(HP1800)→不動の巨神(HP2520)。倍率は盾持ち系と同じ1.45/2/2.8。
3. **BASE_U 7→8**。VBASEに`big`を追加(編成画面の枠も8つに)。UNITPの解放費は位置ぶんずれる。
4. 開始2枠は従来どおり**猟犬+バット**。巨漢は最初の解放対象(160⚙️)。
5. ⚠**操作バーの幅**: 基本8種になったのでスマホでカード54px・gap3px・丸ボタン52pxへ縮めた(実測 中身730 vs 使える幅734)。**これ以上兵科を増やすと横スクロールになる**ので、増やす時は幅を再計算すること。
6. `test_balance.js`のTANK判定に`big`を追加。戦力は猟犬56→**巨漢72**→バット79で逆転なし。

### 2026-07-25 第18弾(砲撃を即着弾→ロケットが飛んでくる演出に)
1. **`airstrike()`は「発射」だけを行い、着弾処理は`airstrikeHit()`へ分離**。発射時に`C.shells`へロケット弾(`rk:1,stk,wave,byMe`)を1発積み、**画面外(目標の左上640×500)から`STK_FLY=0.85`秒かけて飛ぶ**。着弾した瞬間にshellの更新ループが`airstrikeHit`を呼ぶ。
2. **見た目**: 白い機体+オレンジの弾頭+噴射炎、後ろに太い煙の尾。着弾予定地には**黄色の照準(近づくほど小さくなる)**。既存のshell(擲弾タワー)は今までどおり放物線で飛ぶ(`rk`が無いものは弧を描く)。
3. **遊びへの影響**: 0.85秒ぶん敵が動くので**置き撃ち(未来位置を狙う)が必要**になった。チャージは発射した瞬間に消費する。機関銃掃射も同じ0.85秒後に開始。
4. マルチ: shellsは既にスナップショットに座標だけ載っているので、参加側では丸い点として飛んで見える(ロケットの絵は host のみ)。

### 2026-07-25 第19弾(砲撃の飛来物を種類別に+研究所のカテゴリ分け+壁役の作り直し)
1. **砲撃の飛来物を種類ごとに変えた**(`STK_DELIV`): 標準=**ロケット**(斜め上から煙の尾)/絨毯爆撃=**爆撃機が水平に横切る**/凍結・ナパーム=**真上から落ちる爆弾**(色は種類別)/機関銃掃射=**飛来物なし=即発動**(拠点から撃つ設定のため)。描画は`sh.rk`で分岐。
2. **研究所をカテゴリ分け**(`LAB_CATS`): 🆕新しい種類を解放 / 🧬派生キャラ・弾薬 / 🎯砲撃の種類 / 📈系統強化 / 🚀開始時のブースト / 💰収入と効率。見出しには「それが何なのか」の一行説明つき。
3. **⚠値段順の並べ替えを廃止**: 買うと値段が上がって順番が入れ替わり、押した項目が別の場所へ飛んでいた。カテゴリ順+定義順の固定に。スクロール位置の維持は実測で確認(520→520)。
4. **壁役を作り直し(ユーザー指示)**: 巨漢 900→**450**(半分)、盾持ち 1235→**4500**・攻撃12→**3**(攻撃力は皆無・ひたすら硬い)。これに合わせて後続の壁も引き上げ: 重装盾2400→**6500**/装甲車3025→**8500**/タイタン3115→**10500**。
5. **⚠壁役は戦力カーブ(第12弾)の対象外にした**: 戦力式はDPS重視なので、攻撃力ゼロの壁を正しく測れない(盾持ちは-31%と出る)。`test_balance.js`は壁役(big/shd/gld/tnk/titan)を順番検査から外し、**壁どうしでHPが解放順に増えているか**だけを見る。

### 2026-07-25 第20弾(機関銃掃射が動かないバグ修正+砲撃の実行テスト追加)
1. **バグ**: 機関銃掃射を撃つと`ReferenceError: ifisMinesfx is not defined`で何も起きなかった。原因は**第18弾(コミット382f33c)**で`(C.isMe||F.isMe)`→`isMine`をスクリプト一括置換した際に**括弧ごと置き換えてしまい**`if(C.isMe||F.isMe)sfx.shotgun();`が`ifisMinesfx.shotgun();`になっていたこと(凍結爆弾も同じ)。`if(isMine)sfx...`へ修正。※当初この文書は「第19弾で」と書いていたが誤り(第21弾の調査で確定)。
2. **⚠教訓**: `node --check`は通ってしまう(未定義変数は実行時エラー)。**一括置換したら必ず「その行が実行される経路」を走らせる**こと。
3. **`test_headless.js`に砲撃の実行テストを追加**: 5種すべてについて`airstrike()`の発射と`airstrikeHit()`の着弾を直接呼び、例外が出ないか確かめる。⚠**rAFの中の例外はtry/catchに飲まれるので、着弾処理は直接呼ぶ**。バグを再現したファイルで実際に落ちること(FAILになること)も確認済み。

### 2026-07-25 第21弾(一括置換事故の総点検+`test_undef.js`を追加)
1. **置換事故の残りは無いことを確定**: 事故を起こした置換の親コミット(`382f33c^`)を調べたところ`(C.isMe||F.isMe)`は**全部で3箇所**しか無く、うち2箇所が壊れて第20弾で修正済み・残り1箇所(`if(isMine&&stk!=='frost')sfx.boom()`)は元から正しい。**この置換による未修正の壊れは残っていない**。
2. **⚠この文書の記録ミスを訂正**: 事故を起こした置換は第19弾ではなく**第18弾(382f33c)**だった。
3. **★`test_undef.js`を追加=実行しなくても未定義の名前を見つける検査**: `<script>`からコメント・文字列・正規表現を取り除き、変数として使われている名前を全部拾って、どこにも宣言が無い物を報告する。`node --check`は文法しか見ず、`test_headless.js`は「その行を通る経路」を走らせないと気づけない。**この検査は一度も実行されない分岐の中まで全部見る**ので、今回のような事故を出た瞬間に捕まえられる。
   - 使い方: `node test_undef.js`(FAILなら終了コード1)。
   - 判定: **予約語がくっついた名前**(`ifXxx`等)と**宣言が無く1回しか出ない名前**はFAIL。宣言が見つからないが複数回出る物は⚠として出すだけ(誤検出が多いため)。
   - **壊れていた版(382f33c)を食わせると`ifisMinesfx`を検出してFAILすることを確認済み**。現行のindex.htmlは⚠も含めてゼロ。
   - 誤検出だと現物を見て確認できた場合だけ`ALLOW`に足す(安易に足さない)。
   - 実装メモ: テンプレート文字列は入れ子(`` `...${x?`...`:''}...` ``)に対応し、`${}`の中はコードとして残す。クォートを含む物は正規表現と見なさない(誤判定しても文字列の判定がズレないため)。数値の`1e9`から`e9`を拾わないよう数字の直後は除外。
4. **変更のたびに流す検査は4つ**: `node --check` → `node test_undef.js` → `node test_headless.js` →(見た目を触ったら)`node test_shot.js`。数値を触ったら`test_balance.js`、画面寸法を触ったら`test_layout.js`。
5. 今回index.htmlは**一切変更していない**(検査ツールの追加と文書の訂正のみ)。既存テストは全て通る(headless/balance/layout)。

### 2026-07-25 第22弾(実機テスト全部OK+文書の分割)
1. **第20弾までの実機テスト7項目が全部OK**(ユーザー確認済み)。機関銃掃射・凍結爆弾の修正、砲撃4種の飛来演出、研究所のカテゴリ分けとスクロール維持、盾持ち/巨漢のHP、ステージ2の手応え、最終ボス2体と設置猶予10秒、中断→再開の往復——**すべて実機で通った**。
2. **CLAUDE.mdからこのCHANGELOG.mdを分離**(24,024字→約7,000字)。理由=作業のたびに第N弾が増え、その全部を毎セッションの最初に読んでいたためコンテキストを圧迫していた。
   - CLAUDE.mdに残したもの=**今の仕様・絶対ルール・コードスキーマ・テスト手順・チューニングノブ**、そして新設した**「⚠作業の時に必ず守ること」**(過去の弾ログに埋もれていた落とし穴・決まりごとを全部そこへ集約)。
   - CHANGELOG.mdへ移したもの=第1弾〜第21弾の経緯。**普段は読まない**。
   - **⚠今後、新しい落とし穴を踏んだらCLAUDE.mdの「必ず守ること」に1行足す**こと。経緯はこちらに書く。
3. **ついでに古くなっていた仕様の記述を現物に合わせて直した**: 建設スロット初期6→**7**(SLOT0=7)/兵科30種(基本6)→**32種(基本8)**/航空支援4種→**砲撃5種**(初期は🎯のみ)/部隊の段階進化は**廃止済み**と明記/MAXU=20→**40**/ドリフト0.6→**0.45%毎秒**。ステージ制・編成・弾薬・作戦タイム20秒・設置猶予10秒・中断再開・最終ボス・紹介モーダルを「確定済み設計」へ追記。

### 2026-07-25 第23弾(🌑ナイトメアの最終ボス+対戦のHUD事故修正+ステージ2の終盤が軽すぎた件)
1. **🌑ナイトメアにWAVE20専用の最終ボス「原初の獣」を追加**(id=fBeast)。これで3つのプール全部に最終ボスが揃った。
   - すべての獣の源。**装甲+周囲加速(aura)+分裂3(倒すと巨鼠が這い出る)+8体薙ぎ払い**。HP3000/速さ44/攻撃94。
   - 姿=背から仔獣の頭が3つ生え、腹に仔をぶら下げた四つ足の巨獣。角2本と巨大な顎。
   - `FIN_ZI`の条件に`!z.nm`を足し、`FINNM_ZI`を新設。ボス選択は `nmOn?(fin?FINNM_ZI:NM_BOSS_ZI):…`。
   - **⚠この作業で見つけた罠**: ゾンビはスプライトに焼かれる(bakeSet)ため、描ける局所座標は`|x|≦54・y≧-105`まで。
     最初は顎をx=94まで伸ばして**無言で切り落とされた**(画面では顎だけ消える)。CLAUDE.mdに追記済み。
   - `test_headless.js`に**ナイトメアの実走テスト**(獣以外が混ざらないか/最終ボスが出るか/描画で例外が出ないか)を追加。
2. **対戦の死潮ゲージ(#tidebar)を上のHUDの真下・左寄せへ移動**。
   - **元の実装は画面中央上に幅420pxの帯を置いており、右上の道路と侵入口だけでなく、
     コアHP・⚙️・🔩・⏸ の4パネルを丸ごと覆い隠していた**(z-indexが同じでDOM順が後ろ=上に乗る)。
     スマホ実測で topbar 0〜455 に対し tidebar 230〜622。**対戦中は自分のコアHPが見えない状態だった**。
   - 直し方: 絶対配置の中央寄せをやめ、`top=HUDの下`・`left=左端`・幅min(230px,27vw)の1行に。
     ラベルは「🌊 死潮の取り分 — あなた33%」→**「🌊あなた33%」**に短縮してバーの左へ横並び。
   - ミニ盤面(.mini)を詰める指定を`max-width:760px`→**900px**に(852pxのスマホにも効くように)。
     マップの右余白(minisResPx)が減ったぶん**フィールドが横に約40px広がり、倍率が高さ頭打ち=最大**になった。
   - `test_layout.js`に**対戦のセクション**を追加(死潮ゲージ×HUD/ミニ盤面/道路。参考値としてマス・侵入路も表示)。
   - `test_shot.js`に`vs`(対戦を撮る)と`z=id,id`(指定した敵だけ経路に並べて撮る)を追加。
   - **残った件**: ミニ盤面が右上の侵入路(マップ矩形の外)に掛かる。侵入口はmap x≒1671でMW=1600の外にあり、
     minisResPxはマップ矩形しか避けていないため。完全に避けるにはマップを6〜12%縮めることになるので保留。
3. **ステージ2の終盤が、ステージ1より4割も軽かったのを修正**(数値上の破綻・実機では気づきにくい)。
   - 原因: `buildTide`はW4以降、最大40%の確率で**プールの最後の個体**を引く。ステージ1の末尾は重装兵(HP680)だが、
     ステージ2にはレイス/重装兵の相当種が無く**末尾が影サメ(HP195)**だった。W20総HPが ①246k に対し ②149k。
   - 対策: **亡霊エイ**(sRay=レイス相当・すり抜け+周囲加速)と**巨大ウミガメ**(sTurt=重装兵相当・装甲)を追加。
     8回平均で 悪夢W20総HP ①383k ②373k とほぼ同等になった。
   - **⚠プールに敵を足す時は必ず末尾の重さを揃える**(CLAUDE.mdに追記済み)。
4. 検査は全部通っている: `node --check` / `test_undef.js`(未定義ゼロ) / `test_headless.js`(最終ボス3体とも確認) /
   `test_balance.js`(逆転なし) / `test_layout.js`(死潮ゲージ×道路・×HUD ともOK)。見た目は`test_shot.js`で目視済み。

### 2026-07-25 第24弾(第23弾の実機テストOK+設置猶予を10→5秒)
1. 第23弾の実機テスト(ナイトメアの最終ボス・対戦のHUD・ステージ2の終盤)は**全部OK**(ユーザー確認済み)。
2. **ウェーブ開始の設置猶予(WAVE_LEAD)を10秒→5秒**にした(ユーザー指示)。作戦タイム20秒は据え置き。
   10秒は待たされ過ぎで、作戦タイムで組み終えているぶんダレるため。表示は上のHUDの🔨カウントダウンがそのまま短くなる。

### 2026-07-25 第25弾(スナイパー塔と冷却塔のテコ入れ)
1. **スナイパー塔**: 威力175→**875**(×5)・間隔2.4→**4.8秒**(×2遅く)。ユーザー指示。
   - 狙いは「雑魚を掃除する塔」から「最大HPの敵を一撃で抜く塔」への役割変更。単体DPSは73→182に上がるが、
     **1発あたり4.8秒なので雑魚の処理速度は逆に半分になる**(1体/2.4秒 → 1体/4.8秒)。ボス・重装向けの塔になった。
   - ⚠レールガン(研究所・cost680)との比較: 素のDPSはスナイパー182>レールガン103で逆転するが、
     レールガンは**装甲貫通**があるため装甲相手では103>82で勝つ(スナイパーは装甲に×.45)。射程も900>660。
     完全な逆転ではないが**要観察**。実機で「レールガンを取る意味が無い」と感じたら上げる。
2. **冷却塔を「完全凍結」に変更**(ユーザー指示)。間隔1.4→**6秒**とかなり遅く。
   - 新フィールド`frz`(秒)。`z.frzT>0`の間は**移動速度0かつ攻撃不可**(`z.acd`を減らさない)。凍結が解けたあとは
     `slowT`(4.5秒)の減速が残る=「解けかけ」の表現。
   - **⚠永久ロック防止**: 凍結時に`z.frzCd=frz×2`を立て、これが残っている間は再凍結しない。
     これが無いと⏩連射Lv5(間隔×.55)+研究所の🕸強化(凍結+20%×3段)で**凍結時間>再発射間隔**になり、
     範囲内の敵を永久に止められてしまう(6×.55=3.3秒 < 2.6×1.6=4.16秒)。frzCdにより稼働率は最大50%。
   - 見た目: 氷漬けのブロック(半透明の水色+上に氷柱)で囲む。止まっていることが一目で分かる。
   - 通信: スナップショットの`es`に9番目の要素(凍結中か)を追加。クライアント側も氷漬けで描画される。
   - `test_headless.js`に**checkCryo()**を追加(凍る/凍結中に1mmも動かない/解凍直後に再凍結しない を直接検証)。
     ⚠campStepはrAFの中なので、例外もロジック抜けも通しプレイのテストには出ない。直接呼んで数字で見ている。
   - `test_shot.js`に`frz`オプション(z=で並べた敵を1体おきに凍らせて見比べる)を追加。
3. **タワー定義の`slow:`は死んだ値**だと判明(拘束ネット砲の`slow:.45`は参照されておらず、減速率は一律.55)。CLAUDE.mdに明記。

### 2026-07-25 第26弾(レールガンをビーム砲=線状の範囲ダメージに)
1. **レールガンをビーム砲に変更**(ユーザー指示「ビーム砲で範囲ダメージ」)。
   - 新フィールド`beam`(ビームの半幅26)。狙った敵の**方向**へ射程900いっぱいまで線を伸ばし、
     `segDist`(点と線分の距離)で線上の敵を全部拾って**全員に満額ダメージ**(装甲貫通あり)。
   - **`type`は`bullet`のまま**にした。ここを`'beam'`のような新typeにすると
     ①弾薬(AMMO)が乗らなくなる ②研究所の🔫銃器改良(`tlv('bullet')`)が効かなくなる、の2つを失うため。
     撃ち方だけ`if(T.beam)`で分岐している。
   - 📡射程強化(`us.g`)でビームが太くなる(+6%/段)。
   - 演出: 新しいfx種別`beam`。太い紫の帯+白い芯が0.3秒で走って消える(電撃の`zap`と同じ多重ストローク方式)。
     `addFx`経由なので対戦のクライアント側にもそのまま流れる。
   - `test_headless.js`に**checkBeam()**を追加(線上に3体並べて1発で全部削れることを確認)。
   - `test_shot.js`に`t=<タワーid>`を追加(そのタワーを建てて撃たせ続ける=発射エフェクトを撮れる)。
2. **役割の住み分け**(第25弾のスナイパーとの比較):
   - スナイパー塔 = 単体875/4.8秒 → **1体に強い**(対ボス)。DPS182。
   - レールガン = 310/3秒だが線上を全部 → **並んでいる敵に強い**。1体なら103、3体巻き込めば310。
   - 装甲相手はどちらも貫通の有無で差が出る(スナイパーは装甲に×.45、レールガンは貫通で満額)。

### 2026-07-25 第27弾(重テスラを「ゆっくり伝わって加速する連鎖」に)
1. **ユーザー指摘「重テスラってほぼテスラと同じじゃね？」は正しかった**。
   テスラコイル(⚙️340)に対し重テスラ(⚙️700・🧬622pt)は 威力1.5倍/連鎖4→7 だけで、**新しい挙動がゼロ**。
   研究所の目玉なのに「金を払った上位互換」でしかなかった。
2. **ユーザー案を採用: 連鎖の速度を遅くし、跳ねるたびに威力が加速度的に増える**。
   - 新フィールド`arcT`(1跳ねの秒数=0.1)・`grow`(1.45倍/跳ね)・`gmax`(5倍で頭打ち)。
   - 連鎖の実体は`C.arcs[]`。campStepの中で1跳ねずつ進める(砲弾`C.shells`と同じ作り)。
     `newCamp`に`arcs:[]`を追加、`resetCampWave`でクリア(前の波の連鎖が残ると居ない敵を起点に跳ね続ける)。
   - 基礎威力は 68→**15**へ下げた(定義値。ゲーム内は0.5625倍で8.4)。1発の合計は連鎖7で約185=従来の173とほぼ同じだが、
     **配分が「最初は8、最後は42」に変わる**。群れが長いほど後半が重い塔になった。
   - ⚠`gmax`が無いと⚡連鎖数Lv5(+5)+研究所⚡(+3)で15跳ね→1.45^14=約300倍となり青天井。必ず上限で止めること。
   - 見た目: 跳ねるほど電撃が太くなる(`zap`のfxに`wm`=太さ倍率を追加)。
   - `test_headless.js`に**checkCoil()**を追加(1フレームで終わらない=遅延している/4体以上に伸びる/最大が最小の2.5倍以上)。
3. **⚠時間を溶かした反省**: 上の実装後、`test_shot.js`で連鎖のスクショを撮ろうとしたが何度撮っても電撃が写らず、
   「ブラウザだけで壊れている」と疑って長時間デバッグした。**実際は壊れていなかった**。
   ヘッドレスChromeの仮想時間は`setInterval`を仕掛けると進みが鈍り、撮影時にはfx(寿命0.5秒)が消えていただけ。
   最終的に`--dump-dom`で`C.fx`の中身を吐かせ、**zap=4本・太さ1.74→2.20→2.40(上限)・被弾42/42/37/26/18/12/8**と
   数字で確認して決着した。CLAUDE.mdに「写らない=壊れている と早合点しない」と手順を追記。
4. `test_shot.js`の`t=`は**建てる瞬間だけ`unlocked`を上げて戻す**ように修正(上げっぱなしだと画面が止まる)。

### 2026-07-25 第28弾(研究所タワーの強さの並び+鎖使いの取りこぼしバグ)
1. **重テスラ以降の研究所タワーが重テスラより弱かったのを是正**(ユーザー指示)。
   判定は「実戦DPS=そのタワーが現実的に巻き込める数ぶんのDPS」(単発1体/複数同時=multi体/範囲4体/ビーム3体/連鎖5体)。
   - ガトリング要塞 威力11→**36**(実戦DPS 69→226)。単体をひたすら削る役に。
   - プラズマ砲 威力210→**300**(実戦DPS 215→307)。
   - 要塞砲 威力95→**150**・同時3→**4**体(実戦DPS 229→482)。最後の解放にふさわしい最強に。
   - 結果: 重テスラ92 < レールガン174 < ガトリング226 < プラズマ307 < 要塞砲482 と単調増加になった。
     全強化でも 412 < 783 < 1013 < 1378 < 2166 で順番は保たれる。
2. **鎖使いが居てもすり抜け敵が素通りするバグを修正**(ユーザー報告)。
   - 原因: 部隊1体が同時に足止めできる数が3体固定(`u.eng>=3`)だった。普通の敵は他の部隊へ回れるが、
     **すり抜け敵は鎖使いにしか止められない**ので、鎖使いが3体で埋まると4体目から素通りしていた。
     敵が重なって到着した時に必ず起きる。
   - 対策: `ENG_MAX=3`(通常)と`ENG_HOOK=8`(鎖使いがすり抜け敵を抱える時)に分けた。
   - `test_headless.js`に**checkHook()**を追加。⚠テストの期待値は`ENG_HOOK`ではなく実数6で書いてある
     (定数を使うと「上限を下げただけ」でもテストが通ってしまうため)。壊れた版(ENG_HOOK=3)で
     「3/6体 素通りした」とFAILすることを確認済み。

### 2026-07-25 第29弾(デッキ長押しの廃止+進化を「好きな兵科から・大幅強化・高コスト」に)
1. **デッキ長押しの部隊レベルアップを廃止**(ユーザー指示)。デッキは「押したら出撃」だけになった。
   部隊レベルは**作戦タイムの強化カード**に一本化。`buyUnitLevel()`は残す(🔩払いなので⚙️払いの共通処理に乗らない)。
   チュートリアル本文の「デッキを長押しすると部隊レベルアップ」も書き換え。
2. **研究所の進化(派生キャラ)を「どの兵科からでも選べる」ように**(ユーザー指示)。
   - 以前は`VARLIST`の固定順(猟犬→バット→…)で1件ずつしか出なかった。今は**兵科ごとに『次の段階』を同時に提示**する
     (基本8種+解放済みの上級)。段階の飛ばしはできない(1段目→2段目→3段目)。
   - 上級兵科(戦車/メカ/タイタン/ヴァルキリー)の進化は、**その兵科本体を研究所で解放してから**出す(`uvBaseOK`)。
3. **進化の伸びを大幅に強化**。33個の数値を手で書き換えるのではなく、定義値の「1からの伸び」を
   `VAR_BOOST=1.8`倍でまとめて増幅した(設計の比率が崩れない)。出撃費の倍率(cm)も`VAR_COSTB=1.45`で少し上げ、
   強さのぶん⚙️を払わせる。rate(連射)とsp(速さ)は据え置き。最終段階は素の**3.16〜6倍**になった。
4. **進化の研究pt価格を90→240(×1.13^n)に**。1個目240 / 10個目815 / 33個目13,500。全部で約10万pt=特別な投資に。
5. `test_headless.js`に**checkEvo()**を追加(同時に8兵科から選べる/買った物は消える/上級は本体未解放なら出ない/
   最終段階が素の2.5倍以上)。研究所の項目一覧を`LAB_ITEMS`に控えてテストから中身を見られるようにした。

### 2026-07-25 第30弾(ステージ1の道を引き直し+角に必ずマス+支援施設エリアを新設)
1. **ステージ1の道を引き直した**(ユーザー指示「拠点の左にスペースが空いているのでフィールドを伸ばして中央を空けて」)。
   - 新経路: 右上から入って**下半分をジグザグ**し、拠点は左端(x=80)まで伸ばした。PLEN 3120→3025でほぼ据え置き。
   - これで**画面中央〜右上が大きく空き**、そこに支援施設エリアを置けるようになった。
2. **道の内角には必ず建設マスを置いた**(ユーザー指示)。両ステージとも`slots`の**先頭6つが角のマス**。
   - コの字(角が2つ並ぶ所)は**真ん中に1マス**にした。角ごとに置くと60px程度しか離れず「マス同士110」を満たせないため。
   - 解放順(`order`)も0始まりに直したので、**初期7枠=角6つ+1**になる。開幕から要所を押さえられる。
   - ⚠マスは手で並べず**自動配置スクリプト**で出した(道95/コア150/工房135/施設135/BRANCH120/HPバー帯/マス同士110/画面内)。
3. **🏗 支援施設エリア(3枠)を新設**(ユーザー指示「タレットとは別軸のなにかを建てられる場所」。中身は当方で提案)。
   - 場所は`SUP_SLOTS=[[980,310],[1120,310],[1250,310]]`(全ステージ共通・画面中央〜右上)。`SUP_BASE=25`。
   - **解放不要で最初から建てられ、専用枠にしか建たない**(逆に専用枠には施設しか建たない)。
   - 中身3種(`type:'sup'`): 📡**管制塔**⚙️400=全タワー射程+20% / ⚡**発電所**⚙️520=全タワー間隔-16% /
     🏥**野戦病院**⚙️460=部隊全員を毎秒16回復。**撃たない代わりに盤面全体を底上げする**=タワーとは別軸。
   - ⚠実体は`TOWERS`の**末尾**。`T_PLAY=TOWERS.length-SUP_N`で解放チェーン・研究所と切り分けているので、
     施設を足す時は必ず末尾に置くこと(途中に入れると研究所の解放順がずれる)。
   - CPUも⚙️に余裕がある時に1つ建てる。強化(⚔⏩📡)は無し=建てるだけ。
   - `test_headless.js`に**checkSup()**を追加(専用枠のみ/解放不要/3種の効果が実際に乗る/研究所に出ない)。
4. マス数は全ステージ**28**で固定になった(通常22+工房3+施設3)。CLAUDE.mdの「22で固定」を訂正。

### 2026-07-25 第31弾(冷却塔の回数制/鱗の鎧×電撃/難易度を新兵から順に開く方式へ)
1. **冷却塔**(ユーザー指示): **同じ敵を凍らせられるのは初期5回まで**(`frzN`・敵側は`z.frzUsed`で数える)。
   強化項目を**⚔攻撃→❄冷却回数**に差し替え(+1/段・Lv5で**最大10回**)。攻撃力を上げても意味が無い塔だったため。
2. **ステージ2の装甲を「鱗の鎧」に変え、電撃タワーを特効に**(ユーザー指示)。
   - `armor`→`scl`。ダメージの通り方は同じ(通常弾45%・貫通は無視)だが、**電撃系タワーだけ1.9倍**(`ELEC_VS_SCALE`)。
   - これで「ステージ1=火炎が刺さる装甲」「ステージ2=電撃が刺さる鱗」と、ステージごとに組む塔が変わる。
3. **ゲームの構造を作り替え**(ユーザー指示)。
   - **難易度は新兵から順に開く**。1つ下をクリアすると次が選べる(`diffOK`)。未解放は🔒で押せない。
   - **最終ウェーブが難易度ごとに違う**: 新兵5 / 兵長7 / 古参10 / 鬼軍曹15 / 悪夢20 / 🌑ナイトメア20(`D5[].w`)。
     ⚠ソロの勝利判定・表示は必ず`curW()`を使うこと(`STAGE_W`は協力/対戦専用になった)。
   - **港ステージは「ステージ1を🌑ナイトメアでクリア」しないと出ない**(以前はどれか1つの難易度でよかった)。
   - **港は廃線ハイウェイより重い**: `STAGES[1].hpM=1.35`(同じ難易度でも敵のHP・攻撃が1.35倍)。
   - クリア記録を**ステージ×難易度**(`META.sc[stage][diff]`)に作り替え。旧`META.clr`からは自動移行する。
4. **ボスの出る波を変更**(ユーザー指示)。通常ボス(ジャガーノート等)は**WAVE10と15だけ**、
   **WAVE20は各ステージ専用の最終ボス**。以前は5波ごとに毎回出ていた。新兵(5)・兵長(7)にはボスが出ない。
5. `test_headless.js`に**checkProgress()**を追加(難易度の解放順/港の解放条件/最終ウェーブ表/港の重さ)。
   checkCryo()に冷却回数の検査(初期5・強化で10・⚔が消えている・使い切ったら凍らない)を追加。
   PvEの通し実走を**古参(W10)と悪夢(W20)**の2本に変更した(難易度で長さが変わるようになったため)。

### 2026-07-25 第32弾(🛠 マス編集モード ?edit=1)
1. **建設マスを実機でドラッグして置き直せる開発モードを追加**(ユーザー要望「思うところに確実に置く方法」)。
   - 公開URLに **`?edit=1`** を付けた時だけ動く。付けなければ普通のプレイヤーには一切見えない(公開ビルドに入れて安全)。
   - マスを指で掴んで移動。**番号と座標(x,y)を常時表示**し、置き場所の条件に反しているマスは**赤枠+理由**を出す
     (画面外/道○○/拠点/テント/HPバー/マス○○)。判定は`edBad()`にまとめてある。
   - **📋出力**で `slots:[...]` / `ECO_SLOTS=[...]` / `SUP_SLOTS=[...]` を書き出し(クリップボードにもコピー)。
     これを貼るだけで確定できる。➕マス/🗑削除もあるが、数を変えるとECO_BASE/SUP_BASEがずれるので要報告。
   - `test_shot.js`に`edit`オプションを追加(編集モードのまま撮れる)。
2. **編集モードを入れた直後に、実際に2件の不備が見つかって直った**(作った甲斐があった):
   - 工房枠3つが「画面外」判定だった。工房は仕様として画面最上部(y=190)に置くので、上端の制限は通常マスだけに掛けるよう修正。
   - 支援施設枠と、ステージ2の角マス(1382,340)が135px未満で近すぎた。角マスを(1382,354)へ寄せて解消。
     ついでに「同じ専用エリアの中は隣どうしなので120でよい」というルールを`edBad`に入れた。
3. `test_shot.js`の`st2`オプションが新しい解放条件(ナイトメアクリア)で弾かれていたので、撮影時は全クリア扱いにするよう修正。

### 2026-07-25 第33弾(ユーザーが編集モードで決めたマス配置を反映)
1. **ステージ1の建設マス22個・工房枠3個をユーザーの配置に差し替え**(?edit=1 の📋出力をそのまま採用)。
   検算の結果**28マス全部が条件OK**。副産物として「死潮ゲージが建設マスの上端をかすめる」も0件になった。
2. **⚠施設枠(支援施設エリア)をステージごとに持つように変更**。
   - ユーザーがステージ1で決めた位置(y≒206)は、**ステージ2では道(y=200)の真上**だった(道まで6px)。
     工房枠と違い、施設枠は道の形しだいで置ける場所が変わるため、`STAGES[].sup`を新設して
     ステージ1=`[[864,206],[989,207],[1110,206]]` / ステージ2=`[[960,300],[1090,300],[1220,300]]` に分けた。
     `SUP_SLOTS`は既定値として残す(`sup:`が無いステージ用)。
   - **教訓**: 工房枠・施設枠は全ステージ共通だと思い込まない。**共通の枠を動かしたら必ず全ステージで検算する**。
3. ステージ2のマスも、新しい工房枠・施設枠に合わせて置き直した(角の6マスは維持)。
4. 編集モードのパネルを小さくした(🛠で出力欄の開閉・ボタンを小型化)。出力欄がマスを隠していたため。

### 2026-07-25 第34弾(マスをくっつけて置けるようにした)
1. ユーザーから「編集時に他のマスと近いと置けないのをどうにかできないか」。
   **原因は見た目ではなくタップ判定だった**: 建設メニューを開く判定が「範囲(±60)内で**最初に見つかったマス**」で、
   マスを近づけると手前のマスばかり反応して奥のマスが押せなくなるため、110px空ける決まりにしていた。
2. **タップ判定を「範囲内で一番近いマス」に変更**。これで隣接していても押し分けられる。
3. それに伴い**マス同士の最小距離を110→72**(枠64pxが重ならない距離)に緩和。エリアごとの例外(120/135)も廃止して一本化。
   自動配置スクリプト側は見た目のゆとりのため110のままにしてある。

### 2026-07-25 第35弾(編集モードの使い勝手)
1. **?edit=1 の時は敵が一切出ない**(ユーザー要望)。`buildTide`で敵プールを空にして返す。
   ⚠敵が0だと「ウェーブ制圧」判定が即成立して波だけ進むので、`campStep`のwaveDone判定も編集モードでは止めてある。
   HUDの🔨カウントに桁が出ないよう`T.lead`は0にする(大きい値を入れると1000000000と表示された)。
2. **?edit=1 の時は全ステージ無条件で選べる**(`stageOK`)。ナイトメアクリアを待たずに港のマスを配置できる。
3. `EDIT`の宣言を**ファイルの上の方へ移した**。`stageOK`(1070行)や`buildTide`(1930行)から参照するため、
   編集モード本体(3350行)に置いたままだと読み込み順で危ない。

### 2026-07-25 第36弾(港のマス配置を反映+支援施設の概念を撤去)
1. **ステージ2(沈んだ港)の建設マス22個をユーザーの配置に差し替え**(?edit=1 の📋出力)。工房枠も微調整を反映。
2. **支援施設(🏗)の概念を全部撤去**(ユーザー指示)。第30弾で入れたものを丸ごと戻した。
   - 消したもの: `TOWERS`末尾の3種(管制塔/発電所/野戦病院)・`SUP_SLOTS`/`SUP_BASE`/`isSupSlot`・`T_PLAY`/`SUP_N`・
     `STAGES[].sup`・campStepの底上げ処理(supR/supF/supH)・建設メニューの分岐・CPUの建設処理・エリアの看板・
     3種の描画・`checkSup()`テスト。
   - 建設マスの数は**全ステージ25**に戻った(通常22+工房3)。`metaTowerCap`と研究所の判定も`TOWERS.length`基準へ復帰。

### 2026-07-25 第37弾(💎英雄召集ガチャ+支援マス3種の決定)
1. **支援マスの中身が決まった**(ユーザー承認): 📻電波塔(情報) / 🏥野戦病院(部隊) / 📦物資投下所(経済)。
   ⚠タワー強化系は入れない、が条件。**実装はまだ**(枠だけ先に消してある)。
2. **💎魔石とガチャ「英雄召集」を実装**(ヒーローシステムの第1段階)。
   - **💎はボスを倒した時だけ落ちる**(通常ボス+1 / 最終ボス+3・ソロのみ)。`killZ`で付与、結果画面にも表示。
   - タイトルに「💎 英雄召集」を追加。単発💎3 / 10連💎25。
   - **英雄11種**: ★1弱5種(元警官/消防士/猟師/整備士/配達人) ★2レア3種(特殊部隊員/軍医/爆破屋)
     ★3激レア(死潮の預言者) ★4ウルトラ(鋼鉄の巨人) ★5ギガトン(終焉の騎士)。全員に固有の必殺技を定義済み。
   - 排出率: はずれ55 / ★1 35 / ★2 8 / ★3 1.5 / ★4 0.4 / ★5 0.1(%)。
   - **はずれ枠4種は研究所と同じメタ強化**(🧬研究pt / 🔧鍛錬素材 / ⚙️初期資金+40永久 / 🔩開始pt+6永久)＝無駄玉にしない。
   - **重複した英雄は🔧鍛錬素材になる**(レア度×3個)。素材は鍛錬所で使う予定。
   - 英雄図鑑(未所持は???)を同じ画面に表示。
   - `test_headless.js`に**checkGacha()**を追加(11種の内訳/率の合計/魔石不足で引けない/10連25消費/
     2万回の実分布がはずれ55%±3/重複→素材)。
3. **⚠まだ無いもの**: 鍛錬所(中央マス)・ミニゲーム・英雄の出撃と必殺技・英雄の見た目。次はここから。

### 2026-07-25 第38弾(🏗支援枠2つと支援施設3種)
1. **支援枠を2つ追加**。⚙️で解放してから建てる(1つ目600 / 2つ目1500)。`SUP_BASE=25`・`SUP_MAX=2`。
   ⚠座標は**ステージごと**(`STAGES[].sup`)。共通にすると片方のステージで道の上に乗る(第33弾と同じ罠)。
2. **支援施設3種**(`type:'sup'`・TOWERSの末尾・`T_PLAY`で解放チェーンと切り分け)。**同じ施設は1つまで**。
   - 📻**電波塔** ⚙️380 — ウェーブ開始時に**その波の顔ぶれと総数**をトーストで出す(設置猶予5秒で対策を決められる)。
     装甲/鱗/すり抜け/ボスの有無も出す。
   - 🏥**野戦病院** ⚙️420 — 部隊全員を毎秒16回復、出撃CDを20%短縮。
   - 📦**物資投下所** ⚙️460 — ウェーブ開始ごとに補給が1つ(⚙️/🔩/コア回復/CD全リセット/砲撃チャージ完了)。
   - ⚠**タワーの性能には一切触っていない**(ユーザー指示)。効くのは 情報/部隊/経済 の3軸だけ。
3. `test_headless.js`に**checkSup()**を追加(未解放では建たない/専用枠のみ/同じ物は2つ建たない/
   野戦病院の回復とCD短縮/物資投下所の補給が届く/解放チェーンに混ざらない)。
4. ⚠作業中に**TOWERS配列にカンマを二重に入れて穴(undefined)を作る事故**。`node --check`は通り、
   `test_undef.js`も通り、**test_headless/test_layoutのLOAD FAILで初めて気づいた**。配列に要素を足す時は末尾のカンマを確認すること。

### 2026-07-25 第39弾(🦸英雄の出撃と必殺技)
1. **英雄をUNITSの末尾に足した**(`HERO_I0`以降)。戦闘も描画も普通の部隊と同じ経路を通り、見分けは`u.hro`。
   ⚠これに伴い**兵科の数え上げは`U_N`に統一**(`metaUnitCap`/研究所の新型兵科/解放カード/AIの貯金判断)。
   `UNITS.length`のままだと英雄まで研究所で解放できてしまう。`test_balance.js`も`U_N`までしか見ない。
2. **HEROESに戦闘パラメータを追加**(`ty`=攻撃の型/`rng`/`aoe`/`burn`/`uch`=必殺技のチャージ秒数24〜52)。
3. **出撃=1ゲームに1回だけ**。ソロ設定画面の「🦸連れて行く英雄」で1人選び(`META.hsel`)、
   操作バーの🦸ボタンで拠点から出す。**出撃してから**必殺技が溜まり始め、以後そのボタンが必殺技になる。
   **倒れたら二度と出せない**(`C.hOut`)。⚠ボタンは**対戦の🌊押すと同じ位置**=ソロでしか出さないので操作バーの幅は増えない。
4. **必殺技11種**(`heroUlt()`)。制圧射撃/放水(押し戻し+消火)/一斉罠(拘束)/応急修理(コア+8・妨害解除・部隊回復)/
   速達(突進)/精密狙撃(最大HPの敵へ現HP45%+固定)/蘇生(戦死した部隊を最大4体呼び戻す)/大爆破(道沿い5連)/
   時間停止(全敵4.5秒)/地響き(全域+吹き飛ばし+気絶)/死の宣告(全敵に現HP55%・ボスは25%)。
   蘇生のために**戦死した部隊を`C.fallen[]`に控える**(dmgU)。
5. **見た目は共通シルエット1つ**(`drawHeroBody()`=マント+武器の形+★の数がレア度と型で変わる)。
   **11人それぞれの造形は後回し**(ユーザー了承済み)。
6. **中断と再開**に英雄を追加。出撃中(1)は**未出撃に戻す**(盤面の実体は保存していないため)が、**戦死(2)は引き継ぐ**。
7. テスト: `checkHero()`(英雄の位置/解放チェーンに混ざらない/出撃前は溜まらない/2回出撃できない/
   チャージ0で撃てない/必殺技11種が例外なく効く/戦死したら再出撃できない)。`test_shot.js`に`hero=`オプション。
8. ⚠踏んだ罠2つ: ①**`style.display=''`はCSSの`display:none`に戻る**(明示的に`inline-block`が要る)
   ②**px/pyはcampStepの中でしか入らない**ので、`mkZ`で並べただけの敵には範囲判定の必殺技が当たらない。

### 2026-07-25 第40弾(🛠 実機テスト用のDEVモード `?dev=1`)
1. URLに `?dev=1` を付けると、🧬研究pt・💎魔石・🔧鍛錬素材が実質無限になる(`devTop()`を`saveMeta()`から呼ぶ=
   何かを買うたびに満タンへ戻る)。ゲーム中の⚙️と🔩も99,999を切ったら補充(HUDの桁が溢れない上限)。
2. **全ステージ・全難易度・全英雄が最初から選べる**(`diffOK`/`stageOK`/`renderHeroSeg`にDEVの分岐)。
   ⚠英雄は「選べる」だけで所持数は増やしていない=ガチャのNEW/重複の挙動はそのまま試せる。
3. **セーブを別枠に分けた**(`dt_meta_dev` / `dt_run_dev`)。普通に遊ぶ時のデータを汚さないため。
   ⚠裏返すと、DEVモードで買った研究所の解放や引いた英雄は通常モードに持ち越されない。
4. 付け忘れ/外し忘れ防止に、タイトル右上の版数を `v2.0 β 🛠DEV` にし、ゲーム開始時にトーストを出す。

### 2026-07-25 第41弾(🏋ヒーロー鍛錬所+リズム訓練)
1. **鍛錬所はタイトル側の施設にした**(🔬研究所で🧬300で解放→タイトルに「🏋鍛錬所」が出る)。
   ⚠当初案の「戦場の中央の大マス」は不採用。ミニゲームで試合が止まるのと、建設マス27個の形が崩れるため。
2. **ミニゲーム=タイミング&リズム**(2026-07-25ユーザー決定)。指のスワイプ **←/まえ(↑)/→** の3方向だけ。
   奥から流れてくる**玉は矢印の向きへスワイプしてよける**、**まえ(↑)で訓練ダミーを斬る**。
   46ノート・間隔1.02秒→0.42秒(終盤ほど速い)・PERFECT(±0.085秒/100点)/GOOD(±0.20秒/50点)・
   10コンボごとに倍率+0.1(最大×2)。空振り/向き違い/見逃しはコンボが切れる。PCは矢印キーでも遊べる。
3. **スコア=そのまま経験値**。英雄ごとの鍛錬Lv(最大10)が上がるとHPと攻撃が+8%/Lv(最大+80%)。
   `META.hlv`/`META.hxp`に保存。1回の訓練で🔧鍛錬素材10を消費(途中でやめても戻らない)。
4. 描画は専用キャンバス`#trcv`1枚。メインループに `if(SCR==='train')trainStep(dt0)` を足しただけで、
   ゲーム本体のループには触っていない。訓練生は後ろ姿の共通1体(`drawTrainee()`)。
5. テスト: `checkTrain()`(素材の消費/譜面が終盤ほど速い/判定4種/経験値とLv上限/英雄のHPに反映)。
   `test_shot.js`に`train`(と`trainres`)を追加。
6. ⚠見た目で踏んだ罠: ①訓練生を大きく描くと画面の真ん中を塞いでノートが見えない ②判定の文字が頭に被る
   ③コンボを右上に出すと「✕やめる」の下に隠れる。3つとも撮って初めて気づいた(数値では分からない)。

### 2026-07-25 第42弾(メニューUIの作り直し=見やすさ最優先)
きっかけ: 横画面スマホ(852x393)で研究所を開くと**見出し+説明文+情報帯だけで画面が埋まり、買える項目が1つも見えなかった**。
1. **`.bigpanel` の型を作って共通化**。`.bphd`(見出し固定)+`.tabs`(タブ固定)+`.bpbody`(ここだけスクロール)+`.bpfoot`(決定ボタン固定)。
   パネルの高さを画面いっぱい(94vh)にして、中身だけが縦に流れるようにした。二重スクロールも解消。
2. **研究所をタブ+カード化**。🆕新種/🧬派生/🎯砲撃/📈系統/🚀開始/💰経済/📖記録の7タブで、**買える数をタブにバッジ表示**。
   項目は{アイコン/名前/小ラベル/説明3行/価格ボタン}のカード(`.lc`)を4列グリッドに。**一度に見える項目が2〜3件→8件**になった。
   「クリア済み」「解放済み」の帯は📖記録タブへ追い出した(常時上に居座って場所を食っていたため)。
3. 同じ型を**編成(🪖基本/⭐上級/🔫弾薬の3タブ)・ガチャ・鍛錬所**にも適用。鍛錬所は英雄をカードで選び、
   決定ボタン(「〇〇を訓練する」)をパネル下に固定した。
4. **タイトル画面を横画面向けに2カラム化**(`@media (max-height:560px)`=ロゴ左/メニュー右)。
   それまでは**ロゴが画面外へ出て、下2つのボタンが切れていた**。ゾンビのパレードも後ろへ回した。
5. ⚠踏んだ罠: 旧`#lab-items{max-height:52vh}`が残っていて、flexで伸ばした`.bpbody`が52vhで頭打ちになり
   **パネル下に40pxの死に空間**ができた。実測(getBoundingClientRect)で初めて原因が分かった。
6. `test_shot.js`に `title` / `lab` `lab=line` `lab=rec` / `load` `load=am` / `trhome` を追加。

### 2026-07-25 第43弾(AI生成アイコンの取り込み・資源5種)
1. **`tool_slice.js`**: 1枚のシート画像に並べて生成したアイコンを、連結成分で見つけて1個ずつPNGに切り出す。
   背景色を抜いて透過にし、輪郭に残る背景色のフチも「px = a×本来の色 + (1-a)×背景」を解いて引き算する。
   近い塊は1つのアイコンの部品として結合。並びは上の行から左→右。⚠ImageMagick/PILが無いのでヘッドレスChromeのcanvasで処理。
2. **`tool_embed.js`**: `ui_src/` のPNGを96pxへ縮小してbase64にし、index.htmlの`ICONS-START〜END`ブロックを作り直す。
   **単一HTML完結を維持**するため、512pxの原寸ではなく表示に足りる大きさまで縮めてから埋める(5種で90KB)。
3. 資源5種(⚙️スクラップ/🔩強化pt/🧬研究pt/💎魔石/🔧鍛錬素材)を差し替え。
   `IC(html文字列)`で文字列中の絵文字を`<img class="ico">`へ置換する方式にしたので、**差し替え箇所は18箇所で済んだ**。
   ⚠canvasに描く文字(ダメージ表示・作戦ログ)は画像にできないので絵文字のまま。
4. ⚠踏んだ罠: `--dump-dom`はDOM内の`"`を`&quot;`に直すので、**結果はbase64で包んで渡す**必要がある。

### 2026-07-25 第44弾(アイコンをAI感の少ない版に差し替え・切り出しの罠)
1. 資源5種を作り直した版に差し替え。プロンプトに「1アイコン=1物体/陰影とハイライト禁止/
   線を揺らす/**版ズレ**(色の版を黒線からずらす)」を入れたら、AIレンダー感が抜けてベタ塗りの印刷物らしくなった。
   主色も全部変えた(錆/鋼/緑/紫/琥珀)ので**16pxでも5つ全部が見分けられる**ようになった(実測)。
2. ⚠**切り出しの罠**: 背景キーがマゼンタ、アイコンが紫だと**紫まで背景として抜けて白い石になった**。
   `--tol` の既定を60→30へ下げ、**プロンプトの背景色をシアン`#00FFFF`に変更**(パレットにシアンが無いので安全)。
3. 減色パレットに紫の明るい階調を追加。無いと結晶の明るい面が生成り色に落ちる。
4. 減色の副次効果でさらに縮み、5種で35KB(index.html 384KB)。

### 2026-07-26 第45弾(画面アイコン6種+研究所タブ7種+盾を差し替え)
1. `applyIcons()` を追加。**静的なボタンとモーダル見出しの絵文字を起動時に画像へ差し替える**。
   ⚠絵文字が重複する(🧬は研究ptと派生、📖はあそびかたと記録)ので、IC()の絵文字置換ではなく
   「この要素のこの絵文字だけ」を差し替える方式にした。⚠updLabBtnの後に呼ぶこと(先だと数字側の💎を食う)。
2. `icoImg(名前, 絵文字)` を追加。**素材が無ければ絵文字にそのまま落ちる**ので、
   シートが揃っていない途中の状態でも壊れない。
3. 研究所の `LAB_CATS` を {ic,em,t} 構成に変え、タブのアイコンを画像化。
4. ソロ作戦の盾を描き直し(前の防弾盾はスリットのせいで小さいと「カード」に見えた)。
5. 現在19種。埋め込み合計111KB / index.html 465KB。

### 2026-07-26 第46弾(HUDアイコン3種=シェルター/集結旗/英雄)
1. 🏕シェルター(コアHUD)・🚩集結旗ボタン・🦸英雄ボタンを差し替え。**全22種そろった**。
2. 丸い操作ボタン(🚩🦸)は中の文字が小さいぶん、`.ico` を大きめに出す指定を足した。
3. ⚠**canvasに直接描く文字**(コアHPバーの🏕、ダメージ表示、作戦ログの「+30⚙️」)は
   `<img>`にできないので絵文字のまま。揃えるなら画像をcanvasへdrawImageする改修が要る。
4. 埋め込み合計128KB / index.html 481KB。

### 2026-07-26 第47弾(canvasの絵文字も画像化+盤面の錠前を整理)
1. **`drawIcoText(c,文字列,x,y,サイズ)`** を追加。canvasはHTMLを描けないので、文字列を絵文字で切って
   「文字→画像→文字」と順に並べて描く。適用先=コアHPバーの🏕 と 演出テキスト(工房の「+5⚙️」など)。
   素材が無い環境(ヘッドレステスト)では黙って絵文字のまま描くので、テストは壊れない。
2. **未解放の建設マスを小さく薄い印にした**。27マス中20マス前後が未解放で、全部くっきり描くと
   盤面が錠前だらけになり「今どこに建てられるか」が読めなかった。解放済みの7マスだけが目に入るようになった。
   ⚠タップ判定は座標からの距離なので押しやすさは変わらない。編集モード(?edit=1)では従来どおり全部出す。
3. `test_shot.js` の `t=` が廃品工房(type:'eco')にも対応(工房エリアの枠へ建てて生産を早回しする)。
   ⚠`t=` は `z=` と併用しないと動かない(z=の中に入れ子になっている)。

### 2026-07-26 第48弾(ボタン枠・パネル枠を9スライスで適用)
1. `applyFrames()` を追加。**素材があるときだけCSSを足す**ので、無ければ従来の見た目のまま。
   `border-image`(9スライス)で、角の飾りは固定・辺だけ引き伸ばす。枠の中は透明なのでボタンの背景色が透ける。
   適用先= `.btn:not(.sm)`(赤ボタンは赤枠)と `.bigpanel`(研究所/編成/ガチャ/鍛錬所)。
2. `tool_slice.js` に **`--rect`**(正方形に整えず縦横比のまま切る)を追加。
   ⚠枠を正方形に整えると9スライスの角の位置がずれて使えない。
3. `tool_embed.js` に **`--frsize`**(枠だけ大きめ=既定256px)を追加。枠は引き伸ばすので解像度が要る。
4. ⚠**枠のぶん上下に10pxずつ増える**ので、パディングを削らないとタイトルのメニューが画面からはみ出す。
5. ⚠**生成AIに「transparent background」と書いてはいけない**(透明を描けず灰色グラデーションになる)。
   枠は**内側も外側もシアン `#00FFFF` のベタ塗り**を指定して、こちらで抜くこと。
6. 埋め込み合計160KB / index.html 516KB。

### 2026-07-26 第49弾(🏋鍛錬所をまるごと作り替え=洞窟のマザーゾンビ防衛+🧿ゾンビのコア)
きっかけ: ユーザー決定「タップ式にして、もっと本格的に」→「そもそも違うゲーム制に」→
『ゆうしゃのくせになまいきだ。』のオマージュ。ただし**攻めてくる勇者の方が自分の英雄**で、
こちらは**洞窟側(ゾンビ側)で迎え撃つ**。撃退できれば訓練成功=英雄のレベルが上がる。
1. **スワイプのリズム訓練(第41弾)は廃止**。譜面/判定/訓練生(drawTrainee)/ダミー(drawDummy)ごと削除。
2. **借りたのは骨だけ**(徹底調査のうえで線引きした): ①操作は掘るだけ ②掘らないと戦力が育たないが掘ると侵入者の道ができる
   ③直接殴れない ④侵入者は勝手に動く。**本家の顔(食物連鎖の階層/養分・魔分の土/魔王の連行タイム)は入れていない**。
3. **湧きは「そのマスの開放度」で決まる**(周囲8マスの空洞数→格1〜4)。連結成分の広さではない。
   ⚠広さだと一本道を伸ばすだけで格が上がってしまい、「広い部屋=強いゾンビ=英雄の大通り」というジレンマが壊れる。
4. **🌊死潮の満ち引き**(波の中で下から満ちて引く)。水没マスは壁と同じで、こちらにも相手にも効く。引いた跡は12秒だけ湧き倍。
5. **🦠感染**: 倒したお供の英雄が次の波から味方として湧く(HP40%/攻撃60%)。育成対象は訓練なので感染しない。
6. **お供の英雄**はHEROES11人から抽選で、**未所持でも出る**(「こんな強いやつがいるのか」が分かる=ガチャの宣伝を兼ねる)。
   必殺技は名前と演出を11種ぶん個別に出し、効き目は7型(aoe/line/all/snipe/heal/rush/stop)に集約。
7. **🧿ゾンビのコア**: TD側(ソロ)で同種を6%(エリート2倍)で落とす。洞窟に出せる12種の解放(8個)と強化(Lv5・6×Lv個・+12%/Lv)。
   保存は META.zc / META.zl。⚠TD側で atk:0 の敵(クローラー/スライム状体/レイス)は洞窟用の攻撃力を ZCAVE で上書き。
8. 鍛錬所のモーダルを2タブ化(🦸訓練する / 🧿ゾンビ)。難易度3段階(浅層5波/中層8波/深層12波)をフッターに。
   ゾンビのカードは小さなcanvasに drawZombie を焼いて実物を出している。
9. BGMは手続き生成(cvBgm)。⚠setInterval は使わず trainStep の dt で進める(ヘッドレスの仮想時間が鈍るため)。
10. ⚠見た目で潰した点: ①掘れるマスを枠で囲うと盤面がタイルの海になる→**十字(＋)の印**にした
    ②丸い点だと岩肌の粒と紛れる ③空洞は真っ黒・土は上下にベベル、でないと土と穴が見分けられない
    ④「▶攻めてこい」ボタンが下の情報帯に重なっていた(bottom を28px上げた)。
11. ⚠数値で潰した点: **1マスの定員(CV_CELL=2)と、1人に噛みつける数(CV_ENG=3)が無いと英雄が4秒で溶ける**。
    TD側の ENG_MAX と同じ理屈。最初これを入れ忘れてゾンビ18体が1マスに重なった。
12. ⚠drawUnit/drawZombie を小さく描く時のスケールは「欲しい高さ ÷ (素の絵の高さ50 × 内部倍率)」。
    内部倍率(TSCALE / Z.sc)を割り忘れると16倍などになり画面いっぱいに巨大な絵が出る。
13. テスト: checkTrain() を全面的に書き直し(コアの解放と強化/掘る条件と🦴骸/開放度→格/湧きの種類/
    巣作り→侵攻/英雄が掘って進む/必殺技11種/最後まで走る/マザーゾンビが倒されたら失敗/Lv上限)。
    test_shot.js に train(巣作り) / trainraid / trainflood / trainres / trhome+trzoo を追加。
    test_undef.js の GLOBALS に TypedArray 系を追加。

### 2026-07-26 第50弾(洞窟を本格化=盤面5倍・養分・掘ったらゾンビが出る・カメラ)
きっかけ: 実機で遊んだユーザーから「思ってたんと全然違う。ミニゲームすぎる。もっとマップも広くして
『勇者のくせに生意気だ』みたいにして。土を掘ったらゾンビが出てくる感じも再現して」との指摘。
1. **盤面を26×10 → 56×24(面積5.2倍)**。1マス30pxなので画面には28×11しか映らない。
   **右下のミニマップをなぞって見る場所を動かす**+侵攻中は一番奥の英雄をカメラが自動追従。
   端で掘ると画面がその方向へ少し送られる(なぞった先が画面外に出て掘れなくなるのを防ぐ)。
2. ⭐**土に養分(nut 0〜4)を鉱脈状に埋め、掘った瞬間にその場でゾンビが生まれる**ようにした。
   本家の「養分をふくむ土を掘ると魔物が湧く」に当たる部分。格 = 養分 +(開放度6以上なら+1)。
   ⚠**養分の量は割合で割り当てる**(CV_NUT_Q=上位2%が格4/6%が格3/14%が格2/28%までが格1)。
   最初は固定のしきい値にしていたら、乱数のブレで**盤面が緑だらけ**になった(実機スクショで発覚)。
3. 部屋からの自然湧きは**開放度5以上・3.5秒ごと**に落とした(主役は「掘って湧かす」方なので副次的に)。
4. **なぞって連続して掘れる**ようにした。1マスずつタップだと、この広さでは手が回らない。
5. 波の作り直し: 巣作り18→25秒 / 侵攻60→75秒 / 波数 5,8,12 → 4,6,9。
   🦴骸は 消費6→10・上限60→100・毎秒7→8(巣作り15→20)=巣作り25秒でおよそ50マス掘れる。
   ゾンビ上限 18→25。マザーゾンビHP 700/800/900 → 1200/1500/1800。
   報酬 しのいだ波220→380 / 撃破180→200 / 与ダメ0.08→0.06 / 全波700→800。
6. 経路探索を**二分ヒープのダイクストラ**に(マスが1344個あるのでO(n^2)だと重い)。
7. ⚠養分の描画は「粒の数と明るさ」だけにした。大きな半透明の円を敷いたら盤面が緑まみれになり、
   土と空洞の区別が付かなくなった。光らせるのは格4だけ。
8. テスト: checkTrain() に「養分のある土を掘るとゾンビが生まれる/掘ると養分は消える」を追加。
   test_shot.js の掘るパターンを新しい盤面(56×24)に合わせて引き直した。

### 2026-07-26 第51弾(洞窟の循環を閉じる=🩸腐肉1本・徘徊・侵攻中の道)
きっかけ: 実機で遊んだユーザーから「微妙。掘ってゾンビが出てヒーローがきて戦うだけな感じが強い。
ゾンビは徘徊してほしい」。あわせて『ゆうしゃのくせになまいきだ。』の詳細レポートを受領。
⚠**前回までの実装は本家の芯を外していた**。レポートで名指しされた発明は次の3つで、どれも入っていなかった:
  ①資源＝脅威の同一化 ②総量保存則 ③死＝資源の還元。これが無いと一方通行になる。
1. **🦴骸を廃止し🩸腐肉1本に統合**。掘る(8)も🪨崩落(50)も🏠マザー移動(80)も同じ資源。
   資源が2本あると「今使うか、後に残すか」の悩みが生まれない(レポートのRecommendations 1)。
2. **循環を閉じた**:
   ・ゾンビが自然に死ぬと抱えていた腐肉の70%が手持ちへ戻る
   ・**英雄に倒されたぶんは戻らず、英雄が吸って強くなる**(最大+80%・**波をまたいで引き継ぐ**)
   ・**英雄を倒すと腐肉が増える**(300+英雄が吸った分)=総量を増やす唯一の手段
   ・自然産出はマザーゾンビの毎秒4だけ。これだけでは足りないので循環を回すしかない
   ・部屋からの自然湧きも手持ちの腐肉を食う(タダで増えると総量保存が壊れる)
3. ⭐**ゾンビの誘導則を1つだけにした**: **通路を直進し、行き止まりで向きを変える**。
   英雄が5歩以内に来たら襲いに行く。⚠**英雄へ一直線にすると掘る位置に意味が無くなる**(実際になっていた)。
   本家のコケの直進則にあたる位置づけ。レポート「誘導則は1つ・一目で分かるものに絞れ」。
4. ⭐**侵攻が始まった後に掘った道は英雄が通らない**(本家の勇者の癖)。青く表示して区別できる。
   これが無いと侵攻中にやることが無く「見てるだけ」になる。
5. **🪨崩落**: 空洞を土に戻し、その場と四方のゾンビを潰し英雄に最大HPの18%。通路を塞げる。
6. **🏠マザーゾンビの移動**: 2×2ぶん掘れている所へ移せる(本家の「魔王の位置移動」)。囮に使える。
   ⚠マザーの位置は TR.mx/TR.my が正になった(CV_MOM_X/Y は初期値のみ)。
7. ⚠崩落とマザー移動は「ボタンでモードに入ってから盤面をタップ」の2段階(誤爆すると痛いため)。
8. ⚠HUDの🩸バーに埋蔵量まで入れたら枠からはみ出した。手持ちだけをバーに入れ、埋蔵は下の帯の右端へ。
9. テスト: checkTrain() に「循環(戻る/食われる/奪う)」「侵攻中に掘った道は通れない」「徘徊と襲撃」
   「崩落」「マザー移動」を追加。

### 2026-07-26 第52弾(生態系=初期種2つ・🔪間引き・🥩肉塊・進化ツリー・🔦採掘レーザー)
きっかけ: ユーザー指示「掘って出てくるのはマップの初期モンスター2種類ぐらいにして、そこから生態系を派生。
本家にあったタップで殺すのも追加(専用ボタンを押してから殺せるようにして誤タップを防ぐ)。
死体が肉塊になって、何個か食べたらグレードアップ。水生のやつがゾンビ食べたら違うグレードアップ。
強さは基本と同じでTDのリソースで強化。前提としてパクりはだめ=つるはしで掘るをDEADTIDEっぽく」。
1. **🔦採掘レーザー**: 「つるはしで土を掘る」は本家そのものなので、**プレイヤーが操るレーザーで削る**形にした。
   画面の上から削った点へ光条が走る。仕組みは同じで、呼び名(「掘る」→「削る」)と見た目だけ変えてある。
2. ⭐**土から生まれるのは初期種2つだけ**(硬い系ウォーカー / 速い系ランナー)。
   **腐肉レベル1〜2で硬い系・3〜4で速い系**=どの鉱脈を削るかで育てる系統が決まる(レベル4は2匹)。
   巣からの自然湧きも初期種だけ。**上位種は進化でしか手に入らない**。
3. **🔪間引き**: ボタンでモードに入ってからゾンビをタップで殺す(モードは維持=何体も続けて処理できる)。
   ⚠**🥩肉塊を作る唯一の手段**。
4. **🥩肉塊**: 英雄以外に倒されたゾンビが残す。他のゾンビが食べれば進化の餌、
   食べられずに30秒たつと腐って腐肉が手持ちへ戻る。**間引き=「資源として回収するか、進化の餌にするか」の選択**。
   ゾンビは肉塊が3歩以内にあると寄っていく(誘導則に足したのはここだけ。餌に寄るのは一目で分かるので)。
5. ⭐**進化ツリー(ZEVO)**: **何系統の肉塊を何個食べたかで行き先が変わる**。
   同じウォーカーでも 硬い肉塊3→アーマード / 速い肉塊3→スクリーマー。
   初期種2つから全12種へ到達できることは test_headless で検査している。
   ⚠本家の魔物は捕食で**繁殖(数が増える)**するが、こちらは**個体が変異する(数は増えず質が上がる)**=ゾンビものの理屈。
6. **🧿コアは「強化」だけに役割変更**(解放の概念は廃止)。Lv0〜5・+12%/Lv。
   **図鑑**(META.zdex)に載るのは洞窟で実際に出した種類だけで、未発見はシルエット表示。
7. ⚠**起点2種の図鑑登録をMETAの正規化ブロックに書いたら、新規プレイヤーだけ図鑑が 0/12 になった**。
   正規化ブロックは「if(mv&&typeof mv==='object')」の中にあり、**セーブが無いと丸ごと走らない**。
   ZC_SEED を定義した直後で必ず入れる形に直した。
8. ⚠test_shot は1フレームしか進めないので、削った瞬間のレーザーの光条が45本すべて残って画面が縦線だらけになった。
   撮影前に8フレーム進めるようにした。
9. ⚠図鑑カードの .ds は3行までなので、所持数を .tg 行へ移した(進化先2行+HP行で埋まる)。

## 第53弾(2026-07-26) — 🕳洞窟(ゆうなま要素)を全削除。鍛錬所は🔧で鍛えるだけ+📖ゾンビ図鑑
**ユーザー決定=「TDゲームにゆうなまっていう難しいやつ入れるの悪手。ゆうなま要素全削除で」。**
第49〜52弾で4度作り直した洞窟(『ゆうしゃのくせになまいきだ。』式の間接統治型マザーゾンビ防衛)を
**まるごと消した**。第52弾まで積み上げた仕組みは全部無くなっている。

### 経緯(なぜ消したか)
- 遊んでもらったが毎回「なんか違う」。4度目(第52弾)でも解消しなかった。
- こちらの自己診断=「掘る＝戦闘ユニットが1匹出る」にしてしまい、本家の
  「掘る＝生産者(コケ)が養分を運ぶ空間パズル」が消えていた。繁殖を外したので画面も生きていない。
- 一度は「まず本家を丸パクリしてから変える」方針で食物連鎖の設計まで詰めたが、
  **その手前でユーザーが「そもそもTDに入れるのが悪手」と判断**して全削除になった。
- ⚠**この判断を蒸し返さないこと**。鍛錬所に手を入れるなら、洞窟とは別物の設計から始める。

### 消したもの
盤面56×24 / 🔦採掘レーザー / 🩸腐肉(総量保存の循環) / 🥩肉塊と進化ツリー(ZEVO) / 初期種2つ(ZC_SEED) /
🔪間引き / 🪨崩落 / 🏠マザーゾンビ移動 / 🌊死潮 / 🦠感染 / 攻めてくる英雄の必殺技(CV_ULT) / 洞窟専用BGM /
カメラとミニマップ / 経路探索(cvPathHero/cvPathZ) / 🧿コアの所持と強化(META.zc / META.zl) /
洞窟の画面(#scr-train・#trcv・#trbtns)とそのCSS / TR まわり一式。index.html は 6077行 → 4955行。

### 残したもの・作り直したもの
1. **🏋鍛錬所**: 🔧鍛錬素材10を注ぐと選んだ英雄に経験値+150(`TR_XP`)。鍛錬Lvは最大10・HPと攻撃+8%/Lv。
   ⚠**遊びとしての中身はまだ無い暫定の形**。ユーザーは「別の鍛錬所は作る」と言っているので、中身は次に設計する。
2. **📖ゾンビ図鑑**(鍛錬所の第2タブ): **ソロ作戦で初めて倒したゾンビが載る**(`zdexAdd`・`META.zdex`)。
   洞窟の12種縛りを外して**全36種**が対象。ステージごとに3欄(`ZDEX_G`)。未発見は真っ黒のシルエット+???。
   ⚠🧿コアのドロップ処理があった場所を、そのまま図鑑への登録に差し替えてある。
3. 検査も追随: `test_headless.js` の checkTrain を作り直し(🔧の消費/経験/Lv上限/英雄のHPに反映/図鑑の欄分けが
   全36種をちょうど1回ずつ拾うか/同じ種類は二度登録されないか)。`checkDevLoad` が見ていた cvSpawn を renderTrain へ。
   `test_shot.js` の train / trainraid / trainres / trainflood を削除(trhome と trzoo は残っている)。

### 教訓
- ⭐**遊びの土台(ジャンル)が合っているかは、作り込む前に確かめる**。4回作り直して全部捨てた。
  「なんか違う」が続く時は、細部を足すのではなく**そもそもこの遊びがこのゲームに合うか**を先に聞くこと。

## 第54弾(2026-07-26) — 🏋鍛錬所の中身として ⚔冒険(育成RPG) を作った
第53弾で洞窟を消した跡地。ユーザー決定=**「ヒーロー全員が共通世界でドラクエみたいな冒険をして
レベルを上げていく育成RPG」「がっつりコマンド動作」「見下ろしマップを歩く」「所持している人から4人」
「TD側のレベルに反映。あとTD側とリンクしてて、TD側でクリアしたエリアにだけ行けるようにする」**。

### 中身
- **12エリア**(`RG_AREAS`)。⭐**ソロ作戦でクリアした「ステージ×難易度」のエリアにしか行けない**
  (`rgAreaOK` = `META.sc[si][di]`)。ステージ1で5・ステージ2で5・ナイトメアで2。
- **見下ろしマップを歩く**。拠点の町は固定の間取り(やどや/どうぐや/さかば/まちのもん)、
  エリアは**酔歩(drunkard walk)で自動生成**(`rgGen`)。宝箱3つと最深部の**ぬし**。
  操作は画面左下の十字キー(押しっぱなしで歩き続ける)+右上の☰メニュー。
- **がっつりコマンド戦闘**。毎ターン生きている全員ぶん たたかう/とくぎ/どうぐ/ぼうぎょ/にげる を入れ、
  すばやさ順に解決する。⚠洞窟が「見てるだけ」で失敗した反省から、手が止まる時間を作らない設計にした。
- **敵は📖図鑑の36種そのまま**(`rgFoe`)。TDのHP/攻撃を対数寄りに圧縮してエリアの深さで底上げ。
  特性もそのまま効く(装甲=身を固める / オーラ=敵全体の攻撃力up / 分裂=仲間を呼ぶ / すり抜け=回避 / ボス=なぎ払い)。
- **とくぎ24種**(`RG_SK`)・英雄11人ぶんの成長曲線(`RG_HERO`)・どうぐ7種(`RG_IT`)。
- **育成**: Lv1〜40。⭐**RPGのレベルの半分がTD側の鍛錬Lvになる**(`rgTrLv`=(Lv-1)/2・上限10=Lv21で+80%)。
- 全滅=拠点へ戻され所持金半分。ぬし撃破=そのエリアを踏破(`META.rpg.cl`)。
- 鍛錬所のタブを3つにした(**⚔冒険**が既定 / 🦸鍛える / 📖ゾンビ図鑑)。

### 教訓
- **見下ろしマップは床を明るく・壁をほぼ黒く**。最初は明暗が近く、**暗い方が地面に見えて**道が読めなかった。
- **canvasに並べる枠の幅は入れ物の幅から割り出す**。固定値にしたら門のエリア一覧で6列目がパネルからはみ出した。
- **マップが画面より小さい時は中央に寄せる**。町(21マス=546px)を左寄せにしたら右半分が真っ黒になった。

### まだ入れていない(次の候補)
装備 / 状態異常(どく・まひ) / 町の会話 / 冒険のBGM / 🔧鍛錬素材とゴールドのつながり。

## 第55弾(2026-07-26) — ⚔冒険の見た目を作り直した
ユーザー指摘=**「ちょっと安っぽすぎるな」**。素の四角を塗っただけの画面だったので、5点を直した。

1. **窓の意匠を揃えた**(`rgWin`)。影+本体+外枠(ink)+内側の細線+四隅の鋲。
   コマンド/ステータス/メニュー/メッセージを全部これに統一した。⚠素の `fillRect`+`strokeRect` は使わない。
2. **戦闘に味方の姿を出した**。今までは名前とHPの枠だけだった。
   行動する側が**一歩前に出る**(`B.mv`)、被弾で点滅、倒れると横倒しになる。
3. **演出を入れた**(`rgFx`/`rgDrawFx`)。ダメージ数字が飛ぶ・斬撃の弧・魔法のリング・全体技の爆発・
   回復の光の粒・会心で画面が揺れる・レベルアップで画面が光る。⚠これが無いと当たった手応えがまるで出ない。
4. **マップを描き込んだ**。壁に上面の厚みと側面のハイライト、床のきわに落ちる影、市松の目地、
   地形の飾り(草/線路/岩/水たまり/骨/枯れ木/街灯)を自動生成で撒く(`M.dec`)。画面のふちを暗く落とした。
5. **町を建物らしくした**(`rgBld`)。屋根・窓・看板・扉。街灯(光る)と**町の人3人**を置いた。
   ⚠誰もいない町は「作りかけ」に見える。
   さらに**パーティが隊列で付いてくる**ようにした(`RG.trail`)。

### 直した寸法(393pxの画面に収める)
敵の足元=150 / 名前帯=156〜172 / HPバー=174〜179 / 味方の足元=256(絵の高さ54) / 下段の窓=h-132。
⚠この4つのどれかを動かすと必ずどこかが重なる。

### 教訓
- **キャラの枠だけ出して姿を出さないと安っぽく見える**。戦闘は一番長く見る画面なので、ここから直す。
- **NPCをパーティより小さく描くと人に見えない**(最初トカゲに見えた)。同じ大きさで描くこと。

## 第56弾(2026-07-26) — 🏋鍛錬所を一旦しまった
ユーザー決定=**「一旦鍛錬所っていう概念は選べないようにしてあとから作りこもう。ヒーローはそのままで」**。
`TR_OPEN=false` を1つ足して、①タイトルのボタンを出さない(DEVでも出さない)②🔬研究所の解放項目から外す。
⚔冒険・📖ゾンビ図鑑・🔧鍛錬素材・鍛錬Lvのコードは**そのまま残してある**ので、
**再び開ける時は `TR_OPEN` を true にするだけ**。英雄まわり(💎召集・TDでの出撃・必殺技)は今までどおり。
⚠🔧鍛錬素材はガチャの重複で貯まり続けるが、しまっている間は使い道が無い(再開時にそのまま使える)。

## 第57弾(2026-07-26) — 効果音29種を本物の素材に差し替えた
「そろそろ見た目に入っていきたい」の第1段。**効果/手間比が一番大きい音から**着手した。

### 素材
永井さんのDownloadsにあった **Sonniss #GameAudioGDC Bundle**(5zip・6.6GB)を使用。
Readmeに `LICENSE: ROYALTY-FREE / Use them personally or commercially without attribution` と明記=**商用可・クレジット不要**。
ゲームに合うライブラリが揃っていた: Epic Stock Media「Tower Defense Game」「Humanoid Creatures(不死者の声)」
「Halloween Game(狼の唸り/Gore)」、David Dumais「Melee Weapons」、Alexander Kopeikin「Designed Ice」、
Cinematic Sound Design のUI系5パック、Federico Soler「Trailer Alarms/Booms」ほか。

### 仕組み
- **`tool_sfx.js` を新設**(scan / build / embed)。
  ① `silencedetect` で長い多テイク素材から「音のかたまり」を切り出す
  ② ピークを測って-1dBへ正規化 → フェード → モノラル22050Hz
  ③ **MP3**へ変換(⚠ogg/vorbis と ogg/opus は Safari が再生できないことがあるので使わない)
  ④ base64にして index.html の `/* <<SFXB>> */` 〜 `/* <</SFXB>> */` へ流し込む
- **29種で130KB / base64 174KB**。index.html は 0.74MB。単一HTMLのまま。
- 再生は `sfxPlay()`。**素材が無ければ今までの合成音(`SFXSYN`)に落ちる**ので、
  ヘッドレステストは今までどおり通る。呼び出し側(`sfx.shot()` など)は**1か所も直していない**。
- 連射音はピッチを少し揺らす(`SFX_VAR`)+ **同じ音は45msに1回まで**(`SFX_MIN`)。
  これが無いとガトリングで20発ぶん重なって耳が痛い。
- **`test_sfx.js` を新設**: ヘッドレスChromeで実際に `decodeAudioData` が29本とも通るか見る。
  ⚠Node環境にはAudioContextが無いので test_headless では合成音側しか通らない=この検査が要る。

### 教訓
- **ffmpegで音を切り出す時は `-vn -sn -dn -map_metadata -1` を必ず `-i` の後ろに付ける**。
  素材WAVにジャケット画像が埋まっていて、付けないと**oggの中にtheoraの動画が入り1本30KBに膨らむ**
  (合計550KB→170KBになった)。`-i` より前に置くと入力側の指定と見なされて無視される。
- **ブラウザ向けの音はMP3**。ogg/vorbis も ogg/opus も Safari(iPhone)で鳴らない可能性がある。

## 第58弾(2026-07-26) — ゾンビの声を怖くした(9種→ステージ別にくじ引き)
ユーザー指摘=**「ゾンビの声これ以外ない？もっと怖めの」**。バンドルを掘り直して差し替えた。

### 差し替え・追加した声
| キー | 素材 | ねらい |
|---|---|---|
| `growl`(ボスの唸り) | **アステカの死の笛**(InMotionAudio - The Death Whistle) | 実在の「世界一怖い楽器」。狼の唸りより格段に怖い |
| `growl2` | 人狼の唸り(旧growl) | くじの片方として残した |
| `moan` | 凶暴な人型の吐息(SoundBits - Vox Bestiae) | 短く鋭い。通常ゾンビ |
| `moan2` | 旧moan(きしむ吸気) | |
| `moan3` | 幽体の苦悶(Vox Bestiae) | |
| `moanW` | **水棲生物のごぼごぼ**(Vox Bestiae) | ②沈んだ港のゾンビ |
| `moanN` | **蟲の震え声**(Vox Bestiae) | 🌑ナイトメアの獣 |
| `moanBig2` | **T-Rexの咆哮**(344 Audio - Dinosaurs) | 大型・ナイトメア |
| `die` | 途切れる死の声(Humanoid Creatures Vol 4) | **断末魔**。撃破時に混ぜる |
| `bite2` | オークの重い攻撃声 | 噛みつきのくじ |
| `scratch` | 爪で壁を掻く音(Sinister Textures 5) | ナイトメアのBGMに時々混ぜる |
| `elite` | **歪んだ悲鳴のささやき**(Halloween Game・Jumpscare) | エリート出現。前より怖い |

### 仕組み
- ⭐**同じ声が繰り返されると怖くない**。`SFX_SET` で1つのキーに複数の素材を割り当て、鳴らすたびにくじを引く。
- ⭐**ステージで声が変わる**(`sfxScene()`): ①廃線=人型 / ②沈んだ港=水棲 / 🌑ナイトメア=獣・蟲。
  ⚠`isNM()`/`STAGE` は `sfx` を組み立てる時点ではまだ無いので、**鳴らす瞬間に try/catch で見る**
  (constのTDZは typeof でも例外を投げるため typeofガードでは防げない)。
- 全38種で base64 239KB / index.html は 0.80MB。単一HTMLのまま。

## 第59弾(2026-07-26) — 銃声を作り直し、タワーごとに発射音を変えた
ユーザー指摘=**「最初のライフルタレットの音をもっと銃撃っぽく」「タレットの種類によっても音を変えて」**。

### 銃声は「重ねて」作った
⚠**バンドルに銃声の素材が無い**。そこで実際のゲーム音響と同じやり方で組み立てた:
**①鋭い立ち上がり(鞭のスナップ / クラッカーの破裂) + ②胴鳴り(花火の至近爆発)**。
`tool_sfx.js` に `mix:[…]` を足して、層ごとに ピーク合わせ→音量バランス→`asetrate`でピッチと速さ→`adelay`でずらす、
を通してから `amix` で混ぜている。ピッチを変えるだけで「軽いガトリング/重いスナイパー」が作り分けられる。

### タワー20種の発射音
`TW_SFX`(id→音のキー)を1つ置いて、発射する所すべてを `twSfx(C,T)` に通した。
⚠**TOWERSの定義には手を入れていない**ので、音の差し替えはこの表だけで済む。
- ライフル櫓=`shot`(軽い小銃) / ショットガン台=`shotgun`(低く長い) / スナイパー塔=`snipe`(重い一発)
- ガトリング要塞=`gat`(速く軽い) / 要塞砲・重砲台=`cannon` / 迫撃砲・擲弾砲台=`mortar`
- レーザー塔=`laser`(電子ノイズ) / レールガン=`rail`(ジェット+電撃) / プラズマ砲=`plasma`(液体+電撃)
- 音響砲=`sonic`(金属を弓で擦る音) / 酸噴射器=`acid` / 火炎放射塔=`flame` / ドローン基地=`dronefx`(サーボ)
- テスラ系=`zap` / 冷却塔=`frost` / 拘束ネット砲=`net`
全48種 base64 278KB / index.html 0.84MB。

### 教訓
- ⭐**素材を重ねる時は、層ごとに先にピークを揃えてから混ぜる**。録り音の小さい素材をそのまま混ぜたら
  仕上がりが**-43dB(ほぼ無音)**になった。`tool_sfx.js` は2段構え(素の音を一時WAVへ→ピークを測ってMP3へ)にして、
  仕上がりのピークも毎回表示するようにした。
- ⭐**`test_undef.js` の先頭で文法を見るようにした**。`if(C.isMe){…}}` を `twSfx(C,T);}}` に置き換えて
  `}` が1個余った時、名前の照合は素通りして test_headless で初めて落ちた。**括弧のずれは先に文法検査で捕まえる**。

## 第60弾(2026-07-26) — 🔊音の確認画面(?sfx=1)
ユーザー指摘=**「いちいち音確認でゲーム進めないといけない。もっと簡単に確認する方法ない？」**。
公開URLに **`?sfx=1`** を付けると、**効果音48種を一覧でタップして鳴らせる画面**が開くようにした。
- タブ4つ(🔫タワー / 🧟ゾンビ / 🖥UI・経済 / 🏁節目)。カードにキー名と「どこで鳴るか」を書いてある
- 「▶このタブを順に鳴らす」で620msおきに全部流せる。音量スライダーつき
- ⚠くじ引き(`SFX_SET`)を通さず**指定したキーだけ**鳴らす(`sfxTry`)。45msの連打制限も無視する
- 🛠DEVモードならタイトルにも「🔊 音の確認」ボタンが出る
- `test_shot.js` に `sfx` モードを追加

### 教訓
- ⚠**置換パッチの文字列に `\b` のようなバックスラッシュを書かない**。`/\bsfx\b/` と書いたつもりが
  **バックスペース文字(0x08)がそのままファイルに入り**、正規表現が一致しなくなった(撮影モードが無反応になった)。
  文法検査も未定義名検査も素通りする。**`indexOf` など素直な書き方にすること**。

## 第61弾(2026-07-26) — ショットガンの音を第57弾のものに戻した
ユーザー指摘=**「ショットガンの音変えた？前のほうがよかったんだけど」**。
第59弾で「クラッカーの破裂+花火」の合成に変えていたが、第57弾の
`EXPLDsgn_Explosion Small Blast Enemy Death Crunchy Boom`(1本もの・0.30秒・ピッチ0.80)に戻した。
⚠**ショットガンだけは重ねない**。tool_sfx.js のレシピにその旨をコメントで残してある。

## 第62弾(2026-07-26) — 重テスラの音をテスラコイルと分けて重低音を足した
ユーザー指摘=**「テスラと重テスラ同じにせず、重テスラの音は少し重低感だして」**。
`zap2` を新設して `coil`(重テスラ)に割り当てた。テスラコイル(`zap`)は**触っていない**。

3層を重ねて作ってある:
1. 電撃(`Impact Electric Tonal Deep`)をピッチ0.72まで下げたもの ← 電気らしさは残す
2. トレーラーのブーム(`Booms_Vol2_075`)をピッチ0.80 ← 胴鳴り・重さの主役
3. 金属の低音(`Metal Scrape Low Tonal LFE`)を薄く ← 芯の低域

長さも 0.32秒 → **0.62秒** に伸ばした(重テスラは「ゆっくり伝わって跳ねるほど重くなる」タワーなので、
テスラコイルと同じ短さでは役割が伝わらない)。

### 数字での確認(音は聴けないので帯域で測った)
| | 低域(200Hz以下) | 高域(1.2kHz以上) | 差 |
|---|---|---|---|
| `zap`(テスラコイル) | -12.6dB | -21.9dB | 9.3dB |
| `zap2`(重テスラ) | -9.1dB | -27.0dB | **17.9dB** |

低域が高域に対して 9.3dB → 17.9dB 優勢になった=はっきり重くなっている。

## 第63弾(2026-07-26) — 噛みつかれているのに反撃できない部隊を直した
ユーザー指摘=**「ユニットが旗まで進行してるときに、敵に攻撃されてるのにユニットは旗までしか行けないせいで攻撃できない」**。

### 原因
- ゾンビが部隊に噛みつける窓は `gap=u.d-z.d` が **-6〜36**
- 部隊の索敵は `gap<=U.rng`。ところが**近接兵科の射程は34〜40しかない**
- つまり **猟犬(射程34)** は、gap 35〜36 で止まったゾンビに**届かない**。
  しかも部隊は旗より前へ出られないので、**詰めることもできず一方的に殴られ続ける**。
  バット(36)と盾持ち(36)も境界ぎりぎりで、止まる位置しだいで同じことが起きていた。

### 直し方
`ENG_GAP=36` を定数にして、噛みつきの窓と部隊の索敵の両方から参照するようにした。
索敵は **`Math.max(U.rng, ENG_GAP+2)`** =**噛みついてくる相手には必ず届く**。
⚠射程そのものは変えていない(遠くの敵に届くようにはなっていない)。届くのは「自分に噛みついている敵」だけ。

### 検査
`test_headless.js` に `checkBite()` を新設。近接5兵科すべてについて、
gap=36 に置いたゾンビへ反撃できるかを見る。**修正前のコードに対して流すと猟犬で落ちる**ことを確認済み。

## 第64弾(2026-07-26) — エフェクト第1弾(タワーの発射・着弾/近接の斬撃)
「見た目」の2番目(音の次)。外部素材は一切使っていない=全部コード。

### 足したもの
- **`mzl` 銃口の閃光** — ⚠丸い光を1つ置くだけでは撃った感じが出ないので、**向きを持った扇形**にした。
  色と大きさをタワーの型で変える(実弾=白金/火炎=橙・酸=黄緑/榴弾=山吹/電撃=水色/ネット=翠)。
  要塞砲・重砲台・レールガン・プラズマ砲は一回り大きく、足元に**`shock` 衝撃波**も出る。
- **`hit` 着弾** — ⚠**飛来した向きの逆へ**破片が散る(向きが合っていないと当たった感じにならない)。
- ⭐**`ric` 装甲に弾かれる火花** — **装甲持ちに通常弾(貫通なし)が当たると、金色の火花が浅い角度で跳ねる**。
  相性が悪いことが音を消しても目で分かる。
- ⭐**`slash` 近接の斬撃** — ⚠**今まで近接攻撃には絵が1つも無かった**。当たった手応えが皆無だったので追加。
- **進化段階が上がるほど豪華に**(ユーザー指示)。`uFxTier()` が 0〜3 を返し、
  斬撃の太さ・銃口の閃光の大きさ・弾道の太さ・色(白→金→水色→紫)が段階で変わる。英雄は最大。

### 直したついで
- エフェクトの持ち時間が2か所にベタ書きされていたので `fxLife()` にまとめた。

### 検査
`test_headless.js` の `checkBite()` を拡張し、**斬撃・着弾・銃口の閃光・装甲に弾かれる火花が実際に出ているか**を数える。

### 教訓
- ⚠**エフェクトの検査は1ステップごとに拾う**。斬撃0.26秒・銃口の閃光0.14秒しか生きていないので、
  まとめて回してから `C.fx` を見ると**消えた後で何も見つからない**(実際にそうなった)。

## 第65弾(2026-07-26) — エフェクト第2弾(投擲の軌道/撃破/砲撃の着弾)
第64弾の続き。**遊びの数値には一切触っていない**=当たり判定は今までどおり即時で、見た目だけを並べ替えている。

### 仕組みを2つ足した
- **`lf`(1つずつ違う持ち時間)** — `FX_LIFE` の表は種類ごとの固定値だったが、
  投擲は飛ぶ距離で、死体は大きさで持ち時間が変わる。ex に `lf` を入れれば個別に指定できる。
- ⭐**`later(C,秒,関数)`(少し後で起こす待ち行列)** — 投げた物が飛んでいる間に爆発の絵が出てしまうと
  当たった感じにならないので、**着弾に合わせて演出だけ遅らせる**。campStepで進む。
  ⚠ここにダメージ計算を入れないこと(入れると弾が外れる・バランスが変わる)。
  ⚠中身が関数なのでセーブには乗らない(`saveRun`は項目を並べて書き出す方式なので問題ない)。
- 描画の頭で `e.d0` を見て「待ち時間ぶんは描かない」ようにしたので、`d0` がどの種類でも使えるようになった。

### 足した絵
- ⭐**`toss` 投擲の軌道** — 擲弾兵・迫撃兵・ロケラン・重擲弾兵・メカスーツ・ヴァルキリー・火炎瓶・冷凍弾が
  **山なりに物を投げる**。手榴弾/火炎瓶/冷凍弾で見た目が違い、尾を引くので次にどこへ落ちるか目で追える。
- ⭐**`flame` 炎の帯** — **火炎放射兵は今まで銃と同じ曳光線を出していた**(何を撃っているのか分からなかった)。
  火炎放射塔・酸噴射器にも吹かせた(こちらは銃口の粒しか無く、届いている感じが出ていなかった)。
- ⭐**`corpse` 倒れる死体** — 今までゾンビは**消えて無くなるだけ**だった。倒した方向へ倒れてしばらく残る。
  大きいほど長く残り(ボス2.4秒/エリート1.6秒/雑魚1.0秒)、大物は足元に土煙(`dust`)が出る。
- **`pool` 火の海** — ナパームと火炎瓶の着弾地点がしばらく燃え続ける(燃焼時間に合わせてある)。
- **`ice` 氷の破片** — 凍結爆弾の着弾。地面が白く凍り、輪郭付きの氷が突き立つ。
- **`dust` 土煙** — 着弾・大物の撃破で低く速く広がる。
- **砲撃5種の着弾を作り直した** — 標準=衝撃波+土煙+残り火 / 絨毯爆撃=5発それぞれに土煙と衝撃波(時間差) /
  凍結=氷+白い霜 / ナパーム=火の海 / 機関銃掃射=土煙。どれも焦げ跡(凍結だけ白い霜)が地面に残る。

### 検査
- `test_headless.js` に **`checkFx2()`** を新設。投擲・炎・火の海・氷・土煙・死体・砲撃5種の着弾が
  実際に出ているかを1ステップずつ数える。火炎放射兵が**もう曳光線を出していない**ことも見る。
- `test_shot.js` に撮影用の口を3つ足した: **`u=<兵科id>`**(3体出して撃たせ続ける・`die`で倒れるまで放置) /
  **`stk=<砲撃id>`**(着弾を撮る) / **`fxdemo`**(新しい演出を並べて寿命の途中で止めて撮る)。

### 教訓
- ⭐**一瞬の演出は「実戦を撮る」より「並べて止めて撮る」方が速い**。`fxdemo` を作るまで、
  実戦の撮影で当たりを待って何度も撮り直していた。
- ⭐**ヘッドレスの `setInterval` は間隔が長いとほとんど回らない**。撮影用の仕込みを1300msにしたら
  砲撃が1発も撃てなかった(90msに縮めて条件で間引いたら通った)。
- ⭐**撮影用に敵を並べる時 `zombies[0]` を掴んではいけない**。倒したり漏れたりで入れ替わるので、
  **足したばかりで px=0 の個体**を掴むことがあり、距離で効く処理が永久に空振りする。
- ⭐**新しい絵も「太い黒フチ+べた塗り」に合わせる**。氷を白い線だけで描いたら**宙に浮いた三角**に見えた。
- ⭐**火は「丸を並べる」と炎に見えない**。根本が太く先が細い舌の形にする。逆に重ねすぎると画面が一面の光になる。

## 第66弾(2026-07-26) — タワーの「撃ち方」と発射音をタワーごとに分けた
ユーザー指摘=**「冷却塔の音をひんやり感な音に。要塞砲がただのショットガン。ドローン基地ももっとドローンぽく。
プラズマ砲もただの迫撃砲にしか見えない。その他、威力とかが多少変わってるだけで変化ないやつはしっかり変化させて」**。

⚠**数値(威力・射程・間隔・体数・範囲)には一切触っていない**。変えたのは絵と音だけ。

### 🔊 音(4つ作り直し・3つは使い回しをやめた)
使い回していたもの: 要塞砲=重砲台(`cannon`) / 擲弾砲台=迫撃砲(`mortar`) / 冷却塔=凍結爆弾(`frost`)。全部分けた。
| | 前 | 後 |
|---|---|---|
| ❄冷却塔 | 凍結爆弾と同じ | **専用の`cryo`**=地面が凍るパキパキ+鈴の煌めき+氷が締まる音。**爆発系の音を1つも混ぜない**のが肝 |
| 🏰要塞砲 | 重砲台と同じ | **専用の`fort`**=閂を落とす金属音 → 巨砲の一発 → 破片が飛び散る尾 の3段 |
| 🟣プラズマ砲 | 水のはじけ(迫撃砲と紛らわしい) | 電気の充填 → 放電 → 低音のバースト |
| 🚁ドローン基地 | 「建設」と同じ素材の早回し | 回転翼のブーン + サーボの立ち上がり |
| 💣擲弾砲台 | 迫撃砲と同じ | 軽く短い「ポンッ」 |

素材はいずれも Sonniss #GameAudioGDC Bundle。埋め込みは49種→**52種**(base64 309KB)。

### 🎨 撃ち方(絵)
- ⭐**ドローン基地** — **機体が本当に飛び出す**。塔から2機が出て、敵の上まで飛び、その場で撃って戻る
  (回転翼のブレ・標識灯の点滅つき)。⚠今までは塔から曳光線が出るだけで、ライフル櫓と見分けが付かなかった。
- ⭐**プラズマ砲** — 黒い砲弾ではなく**プラズマ球**(光る球+電気の触手+尾)が低く速く飛び、
  着弾は火薬の爆発ではなく**エネルギーの炸裂**(`pboom`)になる。
- ⭐**要塞砲** — 細い曳光線4本 → **極太の貫通弾4本**(`beam`)+反動の揺れ+薬莢。最終兵器らしくした。
- ⭐**冷却塔** — 凍らせた敵の足元に**氷が張る**(`ice`)+塔から冷気の白い波。拘束ネット砲と同じ輪1つではなくなった。
- **音響砲** — 弾を撃たず、**音の輪**(`wave`)が狙った向きへ押し寄せる。
- **ショットガン台** — 銃口から**散弾の扇**(`spread`)。
- **レーザー塔** — 弾ではなく細いピンクの**ビーム**。
- **ガトリング要塞** — 薬莢が滝のように落ちる。
- **砲弾の大きさを砲ごとに変えた**(重砲台=大玉 / 迫撃砲=中 / 擲弾砲台=小玉)。
- `beam` が色を受け取れるようにした(レールガン=紫 / 要塞砲=金 / レーザー塔=桃)。
  ⚠レールガンは以前から `col` が無視されて紫固定だったので、見た目を変えないよう紫を明示してある。

### 検査
`test_headless.js` に **`checkTwFx()`** を新設。
- 冷却塔/ドローン/要塞砲/音響砲/ショットガン/レーザー/プラズマ/レールガン が**それぞれ狙いの絵を出しているか**
- 撃ち方を分けたタワーが**汎用の曳光線(tr)に戻っていないか**
- `TW_SFX` に**同じ発射音の使い回しが残っていないか**+その音が埋め込まれているか

### 道具
`tool_sfx.js build` に**キー指定**を足した(`build sfx_src cryo,fort` で3つだけ焼ける)。
⚠生WAVは消してあるので、作り直す時は zip から必要な数本だけ取り出せば済むようにするため。

### 教訓
- ⭐**「威力が違うだけ」は手抜きに見える**。タワー/兵科を足す時は**絵と音を必ず1つずつ変える**。
  数値だけ違って絵が同じだと、遊ぶ側には**同じものが並んでいる**ようにしか見えない。
- ⚠**氷の破片のような「部品」は、大きさを範囲に比例させる**。固定にすると、足元に出す小さい氷で
  ゾンビより破片の方が大きくなった。

## 第67弾(2026-07-26) — レールガン=「きゅいーん」と充填してから撃つ超電磁砲
ユーザー指示=**「レールガンは近未来な感じの音で、きゅいーんってチャージしてから超電磁砲を放つ感じにしたい。
んで超電磁砲は長めの砲撃(ビームが長め)にしたい」**。

⚠**威力・射程・撃つ間隔は変えていない**。変えたのは音と絵だけ。

### 音を2本に分けた(埋め込み52種→53種)
- **`railChg`(充填)** … 上がっていく電気(ArcPowerUp)+高い鈴の煌めき=「きゅいーん」。
  ⚠上がっていく系の素材でないと「きゅいーん」にならない。
- **`rail`(発射)** … 電気の放電を主役に、ジェットの抜けと低い胴鳴りを重ねた=近未来の超電磁砲。
  前は0.42秒の軽い音だったので0.95秒に伸ばした。

### 充填の仕組み(⚠見た目と音だけ)
`tw.cd`(次に撃つまでの秒数)が **`RAIL_CHG`=0.55秒**を切ったところから `tw.chg` が 0→1 で溜まり、
0になった瞬間に撃つ。**撃つ間隔(T.rate=3秒)そのものは1ミリも変えていない**。
- 溜まり始めに `railChg` を1回だけ鳴らす
- 塔の絵: 2本のレールの間を電気が走り、砲口へ輪が吸い込まれ、光が溜まっていく
- ⚠⏩連射を強化して間隔が0.55秒より短くなった時のために、実際の間隔の6割で頭打ちにしている

### ビームを「長め」に
`beam` に **`sus`(撃ちっぱなし)** を足した。普通のビームは出た瞬間が一番強くて素直に細く消えるが、
susは**太いまま残って最後に一気に切れる**。レールガンは **0.3秒 → `RAIL_BEAM`=0.85秒**。
発射時に砲口の衝撃波と画面揺れも足した。
※ビームの**長さ**(射程900=ゲーム内で最長)は数値なので触っていない。伸ばすかは要相談。

### 検査
`checkTwFx()` に追加: `tw.chg` が 0→1 まで溜まってから撃っているか / ビームが普通(0.3秒)より長く残るか /
充填音が埋め込まれているか。

## 第68弾(2026-07-26) — ドローンは常時2機が漂う / レールガンは並びを狙う
ユーザー指示=**「ドローンは常時2体のドローンが浮きながら軽く移動して小粒の弾を打つ感じにして。
あとレールガンは敵に一番多く当てれるとこ狙うようにとかできる?」**。

### 🚁 ドローン基地: 2機が常に浮いている
第66弾では「撃つたびに機体が飛び出して戻る」だったが、**常時2機が塔の周りを漂う**形に変えた。
撃つ時は**その2機がそれぞれ小粒の弾(`pel`)を撃つ**。塔の本体は発着台になった。
- ⭐**2機は状態を持たない** … 位置は `droneOff(機体番号, 枠番号, 時刻)` **だけ**で決まる。
  こうすると **①描画と発射で必ず同じ場所になる ②スナップショットに載らない対戦の相手盤面でも同じに見える**。
  ⚠速度や慣性を持たせると、対戦で相手側だけドローンが消える。
- ⚠**威力・射程・間隔・体数(multi:2)は変えていない**。当たり判定も撃った瞬間のまま。

### ⚡ レールガン: 一番多く貫ける向きを狙う
今までは「HPが一番高い敵」に真っ直ぐ撃っていた。**貫通するのだから狙うべきは敵1体ではなく並びの向き**。
射程内の敵それぞれへの向きについて**線上に何体乗るか**を数え、一番多い向きを選ぶ(同数なら合計HPが多い方)。
- ⚠**総当たりなので毎フレームやると重い**。0.1秒ごと(と撃つ直前)だけ引き直し、向きの候補は24本までにしてある。
- ⚠これは**当て方が良くなる=実質的な強化**。ただし数値(威力310・射程900・間隔3秒)は1つも触っていない。
  CLAUDE.mdの実戦DPSはもともと「3体に当たる前提」で174としていたので、**その前提が実際に満たされるようになった**という位置づけ。
  解放順(重テスラ92 < レールガン174 < ガトリング226 < プラズマ307 < 要塞砲482)は崩れていない。
- 説明文も「一番多く貫ける向きを自動で狙い、線上の敵を全部貫く」に更新。

### 検査
`checkTwFx()` に追加:
- ドローン2機が重ならず・時間で動き・塔から離れすぎないこと(`droneOff` を直接呼ぶ)
- レールガンが**まとまった群れの並び**を選んで1発で3体以上に当てること / `tw.aim` が決まっていること

## 第69弾(2026-07-26) — 🛠DEVモードはタワーと兵科を最初から全開放
ユーザー指示=**「実機テストのときは最初からユニットもタレットも全開放しといて」**。
毎回⚙️を貯めて解放していては実機確認が回らないため。⚠**通常モードは今までどおり**(タワー2種・兵科2種・建設マス7つ)。

### 開けたもの(`?dev=1` のときだけ)
- **タワー20種**(基本10+研究所10) / **兵科32種**(基本8+研究所24)
- **建設マス全部** = 通常22 + 廃品工房3 + 支援2
- **砲撃5種**(🎯🔫🛩❄🔥)と研究所の「新しい種類」(`META.nt`/`META.nu`)

### ⚠仕込みが3か所ある(片方だけ直すと元に戻る)
1. `metaTowerCap()`/`metaUnitCap()` … DEVでは全種類を返す。
   ⚠`soloMeta()` は `G` を見るので、**拠点を作る瞬間(newCamp)はまだ false になり得る**。
   実際これで最初「タワー10/20」しか開かなかった。DEVの判定はここに置くのが確実。
2. `newCamp()` … `unlocked`/`uUn`/`ecoN`/`supN`/`slk` の初期値
3. `startSolo()` の「開始時ボーナス」(`me.uUn=` と `me.slk=`)と `resumeRun()` の復元
   … **ここで上書きされて巻き戻る**。実際これで最初「兵科2/32」のままだった。

研究所側の解放(`META.nt`/`META.nu`/`META.st`)は**起動処理の最後**で開けている。
⚠`devTop()`(METAを読んだ直後)でやると `T_PLAY` などの後で定義される const が **TDZ で落ちる**
(CLAUDE.mdの「constのTDZはtypeofでも投げる」と同じ罠)。

### 検査・道具
- `checkDevLoad()` を拡張し、DEVで1戦始めて **タワー/兵科/マス/工房/支援/砲撃が全部開いているか**を数える。
- `test_shot.js` に `dev` を足した(`node test_shot.js out.png 852 393 dev`)。
  デッキは32枚でも**横スクロールで収まる**ことを実画面で確認済み(操作バーは崩れない)。

## 第70弾(2026-07-26) — 重テスラの音を重く・電気感を強く・「ぶぅぅん」を足した
ユーザー指摘=**「重テスラの音が軽いからもうちょっと重くしてもっと電器感が欲しい。ぶぅぅんみたいな高電圧感も欲しい」**。

### ⭐原因は「低音の置き場所」だった
第62弾で低音を足したはずなのに軽い——測ってみたら、**低音が全部120Hz以下に溜まっていた**。
**スマホのスピーカーは120Hz以下をほぼ鳴らせない**ので、その低音は聴こえないまま音量だけ食っていた。
(旧zap2: 超低域 -9.2dB に対し、**実際に聴こえる 120〜400Hz は -28.1dB しかなかった**)

### 直し方
- `highpass` で58Hz以下を削り、空いた分を **200Hz付近に+9dB** 回した=同じ音量でずっと重く聴こえる
- 4層に組み直した: ①低く落とした放電(主役) ②**Buzzを大きく下げて唸らせた「ぶぅぅん」** ③実物の電気コイルのハム ④低い胴鳴り
- 2.6kHzを少し立てて放電の「バチッ」を出し、4.5kHz以上は下げて軽さを消した
- 長さ 0.62秒 → **0.90秒**(ハムが鳴り続けて「高電圧感」が出る)

### 数字での確認(耳では確かめられないので測った)
| | 長さ | 超低域<120 | **120-400(スマホで聴こえる重さ)** | 中域 | 高域>1.2k |
|---|---|---|---|---|---|
| `zap` テスラコイル | 0.32秒 | -12.9 | -22.2 | -27.8 | -21.9 |
| `zap2` 旧 | 0.62秒 | -9.2 | **-28.1** | -34.2 | -27.0 |
| `zap2` 新 | 0.90秒 | -18.9 | **-20.3** | -26.0 | -26.0 |

残り方(前半/中盤/後半): 旧 -8.1/-12.0/**無音** → 新 -13.3/-18.3/**-21.7**=最後まで唸り続けている。
⚠テスラコイル(`zap`)は軽いままにしてある=役割の違いが音で分かるのが狙い。

### 道具
`tool_sfx.js` のレシピに **`af:`(EQなどの追加フィルタ)** を足した。
⚠EQで持ち上げたぶんも含めてピークを合わせるため、**測る前にEQを掛ける**ようにしてある。

## 第71弾(2026-07-26) — 💎英雄召集に専用の演出を付けた
「見た目に入る」計画の**③ガチャ専用画面と演出**。
今までは押した瞬間に**結果がテキストの一覧で出るだけ**で、引いた手応えが何も無かった(レシートのようだった)。

⚠**確率と配るもの(`gcPick`/`gcApply`)には一切触っていない**。演出は結果を受け取って見せるだけ。

### 演出(パネル全面のcanvas `#gc-cv`)
1. **魔法陣**(1.15秒) … 外から光の粒が中心へ吸い込まれ、二重円+目盛り+内側の三角が回りながら光る
2. **炸裂**(0.45秒) … 白の閃光 → 色の光条 → 中心の芯
3. **1枚ずつめくる** … タップで次へ。「≫ まとめて見る」で途中から一覧へ飛べる

- ⭐**光の色がその回の最高レア度を予告する**(ガチャの肝)。
  はずれ=緑 / ★1=白 / ★2=青 / ★3=紫 / ★4=金 / ★5=赤金。★4以上は光条が倍に増える
- カードは**左に姿・右に文字**の2段組(横画面は高さが足りないので縦積みだと姿が潰れる)。
  **英雄の姿が実際に大きく出る**ようになった(宿題6「プレビューを全容が見える大きさに」の一部)
- はずれ枠も大きなアイコン+中身+説明のカードで出す=「無駄玉ではない」のが見て分かる
- 音もレア度で変える(★5=勝利 / ★4=ボス撃破 / ★3=エリート / ★1-2=制圧 / はずれ=購入)。
  ⚠音は**炸裂の瞬間**に鳴らす。押した瞬間に鳴らすと魔法陣より先に結果が分かってしまう

### 検査・道具
- `test_headless.js` に `checkGachaFx()` … 段階が進むか / 押すと次の1枚へ行くか / 最後まで見せたら閉じるか /
  「まとめて見る」で閉じられるか / **演出が結果の中身を書き換えていないか**
- `test_shot.js` に `gacha5`(★5を引いた状態)・`gburst`(炸裂)・`gcard`(カード)を追加。
  ⚠ヘッドレスの仮想時間ではrAFがほとんど回らず、放っておくといつまでも魔法陣のままなので段階を直に進める

### 教訓
- ⭐**パネル全面を覆う演出は、見出し(✕)だけ上に出す**。でないと途中で閉じられなくなる。
- ⭐**canvasの寸法はモーダルを開いた瞬間には確定していない**。1回測って持つと**円が楕円に潰れる**。
  毎フレーム見比べて測り直すこと。
- ⭐**閃光は白を先に敷いてから色を重ねる**。逆にすると全部混ざってセピア色の日章旗になった。
- ⭐**絵文字は `str[0]` で切ってはいけない**(2文字ぶんのコード単位)。`Array.from(str)[0]` を使う。

## 第72弾(2026-07-26) — 💎召集に「撃て!」の演出を足した(展開が読める前振り)
ユーザー指示=**「タップするとタレットがゾンビをうって、軽傷/重傷/木っ端みじんの演出。木っ端みじんが一番いい。
タレットもライフル/ショットガン/レーザーでレーザーが一番レア。ゾンビも3種類。展開がある程度読める演出に。
最終的なGET演出は今の感じでいい」**。

### 流れ
魔法陣 → 炸裂 → **「撃て!」でタップ → タレットがゾンビを撃つ → 傷** → GETのカード(第71弾のまま)

### ⭐展開が読める仕掛け(ここが肝)
| | ライフル櫓 | ショットガン台 | レーザー塔 |
|---|---|---|---|
| **軽傷…** | はずれ / ★1 | — | — |
| **重傷!** | ★2-3(下振れ) | ★2-3 | — |
| **木っ端みじん!!** | ★4-5(下振れ) | ★4-5(下振れ) | ★4-5 |

- **ダメージ = 結果そのもの**(軽傷=はずれ/★1 / 重傷=★2-3 / 木っ端みじん=★4-5)
- **タレット = 予告**。基本はダメージと同じ段だが、**35%で1段下がる。上振れは絶対にしない**
  → **レーザーが出た時点で★4以上が確定**する=見た瞬間に沸ける。
    逆にライフルでも木っ端みじんなら大当たり=最後まで見る理由が残る
- ★5だけは必ずレーザー+木っ端みじんの最上段で見せる

### 絵と音
- タレットは**実物の `drawTower`**、ゾンビは**実物の `drawZombie`**(ウォーカー/アーマード/ブルートの3種)。
  新しい絵を描き起こさず、ゲーム本体と同じ姿が出る
- 弾はタレットごとに違う: ライフル=1本の曳光線 / ショットガン=9本の扇 / レーザー=太いビームが残る
- 傷: 軽傷=のけぞって血が少し / 重傷=吹き飛んで傾き血が大量 / 木っ端みじん=画面が白飛び+爆風の輪+**肉片が飛んで地面に転がる**+血だまり
- 音もタレットとダメージで分岐(発射=shot/shotgun/laser、着弾=thunk/splat/boom+eliteKill)
- **「撃て!」は1.6秒待つと自動で撃つ**=止まったままにならない

### 検査
`checkGachaFx()` を拡張。段階が進むこと・自動発射・**ダメージ段が結果と一致すること**・
**予告が上振れしないこと**・**レーザーなら★4以上**・★5は必ず最上段、を全レア度で回して確認する。

### 教訓
- ⭐**飛び散る破片や血は、落ちたら地面に転がしておく**。画面外へ消す実装にすると、
  少し経った時点では**何も残っていない**画面になる(実際に「木っ端みじん」なのに空っぽの絵になった)。
- ⭐**被弾の白フラッシュは当たった瞬間だけ**。出しっぱなしにすると真っ白な人形にしか見えない。
- ⭐**`drawTower` は基部の円の中心が原点**。そのまま地面に置くと半分埋まる。

## 第73弾(2026-07-26) — 召集結果を別ウィンドウのアイコン一覧にした
ユーザー指摘=**「召喚結果が文字だらけになっちゃってるから、ソシャゲでよくあるアイコン制にしよう。
この召喚結果は別ウィンドウで、閉じるボタン的なの押したら元の画面に戻る感じで」**。

### 直したこと
- 結果を**別ウィンドウ `#md-gcres`** に出すようにした(召集画面の中に積まない)。**閉じる**で召集画面へ戻る
- **アイコンの一覧**にした。英雄は**デッキと同じサムネ(`UTHUMBS`)**、はずれ枠は資源のアイコン
  - 枠の色でレア度が分かる(★1灰 / ★2青 / ★3紫 / ★4金+光 / ★5赤+強い光)
  - 新規は左上に **NEW** の帯、下に★の数。はずれ枠は下に「**+いくつ**」だけ
  - ⚠**枠の中に入れてよい文字は★と「+数字」だけ**。文章を入れると元の文字だらけに戻る
- **押すと下の帯に説明が出る**(名前・NEW/重複・必殺技)。開いた時点で**一番レアなものが選ばれている**
- 見出しに「英雄N体(✨NEW n) / 💎残り」のまとめを出す

### 検査
DOMを組み立てる部分から **`gcResRows()`(DOMを触らないデータの関数)** を切り出して、そちらを検査する。
アイコンの数 / 英雄に絵が割り当たっているか / NEWと重複の印 / 一番レアを最初に選ぶか /
**枠の文字が6文字を超えていないか**(アイコン制が崩れていないか)を見る。

### 教訓
- ⭐**DOMを組み立てる処理は「中身を作る関数」と「差し込む関数」に分ける**。
  ヘッドレスのDOMは**差し込んだ要素を数えられない**ので、DOM側だけを見る検査は
  **0個でも通ってしまう=何も見ていないのにOKと出る**(実際に一度そうなった)。
- ⭐**書き込んだファイルに見えない制御文字が紛れることがある**(今回は0x0C)。
  症状は「どう見ても正しいコメント行で `SyntaxError: Unexpected identifier`」。
  `grep -P '[\x00-\x08\x0b\x0c\x0e-\x1f]'` で走査し、**その行を書き直せば直る**。原因探しで時間を溶かさない。
  ついでにCLAUDE.mdに残っていた同種の混入も掃除した。

## 第74弾(2026-07-26) — 💎召集の演出を構想どおりに作り直した
ユーザー指摘=**「ガチャ演出が俺の構想と違う」**。以下のとおり作り直した。

### 流れ(前 → 後)
- 前: 魔法陣 → 炸裂 → **1件ごとに**「撃て!」→ 軽傷/重傷/木っ端みじん → カード
- 後: **単発/10連を押したらいきなり「撃て!」** → タップで発射(弾が0.5秒かけて飛ぶ)
  → **確定で木っ端みじん** → **跡地に出るもの**が結果を示唆 → 1件ずつ結果カード → 召集結果ウィンドウ → 戻る

### 直したこと
- ⭐**撃つ場面は召集1回につき1回だけ**(10連でも1回)。示唆するのは**その回の一番いい結果**
- ⭐**魔法陣と炸裂は廃止**。押したらすぐ「撃て!」から始まる
- ⭐**軽傷/重傷は廃止。確定で木っ端みじん**。倒した跡地から**光る宝珠**が立ち上り、
  **★の数と色**で結果を示唆する(★4以上は「すごい気配…!!」+光条)
- ⭐**レアでない時は必ずライフル+通常ゾンビ**。予告は**下振れだけ**なので**レーザー=★4以上が確定**
- ⭐**奥行きを付けた**: タレットが手前(大きい)・ゾンビが奥(小さい)。地面は消失点へ収束する線で斜めに見せる
- ⭐**弾が0.5秒かけて飛ぶ**(目で追える)。ライフル=尾を引く1発 / ショットガン=広がる粒 / レーザー=伸びるビーム。
  奥へ行くほど小さくなる
- ⭐**「撃て!」は押すまで絶対に進まない**(前は1.6秒で自動発射していた)。
  弾が飛んでいる途中もタップで飛ばせない=何が起きたか分かるようにする

### 検査
`checkGachaFx()` を作り直し: 最初から「撃て!」であること / **押さずに6秒進めても進まない**こと /
弾が飛ぶ間は飛ばせないこと / 着弾すること / **2枚目以降は撃つ場面に戻らない**こと /
予告が上振れしないこと / **レアでない時は必ずライフル** / ゾンビの段がタレットと揃っていること /
10連の示唆が「一番いい結果」であること。

### 教訓
- ⭐**`test_shot.js` のオプション名に「w+数字」を入れてはいけない**。時間指定が `/w(\d+)/` の
  **最初の一致**を拾うので、`gtw2` と名付けたら**仮想時間が2msになり画面が何も出なかった**
  (タイトル画面が撮れるだけで、原因が分からず時間を溶かした)。`gt2` に改名した。
- ⭐**0.05を10回足しても0.5には届かない**(浮動小数)。「N秒後に起きること」を検査する時は
  ステップ数を少し多めに回すこと。

---

## 第75弾(2026-07-26) — 書体と色を「表」にまとめて統一した

「見た目に入る」計画の④。**フォント+パレット統一**。⚠外部フォントは埋め込まない(ユーザー判断)。

### 何が揃っていなかったか
- **canvasとHTMLで書体が違った**。canvasの文字は74か所すべて `900 Npx sans-serif`、
  パネル側は丸ゴシック指定。**同じ画面の中で2種類の書体が混ざっていた**
- **CSS変数が8色しかなかった**。UIのCSSだけで直書きの色が57種あり、
  資源色(⚙️金/🔩水色/🧬緑)やレア度色が場所ごとにバラバラ。レア度色は `.hcard` と `.grc` に同じ値が二重に書いてあった
- ファイル全体で**334色**まで増えていた

### やったこと
- **色と書体の表を2か所に置いた**(同じ値):
  CSSの `:root` と、JSの「見た目の決まりごと」ブロック(`ICONS-END` の直後)
- **書体は `FF` 1本に統一**。canvasの74か所を `'900 '+px+'px '+FF` に置換し、CSSの body は `var(--ff)` にした
- **役割で選ぶ色にした**: 基調(`INK`〜`INK4`/`VOID`・`PANE`/`PAPER`/`PAPER2`/`PAPER3`/`DIM`/`DIM2`)、
  差し色(`RUST`/`AMBER`/`GOLD`/`GLOW`/`TOXIC`/`LIFE`/`TECH`/`FROST`/`STEEL`/`HEROC`/`DANGER`/`GONE`)、
  レア度(`RARE[]`・CSSは `--r1`〜`--r5` と地の色)
- **近い色を寄せた**: 灰系6色→2色 / 暗色8種→4段階+`VOID` / 朱3種(#ff5a4a/#ff6a4a/#ff5a3a)→`DANGER` /
  戦闘不能の名前2種→`GONE` / 純白 `#fff` → セピア白 `PANE`(暗い地の上の文字とパネルの地)
- **HTML本文の直書き色をゼロにした**(インラインstyleの4か所を `var()` へ)
- 結果: **334色 → 314色**。残っているのは**キャラ/ゾンビ/タワー/背景の絵の色だけ**
  (⚠絵の色は⑤⑥で作り直す時まで触らない=今寄せると絵が壊れる)

### 検査
`checkPalette()` を追加(ソースを読む静的検査):
①canvasの文字が全部 `FF` を通っているか(`.font=` の数と `px '+FF` の数が一致・`sans-serif` 直書きが無い)
②CSSの `:root` とJSの定数**27組**が同じ値か ③canvasの文字色に直書きが残っていないか
④HTML本文に直書き色が無いか。
※②が本命=**片方だけ直すとHTMLとcanvasで色がずれる**が、目では気づけない。

### 教訓
- ⭐**色は「役割の表」で持つ。直に書くと必ず散らばる**。同じ役目の色が少しずつ違う値になり、
  334色まで増えていた。新しい色を足す前に、まず表に近い役割が無いか見ること
- ⭐**同じ値の表を2か所に置いたら、必ず突き合わせる検査も一緒に作る**。
  CSSとJSで色がずれても画面は普通に描けてしまうので、目視では見つからない

---

## 第76弾(2026-07-26) — 支援枠の見分け / 開いたままの画面が更新される / 上級廃品工房 / 火炎とレーザーの個性

ユーザー指示5件。

### 1. 支援枠が他のタワー置き場と見分けが付かない
🏗支援枠を**青い二重枠**にし、空いている専用枠には**名前(`支援施設` / `廃品工房`)**を出すようにした。
⚠アイコン(🏗/🏭)だけでは足りなかった=枠の形・色・名前の3つでようやく一目で分かる。

### 2. 建設メニューが開いた瞬間の⚙️で固まっていた
強化に150要るのに開いた時は100しか無く、**開いている最中に敵を倒して150を超えても押せない**
(閉じて開き直すまで強化できない)状態だった。
`refreshBM()` を足し、0.12秒ごとに**押せる/押せないだけ**を付け直す。
⚠中身(innerHTML)は作り直さない=押している最中に作り直すとタップを取りこぼす。
価格は各ボタンの `data-p` に持たせた。**新しくボタンを足す時は `data-p` を付ける**。

### 3. 🏭廃品工房 → ⬆上級廃品工房(グレードアップ)
⚙️産出・⏩サイクルが両方MAXになると、建設メニューに建て替えボタンが出る(⚙️700)。
- 強化Lvは0に戻るが、**上級の素(6.25/秒)が工房MAX(5.56/秒)より上**なので下がらない。上級MAXで20.8/秒
- `grd:1` を付けてTOWERSの末尾に置き、`T_PLAY=TOWERS.length-SUP_N-GRD_N` で解放チェーンから外した。
  建設リスト・研究所・`buildTower` のどれからも出せない=**建て替えでしか手に入らない**
- 絵も分けた=**解体炉(中で火が揺れる)+煙突+噛み合う歯車2つ**。⚠工房と同じ絵にすると何が変わったのか分からない
- 元を取るのに336秒(1試合の半分)。⚠安くすると工房を並べるだけで勝ててしまう

### 4. 火炎放射塔=単体+周囲に1/4
`fspl:62` を追加。狙った敵の周りにも直撃の1/4が通る。装甲特効は敵ごとに見る。
⚠燃焼が付くのは直撃した1体だけ。酸噴射器には付けていない。

### 5. レーザー塔=同じ敵を焼き続けると最大2倍
`heatN:20`(発)で `heatM:2`(倍)。**狙いが別の敵に移った瞬間に0へ戻る**
=雑魚を薙ぐ塔ではなく「1体を焼き切る」塔になった。溜まり具合はビームの太さと白さにも出る。
⚠`tw.hz`(狙っている敵)はホスト側だけが持つ参照=対戦のスナップショットには載せない。

### 検査
`checkTwNew()` を追加: 火炎の周囲ダメージが直撃の1/4か / レーザーが20発で2.00倍・次の敵で戻るか /
建て替えで産出が下がらないか・強化Lvが0に戻るか・上級をさらに建て替えられないか /
**元を取るのにかかる秒数**(150〜600秒の範囲)/ 上級が T_PLAY に入っていないか。

### 教訓
- ⭐**「開いている画面」は開いた瞬間の値で固まる**。買える/買えないをHTMLに焼き込むと、
  裏で資源が増えても反映されない。**押せる状態だけを定期的に付け直す**形にすること
- ⭐**専用の枠はアイコンだけでは見分けが付かない**。枠の形・色・名前の3つで区別する

---

## 第77弾(2026-07-26) — 総点検。保存漏れ・通信漏れ・撮影ツールのバグを潰した

ユーザー指示「ゲームを総点検して修正が必要なところを修正」。**8件見つけて全部直した**。

### 直したもの(ゲーム本体)
1. ⭐**中断→再開で支援枠が閉じる**(`saveRun` に `supN` が無い)。
   建ててあった支援施設が「未解放の枠の上」になって消えていた。**一番重いバグ**
2. ⭐**タワー強化の入れ物(`us`)の形が場所ごとに違った**。`f`(❄冷却回数)や `c`(⚡連鎖)が
   抜けた形が混ざっていて、`stCost` が NaN になりかねない状態だった。`newUs()` に統一
3. **対戦のスナップショットに `us.f` が無い** → クライアント側で冷却塔の強化Lvが0に見えていた
4. **`chg`(レールガンの充填)と `ang`(砲身の向き)が無い** → 相手盤面では充填演出が出ず、
   **全タワーの砲身が同じ向き(-0.6固定)**だった
5. **`spent`(注ぎ込んだ⚙️)が無い** → クライアント側の売却額の表示が嘘になっていた
6. **巨大ウミガメの説明が重複**「ぶ厚い甲羅。分厚い鱗甲羅。」→ 「ぶ厚い鱗の甲羅。」

### 直したもの(開発ツール)
7. ⭐**`test_shot.js` の `t=` が `z=` と併用しないと丸ごと無視されていた**。
   敵を並べずにタワーだけ撮りたい時、**何も建たないまま撮れてしまう**(=絵を確認したつもりで何も見ていない)
8. ⭐**`t=` の正規表現が数字を拾わなかった**(`/t=([A-Za-z]+)/`)。
   `t=scrap2` が `scrap` と解釈され、**普通の廃品工房を撮って上級だと思い込んでいた**

### 検査
- ⭐**`localStorage` のスタブを入れた**。これまで「保存系はヘッドレスでは検証されない」ままだったので、
  上の1(supN の保存漏れ)を長く見逃していた
- **`checkResume()` を追加**: 支援枠・工房枠・支援施設2基・上級工房・冷却塔の強化(us.f)・
  解放数・建設マスを作ってから中断→再開し、**14項目が1つも失われていないか**を突き合わせる。
  さらに再開後の上級工房が⚙️を生むか、強化費が NaN になっていないかも見る
- 検査が本当に効くか、`supN` の保存をわざと外して FAIL することを確認した

### 教訓
- ⭐**「検査していない領域」は必ず壊れている**。保存/復元はヘッドレスで見ていなかった1か所で、
  実際にバグが眠っていた。**検査できない理由(localStorageが無い)は、スタブを書けば消える**
- ⭐**撮れた絵が「思っていたものと違う」時は、まずオプションが本当に効いているかを疑う**。
  上の7・8は両方「撮影ツールが黙って別のものを撮っていた」事故で、
  絵を直しても直らない/直っていないのに直ったと思う、どちらにも転ぶ

---

## 第78弾(2026-07-26) — ボタンの音 / 古い操作案内の見直し / CPUも工房を育てる / ミニ盤面の重なり解消

### 1. ボタンを押した時の音
タイトル・研究所・編成・召集などが**全部無音**だった。
- `sfx.tap`(進む・高い) / `sfx.back`(閉じる・戻る・低い)を追加。**`beep` の合成音**なのでファイルサイズは増えない
- ⭐**1本のリスナーにまとめた**(`UI_TAP_SEL` + `pointerdown` の capture)。
  ボタンごとに書くと必ずつけ忘れるため。⚠すでに音があるもの(デッキの出撃・建設メニューの品目・小窓)は対象外=二重に鳴る
- `?sfx=1` の🖥UI・経済タブにも出るようにした(素材が無い合成音も一覧に載るよう `ks` の作り方を直した)

### 2. WAVE1の操作案内(tips)が古かった
**2つ嘘をついていた**:
- ⭐「⬆ デッキのボタンを長押しで部隊レベルアップ!」= **2026-07-25に廃止した操作**を案内し続けていた
- ⭐最終ウェーブを `STAGE_W`(=20)固定で出していたので、**新兵(5波)でも「WAVE20を守り切れば」**と出ていた
直したうえで、中身を **`waveTips()`**(DOMを触らないデータの関数)に分離。
航空支援の案内を足し、英雄を連れている時だけ🦸の案内が増えるようにした。

### 3. CPUも廃品工房を育てる(ユーザー指示)
以前は工房を強化対象から外していたため、対戦で**人間だけが経済を伸ばせた**。
工房の強化と**上級化**もするようにした。⚠序盤(W4まで)と終盤(残り3波)は塔を優先=勝負が決まる時間帯に⚙️を寝かせない。

### 4. ミニ盤面が侵入路に掛かる問題を解消(ユーザー指示「完全にかからないように」)
ずっと宿題に残っていたもの。原因は**2つ**あった:
- 侵入路はマップ矩形の外(x≒1671>MW1600)にあるのに、`fitCanvas` は MW しか収めていなかった
  → **`PMAXX`**(道路の右端)を作り、ミニ盤面がある時だけそこまで収める
- ⭐`minisResPx()` が**幅+8**で測っていた。小窓は `right:0` でも safe-area の分だけ右に隙間が空くので足りない
  → **「小窓の左端から画面右端まで」**で測るようにした
対戦だけマップが1割ほど小さくなる(615x292CSSpx)。ソロ/協力は1pxも変わらない。
`test_layout.js` は**参考表示をやめてNG扱い**にした。

### 5. 悪夢の難易度についての扱い
「研究所を解放すれば行ける」= **設計どおり**(ユーザー明示・何度も同じ話になっていた)。
`test_headless.js` の WARN を INFO に変え、CLAUDE.md に「この結果を見て難易度を下げないこと」と明記した。

### 検査
`checkTips()` を追加: 廃止した操作の語(長押し/段階進化/洞窟)が案内に無いか /
新兵と悪夢で最終ウェーブの数字が**その難易度の値**になっているか / 英雄を連れると案内が1つ増えるか /
`sfx.tap`・`sfx.back` があり🔊音の確認に出るか / 押した音の対象に主要なボタンが入っているか。
※「長押し」の案内をわざと戻して FAIL することを確認した。

### 教訓
- ⭐**案内文・チュートリアルは仕様変更に取り残される**。廃止した操作を案内し続けても誰も気づかない
  (コードは動くので検査も通る)。**廃止した機能の名前を検査で禁止語にする**のが確実
- ⭐**「参考表示」にした検査は守られない**。要件になったものは必ずNGにする(ミニ盤面がまさにそれで、
  参考のまま22点掛かり続けていた)

---

## 第79弾(2026-07-26) — 難易度解放のバグ / セーブのリセット / 未解放の専用枠 / 廃品工房3段 / レーザー塔の作り替え

### 1. ⭐難易度が正しく解放されないバグ(ユーザー報告)
症状=「新兵をクリアしたのに兵長が解放されない」「新兵未クリアなのに鬼軍曹が解放されている」。
原因は **`const d9=G.pveDiff||2;`** の1行。**新兵は 0 なので `0||2` で古参(2)としてクリアが記録**されていた。
- `G.pveDiff==null?2:clamp(...)` に修正
- ⚠**0が有効な値の変数に `||` の既定値を使わない**こと
- 検査を **awardMeta() を実際に通す形**に変えた。以前は `scArr(0)[d]=1` と直に書くだけだったので捕まらなかった

### 2. セーブの1回だけのリセット(ユーザー指示)
狂った解放状態を全プレイヤーで作り直す。`META_RESET` の番号(現在1)を上げると1回だけ `metaReset()` が走る。
- 消すもの: 難易度/ステージの解放・研究所で買ったもの全部・研究pt
- ⚠**残すもの: 💎魔石・英雄・🔧鍛錬素材・📖図鑑・⚔冒険の進捗・鍛錬Lv・鍛錬所の解放**
- `checkMetaReset()` が「消す10項目」「残す9項目」を両方検査する

### 3. 未解放の専用枠が普通のタワー枠と同じ見た目だった(ユーザー指摘)
「支援枠が分かりづらい」の本当の理由は**解放前**の見た目だった。前回は解放後だけ直していた。
🔒未解放: 支援枠=**青い破線枠+🏗+「🔒支援」** / 工房枠=**琥珀の破線枠+🏭+「🔒工房」** / 普通の枠=小さく薄い🔒だけ。

### 4. 廃品工房が3段になった(ユーザー指示)
**🏭廃品工房 → ⬆上級廃品工房(⚙️700) → ⬆⬆廃品プラント(⚙️2000)**。
- 産出は 工房MAX5.6 → 上級MAX20.8 → **プラントMAX70.0** 毎秒
- 段のつながりは `up2`(次のid)と `gc`(費用)で表す=**段を足す時はTOWERSに1行足して `GRD_N` を増やすだけ**
- 廃品プラントの絵は**炉が二基+運び出すベルト+大きな歯車1つ**(上級の「炉1つ+歯車2つ」と一目で違う)
- `checkTwNew()` が段を最後まで通しで確認する(産出が下がらないか・強化Lvが0に戻るか・どの段も採算150〜600秒)

### 5. レーザー塔の作り替え(ユーザー指示)
- 焼き切りの最大倍率を **2倍 → 3倍**
- 射程を初期から **1.2倍(300→360)**
- ⭐**📡射程の強化を消して🔥昇温速度に差し替え**。1段ごとに「最大倍率まで要る発射数」が13%減る(Lv5で20発→7発)。
  伸ばすより「早く焼き切る」方が塔の役目に合う
- `us.h` を追加したので `newUs()` と対戦のスナップショットにも載せた

### 検査
- `checkProgress()`: awardMeta() を通して6難易度ぶん「記録された難易度が合っているか」「次だけが開くか(飛び越しなし)」
- `checkMetaReset()`: リセットが消すもの/残すものを19項目
- `checkTwNew()`: 廃品工房の段を最後まで / レーザーの3倍・射程360・強化の内訳・昇温速度Lv5で7発

### 教訓
- ⭐**0が有効な値に `||` を使うと、0だけ静かに別の値へすり替わる**。しかも「クリアしたのに開かない」のような
  遠い症状になって原因が見えにくい
- ⭐**「直に書いて確かめる検査」は本番の経路を通らない**。記録や保存の検査は**本番の関数を呼ぶ**こと
- ⭐**test_headless.js の body 内のコメントにバックティックを書くと SyntaxError**(文字列が途中で閉じる)
- ⭐**検査どうしがMETAを汚し合う**。前提にする値は検査の中で明示的に設定する

---

## 第80弾(2026-07-26) — 研究所の強化を「タワー1種ごと・兵科1種ごと」にした / 砲撃の威力強化

ユーザー指示3つ: ①系統強化(実弾系/火炎系…)を**タワーごと**に ②**兵科側も同様**に ③**砲撃の威力**を強化できるように。

### 何が問題だったか
「📈系統強化」は**型ごとのまとめ強化**(タワー6型・兵科7型)で、
- どのタワーを使っても同じ伸び方=**選ぶ楽しみが無い**
- 型に1つでも当たりがあれば型全体が得をする(bullet型は9種もあった)
- 🎯砲撃は**研究所で一切強化できなかった**(ウェーブ数以外の倍率がゼロ)

### やったこと
- **タワー20種・兵科32種を1種ずつ強化**(各Lv3)。保存は `META.tw{id:Lv}` / `META.un{id:Lv}`
- 対象が増えたぶん1段の伸びを大きくした: **威力+10% → +15%/段**(Lv3で×1.45)
- **型らしさは残す**(`TW_TRAIT`): 🔥燃焼+25% / 🕸減速・凍結+20% / 💥爆発範囲+12% / ⚡連鎖+1 / 🏭産出+15%
- ⭐**枠を共有するもの**:
  - 工房の3段(廃品工房/上級/プラント)は1枠(`twKey`)=**建て替えで強化が無駄にならない**
  - 派生キャラは元の兵科と1枠(`unKey`=`U.base`)=**猟犬を鍛えれば狼/ケルベロス/フェンリルにも効く**
- ⚠**支援施設は対象外**(タワーの性能に触らない約束)。⚠**未解放のタワー/兵科は一覧に出さない**
- **⭐🎯砲撃の威力**(5段・+12%/段=最大×1.60)。⚠**直撃だけでなく燃焼にも掛ける**
  =掛けないとナパームだけ効きが半分になる(ナパームは燃焼が主火力)
- 価格: タワー60×1.5^Lv / 兵科45×1.5^Lv / 砲撃130×1.5^Lv

### 画面
- タブを「📈系統」から **「🗼タワー」「🪖部隊」の2つ**に分けた(8タブ+記録が1行に収まる)
- 20〜32枚並ぶタブは **`.lgrid.tight`(6列・説明は小ラベルに1行)**=1画面に18枚。普通のカード(4列)だと8枚しか入らない
- アイコンは**型ごとの絵文字**(`TW_IC`/`UN_IC`)。⚠最初は全部🗼にしていたが**20枚が全く見分けられなかった**

### 検査
`checkPerUp()` を追加(6項目):
①タワー1種を鍛えると×1.45になり**他のタワーには乗らない** ②型の持ち味も伸びる(燃焼・連鎖)
③工房3段が1枠を共有し、鍛えると産出が増える ④支援施設が一覧に出ない・項目数(タワー20/兵科32/砲撃1)・
**未解放のタワーは出ない** ⑤兵科の攻撃とHP・**派生キャラが元の枠を共有** ⑥砲撃5種すべてに威力が乗り、燃焼にも乗る
- `checkMetaReset()` を新キー(tw/un/st0)に対応
- ⭐**既存の検査バグを1つ直した**: `checkEvo()` の「上級進化が出ていないか」が `o.t`(名前)を見ていたが
  '上級進化' は `o.tag` 側にあるため**常に0件=何も検査していなかった**

### 下調べ
実装前に調査を4本並列で走らせ、系統強化の全効果箇所・兵科の倍率の流れ・砲撃の威力の出どころ・
研究所UIの寸法(852×393で1画面8枚)を洗い出してから設計した。

### 教訓
- ⭐**「型ごと」を「1つごと」に割るときは、何が1枠を共有すべきかを先に決める**。
  工房の段と派生キャラを別枠にすると「鍛えたのに進化させたら無駄になる」という理不尽が生まれる
- ⭐**一覧のアイコンを全部同じにしてはいけない**。20枚並べた瞬間に何がどれか分からなくなる
- ⭐**倍率を足す時は「同じ数値から作られている別の値」を探す**。砲撃は直撃(dm)と燃焼(burnD)が
  別々に作られており、片方だけに掛けるとナパームだけ効かない

---

## 第81弾(2026-07-26) — タワーの見た目を作り直した(Blenderは捨てた)/ レビューで見つかった穴を潰した

### 1. アートの方針を決めた(ユーザー指示「タレットなどの見た目を作っていこう」)
まず**実機での大きさを数えた**: 建設マス64px × 倍率0.446 = **タワーは画面35px、ゾンビは22px**しかない。

⭐**Blender を実際に試して、捨てた**。Blender 5.1 がインストール済みで `--background --python` から叩けたので、
ライフル櫓を「真上寄り・べた塗り」と「斜め俯瞰・陰影」の2通りで焼き、現行のコード描画と35pxで並べた。
- **コード描画が明確に勝った**。canvasの `lineWidth 3` は実ピクセルで必ず3px確保されるが、
  128px→35pxに縮小したスプライトは**輪郭がアンチエイリアスで灰色に溶ける**
- 35pxでは陰影の情報が入らない(立体感は3倍に拡大して初めて分かる程度)
- ⚠この結論は「解像度が小さいから」であって、Blenderが悪いわけではない。**大きく映る物には有効**

### 2. 「ハリボテ感」の本当の原因を直した
比較して分かったのは、**20種すべてが「二重の円+砲身」**で色しか違わなかったこと。解像度ではなく形の問題だった。
- `TW_SHAPE` の表を作り、20種に**8種類の輪郭**を割り振った:
  **tower**(櫓・脚が外へ出る)/ **box**(角丸の四角)/ **tripod**(三脚)/ **dish**(平たい皿)/
  **hex**(六角)/ **oct**(八角の要塞)/ **mast**(細い支柱+上に円盤)/ circle(従来)
- **陰影を1段**入れた(`clip` で下半分だけ暗く塗る)。⚠2段以上は35pxでは判別できないので入れない
- 結果、35pxでも**形+色**で見分けられるようになった

### 3. レビュー(53エージェント)で見つかった穴を潰した
第80弾の実装を5観点で検証し、25件の指摘のうち裏取りを通ったものを直した。

**⭐今日入れてしまったバグ**
- **ドローンの弾の座標がNaN**: `const TL=..., TT=TW_TRAIT[T.type]` が**グローバルの `TT`(ゲーム時刻)を
  シャドーイング**していた。ドローンは type:'bullet' で持ち味が無いため `TT=undefined` →
  `droneOff(…,undefined)` が `[NaN,NaN]` を返し、**2機から出る弾の演出が丸ごと消えていた**。`TRT` に改名
- **研究所の兵科強化が「装備中の派生キャラのid」で保存されていた**: `applyLoadout(true)` が UNITS[i] を
  派生キャラに差し替え、タイトルへ戻っても元に戻らない。そのため研究所が `META.un['wolf']` に書き込むのに
  出撃側は `META.un['hnd']` を読む=**買った強化が乗らず🧬が丸損**、しかも Lv0/3 と出て重ね買いできた。
  研究所は**不変の素の表(UBASE)**から引くようにした

**既存の取り残し**
- **ウェーブ放棄の精算に工房の産出強化が乗っていなかった**(鍛えた工房ほど損をしていた)。
  同じ式が2か所にあったのが原因なので **`ecoPer(T,tw)` に切り出して両方から呼ぶ**ようにした
- **冷却塔の「凍結+20%」を買っても実質の拘束時間が増えなかった**: 再凍結の耐性を強化後の値(`ft*2`)で
  作っていたため、凍結も待ちも同じ比率で伸びて**稼働率が33%に張り付いていた**。
  素の値の2倍で固定し、強化するほど稼働率が上がる形にした(素33%→Lv3で44%)
- **🛠DEVモードで個別強化が開いていなかった**(実機で効果を確認できない)。タワー/兵科/砲撃威力を最大に
- **⚡連鎖数の表示**に研究所ぶんが入っていなかった(実際に飛ぶ数より少なく見えていた)

### 4. ⭐「何も見ていない検査」を2つ作り直した
レビューは**私が書いた検査の欠陥**も指摘してきた。
- **連鎖数の検査が期待値を実装と同じ式で作っていた** → 実装が壊れても検査も同じように壊れて必ず通る。
  **実際に何体に当たったかを数える**形にした(素4体 → Lv3で7体)
- **支援施設を除外する検査が構造上ぜったい落ちなかった**(支援は T_PLAY の外なのでループに入らない)。
  ①持ち味の表に無い ②形の表にも無い ③T_PLAYの範囲に居ない ④一覧に出ない、の4つを見る形にした

あわせて `checkPerUp()` に3本追加(派生装備中のキー/工房の産出が2か所で一致すること/ドローンの弾の座標が有限)。
どれも**わざと壊して FAIL することを確認**した。

### 教訓
- ⭐**絵の作り方を決める前に「実機で何pxか」を数える**。35pxと分かった時点で、
  リッチなモデリングは報われないと判断できた。数えずに始めると88体作ってから気づく
- ⭐**ローカル変数にグローバルと同じ名前を使うと、無言で別の値になる**。`TT`(ゲーム時刻)を
  隠してしまい、NaNが座標に流れた。短い名前ほど危ない
- ⭐**同じ式を2か所に書くと、片方だけ直し忘れる**。工房の産出がまさにそれで、
  放棄の精算だけ強化が抜けていた。**関数に切り出して両方から呼ぶ**
- ⭐**検査の期待値を実装と同じ式で作ってはいけない**。実装が壊れると検査も同じように壊れ、
  「必ず通る検査」になる。**結果を実測して数える**こと
- ⭐**「出ていないこと」の検査は、構造上ぜったい起きない条件を見ていないか確かめる**

---

## 第82弾(2026-07-26) — ゾンビの見た目を作り直した(ステージ1の13種)

### 1. まず撮って並べた
第81弾の `mk_tw.js` をゾンビ版(`mk_z.js`)に作り替え、**実機と同じ22px**と2.6倍拡大を1枚に並べて撮った。
分かったのは、**問題はステージ1に集中している**ということ。

- **ステージ2(海の13種)とナイトメア(獣7種)、ボスは、もう1種ずつ違う絵になっていた**(カニ/イワシ/フグ/
  ウツボ/ヒトデ/クラゲ/アンコウ… 蝙蝠/鴉/熊…)。ここは触る必要がなかった
- **ステージ1の13種のうち10種が「丸い頭+まったく同じ胴+2本脚」を共有**していて、
  違うのは big(胴が一回り大きい)/ hel(兜)の2つの真偽値と、あとから足す小さな飾りだけだった。
  = **実機ではウォーカーとランナーとスクリーマーが同じ絵**、アーマードと重装兵とジャガーノートも同じ絵

### 2. 13種に1つずつ輪郭を与えた
第81弾のタワー(`TW_SHAPE`)と同じ考え方。**22pxで効くのは ①輪郭の形 ②明暗 ③色 の3つだけ**。

| ゾンビ | 輪郭の狙い |
|---|---|
| ウォーカー | 直立。首と肩が見えるので「人」に読める(基準) |
| ランナー | 深く前傾。手足を前後に大きく振る |
| アーマード | 胸当てと**半分だけの兜**。顔も手足もむき出し |
| ブローター | ほぼ球。膨れた腹に小さな頭が埋まる |
| スクリーマー | のけ反って**両腕を上げる**+叫びの輪(唯一の「腕が上がった」形) |
| 感染ハウンド | 四つ足。低く長い胴と突き出た鼻面 |
| クローラー | 平たい。地面すれすれ |
| スライム状体 | 頭も脚も無い塊 |
| ブルート | **逆三角形**。肩が張り出し、長い腕を地面まで引きずる |
| ストーカー | 低く構えた二足の狩り手。前腕と爪を前下へ |
| レイス | **脚が無い**。裾が霧に散る(唯一「足が地に着いていない」) |
| 重装兵 | 横に広い低い箱。全身鉄板で顔はバイザーの隙間だけ |
| ジャガーノート | 棘の肩当てと鉄球の鎖 |

あわせて **`zFill()`**(上を明るく下を暗く**1段だけ**の陰影)と **`zEye()`**(白目を大きめに取った目)を作り、
全部そこを通した。タワーの `twBase` と同じ理屈で、平たい図形が塊に見える。

**重装兵の色を `#5a6470` → `#39424c`(ほぼ黒い鋼)に変えた**。アーマードの STEEL(`#5c6b78`)と
ほとんど同じ青灰色で、**大きさ以外に見分ける手がかりが無かった**ため。これで形・大きさ・色の3つとも別になる。

### 3. 腐敗の印(`rotMark`)を控えめにした
海の13種すべてに**体の真ん中へ不透明な白い骨を3本**引いていたので、22pxでは
**12種に同じ白い印が並ぶ**のが一番目立ち、かえって見分けを邪魔していた。2本・半透明・体の後ろ寄りにした。

### 4. 検査 `checkZLook()` を足した
2段構えにして、**わざと壊して FAIL することを確認**した(作り直す前の index.html で両方とも落ちる)。
- **静的**: 全ゾンビが `drawZombie` の中に自分の枝(`Z.id==='…'`)を持っているか
- **実走**: drawZombie が実際に積んだ**図形の並びを記録**して、同じ並びの組が無いか
  ⚠**色も数値も見ない**。ゾンビごとに `Z.sc` と歩幅が違うので、数値を入れると
  「胴が丸ごと同じでも別物に見える」=必ず通る検査になってしまう

### 教訓
- ⭐**撮る → 直す → また撮る、を3周した**。1周目でランナーが四つ足の生き物に、
  ストーカーが鳥に見えた。**数値だけ見ていたら気づけない**
- ⭐**胴を横に寝かせると四つ足の生き物に見える**。二足で走らせたいなら胴は斜めに「立てる」
- ⭐**爪や武器を頭と同じ高さに置くと「開いた嘴」に読める**。牙に見せたくないなら高さをずらす
- ⭐**腕を2本並べて描くと22pxで1本の突起に潰れる**。胴の前と後ろに分けて明暗を変える
- ⭐**同じ印を全種に入れると、その印が一番目立って見分けを邪魔する**。共通の記号ほど薄く小さく
- ⭐**「全部作り直す」前に、どこが本当に同じなのかを撮って確かめる**。
  今回はステージ2とナイトメアが既に別物だったので、手を入れるのは13種で済んだ

---

## 第83弾(2026-07-26) — ゾンビの声を下げた / BGMを少し上げた

ユーザー指示「ゾンビの声を全体的に音量さげて。特にボス登場時の叫び声。あとBGM少しだけ音量上げて」。

### 1. まず測った(耳で確かめられないので)
ヘッドレスChromeで `decodeAudioData` した波形から **RMS(体感の大きさに近い)を53種すべて測った**。分かったこと:

- ⭐**ボス登場の咆哮 `growl` が53種で一番大きかった**(RMS **0.441** / 全体の平均0.150の約3倍 / 1.1秒)。
  ユーザーの体感どおりで、数字にもはっきり出ていた
- ⭐**同じ役割の変種どうしで音量がバラバラ**だった。`growl`(0.441)と `growl2`(0.191)で**2.3倍差**。
  くじで引いて鳴らしているので、**同じ場面なのに鳴るたびに大きさが変わっていた**
- うめき声も `moan3`(0.188)と `moanW`(0.059)で3.2倍差
- 一方 `bossKill`(0.309)が2番目に大きく、これもゾンビの叫び声

つまり問題は「ゾンビの声が全部大きい」ではなく、**一部だけ突出していて、しかも揃っていない**ことだった。

### 2. 「揃えてから下げる」
`SFX_GAIN` の表を作り、**役割ごとに同じ音量へ揃えてから下げた**。
狙いは うめき≒0.065(始終鳴る環境音なので他より小さく)/ ボスの咆哮≒0.11 / ボス撃破≒0.18。

| | 直す前 | 直した後 |
|---|---|---|
| ボス登場の咆哮 | 0.441 / 0.191(バラバラ) | **0.111**(揃えた) |
| うめき | 0.059〜0.188 | **0.063** |
| エリートの大うめき | 0.096 / 0.136 | **0.075** |
| ボス撃破 | 0.309 | **0.179** |
| (参考)ゾンビ以外 | 平均0.148・最大0.402 | 変えていない |

⚠**素材は焼き直していない**(`tool_sfx.js` を回さずに済み、あとから数字だけで調整できる)。
⚠**1.0を超える値は書かない**=下げる指示なので、元から小さい `moanW`/`scratch` は素のまま。
⚠`bite`/`bite2` は「噛みつく音」で声ではないので触っていない(素0.102/0.117=平均並み)。
⚠合成音(`SFXSYN`)側の値も同じ倍率で下げた=素材が無い環境でも釣り合いが崩れない。

### 3. BGMを一律 x1.25
⚠**BGMは `beep()` を直に呼ぶので音量スライダー(`SFXVOL`)を通らない**。数字がそのまま音量。
通常/ボス/ナイトメアの3種すべてを x1.25 にした。

### 4. 検査(`test_sfx.js` に追加)
**耳で確かめられないので、鳴る音量を測って数字で見る**。`SFX_GAIN` を掛けたあとのRMSで3つ見る:
1. ゾンビの声が「ゾンビ以外の一番大きい音」を超えていないか(=ゾンビが一番うるさい状態に戻っていないか)
2. 始終鳴るうめきが、ほかの音の平均より小さいか
3. **同じ役割の変種どうしで音量が離れていないか**(離れていると鳴るたびに大きさが変わる)

`SFX_GAIN` を外すと `growl(0.441)が非ゾンビの最大0.402を超えている` で落ちることを確認した。

### 教訓
- ⭐**「全体的に下げて」と言われても、まず測る**。今回の本当の問題は「全部が大きい」ではなく
  **1つだけ突出していて、しかも同じ役割の変種どうしが揃っていない**ことだった。
  一律に下げていたら、元から小さい声が聞こえなくなるだけで、突出は残っていた
- ⭐**くじで引く音は必ず音量を揃える**。揃っていないと「同じ場面なのに鳴るたびに大きさが変わる」
  という、原因の分かりにくい違和感になる
- ⭐**素材の音量は焼き込まず、コード側の表で調整できるようにしておく**。
  ツールを回さずに直せて、数字で記録も残る

---

## 第84弾(2026-07-26) — ★音の確認画面がゲームと違う音量で鳴らしていた / ボスの唸りをさらに下げた

ユーザー指摘「ボスのうなり(死の笛)ってやつがでかすぎる」。第83弾で下げたはずのものだった。

### 1. ★原因: 確認画面が音量の表を通していなかった
`?sfx=1` の音の確認画面は `sfxTry()` という別の入口で鳴らしており、そこだけ
**`sfxPlay(k,1,0)` と素の音量を渡していた**。第83弾で入れた `SFX_GAIN` を通っていない。

つまり:
- **ゲーム中は 0.111 に下がっていたが、確認画面では素の 0.441 のまま鳴っていた**
- **音量を下げる直しを入れても、確認画面では下がって聴こえない**
  = 「ゲームを進めずに音を確かめる」というこの画面の存在意義が丸ごと無効になっていた

⚠ユーザーが `growl`(死の笛)だけを名指しし、同じ役割の `growl2`(人狼)には触れなかったことが手がかりになった。
第83弾で**両方0.111に揃えてある**ので、ゲーム中なら片方だけ大きく聴こえることはない。
素の音量(growl 0.441 / growl2 0.191=2.3倍差)のままなら片方だけ突出する ⇒ 確認画面を疑うのが筋だった。

`sfxTry()` も `SFX_GAIN` を通すようにした。

### 2. ボスの唸りをさらに下げた(0.111 → 0.071)
⚠**1.1秒鳴りっぱなしの音は、RMSが同じでも短い音よりずっと大きく感じる**。
`growl` は1.10秒あり、他の効果音(0.2〜0.6秒)と同じRMSでは釣り合わない。
エリートの大うめきと同じ 0.07 まで落とした(`growl:.16` / `growl2:.37`)。

### 3. 検査 `checkSfxGain()` を追加(test_headless.js)
**同じ入口の取りこぼしを静的に見張る**。`sfxPlay(` を呼ぶ行が定義以外すべて `SFX_GAIN` を渡しているか。
あわせて①表のキーが実在するか(打ち間違えると**黙って効かない**)②1.0を超える値が無いか も見る。
`sfxTry` を元に戻すと FAIL することを確認した。
⚠`test_sfx.js` はブラウザが無いと飛ばされるので、**必ず走る test_headless.js に置いた**。

### 教訓
- ⭐**「直したのに直っていない」と言われたら、まず"確認に使った経路"を疑う**。
  同じ機能に入口が2つあると、片方だけ直して**確認画面が嘘をつく**。
  第81弾の `test_shot.js` の `t=` が効いていなかった件とまったく同じ形の事故
- ⭐**確認用の画面・ツールは、本番とまったく同じ経路を通すこと**。
  通らないなら、それは確認になっていない
- ⭐**長く鳴る音は、RMSが同じでも大きく感じる**。音量を揃える時は秒数も見る
- ⭐**ユーザーが「どれが」を名指ししたら、それ自体が手がかり**。
  揃えたはずの2つのうち片方だけ挙がった=揃えた処理を通っていない、と読める
- ⭐**test_headless.js の body の中に入る検査は、正規表現も改行のエスケープも使わない**。
  テンプレート文字列なのでバックスラッシュが食われ「Invalid or unexpected token」で落ちる(今回踏んだ)

---

## 第85弾(2026-07-26) — ゲーム中の「WAVE〜でクリア」の案内を削除

ユーザー指示。WAVE1に出る操作案内(`waveTips()`)から
**`🛡 WAVE 〇 を守り切ればクリアだ!`** を消した。
クリア条件は左上のWAVE表示(`WAVE 1/10`)とステージ選択の説明に出ているので、操作案内に混ぜる必要がない。

⚠**検査を「出ていないこと」側に裏返した**。`checkTips()` は
「最終ウェーブが `curW()` になっているか」を見ていたので、**消した瞬間に落ちる**状態だった。
`NG`(禁止語)の表に `'WAVE '` を足す形にして、**足し直されたら落ちる**ようにしてある
(実際に足し直して FAIL することを確認した)。

⚠検査の**報告文**も直した。`操作案内: 新兵は「WAVE 5」・悪夢は「WAVE 20」…` と出したままでは、
**検査の出力そのものが古い仕様を語り続ける**ことになる(案内文が取り残される事故の、検査側での再演)。
禁止語の表から自動で組み立てる形にした。

### 教訓
- ⭐**案内文を消す時は、検査も「出ていないこと」側に裏返す**。ただ消すと
  ①検査が落ちる ②あとから復活しても気づけない、の両方になる
- ⭐**検査の報告文も仕様変更に取り残される**。固定の文言を書かず、見ている表から組み立てる

---

## 第86弾(2026-07-26) — ★操作の説明を「🎓 実際に操作させるチュートリアル」に一本化した

ユーザー指示「案内いらんな。チュートリアルを最初に用意して、そこで全部説明すればOKだわ」。
形式は**操作させる形**、📖あそびかたは**チュートリアルに置き換え**(どちらもユーザー選択)。

### 1. 廃止したもの
- **WAVE1のトースト案内(`waveTips`)** … 5.4秒おきに上から流れてくるやつ
- **📖あそびかたのモーダル** … 10段落の文章。**古い記述の巣になっていた**
  (「WAVE20を守り切ればステージクリア」=難易度ごとに違う / 「置き場は最初6ヶ所」=`SLOT0`は7)

### 2. 🎓チュートリアル(11段・うち7段が「実際に操作させる」段)
拠点の説明 → **タワーを建てる** → **部隊を出す** → **🚩集結旗を立てる** → **🎯航空支援を落とす**
→ ウェーブ開始 → 足止めは3体まで → **守り切って🔩** → 🔬研究所 → 終わり。

- **その操作を実際にした時だけ進む**(時間で勝手に進めない)。読むだけの段だけ▶で進む
- 覚えるまで**作戦タイムの時間を止める**ので、追い立てられない
- **本編の記録を汚さない**(`G.tut=1` で `awardMeta`/`saveRun` を素通り。新兵・ステージ1固定)
- タイトルの「🎓 あそびかた」でいつでもやり直せる。**はじめての人には起動0.6秒後に自動で始まる**
  (⚠難易度を1つでもクリアしていれば出さない=すでに遊んでいる人の邪魔をしない)

### 3. 撮って直したもの(全部、撮らなければ気づけなかった)
- **盤面の印が消えている時間があった**。輪(`addFx`)を0.7秒おきに打ち直す作りだったので、
  静止画で見ると「光っているマス」がどこにも無い。`tutMark()` で**ずっと出ている
  太い輪+下向き矢印**に変えた
- **印を出したマスが上の帯に隠れていた**。一番上の枠が選ばれていたため、
  チュートリアルの文が印を覆っていた → **下寄りのマスを選ぶ**ようにした
- **マスを毎回選び直していた**。建てた瞬間に「空いている枠」が別のマスへ移るので、
  印が飛ぶうえ「建てたか」の判定が別のマスを見て**永久に進めない** → 一度決めたら動かさない
- ★**1波目の前には作戦タイムのパネルが出ない**(2波目以降だけ)。そこで「✅配置完了!」を
  押させようとしていたので、**見えないボタンを待つうえ時間も止めていて詰んでいた**

### 4. 検査 `checkTut()`(`checkTips` を作り替え)
`tutSteps()` はDOMを触らないデータの関数なので直に検査できる。
①古い言い回しが無いか ②説明の抜けが無いか(タワー/デッキ/🚩/🎯/⚙️/🔩/🔬)
③**操作させる段が4つ以上あるか**(読むだけの紙芝居になっていないか=ユーザー指示は「操作させる形式」)
④戦績を汚さないか ⑤**進めなくなる段が無いか**(実際にウェーブが始まるところまで走らせる)。
⑤は**わざと元に戻して FAIL することを確認**した。

### 教訓
- ⭐**「あとで見る条件」は待ってから見る**。読み込んだ瞬間に条件を判定して、あとで待つ形にすると
  **待っている間に条件が変わっても効かない**。撮影ツールが自動起動を止めているのに始まってしまい、
  タイトルの撮影が全部チュートリアルの絵になった
- ⭐**「光らせる」ものは点滅させない**。消えている瞬間に「どこ?」となる
- ⭐**光らせる対象を毎回選び直さない**。対象が動くと、判定も一緒に動いて詰む
- ⭐**説明を置く帯は、光らせる相手と反対側に置く**。同じ側だと自分で隠す
- ⭐**シェル経由で日本語やバッククォートを含む長文を書かない**。バッククォートが実行される。
  パッチはファイルに書いてから node で流すこと(今回また踏んだ)

---

## 第87弾(2026-07-26) — チュートリアルを「開くところ」から始め、💎3個+ガチャ1回で締める

ユーザー指示「いきなり始まるんじゃなくてチュートリアルを開くとこからにして。
あとチュートリアルクリアしたらガチャ石3個プレゼントして、一回ガチャするまでがチュートリアルで」。

### 1. 入り口を「開くところ」に
第86弾は起動0.6秒後に**いきなり戦場が始まっていた**。これをやめ、
**タイトルで「🎓 あそびかた」のボタンを光らせる段**から始めるようにした。
押すと戦場へ入る。⚠ボタンから自分で開いた時は、その段を飛ばしてそのまま戦場へ。

### 2. 締めを「💎3個+ガチャ1回」に
戦場の説明が終わると**タイトルへ戻り**、そこで続きが始まる。
- **💎を3個渡す**(⚠その段に入った時点で渡す=途中で抜けても手元に残る)
- 「💎 英雄召集」を押させる → **単発を1回引かせる** → 終わり
- ⚠🛠DEVでは💎が補充されるので「減ったか」だけでは判定できない。演出(`GC`)が始まったかも見る

段が**画面をまたぐ**ようになったので、`scr:'title'`/`'game'` を持たせて `tutGo()` が行き来する。
`TUT.mv`(チュートリアル自身が画面を移している最中)の間は `backTitle` で畳まないようにした。
毎フレームの `tutStep` も `gameActive` で絞るのをやめた(タイトル側の段があるため)。

### 3. 撮って直したもの
- **帯がタイトルの「ソロ作戦」ボタンに被っていた** → タイトル側の段だけ帯を下に出す(`#tut.tl`)
- **1画面に2種類の💎が並んでいた**。帯は `textContent` だったので💎が絵文字(青)のまま、
  ボタンは画像(紫の結晶)。`IC()` を通して揃えた(⚠過去に指摘された事故とまったく同じ形)
- `test_shot.js` の `tut` の正規表現が `tutauto=11` の数字を拾えず、**いつまでも1段目が撮れていた**
  (⚠**撮れた絵が思っていたものと違う時は、まずオプションが効いているか疑う**——今回もこれ)

### 教訓
- ⭐**「同じ画面の同じ資源は同じ絵にする」は、新しく文字を出す所すべてに掛かる**。
  文字を出す口を増やしたら `IC()` を通すこと。忘れると必ず絵文字と画像が混ざる
- ⭐**画面をまたぐ状態は「今どちらが動かしているか」を持つ**(`TUT.mv`)。
  持たないと、自分で画面を移した時に自分の後片付け処理に消される

---

## 第88弾(2026-07-26) — 研究所の個別強化を3段→20段に刻んだ(ビルドを選ばせる)

ユーザー指示「研究所の強化がマックス3段階とかだけど、もっと細かく刻んで20段階とかにしてほしい。
いまのままだとすぐ強化が終わって全部マックスになっちゃうから、**おれはこの火炎系を上げていくぞ**
みたいな各々のビルドを作っていくっていうのを大事にしたい」。

### 1. 「選ばせる」ための3点セット
段数を増やすだけでは意味がない(全部マックスになる速さは変わらない)。**1段の伸びを小さく・段数を多く・
全部取りは手が届かない**の3つを揃えて、はじめて「何を伸ばすか選ぶ」遊びになる。

| | 直す前(3段) | 直した後(20段) |
|---|---|---|
| タワー威力 | +15%/段(最大×1.45) | **+5%/段(最大×2.00)** |
| 🔥燃焼 | +25%/段(×1.75) | +8%/段(**×2.60**) |
| 🕸減速・凍結 | +20%/段(×1.60) | +5%/段(**×2.00**) |
| 💥爆発範囲 | +12%/段(×1.36) | +2.5%/段(×1.50) |
| ⚡連鎖 | +1/段(+3体) | +0.25体/段(**+5体**・4段ごとに1体) |
| 🏭産出 | +15%/段(×1.45) | +3.5%/段(×1.70) |
| 兵科(近接) | 攻撃+10%/HP+10% | +3%/+3%(**×1.60**) |
| 兵科(その他) | 攻撃+12%/HP+8% | +4%/+2%(攻撃**×1.80**) |
| 🎯砲撃 | 5段・+12%(×1.60) | **20段**・+4%(×1.80) |
| **1本フルの値段** | タワー285 / 兵科214🧬 | **タワー2,428 / 兵科1,781🧬** |
| **全部取り** | 12,548🧬 | **約110,000🧬** |

⭐**伸ばし切った1本は3段だった頃より強い**(威力+45%→+100%)。20段ぶん注ぎ込むのだから、
伸ばした1本は「別物」になるべき、という考え。
⚠価格は `1.5^Lv` のままだと20段目が天文学的になるので **`1.13^Lv`** にした。
1段目は タワー30 / 兵科22🧬 と手軽で、20段目は306 / 224🧬。

### 2. 検査 `checkLabSteps()` を追加
**狙いそのものを数字で見張る**。①十分細かく刻んであるか ②1本は手が届くか ③全部取りは手が届かないか。
⚠**②と③の両方を見る**。片方だけだと「全部安い」「全部高い」に倒れても気づけない。
段数を3に戻す/価格の伸びを緩める、のどちらでも FAIL することを確認した。

### 3. 既存の検査の直し
⚠**工房の産出の検査が「倍率(割り算)」で見ていたため、正しく効いているのに落ちた**。
`ecoPer` は `Math.round` した整数を返すので、素が5⚙️だと ×1.70 の期待に対して
`round(8.5)=9` → 実測 ×1.80 になる。**丸めの1⚙️ぶんを許して「値」で見る**形に直した。

### 教訓
- ⭐**「細かく刻む」は段数を増やすだけでは実現しない**。1段の伸び・価格の伸び・全部取りの総額を
  セットで動かさないと、体感は何も変わらない
- ⭐**整数で扱う値(連鎖の体数など)を刻む時は `Math.floor` を忘れない**。
  端数のまま体数に使うと、表示も挙動も中途半端になる
- ⭐**丸めた整数を「倍率」で検査してはいけない**。素の値が小さいほど倍率は跳ねる。
  刻みを細かくした瞬間に、正しい実装が落ちるようになる
- ⭐**遊びの狙い(=何を大事にしたいか)は、そのまま検査に落とせる**。
  「全部マックスにならないこと」は総額で表せる

## 第89弾(2026-07-26) — ★チュートリアル中は「光っている所」以外を押せなくした

ユーザー指示=**「ここを選べって案内はあるけど、ほかのところをタップできちゃう。強制的にやらせて」**。
それまでは、タイトルで「🎓あそびかた」を光らせていても**🛡ソロ作戦を押せばそのまま普通の試合が始まり**、
チュートリアルは黙って畳まれていた。

### 1. 押せる所を段ごとに決めた
`tutPass()` が `pointerdown` / `pointerup` / `click` を **capture で握り潰す**。
⚠**押した音のリスナーより先に登録**して `stopImmediatePropagation` する
=先に登録しないと「押せていないのに手応えだけ鳴る」状態になる(同じ document に付いた後続のリスナーは止まらない)。
押せない所を押すと**帯が左右に揺れる**(`tutNope()`)=無反応だと「固まった」に見えるため。

段ごとの許可は3つ:
- **hi** … その段が光らせている所(既存)
- **al** … その段だけ特別に許すセレクタ(新)
- **sl** … 建設マス(新)。**1=光っているマスだけ / 2=どのマスでも**

盤面(canvas)は「🚩🎯を押して狙っている最中(AIM/FLAGAIM)」と「slが許すマス」だけ通す。
マスの当たり判定は `slotHit()` に切り出して、盤面のタップ処理と共用にした(2か所に分けると食い違うため)。

### 2. ⚠詰みの逃げ道(全部**実機のブラウザで実際にタップして**見つけた)
塞いだ先に「閉じるまで進まない物」が3つあり、どれも**永久に詰む**状態になっていた。

1. **建設メニューを閉じると二度と開けない** → 建てる段を `sl:1` にして、光っているマスを押し直せるようにした。
   メニューを閉じている間は**印(輪+矢印)も出し直す**
2. **新登場の紹介モーダルのOKが押せない** → これは `PAUSED=true` にしてOKを待つので、
   塞ぐと**止まったまま二度と動かない**。`TUT_FREE`(=`#md-intro`)と **`PAUSED` 中は全部通す**ようにした
3. **戦っている最中に手が縛られる** → fight/clear の段に `al:'#deck,#flagbtn,#strikebtn'` と `sl:2` を付けた。
   塞ぐと押せないまま拠点を削られる

さらに保険を2つ:
- **リザルト(`SCR==='result'`)は全部通す**(1波目で負けた時に閉じられないと詰む)
- **`tutBegin` で `META.tut=1`**=万一どこかで詰んでも**再読み込みすれば自動起動しない**(最後の逃げ道)

### 3. 検査
`checkTutLock()` を追加。**closest だけを持つ偽の要素**を作り、段ごとに
①帯の▶は必ず押せる ②光っていない所(`#bt-solo`/`#pausebtn`)は押せない ③光っている所は押せる
④alで許した所は押せる ⑤紹介モーダルはどの段でも押せる ⑥建てる段は `sl===1`・戦う段は `sl===2`
⑦リザルト中は通す ⑧`META.tut` が始めた時点で立つ — を見る。

### 教訓
- ⭐**「操作を塞ぐ」仕組みを入れたら、塞いだ先に"閉じないと進まない物"が無いか全部洗う**。
  今回は建設メニュー・紹介モーダル・リザルトの3つが引っ掛かった
- ⭐**塞ぐ側の実装は「迷ったら通す」に倒す**。例外が出た時も通す=どんな不具合でも操作不能にしない
- ⭐**逃げ道を1本残す**。今回は「再読み込みすれば自動起動しない」を保険にした
- ⭐**この種の不具合はヘッドレスでは出ない**(イベントを飛ばさないため)。
  **実機のブラウザで1回通しでタップして確かめること**

## 第90弾(2026-07-26) — チュートリアルに「ここだ」の矢印を足した

ユーザー指示=**「いまって案内を上部とか下部に置いてるだけ。矢印とか"ここだよ〜"みたいな表示が欲しい。
左右とかに反復しながら案内印」**。
それまで、盤面のマス/拠点には輪+矢印が出ていたが、**画面のボタン(デッキ・🚩・🎯・建設メニュー・
タイトルのボタン)は金色の枠が光るだけ**で、帯の文を読まないとどこを押すのか分からなかった。

### 出るもの
`#tutptr`=**金色の矢印**(太い黒フチ・べた塗り。絵は左向きのSVG1つで、向きは回転で作る)。
光らせている所の**すぐ外側に置き、指している方向へ往復**する(0.68秒で寄って離れる)。
⚠`pointer-events:none`=印そのものがタップを食わない(押す所を隠したら本末転倒)。

### 置き場所(`tutPtrPos()`)
⚠**DOMを触らない関数**に分けてある=ヘッドレスで検査できるようにするため。
- **画面の下端に貼り付いている物(操作バー=デッキ・🚩・🎯)は真上から下を指す**。
  横から指すと**隣のボタン(🚩の横に🎯)に印が重なる**ため
- **上端に貼り付いている物(HUD)は真下から上を指す**
- それ以外は**横から**指す(横画面は縦が393pxしかなく、上下に置くと他の物に被る)。
  右寄りの物は左から、左寄りの物は右から
- 画面からはみ出す時は**反対側へ回す**(端で潰れて見えなくなるのを防ぐ)

### 出さない場面
- **盤面のマス/拠点の段**(`tutMark()` が輪と下向き矢印を描いているので二重にしない)
- **止まっている間(PAUSED)**=新登場の紹介モーダルの上に浮いて残るため
- 光らせている物が消えている時(建設メニューを閉じた等)=`getBoundingClientRect` が空なら引っ込める

### 検査
`checkTutLock()` に追加。寸法だけを渡して
①下端の帯は真上から ②右寄りは左から ③左寄りは右から ④画面の四隅・全画面の対象でもはみ出さない
⑤印が対象に重ならない — を見る。

### 教訓
- ⭐**「光らせる」だけでは気づかれない。動く印を添える**。枠の点滅は画面の情報量に埋もれる
- ⭐**印の置き場所は「空いている方」ではなく「隣に何が並んでいるか」で決まる**。
  操作バーのボタンは左右に別のボタンが並ぶので、横から指すと隣に重なる
- ⭐**位置を決める処理はDOMから切り離す**と、寸法を渡すだけで検査できる(端の対象も机上で試せる)

## 第91弾(2026-07-26) — ★見た目の総仕上げ(タワーの砲身/兵科32種/英雄11人/プレビュー)

ユーザー指示=**「タワーの砲身と特徴パーツ / 兵科32種 / 英雄11人 / プレビューを大きく、この4つ全部一気に」**。
(研究所のまとめ買いと硬い名前の見直しは一旦やらない、も同時に指示)

### 0. まず「並べて撮る道具」を作った
`node test_shot.js out.png 1400 800 grid=tw|u|hero|z` で、**左が実寸(852px幅と同じ縮尺)・右が3倍**の
一覧を撮れるようにした。絵を直す作業は「撮る→直す→また撮る」の繰り返しなので、
1種ずつ実戦で撮っていては回らない。⚠**実寸で見分けが付くか**が本番。
新登場の紹介モーダルは `intro=t|u|z` で撮れる。

### 1. タワーの砲身と特徴パーツ(20種+支援3+工房2)
基部の形は第81弾で分けたが、**上に乗る物が「細い棒1本」ばかり**で実機35pxでは同じ塔に見えていた。
- 共通パーツを用意: `twGun`(筒・下半分を暗くして丸みを出す) / `twMuz`(砲口=制退器・ラッパ・先細り・輪・照星) /
  `twFins`(放熱フィン) / `twDrum`(弾倉ドラム) / `twBelt`(弾帯) / `twTank`(タンク)
- ⭐**特徴パーツは基部(回らない側)に置く**。砲身は狙う向きで回るので、そこに個性を置くと向き次第で消える
- 主な変更: ライフル台=細身+照星+箱弾倉 / ショットガン台=二連+弾帯 / 拘束ネット砲=網カゴ /
  火炎放射塔・酸噴射器=燃料タンク2本(酸はホースも) / 迫撃砲=上を向いた太い筒+底板 /
  スナイパー塔=長身+大きなスコープ+制退器 / 擲弾砲台=回転弾倉 / 冷却塔=放熱フィン+霜の結晶 /
  レーザー塔=先端に集光レンズ1枚+足元にフィン / 音響砲=大きなホーン+共鳴リング /
  重砲台=極太+制退器+駐鋤 / 重テスラ=碍子3枚 / レールガン=蓄電器を左右に /
  ガトリング=弾倉ドラム+弾帯 / プラズマ砲=放熱フィン+でかい球 / 要塞砲=3連装+防盾
- ⚠実機で撮って直したもの: フィンが宙に浮いて見えた(基部に食い込ませた) /
  レーザーのレンズを3つ並べたら**芋虫**に見えた(先端の1枚だけにした)

### 2. 兵科32種の見た目
それまで**全員が同じ胴+同じ頭**で、違うのは色と手元の小さな武器だけ=実機25pxでは色しか違わなかった
(ゾンビ36種で踏んだのと同じ失敗)。1種ずつに **体型・かぶり物・背負い物** を与えた(`U_LOOK`)。
- 体型 `uBody`: 細身/普通/ずんぐり/のっぽ/前傾 の5つ。**上を明るく下を暗く1段**(zFillと同じ理屈)
- かぶり物 `uHead`: 野球帽/ベレー/鉄帽/椀型/工事帽/角/前立て/全面兜/面頬/ガスマスク/バイザー/
  ゴーグル/溶接面/鉢巻/覆面/ヘッドセット/看護帽/ギリースーツ/氷の冠/制帽/翼付き兜/耳当て/
  フード/頭部アンテナ/横一文字の面頬 の25種
- 背負い物 `uBack`: リュック/ボンベ/ボンベ2本/弾倉/瓶3本/手榴弾/ロケット弾/迫撃砲筒/電池/縄/
  背の刀/工具袋/医療鞄/マント
- ⚠実機で撮って直したもの: 角が**兎の耳**に見えた(横に太くした) / 鎖の輪が**石鹸の泡**に見えた(太い縄にした) /
  同じかぶり物が5種以上に偏らないよう配り直した

### 3. 英雄11人それぞれの見た目
共通シルエット1つ(マント+武器+★)だったのを、`H_LOOK` で11人ぶんに分けた。
元警官=制帽+拳銃+盾 / 消防士=つば広ヘル+放水銃+ボンベ / 猟師=つば帽+弓+矢筒 /
整備士=作業帽+レンチ+工具袋 / 配達人=帽子+鞄+短マント / 特殊部隊員=暗視装置+長い小銃 /
軍医=白帽+医療鞄 / 爆破屋=ゴーグル+爆弾+ダイナマイト / 預言者=頭巾+長い杖 /
鋼鉄の巨人=全身鉄+巨大な拳(体格1.24倍) / 終焉の騎士=髑髏兜+大鎌+マント。
- ⭐**光の色をレア度の色に**した。全員を同じ琥珀色で光らせていたので、
  せっかく分けた色が光に飲まれて**全員が金色**に見えていた(並べて撮って気づいた)
- ⚠**ラッパ口は自分で描く**。`twMuz` は y=0 に描くので、消防士の放水銃で呼んだら**足元に落ちた**

### 4. プレビューを「全容が見える大きさ」に
`fitDraw(canvas,drawFn,余白)` を用意した。**一度下絵に描いて絵のある範囲を測り、
枠いっぱいになる倍率でもう一度描き直す**(焼いた絵を引き伸ばすとぼやけるので、本番はベクタで描き直し)。
- 🔬**研究所のカードに実物の姿**(タワー/兵科/派生)。それまで絵文字だけで「何を解放するのか」が分からなかった
- 🎖**編成のプレビューを26px→42px**+枠いっぱい
- **新登場の紹介モーダル**も枠いっぱい(決め打ちの1.5倍では、大きい物ははみ出し小さい物は豆粒だった)
- 派生キャラを描く時は `drawUnitAs(c,i,def)`(⚠UNITSを必ず元に戻す=戻し忘れると編成が壊れる)

### 5. 検査
- `checkTwLook()` / `checkULook()` / `checkHeroLook()` … **図形を積む命令の並び**を記録して、
  同じ並びの組が無いか見る(⚠色も数値も見ない=色を入れると必ず通る検査になる)。
  `checkULook` は「同じかぶり物が5種以上に付いていないか」も見る
- `checkPreview()` … 研究所の項目に姿の指定(pv)が付いているか / `fitDraw` が例外なく描けるか /
  `drawUnitAs` が UNITS を元に戻すか
- 見つかった重複: **火炎放射塔と酸噴射器が同じ形**だった(タンク2本+筒+ラッパ口)。酸にホースを足して分けた

### 教訓
- ⭐**「並べて実寸で撮る」道具を先に作る**。1種ずつ実戦で撮っていたら4つ全部は終わらなかった
- ⭐**武器だけを変えても見分けは付かない**。頭と背中(=輪郭に出る場所)を変えること
- ⭐**光の色は絵より強い**。全員同じ色で光らせると、描き分けた色が全部その色に染まる
- ⭐**プレビューは倍率を決め打ちしない**。測って枠に合わせる方が、絵の大きさがバラバラでも破綻しない
- ⭐**形の重複は検査で捕まえられる**(色を見ない図形の並びで比べる)。目視では気づけなかった


---

## 第105弾 2026-07-30(CLAUDE.md から移した明細)

### 攻撃モーションと固有演出(2026-07-30 第105弾に実装した)
⚠**もう「GOサイン待ち」ではない**。方針(DESIGN.md「これから作るもの」)のとおり1→4の順で全部入れた。
- **①土台=攻撃の4段階**(待機→溜め→発射→硬直)。`u.chg`(0→1)/`u.ct`(溜め中の印)/`u.cd`(硬直)。
  溜め中は**歩行を止めて足を開く**(`drawUnit` の `o.chg`)。⚠焼いたコマ(SPR)は歩いているので直描きへ回す。
  溜めの絵は `drawUChg()`=**fxには積まない**(溜めている間ずっと出るので毎フレーム積むとあふれる)。
- **②型8種**=`U_MOT`(cast/mag/bolt/snipe/spin/toss/heat/now)。兵科32種+英雄18人の50種すべてに割り当て済み。
  弾・銃口・着弾も型ごとに分けた(狙撃=白い一条+貫通線 / 回転=短い曳光線+薬莢の滝 / 術者=光の珠+輪 /
  落雷=上から折れた線 / 磁力=吸われる粒 / 炉熱=炎の帯+熱波の輪)。
- **③固有**=`uUniq()` が真の者(英雄★4以上の5人 + ★が付く上級兵科 `uTier>=3` の18種)は
  **溜め・銃口・弾・着弾がそのキャラ自身の色**になる。英雄★4以上はさらに**専用の溜め**(`drawUChgHero`)と
  **必殺の0.5秒の決めポーズ**(`u.ulT`)が付く。
- **④威力の戻し**=`uChgM()`=(rate+溜め)/rate を1発に掛ける。**実効DPSは溜めを入れる前とぴったり同じ**。
  ⚠⚠**それだけでは足りない**(2026-07-30ユーザー指摘)。溜めは**攻撃間隔の0.6倍で頭打ち**(`CHG_CAP`)にし、
  **歩いている間は溜めておく**こと。DPSが同じでも1発が重いとオーバーキルで数に弱くなり、接敵の初弾も遅れる。
  ⚠だから `test_balance.js`(表の atk/rate で見る)はそのまま正しい。**この掛け算を外さないこと**。
- **歩きの振れ幅**=`WALK_AMP=1.7`。⚠以前は腕が±1.2pxしか振れておらず、実質静止していた。
⭐残っているのは**実機で見て気になった所を撮り直す**だけ。撮り方=`u=snp+uchg=0.8`(溜めの途中で止めて撮る)。

### 2026-07-30 に受けた修正依頼(全部済み)
1. ✅💎召集の目玉を**スワイプで入れ替え**(自動回転をやめて中央で止める)。`GCH.idx / GCH.a / GCH.dg`
2. ✅レーザー塔の🔥昇温速度が進化後に**死んだ強化**だったのを掛け算に(`twHeatN`)
3. ✅**ボス撃破の💎を大幅増**(素5/18・難易度で最大2.4倍=`gemBoss`)。タイトルのロブスターが主な入手源だった
4. ✅**タレット/工房の進化を🔬研究所の解放制に**(`META.tg` / 派生タブ / `tgOK` / `TG_ALL`)
5. ✅ステージ1の序盤ゾンビ8種のHPを約1.3倍(ショットガン1台で3波捌けていた)
6. ✅「準備完了」のトーストに**編成で選んでいる砲撃の名前**を出す
7. ✅🦸**英雄ステータス画面**(`#md-hstat`)。ソロ設定の「📋 性能」と召集の📖図鑑から開く
8. ✅対戦の2人目=準備タイムが「0100010」になる不具合(スナップショットの `sk` キー衝突)と
   ⚙️の解放カードが押せない不具合(WAVE3ロックがクライアント側に残っていた)
9. ✅💎召集の演出を増やした=**背景の朝夜**(夜が期待度高) / **「撃て!」の白・赤・虹** /
   **★4以上を引いた回は3通りをピラミッドで**=①✨黄金のロブスターが的20% ②どんでん返し35% ③通常の最上位演出45%
   ⭐**排出率は「英雄1体あたり」で決める**(`RK_EACH`)=英雄を足すほどそのレア度の枠が太くなる
   ⚠**どれも下振れだけ**(上振れは絶対にしない)=検査が実際に1200回引いて見張っている
10. ✅特殊部隊員の必殺を**ボスだけ最大HPの12%**に(ワンパンしていた)
11. ✅**最終ウェーブの主は拠点に取り付いて何度も殴る**(`z.siege` / `SIEGE_D`)。素通り即クリアをやめた
12. ✅スナイパー塔に**💀ボス/👁オーラ特攻**(素2.4倍・進化3倍=`T.vsB`)

### 済んだ宿題(2026-07-25の一覧から)
- 対戦のミニ盤面が右上の侵入路に掛かる → ✅2026-07-26。`PMAXX`(道路の右端)を作り、ミニ盤面がある時だけ
  `fitCanvas` が MW ではなく PMAXX まで収める / `minisResPx()` を「小窓の左端から画面右端まで」で測る。
  対戦だけマップが1割ほど小さくなる(615x292CSSpx)。`test_layout.js` はNG扱いにしてある。
- アートのBlender移行 → ✅使わないと決定(第81弾)。タワーの砲身・兵科32種・英雄・プレビュー拡大 → ✅第91弾。
- 研究所の「×5 まとめ買い」 → ✅2026-07-27。カードにボタンを増やす余地が無い(横画面は高さ393px・
  `.lgrid.tight`は6列=1枚60px)ので、画面にひとつだけ ×1/×5 の切り替え(`.mulsw`)を置いて全カードに効かせた。
  効くのは段があるものだけ(`LAB_MSTEP`)。残りが足りない時は×3などに縮める。検査は `checkLabMul()`。
- 硬い名前の見直し → ✅2026-07-27。擲弾砲台→爆裂砲台 / 擲弾兵→爆弾兵 / 重擲弾兵→重爆弾兵 /
  拘束ネット砲→ネット砲 / 酸噴射器→酸噴射砲。⚠名前は表示だけでセーブは id なので互換は壊れない。
  ⚠デッキのボタンは幅に余裕が無いので**兵科の名前は元の字数以下に収める**こと。

### 2026-07-30 実機の指摘を直した(💎召集の演出)
1. ✅**✨黄金のロブスターの姿が見えない**(ユーザー指摘)。金のグラデを source-atop で乗せると
   **黒フチまで金色に潰れる**ので、朝(明るい背景)では絵が丸ごと「金色の靄」になっていた。
   ⭐`destination-over` で `silLayer` の黒シルエットを背面に置いて**輪郭を描き直す**(`GLD_W`=3.4)。
   あわせて金のグラデの青成分を下げた(白飛びを止めて金に寄せた)。⚠明細と再発防止は [[NOTES]]。
2. ✅**どんでん返しを作り替えた**(ユーザー指示「弾がゾンビに当たるまでは通常演出と同じで、
   当たったらゾンビが爆発して金色のゾンビが現れる感じで」)。
   それまでは「当たっても倒れず、その場で金色に変色」=**当たった手応えが無かった**。
   いまは ①普通に木っ端みじん(文字も「木っ端みじん!!」で通常と見分けが付かない)
   ②爆発の跡から**金色の個体がせり上がる**(光の柱+「金色の個体が現れた…!?」)
   ③金色のまま砕けて跡地の示唆へ。尺は `GC_TWIST`=1.5秒・切り替えは `GC_TW_R`=.45。
   ⚠肉片は `gcGib()` に切り出して1回目/2回目で共用(2回目は金色の肉片)。
   ⚠カメラの揺れを **hk ではなく着弾からの実時間**で引くように直した(hkのままだと1.5秒揺れ続ける)。

### 2026-07-30(2) 見た目の作り込みを開始(ウォーカー1体・バット1体)
⭐ユーザー指示「見た目の実装に入ろう。時間かかってもいい。全力でプロ顔負けの完璧な見た目に。
  まずはゾンビはウォーカー1体、ユニットはバット」。**この2体が以後の基準**になる。
1. ✅**大写しの撮影を作った**(`pose=z:walk` / `pose=u:bat`)。x6/x3/実寸を、暗い地面・明るい地面・紙の
   3つの背景に並べて1枚にする。⚠実寸だけでは細部が見えず、拡大だけでは実機の見え方が分からない。
2. ✅**ウォーカー**=姿勢・関節・接地を作り直した(色の区分けは第96弾のまま)。
   膝と横に長い靴で接地させ、腕を肘で折り、上体を脚と位相をずらして傾けた(よろめき)。
   ズボンと靴は `ZW` より明るいローカル色にした(⚠`ZW` 自体は他の12種に効くので触っていない)。
3. ✅**バット**=共通の体をやめて専用の枝にした。釘バットを体の前へ斜めに構え、
   たすき1本で胴の色を分け、脚を伸ばして頭を小さくした(割り付けを掟に寄せた)。
4. ⚠**捨てた案**(全部撮って判断した): バットを肩に担ぐ(先端しか出ず「肩に乗った岩」)/
   前開きのベスト(腕に隠れて「黄色いネクタイ」)/下半分の腹当て(黄土が肩の三角しか残らない)/
   手を暗い手袋(両手が1つの黒い塊)/肩に丸みの楕円(「肩章」)。
5. ⭐**5観点の並列レビュー**(実寸の可読性/形の破綻/掟の照合/歩行の動き/色と明暗)で、
   画素を実測した指摘が出た。**「1周期に1歩しか出ていない」「釘の円に stroke を掛けると黒い円盤になる」
   「腕が実は1°しか曲がっていない」**は自分の目では見つけられなかった。教訓は [[NOTES]] に追記。

### 2026-07-30(3) バットの攻撃を「野球のスイング」にした
⭐ユーザー指示「バットを野球みたいに振るようにして。振りかぶってしっかり振る感じ。
  バットと同様そういうモーションはしっかり忠実に再現してほしい」。
- ✅**攻撃の全周期を1本の進行度で描く**(`uBatP`=`1 - u.cd/U.rate`)。
  振りかぶり(後ろへ引く)→ 振り出し(周期の14%=約0.1秒で一気に)→ 当たる → 振り抜き → 構えへ。
  あわせて**上体の捻り**と**手前の脚の踏み込み**、腕と得物の一体回転も入れた。
- ⚠**溜め(`u.chg`)は使っていない**=バットは `U_MOT` の「即時」枠で溜め0。
  ここに溜めを足すと1発が重くなって撃つ回数が減り、数の多い相手に押される(前にユーザーが指摘した現象)。
  **見た目だけで振りかぶらせた**ので、強さは1ミリも変わっていない。
- ⚠踏んだ間違い2つ(どちらも撮って気づいた): ①振り出しを周期の頭に置いて**当たってから振っていた**
  ②振り切った姿(真下)を当たる瞬間に置いて**当たる時にはもう振り終わっていた**。
  作り方は [[NOTES]]「攻撃モーションを『振りかぶって振る』形にするやり方」に残した。

### 2026-07-30(4) バットのスイングを「本物の野球」に作り直した
⭐ユーザー指摘「野球のバットの振り方ってこれ? 本当の野球の」→ 1つ前に出したものは
  **「振り下ろし」=斧や剣の振り方**だった。ユーザー指示「いいから本物の野球の振り方にして」。
- ✅**バットが1周期でほぼ1回転する**軌道にした(構え-1.75 → 引く-2.35 → インパクト0 →
  振り抜き2.2 → **背中側へ巻き付く**3.7 → 構えへ4.53)。⚠一貫して同じ向きに回る。
- ✅**構えをバットを立てた形**にした(肩の後ろ・手は耳の高さ)。横から見ても野球と分かる。
- ✅**下半身リード**=振り出しの区間だけ、腰と踏み込みは線形・バットと腕は q*q で遅らせた。
  これが運動連鎖に見える。あわせて腰の前後移動(hip)も入れた。
- ⚠**2Dの真横では水平面のスイングは描けない**(奥から手前なので画面上ほとんど動かない)。
  横から見ても伝わる要素(立てた構え/1回転/巻き付き/腰が先)に置き換えてある。教訓は [[NOTES]]。

### 2026-07-30(5) バットを「縦振り」から「横振り」にした
⭐ユーザー指摘「いまって縦振りか横振りかどっちっていう認識?」→ **縦振り**だった
  (画面の平面で回していただけ)。指示を受けて横振りに作り替えた。
- ✅**遠近でバットを短縮する**ようにした。水平の円を真横から見ると、バットは
  **奥や手前を向く所で短く・真横(インパクト)で最も長く**見える。
  `flen`(長さ)/`fw`(太さ)/`dyB`(上下のずれ。奥は上・手前は下)を角度に合わせて動かす。
- ⚠**これを入れないと、どれだけ軌道を凝っても「風車のように縦に回る棒」にしか見えない**。
  1つ前の「1回転する軌道・巻き付くフォロースルー・下半身リード」はそのまま活きている。
- ⚠構えとテイクバックは短縮しない(本物もそこだけはバットを立てていて円の面に乗っていない)。

### 2026-07-30(6) 横振りを「上から見た角度の射影」で作り直した
⭐ユーザー指摘「めっちゃ縦ぶりだけど」→ (5)で短縮を足したが、**画面の角度を1周させる設計のまま**
  だったので縦振りに見えていた。3度目の作り直し。
- ✅**バットは「上から見た角度 TH」で持ち、画面上の見え方をそこから計算する**形にした。
  画面x=cos(TH) / 画面y=sin(TH)*TZ → 長さ=√(x²+y²)・角度=atan2(y,x)。
  **TZ(スイング面の傾き)を 0.34(≒20°)** にしたことで、縦の振れ幅が横の1/3になり横振りに見える。
  ⚠TZ=1 が完全な縦回転=これが「めっちゃ縦振り」の正体だった。
- ✅構えとテイクバックは円に乗せない(`lay`=0でバットを立てたまま)。寝かせるのは振り出しの最初だけ。
- ⚠角度の補間が±πを跨いで逆回転しないよう、立てた時の角度は +2π 側で持つ。

### 2026-07-30(7) 🧪検証場(動きを確認するための一本道)
⭐ユーザー指示「ユニットとかゾンビとかヒーローの動きとかを確認するときの専用ステージが欲しい。
  一本道で左からユニットがでて右から敵がでてきて、近接ユニットの時は短い道、遠距離ユニットの時は遠い道で」。
- ✅DEV(`?dev=1`)のタイトルに**🧪検証場**を足した。味方(兵科32+英雄18)と敵(ゾンビ46)を選んで開始。
  一本道・資源無限・拠点は落ちない・波もクリアも無い。味方と敵は一定間隔で補充される。
- ✅**道の長さは味方の射程で決まる**(rng>60 なら長い1640 / それ以下は短い880)。
  道は**画面の横中央**に置く(右端で切ると絵として途中で途切れる)。
- ⚠`G.tst` が立っている間は `gameStep` が丸ごと `tstStep` に逸れる=波・勝敗・戦績の処理を一切通らない。
- ⚠**ヘッドレスの撮影では rAF がほとんど回らない**ので「湧かない・動かない」に見える。
  `arena=bat:walk+arn=16` は撮る前に `tstStep` を直に回して時間を進めている(原因を1時間探した)。

### 2026-07-31(1) 🧪検証場を「押した時だけ出る」形に/選ぶ画面を廃止
⭐ユーザー指示「自動でユニットでないように。ゾンビも手動で出せるように、ユニットみたいに↑に出して」
  → 続けて「選ぶ画面いらない」。
- ✅**自動湧きを廃止**。味方=下のデッキ(⚠検証場だけクールダウンを毎フレーム0に戻す)/
  敵と🦸英雄=**画面上の帯 `#zbar`**(横スクロールの名前ボタン。敵46種→英雄18人の順)。
- ✅選ぶ画面(`md-tst`/`openTst`/`TSTSEL`)を削除。タイトルの🧪から直に入る。
  ⚠道の長さは選んだ味方の射程で決めていたので、**常に長い方に固定**(近接は🚩集結旗で前に出す)。
  ⚠英雄はデッキに載っていないので、上の帯に受け皿を作らないと出し口が消える。

### 2026-07-31(2) バットの横振りを根本から作り直した(4度目)+担ぎと滑り台
⭐ユーザー指摘「全然横振りに見えない。根本的に変えて」。
- ✅**原因は構えだった**=1周期の約6割でバットを立てていたので「縦→横」=振り下ろしに映っていた。
  **1周期のどこでもバットを立てない**形にし、画面上の角度を水平から±17°(`TILT`)に必ず収めた。
  奥/手前は角度ではなく「短く・太く」で表す。掃いた跡を平たい楕円の弧で置いた。
- ✅続けて「最初の持ち方を担いでる感じに、そこから斜めに滑り台みたいな軌道で」→ `sh`(担ぎ度合い)を追加。
  **担ぐのは構えとテイクバックだけ**で、振り出しの前半で0まで落とし切る=肩から滑り落ちて水平の輪に乗る。
- ✅ユーザー指摘「出した途端に攻撃モーションに入る/敵がいないのに振る」→ `u.cd` だけで振らせていたのが原因。
  `u.eng`(狙う相手が居るか)と `u.fired`(一度でも攻撃したか)の両方を見る。検査 `checkAtkMotion`。

### 2026-07-31(3) 兵科32種の骨格をバットへ揃えた(共通部品の底上げ)
⭐ユーザー指示「全キャラ完璧にしよう。まずユニットから」。**共通部品なので1回で32種に効く**。
- ✅割り付けを **頭17/胴23/脚10 → 頭12.6/胴16/脚18.8** に(バットは12.6/13.4/20.5)。
  脚=腿→膝→すね→靴の4段・基準ほぼ0で1周期2歩 / 胴の丈を詰める(`uShoulder`も同時) /
  頭をあごを軸に0.74倍 / 背負い物を肩を軸に0.82倍 / 胴の模様を暗い側から明るい側へ(負い革は廃止)。
- ✅腕の付け根を胴の前ぶちへ・肩の丸み(pad)は廃止・武器をまとめて0.86倍・銃と靴を明るく。
- ✅指摘5件=ヴァルキリーの円柱/医療兵の白い箱2つ/長槍兵の槍の向き/重装盾の大きさ/★4の光の濁り。

### 2026-07-31(4) ヴァルキリーを「光の剣の女剣士」にした(専用の姿)
⭐ユーザー指示「光の剣をもった女剣士にしたい」→ 射程340・範囲130の `blast` 型と噛み合わないため、
  **数値は1つも触らず攻撃の見せ方を『飛ぶ光の斬撃』に変えて辻褄を合わせた**(`U_MOT` に `slash` 型を新設)。
- ✅参考絵を受けて**専用の姿**へ(共通の体に白い板を貼るだけでは鎧のシルエットが出ない)。
  肩を張らせ腰を絞る/白のすね当てとタセット/翼を胴より奥へ/長い金髪/細く長い光の剣。
- ⚠外したもの=冠の前立て(黒シルエット7pxが三角の間を埋めて猫耳になる)・白い額あて(目隠しに見える)・
  刃の上の半透明の光(暗い地面で灰色の帯になる)・手前の髪(顔が隠れる)。
- ⚠**専用の姿は途中で return する**ので、ティア章(★★)を自前で描くこと(一度消えた)。

### 2026-08-01 残り25兵科に「構え・手の位置・装い」を入れた
⭐ユーザー指示「ユニット完成まで」。⚠それまで首から下は32種すべて同じ立ち姿・同じ服・同じ手の位置で、
  武器だけが手の届かない所に浮いていた。**共通部品に兵科ごとの差を持たせる**形にした(表=`U_PZ`)。
- ✅**構え**(`uLegs`): 歩幅/脚の開き/腰の落とし/上体の前傾。腰から上をまとめて動かす。
  狙撃と迫撃は腰を落とし、ミニガン・火炎放射・ロケランは踏ん張り、斥候と影は前傾で速そうに。
- ✅**手の位置**(`uArmTo`+`wkp`): 手の座標を武器の座標で書き、握った手を武器の上にもう一度描く。
- ✅**装い**(`uCos`) 15種: 防弾チョッキ/胸当て/長いコート/白い前掛け/革の前掛け/弾帯/ハーネス/
  耐熱/防寒/発光パネル/作業着/マフラー/陣羽織/ギリー/毛皮。
- ✅武器の描き直し: 斧(双刃を前へ・顔の高さを避ける) / チェーンソー(前の握り+溝+尖った先) /
  火炎瓶と手榴弾(実寸で読めるまで拡大) / 拳銃(厚く明るく) / 銃に握りを追加 / ミニガンに両手の握り。
- ✅共通の直し: かぶり物のつばが目にかぶっていたのを上へ / 脚と胴の下半分と銃を明るく /
  隊長の外套を濃い色へ(白い帆に見えていた) / 装いのある兵科はたすきを描かない。

### 2026-08-01(2) レール狙撃を超電磁砲に / 兵科を「専用の体」へ描き直し始めた
⭐ユーザー指示 ①レールガン兵の弾をレールガンらしいビームに(音も) ②各キャラ専用の体と見た目に ③斧が斧に見えない。
- ✅**レール狙撃=超電磁砲**。狙撃塔と同じ細い白い一条だったのを、タワーのレールガンと同じ語彙へ:
  **紫の極太ビームが射程いっぱいまで走り `sus` で太いまま残ってから消える**+砲口の輪+床を薙ぐ衝撃+揺れ。
  音は `rail`(超電磁砲)、溜め始めに `railChg`(きゅいーん)。銃のレールも溜めに応じて光る。
  ⚠**数値(威力・射程・間隔・貫通)は1つも触っていない**。
- ✅**斧使いを専用の体に**。斧に見えなかった原因は刃ではなく**柄が体の裏に隠れていたこと**。
  柄を体の外へ長く斜めに出し、頭の先まで突き出させた。体も蛮族の戦士(毛皮の肩掛け/剥き出しの腕/角の兜)へ。
- ✅**盾持ちを専用の体に**。腰を落として盾の後ろに半身で隠れる構え+板と鉄帯の大盾+口元の布と鉄帽。
- ⬜**残りの兵科は「共通の体+構え/手/装い」のまま**(専用の体は bat/valk/axe/shd の4体)。

### 2026-08-01(3) 兵科32種を全部「専用の体」にした
⭐ユーザー指示「残り全ユニットやって」。表 **`UBODY[id]`** に1体ずつ描画関数を登録する形にし、
  登録した id は共通の体(uBody/uLegs/uArmTo/uCos/武器の分岐)を**一切通らない**ようにした。
- ✅2000行を超えないよう**描くための道具**を5つ用意 = `bLeg`/`bArm`/`bTorso`/`bNeck`/`bFace`。
  各体は30〜50行。⚠道具に兵科ごとの分岐は書かない。
- ✅25体ぶんの姿: ガンナー(鉄帽+防弾チョッキ+背嚢) / 斥候(ベレー帽+なびくマフラー) /
  鎖使い(覆面+革のハーネス+腰の縄) / 短機関銃(ヘッドセット+胸の予備弾倉) /
  火炎瓶(鉢巻+瓶を掲げる) / 爆弾兵(手榴弾のハーネス) / チェーンソー(革の前掛け+ゴーグル+剥き出しの腕) /
  医療兵(膝まで届く白衣+ナース帽+注射器) / 冷凍兵(密閉頭巾+襟のふさふさ) /
  スナイパー(腰を落とす+ギリー) / ミニガン(溶接面+腰の弾薬箱) / ロケラン(肩に担いだ筒) /
  長槍兵(のっぽ+前立て+陣羽織) / 火炎放射兵(ガスマスク+タンク2本) / 重装盾(全面兜+鋼の大盾) /
  電磁兵(アンテナ+発光パネル) / 迫撃兵(イヤマフ+腰を落とす) / 工兵(工事ヘルメット+反射帯) /
  影(フード+長いコート+赤い目元) / 重爆弾兵(面頬+束ねた爆薬) / レール狙撃(バイザー+光るレール) /
  絶対零度(氷の冠+二連タンク) / 隊長(制帽+濃い外套+階級章) / タイタン(巨大な肩甲+腕の大盾) /
  (+ 斧使い・盾持ちは同日の先行ぶん)。
- ⚠★の印を -57→**-64** へ(氷の冠・アンテナ・前立てに食い込んだ)。

### 2026-08-01(4) 装甲車とメカスーツを削除 / 投げた物は着弾してから当てる
⭐ユーザー指示 ①メカスーツと装甲車は削除 ②投擲系が「攻撃モーションに入った瞬間にダメージが入っていて、
  爆発物が着弾していないのに敵が倒れる」。
- ✅**装甲車(tnk)とメカスーツ(mech)を廃止**。UNITS/進化(VAR/FVBASE)/U_LOOK/U_MOT/専用の絵をまとめて外した。
  兵科は32→**30種**。⚠`UNITP`(解放費の表)が28個しか無く最後の枠が undefined になっていたので2個足した。
- ✅**投げた物・撃ち込む物は `later` の中で当てる**ようにした(火炎瓶/爆弾兵/重爆弾兵/ロケラン/迫撃兵/
  冷凍兵/絶対零度/ヴァルキリーの斬撃)。⚠**中心は投げた瞬間の狙い先で固定**=着弾時に取り直すと追尾弾になる。
  ⚠威力・範囲・間隔は1つも触っていない(当たるのが飛行時間0.16〜0.42秒ぶん遅れるだけ)。
  ⚠演出オフ(FXLV=0)は tossFly が0を返すので今までどおり即時。

### 2026-08-01(5) ロケランを「筒の先からまっすぐ飛ぶロケット」にした
⭐ユーザー指摘「ロケランは山なりじゃなくて筒の先からロケットを撃つんだよ?」。
- ✅**専用の飛翔体 `rktFly`/fx `rkt`**を新設。爆弾兵と同じ `toss`(放物線)をやめ、
  **肩の筒の先から・速く・まっすぐ**飛ばす。弾体+赤い弾頭+尾翼+噴射の炎、**白い煙の尾**を後ろへ引く。
  発射の瞬間に筒先の閃光と**後方噴射**(後ろへ吹く煙)を出す。
- ✅溜めの見せ方を「投擲(腕を引く)」から**狙撃(狙いを定める)**へ。⚠溜めの秒数(.55)は据え置き。
- ⚠迫撃兵・爆弾兵・重爆弾兵・火炎瓶は**山なりのまま**(本物も曲射・投擲)。

### 2026-08-01(6) 溜めを体の動きで見せる / ヴァルキリーを連撃に
⭐ユーザー指示 ①ヴァルキリーはクールタイム短め+光の斬撃+剣を複数モーションで連撃っぽく+音も光の斬撃っぽく
  ②全キャラ、エフェクトでクールタイムを表すだけでなく**身体の動き**で表す。
- ✅**溜めは体の構えで見せる**(表 CHG_LEAN / uChgUp)。攻撃の型ごとに上体を腰から傾ける
  =投擲は後ろへ大きくひねり、狙撃・回転・炉熱は前へかがむ。30兵科+英雄18人すべてに効く。
  足元の輪(drawUChg)は薄くして主役を体に移した。
- ✅**ヴァルキリーの連撃**。rate 1.8→1.0 / atk 351→200 / 溜め .55→.3(**実効DPSは据え置き**・1周期2.35→1.3秒)。
  剣の振りを1本の進行度で描き、**斬り方を3種**(袈裟斬り/逆袈裟/水平の薙ぎ)を1発ごとに切り替える。
  腕と剣は肩を軸に一体で回し、上体の捻りと掃いた跡も付けた。
- ✅**専用の音 `lslash`**(光の斬撃)=ノイズの「シャッ」+金属比の和音+低い芯。⚠高いキラキラ音の禁止は遵守。

### 2026-08-01(7) ヴァルキリーの縦振りを直し、斬撃に光感を足した
⭐ユーザー指摘「縦振りだけになるのなんなの? / 斬撃も全然光感ない」。**バットと同じ穴を剣で踏んでいた**。
- ✅**振りをバットの射影に置き換えた**=上から見た角度 TH で持ち、画面x=cos(TH)/画面y=sin(TH)*TZ、
  画面上の角度は ±TILT に収める。TH は1周期で -200°→+160° と一方向にだけ回る。
- ✅**3種を「面の傾き」で分けた**=①水平の薙ぎ(TZ.16) ②斜めの袈裟(TZ.34/面を-.52傾ける) ③**突き**(回さない)。
  ⚠3種とも縦には回らない。掃いた跡も平たい楕円の弧に。
- ✅**光感を加算(lighter)の3枚重ねで出した**=外のにじみ→中の光→細い白い芯。
  刃そのものの発光 / 掃いた跡 / 飛ぶ斬撃(残光2枚を追加) / 着弾の斬撃(lv3のみ)。

### 2026-08-01(8) ヴァルキリーの構えと剣の形
⭐ユーザー指示「フェンシングみたいに前に構えるんじゃなくて剣が斜め↓前に傾いてる感じで」
  「剣の感じもダサいから もっと刀身を細くしてかっこよく」。
- ✅**構え(`VR`)を -.24 → +.52**(≒30°の斜め下前)。⚠腕は `sa-VR` ぶん回すので、構えだけが変わり振りは変わらない。
- ✅**細身の長剣に描き直し**=長さ28→36 / 半幅3.4→2.9(細長さ8→12)+柄頭の丸+前へ反った鍔。
  ⚠これ以上は細くできない(黒フチが局所2.4あるため)。細さは幅ではなく**長さ**で出す。
- ✅**突きは腕ごと動かさず「伸ばす」**=まるごと平行移動すると肩から腕が離れて剣が浮いた(撮って直した)。

### 2026-08-01(9) ヴァルキリーの持ち方を腰の横へ
⭐ユーザー指摘「首から剣生えちゃってる。腰の横でより下で手でシンプルに持って。体の前でついてる感じじゃなくて横で」。
- ✅**握りを胸(-29.2)から腰の横(-21.7)へ**下げ、肩から腕を下ろす形にした。構えの角度も .52→.42 に寝かせた。
- ✅**腕の付け根を胴の前ぶちへ**=内側から生やすと二の腕が白い胸当てを覆って「胸の飾り」に見えた。
- ✅突きは肘から先だけ伸ばす(腕ごと平行移動しない)。

### 2026-08-01(10) ヴァルキリーの腕を出し、構えを斜め上へ
⭐ユーザー指摘「腕がないから変に見える。ちゃんと腕と手を表現して手で持って。右斜め下じゃなくて右斜め↑に」。
- ✅**腕を太く長く**(肩→肘→手の3節をはっきり)+**手を肌色で大きめ**に。細く短い帯だと胴に埋もれて
  「剣だけが腰から突き出た人」に見えていた。
- ✅**構えを斜め下(+.42)から斜め上(-.46≒26°)へ**。⚠画面のyは下が正なので上向きは負の角度。

### 2026-08-01(11) タイトルの確率 / 足踏み / 常時炎 / 進化の研究P / 女性英雄
⭐ユーザー指示6件のうち5件。
- ✅**タイトルの珍しい個体**= 🌈虹犬 1%→**0.5%** / ✨金ロブ 0.1%→**0.05%** / 🐻🌈**虹の熊(新設)0.001%・💎100**。
  ⚠🛠DEVでは**3種を均等33%ずつ**で出す(0.001%は実機で一度も見られないため)。絵は屍熊の使い回し。
- ✅**移動していない部隊は脚を止めた**(`u.mv`)。焼いたコマも1コマ目で固定=その場で足踏みして見えていた。
- ✅**火炎放射兵は常に火を噴く**。種火の丸1つ → 根本が太く先が細い炎の舌3枚(芯ほど明るい)。
- ✅**タレット進化の研究Pを強さ順に**。`LAB_TG(k,i)` の `i`=素のタワーの添字(=解放順=強さ順)。
  ライフル380 → 要塞砲約2,400。取った総数でも少し上がる(1.02^k)。
- ✅**女性の英雄(軍医・毒霧の魔女・雷鳴の巫女)を一目で分かるように**=①細身の胴 ②背中へ垂らす長い髪
  ③腰から広がる裾。⚠髪を広げると翼やフードに見えるので真下へ垂らす。
- ⬜**残り1件**=固有モーションで**装備そのものを動かす**(杖・ガトリングの回転など)。剣(ヴァルキリー)と
  レール銃の充填は入っているが、他の兵科・英雄はまだ体の動きだけ。

### 2026-08-01(12) 🚌ゾンビバス(バスくん)=タイトル専用の巨大ゆるキャラ
⭐ユーザー決定「案①ゾンビバスでいく」。**🐻🌈虹の熊(屍熊の使い回し)は廃止**し、
  確率0.001%・💎100個・8発 の枠だけそのまま引き継いだ。
- ✅**廃バスが脚を生やして のっそり歩く**。ヘッドライトが丸い目/へこんだバンパーが口(不揃いの歯)/
  **窓からゾンビの手が4本わらわら振っている**/フロントガラスに運転手のゾンビと行先表示「墓地行」/
  屋根の荷台と積み荷/折れたアンテナとぼろ布/排気の煙/ヘッドライトの光の帯(加算)。
- ✅**ZOMBIES には足していない**(図鑑・波・冒険に出さない)=`zi:-1`+`bus:1` で持ち、絵は行進の描画側
  (`drawBusKun`)。検査 `checkBus()` が「図鑑に混ざっていないこと」を見張る。
- ✅**行進canvasはバスが居る間だけ高くする**(120→200px・`BUS_TALL`)。⚠素の高さは**CSSから測る**
  (決め打ちにすると中の寸法とCSSがずれて絵が縦に潰れる)。RBC(160px)は使っていない=バスは直に描く。
- ✅**叩くたびに窓のゾンビが1匹こぼれ落ちる**+**窓の手が2発ごとに1本減る**=8発の手応え。
  倒すと5匹が総崩れになる。専用の音 `busHit`(鉄板の「ゴンッ」)/`busHorn`(警笛→和音3段)。
- ✅当たり判定を100pxに(絵の幅190pxに合わせた)。研究pt+3。🛠DEVは今までどおり3種を均等33%ずつ。
- 撮る: `node test_shot.js out.png 852 393 "title+bus"`(`+busn=2` で残りHPを指定)。

### 2026-08-01(13) 固有モーションで「装備そのもの」を動かす
⭐ユーザー指示「杖を動かしたり剣を振ったりガトリングが回ったり、身体の動きじゃなくて装備もしっかり
  違和感なく動くように」。**体の傾き(`uChgUp`)の上に、装備そのものの動きを足した**。
- ✅**道具 `bHold(c,rc,肩x,肩y,角度,ずれx,ずれy)`** を新設=**手前の腕と装備を一体で肩を軸に回す**。
  ⚠**両手で持つ物は奥の腕にも同じものを掛ける**(奥の腕は胴より先に描くので付け根のずれは胴が覆う)。
  ⚠rc=0 の時は save/restore ごと省く(毎フレーム32体ぶん通るため)。
- ✅**兵科21種**に入れた: 投擲(火炎瓶/爆弾兵/重爆弾兵)=振りかぶる / 術者(医療兵=注射器を掲げて押し子が
  進む・工兵=トーチを掲げて火口が強く燃える・冷凍兵/絶対零度=噴射器を掲げる) /
  狙撃(スナイパー=頬付けして二脚が開き照準が光る・レール狙撃・ロケラン=筒の先を上げて弾頭が出る) /
  **ミニガン=銃身が4本回る**(手前ほど明るく太い。⚠回すのは溜め中と発射直後だけ) /
  短機関銃=遊底が後ろへ引かれる / 落雷(電磁兵=銃を天へ) / 炉熱(火炎放射兵=噴射口を下げる) /
  長槍兵=槍を引き絞る / 影=刀を引いて上段 / **迫撃兵=砲身が起き上がって砲弾が落ちる** /
  ガンナー・隊長=肩付け / タイタン=盾を突き出す。
- ✅**英雄18人**も表 `H_CHG`(得物ごとの[角度,ずれx,ずれy])で同じ仕組みに乗せた。
- ⚠**長い物・大きい物を大きく回すと得物が頭を横切って輪郭が読めなくなる**(火炎瓶で2回・
  手榴弾と爆弾で1回ずつ踏んだ)。**片手の小物だけ大きく回し、長い物は持ち上げ寄りにする**。
- 撮る: `node test_shot.js out.png 1200 740 "pose=u:<id>+posechg=0.95"`

### 2026-08-01(14) 🦸英雄18人を全部「専用の体」にした
⭐ユーザー指示「英雄18人」。⚠それまで英雄は**全員が同じ胴・同じ脚・同じ腕**で、違うのは
  かぶり物/背負い物/武器/色 の4つだけ=兵科で「実機では色しか違わない絵」として捨てた作りのままだった。
- ✅**表 `HBODY[id]` に1人ずつ登録**(兵科の `UBODY` と同じ仕組み)。道具は兵科と共通
  (`bLeg`/`bArm`/`bTorso`/`bNeck`/`bFace`/`bHold`)+英雄用の `hLeg2`/`hTop`/`hFc`/`hHat`/
  `hCape`/`hSkirt`/`hHair`。
- ✅**割り付けを兵科に揃えた**(`HSK`)。⚠それまで **胴26/脚17**(頭は-48)で胴長・脚が短く、
  第95弾以前の「頭でっかち」と同じ形だった。いまは**脚20.6/胴16/頭-47.2**(全高≒53=兵科より一回り大)。
- ✅**★4の光の `shadowBlur` を外した**(NOTESの掟。背面の輪は drawUnit 側が出している)。
- ✅溜めの装備の動きも各体の中へ(表 `H_CHG` は役目を終えたので削除)。
- ⚠**ズボンを明るくし直した**=英雄も「腰から下が1つの黒い塊」になっていた(兵科と同じ手当て)。
- ⚠**フードとローブは大きくすると『一つ目の黒い塊/紫の柱』になる**(闇商人・預言者で踏んだ)。
  頭巾を小さくし、口元の肌と光る目を出して直した。
- ⚠`H_LOOK` は**台帳として残す**=検査 checkHeroLook が「18人のかぶり物・武器・背中の物が全部別か」を見る。
- 撮る: `node test_shot.js out.png 1400 800 grid=hero` / `"pose=u:hSeer+posechg=0.9"`

### 2026-08-01(15) 暁の王を作り直した
⭐ユーザー指摘「暁の王がダサすぎる」。★5なのに18人中いちばん地味だった。効いたのは4つ:
- ✅**色を白と金にした**(上着が**くすんだ茶**だったのをやめた)。差し色は暁の橙。
  金の胸当て+肩甲+帯、白い上着と前垂れ、胸に「昇る陽」の印。
- ✅**翼を羽根にした**=前縁を1本の弧で**後ろへ跳ね上げ**、後縁を**丸い羽根の先**で欠く。
  ⚠**尖らせると蝙蝠の翼**になる(1回踏んだ)。⚠平たい葉を3枚並べても羽根には見えない。
- ✅**王冠を足し、光輪を王冠の上へ浮かせた**。⚠**頭の後ろに楕円を置くと「つば広の帽子」に見える**
  (1回踏んだ)。⚠★の位置も -62→-71 に上げて光輪と重ならないようにした。
- ✅**軍旗を短く太く**した(細くて長い竿は頭上に消えて読めなかった)。旗に暁の印を入れてなびかせた。
- ✅マントを細く短くして**脚を出した**(橙のマントが下半身の主役になっていた)。

### 2026-08-01(16) 短機関銃兵の銃口の閃光でキャラが見えなくなる
⭐ユーザー指摘「短機関銃兵の攻撃時のフラッシュがキャラが見えなくなるレベル」。
- ⚠原因=**連射の型(spin=短機関銃・ミニガン)が、1秒に3〜6発ぶん毎発フルサイズの閃光を出していた**。
  1発ずつは小さくても、連射だと絶えず重なって体が光に埋まる。
- ✅**2発に1回だけ**出すようにし、**大きさを半分(r 7+utl*2 → 4.6+utl*1.1)・濃さ .5・白い芯 .45→.24**に。
  `mzl` に `al`(濃さの上限)と `core`(白い芯の大きさ)を足した=他の型は今までどおり。
- ⚠**威力も間隔も触っていない**=見せ方だけ。⚠ミニガンも同じ枝なので一緒に直る。

### 2026-08-01(17) 建設メニューが画面より高くなり、スクロールで画面ごと動く
⭐ユーザー指摘「タレットの数が増えて、設置の時に下にスクロールすると画面ごと動いてやりづらい」。
- ⚠原因=`#buildmenu` に**高さの上限が無かった**。研究所でタワーを解放するほど縦に伸び、
  画面(高さ393px)を超えると下がはみ出す。中身がスクロールしないので**代わりにページごと動いていた**。
- ✅**中身だけスクロールさせる**=`#bm-body` を挟んで `overflow-y:auto` +
  **`overscroll-behavior:contain`**(端まで来た時にページへ流れないようにする=症状の本体)。
- ✅高さの上限を `openBM` で毎回計算して入れる(画面の高さ − 余白 − 下の操作バー)。
- ⚠**✕はスクロールする箱の外に置く**=中に入れると一緒に流れて、下まで送ると閉じられなくなる。
  ⚠パネル自身に overflow を付けるのも不可=枠の外(right:-10px)に置いた✕が切り落とされる。
- 撮る: `node test_shot.js out.png 852 393 bmenu`(タワーを全部解放した状態で建設メニューを開く)

### 2026-08-01(18) 連射のフラッシュ廃止 / 立ち止まった時の呼吸 / 英雄18人の固有の攻撃
⭐ユーザー指摘3件。
- ✅**連射の型の銃口の閃光を丸ごと外した**(「そもそもこのフラッシュがいらない」)。
  ⚠1秒に3〜6発撃つので、間引き+縮小(前の回)でも足りなかった。**書き戻さないこと**。
  撃っていることは曳光線+薬莢の滝+体の反動で足りている。⚠短機関銃とミニガンが対象。
- ✅**立ち止まっている部隊に呼吸を入れた**(「完全硬直だと味気ない」)。
  焼いたコマのまま**上下1.9Hzの呼吸+わずかな傾き**を掛けるだけ=**重さは増えない**。
  ⚠脚は動かさない(足踏みに戻るため)。
- ✅**英雄18人の攻撃を1人ずつ固有にした**(「全ヒーロー完璧に固有に。安っぽさをなくして」)。
  ⚠それまで英雄も兵科と同じ8つの型を使い回していたので、★5でも★1でも同じ光の珠が飛んでいた。
  ⭐**表 `H_FX[id]`**(UBODY/HBODY と同じ仕組み)。登録した英雄は型の分岐を一切通らない。
  例= 消防士=放水の帯+しぶきの輪 / 猟師=矢と散る羽根 / 整備士=レンチの弧と火花 /
  爆破屋=山なりの爆弾→爆発 / 闇商人=回る金貨3枚 / 預言者=紫の光条と時の輪 /
  傭兵団長=外から3条の斉射+十字の照準 / 鋼鉄の巨人=地を這う衝撃波と砂煙 /
  炉心の番人=熱波の二重の輪と陽炎 / **終焉の騎士=飛ぶ血色の三日月** /
  **暁の王=真上から落ちる暁の光柱**(上から降ってくるのはこの1人だけ)。
  ⚠**数値は1つも触っていない**=見せ方だけ。検査 `checkHeroFx()`(全員居るか/中身が被っていないか)。

### 2026-08-01(19) ★5の新キャラ「暗黒の騎士」
⭐ユーザーが絵を渡して指示。★5の3人目。**18人でただ1人の騎乗**。
- ✅絵(`HBODY.hDark`)=角2本の黒兜+**額の赤い菱形**+**赤く光る2本の目のスリット**/黒紫の甲冑と
  胸の赤い宝石/**ぼろぼろの裾の赤いマント**/肩から斜めに担いだ**巨大な黒い大剣**/
  鎧を着けた黒馬(赤い目・深紅のたてがみと尾・赤い房)。⚠色は黒紫1系統+**差し色は深い赤だけ**。
  ⚠終焉の騎士と題材が近いので**兜(角)/得物(大剣)/馬**の3つで分けた。
  ⚠焼き箱は |x|≦54 なので尾も鼻も±52に収めてある。⚠騎乗は背が高いので★の印を -62→-84 へ。
- ✅性能= HP7000 / 攻撃1020 / 間隔1.1 / **速さ96(全英雄で最速)** / blast 射程80・範囲105。
  ⚠終焉の騎士(近接・遅い・単体)と**役割で分けた**=馬で駆けて薙ぎ払う。
- ✅固有の攻撃(`H_FX.hDark`)=赤黒い斬撃2枚+地の衝撃波+土煙。**光を出さない**のが「暗黒」の決め手。
- ✅必殺技『漆黒の突撃』=**自分の居る所から敵の湧く側へ7段で駆け抜け**、触れた敵を薙いで押し戻す。
  ⚠**道の決め打ちにしない**=英雄が前に出ていると1歩目が誰にも当たらず「不発」に見える(雷嵐で踏んだ穴)。
- ✅溜めの足元=赤黒い炎が4本立つ。✅冒険(RPG)のステータスととくぎも追加。
- ⚠**排出率の数値表は1行も触っていない**=「1体あたり」方式なので★5の枠が 0.2%→0.3% に自動で太くなる。
  検査の想定人数だけ 18→19・★5を2→3 に直した。

### 2026-08-01(20) ★5を2人追加 / 終焉の騎士と暁の王を絵どおりに描き直し
⭐ユーザーが4枚の絵を渡して指示。英雄は19→**21人**、★5は3→**5人**(枠 0.3%→0.5%)。
- ✅**狙撃王ジョルジ**(★5・新規)= 白い羽根のつば広帽/白髪/**顔の右に大きな白い丸の照準眼鏡**/
  深緑の長いコート+金の縁/茶のケープ/二脚と照準器の付いた長銃。
  性能= 射程520(最長)・1発1400の貫通。攻撃は**淡い緑の一条**(⚠白だと特殊部隊員と被る)。
  必殺『一撃必殺』=HPの高い5体を順に撃ち抜いて足を止める。
- ✅**紅蓮の大魔導士**(★5・新規)= 深紅のフードとローブ+金の縁取り/**フードの中は真っ黒で赤い目2つ**/
  額と胸に金縁の赤い菱形/大きく広がる裾/**先に炎の結晶が載った長い杖**。
  性能= fire・範囲120・燃焼40。攻撃は**敵の足元から立つ業火の柱**(⚠飛び道具を出さない=術者に読める)。
  必殺『紅蓮の柱』=自分の前方3か所に柱を立て、4回繰り返して燃やし続ける。
- ✅**終焉の騎士を描き直し**= 頭巾から**尖った耳2本(内側が赤)**/**白い髑髏の顔+牙2本**/
  段になった肩当て2枚/胸の赤い菱形と帯の赤い宝石/**大きな赤い前垂れ**/赤い刃の大鎌。
  ⚠直す前は「丸い髑髏に角2本」で耳の赤も牙も前垂れも無かった=安っぽさの正体。
- ✅**暁の王を描き直し(2回目)**= **黒地+金縁+白**の3段/**胸の金の四芒星**/金と白の翼/
  白と金のマント(内側は赤)/白い前垂れ+赤い縦帯/**黒い柄+金の星の錫杖**(軍旗は廃止)/
  **中を抜いた輪の光輪**。⚠塗り潰した楕円は金のバナナに見える(1回目に踏んだ)。
- ⚠**排出率の数値表は1行も触っていない**(1体あたり方式)。検査の想定人数だけ 19→21・★5を3→5 に。

### 2026-08-01(21) 狙撃王ジョルジの構え・必殺技・薬莢と煙
⭐ユーザー指示(絵の3列目の右のポーズ)。
- ✅**溜めの間はしゃがんでスコープを覗く**。腰の落とし `LH=1.2+6.4*rc` を**脚と上体の両方**に足し、
  前傾も `.10+.26*rc` へ。歩きの振れは溜め中だけ3割に落とす。
  ⚠**必殺技の間は自動でこの構えになる**=ult中は chg=1 が渡るため。
- ✅**必殺技を『撃墜』に変更**=構えて**10発を連続で撃ち込む**。⚠**撃つ→コッキング→撃つ**に見せるため、
  1発ごとに 反動(fireT)+薬莢+銃口の煙 を出し、**0.28秒の間隔**を空ける(詰めると機関銃に見える)。
  ⚠1発目はその場で撃つ(全部 later に回すと「不発」に見える)。
- ✅**通常攻撃も必殺技も1発ごとに薬莢が飛んで銃口から煙**(共通部品 `geoSpent`)。
  ⚠煙は `typ:'smoke'` で上へゆっくり=閃光と違って体を覆わない(短機関銃で踏んだ穴を避けた)。

### 2026-08-01(22) 得物を実際に振る仕組み(bSwing / U_SWG)
⭐ユーザー指示「終焉の騎士の攻撃でしっかり鎌を振ってほしい。ほかも武器があるのに振ってなかったり
飾りになってるやつはちゃんと機能させて」。45体の調査エージェントで棚卸ししてから着手した。
- ⚠**判明したこと**=武器を動かす道具は `bHold` だけで、これは**溜めている間しか動かない**。
  つまり①溜め0の兵科は一度も武器が動かない ②溜めがあるキャラも振り抜きが無い。
  **調査時点で攻撃中に武器が動いていたのは実質3種だけ**だった。
- ✅`uSwP`(硬直から振り抜きの進行度)+`bSwing`(振りかぶり→振り出し→振り抜き→戻りを1つの回転に)を追加。
  表 `U_SWG` に載せた id だけが対象。⚠**振っている間は焼いたコマが使えない**ので窓は硬直の前半45%だけ。
- ✅18種に適用: 終焉の騎士(大鎌)/傭兵団長/雷鳴の巫女/鋼鉄の巨人/整備士/爆破屋/配達人/
  斧使い/チェーンソー/鎖使い(鉤爪を撃ち出す)/長槍兵(突き)/影/重装盾/火炎瓶/爆弾兵/重爆弾兵/電磁兵/タイタン。
- ⚠終焉の騎士は型が `snipe`(近接の大鎌なのに照準線が伸びていた)→ `slash` に修正。秒数は据え置き。
- ⚠共通の反動(fireT の平行移動)と喧嘩するので、振りの間は逆に前へ踏み込ませた。

### 2026-08-01(23) DPSの総点検で見つけた不具合
- ⚠**回復役が溜めを払わないまま `uChgM` の倍率だけ受け取っていた**(医療兵+21%・工兵+22%)。
  回復の枝は `u.chg` を一度も見ないのに倍率だけ乗っていた。`test_balance.js` は atk/rate で測るので捕まえられない。
- ⚠ジョルジの必殺の締めの貫通弾が「盤面の敵ぜんぶ」に当たっていた→射程(520)+貫通6体に制限。
- ⚠範囲(aoe)を持つ英雄7人が共通の「投げて爆発」の枝に入っていて、`H_FX` の専用演出が
  **一度も使われていなかった**。H_FX へ回した(当たり判定と数値は一切触っていない)。
- 残りのずれ(フレーム量子化+2.8〜11.3% / 丸め±1.5% / 凍結1.7秒<周期1.92秒 ほか)は NOTES に記録のみ。

### 2026-08-01(24) 編成制・初期解放を2種へ・研究所と敵ウェーブの作り替え
⭐ユーザー指示5件。調査エージェント5体で全体を洗ってから着手。
- ✅**初期解放は2種ずつ**(兵科=猟犬・バット / タワー=ライフル台・ショットガン台)。以降は全部🔬研究所。
  ⚠対戦/協力は別枠 `PVP_T`/`PVP_U`=10。⚠チュートリアルは上限を外す。
  ⚠**廃品工房をネット砲より前へ**=研究所の1個目=120🧬=初回の出撃で届く(序盤の経済が死ぬのを防ぐ)。
- ✅**研究所の値段カーブを引き直し**(タワー9→17種・兵科22→28種に増えたため)。最終額は据え置きで立ち上がりだけ安く。
- ✅**編成制**=連れて行く兵科は10体まで。戦闘中は**選んだ中の元のコストが安い順**に解放。
  ⚠猟犬とバットは固定枠。⚠自動の穴埋めは「一度も編成を触っていない人」だけ。
  ⚠CPUの「盾役」の添字直書き(3=斥候)が**元から壊れていた**ので `SHD_I` に直した。
- ✅**敵の量**=序盤を強く(`earlyX` W1〜2=2.05→W8で1)・終盤は数を割って1体を厚く(`cut`。W20の体数45→23)。
  ⚠軽い新顔で総HPが逆転していたのでクローラー160→300・ストーカー190→560。
  ⚠砲撃は序盤だけ弱く(W1 −26% / W20 −0.5%)。
- ⚠`META_RESET` 2→3。**今回は「難易度と研究所だけ」戻す**(ユーザー選択)=💎英雄🔧図鑑⚔冒険は残す。

### 2026-08-01(25) 編成画面・検証場の使い勝手
- ✅編成のタブを3→2に(「基本の枠」と「上級」は同じことをカテゴリで割っていただけ)。
- ✅**上=いま連れて行く10体(5列×2段・押すと外す) / 下=入れる兵科**。
  ⚠外れない不具合の原因は①自動で埋めた状態では `META.team` が空で消すものが無かった
  ②自動の穴埋めが常に効いて次の兵科がすぐ入っていた、の2つ。
  ⚠絵の潰れは**canvasの属性とCSSの寸法が違った**のが原因(NOTESの掟)。
- ✅検証場の上の帯を**左端の🧟敵/🦸英雄で切り替え**に。英雄を敵46種の後ろに並べていたので届かなかった。
  ⚠`heroDeploy` は部隊上限(MAXU)で黙って false を返すので、検証場では枠を空けてから出す。
  ⭐検証場は**必殺チャージ満タン**=前置きのモーションを何度でも見比べられる。

### 2026-08-02(6) ⭐必殺技の詠唱モーション(案Aのガチ版)
ユーザー「のけぞるだけだと安っぽい」。それまでの前置きは光+「下がる」+通常の溜めの構えだけで、
しかも**押した瞬間に完成形へ飛ぶ**ので21人がほぼ同じ絵だった。
- ✅**型ごと(`U_MOT` の9種)に体の動きを1つずつ**=表 `U_ULM`。傾き/上下/踏み込み/潰し伸び。
  ⚠即時(now)は英雄7人と多いので、**振る得物持ち(`nowS`)と退がる人(`nowB`)に枝を分けた**。
- ✅**詠唱中も `drawUChg` を呼ぶ**(第5引数で溜まり具合を外から渡す)=それまで詠唱中は `u.chg` を
  0に伏せていたせいで**足元の魔法陣・照準線・雷雲・★4以上6人の専用の溜めが1つも出ていなかった**。
  ここが一番効いた。汎用の `drawUltWind` は逆に薄くした(主役を体へ)。
- ✅**溜め終わりの一拍(タメ)**=`ulKof` は .82 で完成し、残りは止まって震えるだけ。発射で反対側へ抜ける。
- ✅**得物は各英雄の `bHold`/`bSwing` に上乗せ**(`RC_UL` と箱 `UL_HA`/`UL_HX`/`UL_HY`)=
  21人ぶんポーズを書き直さずに「掲げる/振りかぶる/引き絞る」が出る。
  ⚠**縦の得物(杖・槍)は回すと先が後ろへ倒れる**ので、回さず持ち上げる形にした(撮って気づいた)。
- ✅撮る口を2つ追加=`pose=u:<id>+poseult`(5コマ)と `hero=<id>+ult=0.9`(盤面ごと)。検査 `checkUltMot()`。
- ⚠**狙撃だけは体を動かさない**(同日ユーザー指示「ジョルジは構えてるとき体は動かさないように」)=
  `still:1` で bUlt も決めポーズの膨らみも外し、`r0:1` で**構えを解かずに詠唱を始める**
  (0から立ち上げると押した瞬間に一度立ち上がって構え直していた)。
- ⚠**体を縦に伸ばさない**(同日ユーザー「詠唱中身体が縦に伸びるのはだいぶ微妙。巫女とか魔導士のやつ」)=
  術者と落雷の伸び上がり(潰し伸び+浮き)を廃止し、**掲げるのは腕と得物だけ・体は反らすだけ**にした。

### 2026-08-02(7) ⭐🚌ボーナス面「バスの日」(第1版)
ユーザー決定=**難易度の並びの真ん中(兵長と古参の間)に挟むボーナス面 / 遊べば次が開く /
ステージ1と2で別々の地形 / 報酬は初回クリアだけ 研究pt と 💎50**。
- ✅**土台はいつものソロPvEのまま**(敵・タワー・砲撃・研究所がそのまま効く)。差し替えたのは
  ①道(四方から4本=`LANES`) ②ルール(兵科・英雄・集結旗なし+バス) ③波の中身、の3つだけ。
- ✅難易度は `D5` の**末尾(6)に追加**し、並びと解放の鎖は `D_ORD` が持つ
  (真ん中に差し込むとセーブの添字がずれて既存プレイヤーの記録が別の難易度を指す)。
- ✅**バスをタップした所へ走らせて轢く**(操作は🚩集結旗と同じ)。⚙️1500と🔩60を最初に渡し、
  建設マスは全部開いた状態。作戦タイムは挟まず、設置猶予(🔨5秒)だけで始まる。
- ✅遊べば次が開く(`bnsEnter`)/報酬は初回だけ(`META.bcl`)/**中断できない**(周回封じ)。
- ⚠**バス自体の武装と強化は未着手**(いまは轢くだけ)=[[TODO]]。
- 検査 `checkBonus` / 撮る `bns+bnsn=9`(+dbg で中の状態をトーストに出す)。
- ⚠**バスの操作を作り直した**(同日ユーザー「バスの操作とかは全然違う」)=
  「タップした所へ走る」(集結旗と同じ)を廃止し、**画面左下のバーチャルスティック**に。
  手触りも**重い車**へ=倒した向きへ加速し、離すと惰性で流れる(瞬間移動しない)。

### 2026-08-02(8) ⭐物語と目標を決めた(文書だけ・コードは触っていない)
ユーザー決定。**ゾンビアポカリプスで、人類が住める拠点を次々に解放していく**。移動はバス。
主人公は**英雄を呼べる能力者**(騎士や魔法使いが廃墟に居る説明が付かなかったため。
これで💎召集も🦸出撃も「呼んでいる」で筋が通る)。⚠死潮は遊びとしては残すが**文章では使わない**。
- 🚌**開拓便**=次の拠点へバスで向かう道中。**探索→設営(作業台で製作)→ゾンビラッシュ(バスで轢く)**。
  カメラは主人公に追従・マップは広め・敵は小粒を大量。材料は**その面の中だけで完結**(通貨は増やさない)。
  レシピは研究所で恒久解放、**作った武器防具は次回以降も持ち越す**。1面は角材と石材だけ(バット止まり)。
- 🔬**研究所のUIを4枚+2段目へ**(解放/強化/拠点/記録)。⚠新種タブだけは解放の順番なので割らない。
- ⚠**全部 [[DESIGN]] の先頭に書いた。実装は未着手**(いまの「バスの日」は別物の第1版=作り直す)。
- ⭐主人公のHPと力尽きた時も決めた(同日)=HPは低め・拠点に戻ると回復・**力尽きたら材料をその場に落として
  拠点から再スタート**(罰は時間。材料は拾いに戻れる/作った武器防具は落とさない)。
  ⚠これが成立するには**探索フェーズに制限時間**が要る。

### 2026-08-02(9) 🎥カメラ追従(土台)と 🔬研究所のタブを2段に
ユーザー指示「①と②いこう」。⚠開拓便の中身(見た目・敵・マップ)は**ユーザーが貼る画像を見てから**作る。
- ✅**🎥カメラ追従**=`CAM`(見ている点/倍率/追う先)。⚠**`CAM` が null の間は SC/OX/OY に触らない**ので、
  本編の見え方は1pxも変わらない。⭐**画面と世界の変換は `fitCanvas` の SC/OX/OY 1か所しか無い**ため、
  そこを上書きするだけで**建設マスのタップ判定・🎯砲撃の照準・描画が全部そのまま追従**する。
  ⚠マップの外は映さない(端で寄せを止める/収まっている向きは中央)。いまは開拓便でバスを追う。
  検査 `checkCam`。
- ✅**🔬研究所のタブを4枚+2段目へ**=🔓解放[新種/兵科/タレット] / 💪強化[タワー/部隊] / 🏕拠点[開始/経済] / 📖記録。
  ⚠**派生タブに兵科とタレットが同居していた**のを分けた(ユーザー指摘)。
  ⚠**新種だけは対象で割らない**(解放の順番そのもの)。⚠`LABTAB` は下段の名前のままなので `lab=twup` 等が生きる。

### 2026-08-02(10) 🚌開拓便=広いマップ・廃墟の散らばり・小粒の群れ・ミニマップ
ユーザーの参考画像(視点は真上のまま/群れはもっと密度)を見てから着手。
- ✅**マップを2倍角に**(MAPW/MAPH。⚠MW/MHは触らない)。倍率の基準はMWのままなので寄り具合は変わらない。
- ✅**廃墟を種から散らす**(廃車/タイヤ/ドラム缶/木箱/瓦礫/バリア/枯れ木・520個から道と拠点を除いた数)。
  ⚠画面に入っている物だけ描く。⚠工房と支援枠は使わないので画面外へ逃がし、看板も出さない。
- ✅**小粒の群れ**=1体を薄く(hp .30)して数を大幅に増やし(cnt 15・盤面上限260)、
  ⭐**横のずれ(z.off)**で塊にした。⚠これが無いと何百体出しても**一列の行列**にしかならない。
- ✅**🗺ミニマップ**(右上)=道・廃墟・敵・拠点・バス・いま見ている範囲。カメラが動く面だけ出す。
- ⚠**BPROP の宣言をファイル下部に置いて初期化前アクセスで全体が止まった**(TDZの穴・2度目)。上へ移した。

### 2026-08-02(11) 🧍主人公の絵 / 拠点を建物に / もっと引いた画
ユーザー指示「専用に1体描いて(参考画像)」「同じスティックでOK」「画面近すぎ・もっと小粒に」
「タレット置き場は拠点がもう建物としてあって、そこの各場所に設置する感じ」。
- ✅**🧍主人公を描いた**(`drawHero0`)=髭・キャップ・オリーブドラブの戦闘服・プレートキャリア・
  無線機のアンテナ・アサルトライフル・編上靴。⚠**骨格は兵科と同じ割り付け**にして背丈を揃えた。
  ⚠武器は段で差し替わる前提なので**体と装備だけ**をここで描いている。撮る=`h0`。
  ⚠いまは拠点の前に立っているだけ(操作の主役になるのは探索パートを作ってから)。
- ✅**拠点を建物にした**(`drawOutpost`)=八角の外壁・門・櫓台・中庭・司令部。
  ⭐**建設マスはその上に置く**(壁の角/屋上/中庭/前庭)。⚠**門と道の位置は道の実際の角度から出す**
  =面ごとに道の向きが違うので決め打ちにしない。
- ✅**もっと引いた画に**(カメラの倍率 1.15→0.78)=1を切ると全景より引いた絵になる。小粒感が出た。
- ⚠**もっと引いた画へ**(同日ユーザー「今の3倍ぐらい引いて良い」)=倍率 .78→.26。
  ⭐**マップも 9600x4560 へ広げた**=引くとマップが画面に収まってしまい、カメラが動かなくなるため。
  ⭐⭐**道は「拠点からの相対」で置くようにした**=マップの広さで引き伸ばすと、広げた時に道が長くなりすぎて
  敵が何十秒も歩いてこない。**道の長さ=拠点まわりの戦いの尺 / マップの広さ=探索の広さ**として別に持つ。
- ⚠**廃墟をさっぱりさせた**(同日ユーザー「車の残骸とか多すぎる」)=密度を約1/5に、車の割合も下げた。

### 2026-08-02(12) 🧟米粒ゾンビを専用の描き方にして超大量に + 🚌轢き応え
ユーザー①「米粒ゾンビの描き方を差し替えて数を大幅に増やす+轢き応え(弾け飛ぶ/跡が残る/連続で気持ちいい)」。
- ✅**米粒専用の描き方 `drawZRice`**=焼いた絵を1体ずつ貼るのをやめ、**数色の四角を1本のパスに積んで
  色ごとに1回だけ塗る**。500体でも塗りは6回前後。⚠並べ替え(py順)とHPバーは捨てた(米粒では読めない)。
  ⚠ボスとエリートだけは今までどおり焼いた絵で描く。
- ✅**数を大幅に増やした**=`cnt` 15→230(総数 約1900体)/盤面上限 260→`BNS_CAP`(560)/
  湧く間隔 `BNS_GAP`(.0175)/横のばらけ `BNS_OFF`(230)。⚠pay 1.15→.45(体数15倍ぶんの⚙️を戻した)。
- ✅**1体ぶんの演出を全部止めた**(これが「増やせない」正体だった)=米粒は
  死体を残さない/ダメージ数字と血しぶきを出さない/⚙️の文字は**0.7秒に1回まとめて**/
  撃破音は**0.075秒に1回**/**漏れの知らせ(音・赤い明滅・揺れ・粒)も0.4秒に1回**。
  ⚠**コアの減りと⚙️の額は1体ずつそのまま**(見せ方を間引いただけで手加減はしていない)。
- ✅**轢き応え3つ**=①**弾け飛ぶ**(`bnsSplat`。バスの進む向き寄りに肉片が飛ぶ)
  ②**跡が残る**(地面の血だまり+🛞轍。⭐**轢いた直後の轍は血の轍になる**)
  ③**連続轢殺**(バスの上に ×N と段の名前。轢殺→大轢殺→蹂躙→地獄のバス。揺れも段で強くなる)。
  ⚠3つとも**使い回しの輪**で持つので何百体轢いても配列は伸びない。リザルトに轢殺数と最高連続を出す。
- ✅**横へも押しのける**(`BUS_SIDE`)=前へ押し込むだけだと「道の上で潰した」に見えない。
  ⚠押すのは世界座標ではなく `z.off`(法線は位置を出す時に実体へ書いてある)。
- ✅**コアを 50→400 に厚くした**=敵が15倍なのでコア50だと**3%漏らして陥落**する面になっていた。
- 🐞**🚌開拓便に「主」が湧いていた**(検査で判明)=1波しか無いので「最終ウェーブ」の条件に必ず当たり、
  **米粒と見分けの付かないウォーカーが1体、拠点の手前に居座って延々とコアを削り**、しかも漏れず死なない
  ので**試合が終わらなかった**。開拓便では主を置かないようにした。
- 🐞**ミニマップの「いま見ている範囲」の枠がはみ出していた**=カメラは端で止まるのに枠だけ中心を追っていた。
  いま本当に描いている OX/OY から出すように直した。
- 🐞**浮かぶ文字(+⚙️など)が開拓便では読めていなかった**=寄り具合 .26 のまま26pxで描いていたので
  画面では3〜4画素。**CAM.zm で割る**ようにした(本編は CAM が無いので1pxも変わらない)。
- 検査 `checkRice`(盤面415体/跡は上限どまり/死体と文字と漏れの音を出さない/連なりが途切れる/
  **波が必ず終わる**=放っておいて85秒/本編は今までどおり)。撮る=`bns+bnsdrv+bnsn=24`。

### 2026-08-02(13) 🚌バスを大きく / 導線(障害物) / 一度に来るのは1方向 / 音と演出の作り直し
ユーザー実機判断「なんか違う」→「**四方八方すぎて対応できない / バスがちっちゃい /
音と演出(エフェクト)がちゃっちい**」+「**障害物というかゾンビの導線が欲しい**」。
- ✅**バスを大きくした**=当たり 62→**140**・絵 .62→**1.9倍**。⚠**絵と当たりの大きさを揃えた**
  (前は当たりの方が広く「当たっていないのに死ぬ」状態)。⚠絵は当たりの丸の真ん中に置いた。
  ⭐ついでに **通路の幅(300)にバスの幅(280)がほぼ収まる**ので、**関所に停めると完全に塞げる**。
- ✅⭐⭐**導線(`bnsCorrW` / `BWALL`)**=道の両脇に廃車とコンクリのバリアを列で置き、
  **3か所(28%/55%/80%)で通路が150→62に絞れる**。群れが勝手に密集するので、そこが狩り場になる。
  ⚠⚠**壁の絵と敵が歩ける幅を同じ式から生やしている**(別々に持つと必ず「壁の中を歩く」が出る)。
  ⚠バスは壁で止まらない(止めると運転が窮屈になる)。⚠廃墟は通路の外にだけ置くようにした。
- ✅⭐⭐**一度に濃く来るのは1方向だけ**=波を7つの区に割り、濃い道を順に回す(5体に1体だけ他の道へ)。
  ⚠それまでは1体ずつ4本へ均等に配っていたので、**バス1台では原理的に間に合わない**形だった。
  ⭐予告=**その道が赤く脈打つ**(端まで伸びているのでどこに居ても見える)+ミニマップも赤+
  方角のトースト(「⚠ 北西から大群が来る!」)+警笛。
- ✅**横のばらけを 230→150 に絞った**=広げるほど塊にはなるが、道から溢れて迎え撃つ場所が消える。
- ✅**音を作り直した**=轢いた音を `splat`(オシレータ1本の「ピッ」)から **`crush`**
  (ノイズを低く閉じた潰れ+食い込む芯)へ。⚠短く薄く(毎秒10回鳴るので派手にすると雑音になる)。
  ⭐派手にしたのは**段が上がる瞬間 `cmbUp`** の方=低い和音が段ごとに厚くなる(⚠高い音は使わない)。
- ✅**演出を厚くした**=肉片を2〜4個→3〜7個で初速も上げ、**飛び始めは鮮血・終わりは暗い肉片**の2色に。
  血だまりは大小2枚。**轢いている間ずっとバスの鼻先から飛沫**が出る。当たった瞬間は鼻先に血の楔と光。
  連なりの表示は**段の札**+段が上がった瞬間に**光の輪**。
- 検査に⑨導線(関所が絞れる/敵がはみ出さない/壁が通路の中に立っていない)と
  ⑩濃い道が1本に寄る+予告が立つ、を足した。撮る=`bns+bnsdrv+bnsn=24`。

### 2026-08-02(14) 🏚🌲開拓便のマップを「建物と森」で作り直し / タレットは拠点が自動で据える
ユーザー「**TDの時の道路みたいなのとタレット置き場はいらない。しっかり建物とか森でマップを作成して**」
「**タレットは置き場を選択する方式じゃなくて、拠点に自動的に置かれる形**」。
- ✅**TDの舗装路をやめた**=縁石・白線・ひび・タイヤ痕は開拓便では引かない。
  代わりに**踏み固められた土**を2枚薄く敷くだけ。道の形は両脇の建物と森が語る。⚠本編の道は1pxも変えていない。
- ✅**地形を `bnsMap` で作り直した**=格子で区画を切り、区画ごとに **森 / 街区 / 空き地** を決める。
  建物117棟・木958本。壁の色は煉瓦・土壁・コンクリ・板張りを混ぜてある(灰色だけだと石を並べただけに見える)。
  ⚠**導線(通路の縁)は建物の壁面と並木**になった(バリアの列は廃止)。関所は建物が両側からせり出す形。
  ⚠⚠**建物も木も `bnsFreeAt` で通路の外にしか置かない**=道ごとに「その位置での通路の幅」で見る
    (一番近い道までの距離だけでは、関所の細い所と広い所を区別できない)。
- ✅**建設マスの四角を1つも描かない**=灰色の四角が拠点の周りに並んでいるのが「TDの盤面」に見える正体だった。
  盤面をタップしても建設メニューを開かない(見えない物にメニューが出るのを防ぐ)。
- ✅⭐⭐**タレットは拠点が自分で据える**(`bnsPlaceTower`/`bnsAutoTower`)=
  開始時に5基/以後2秒ごとに⚙️が届いていれば1基ずつ増設 → 枠が埋まったら**強化と進化**へ回す。
  ⚠**いつもと同じ道具(`upTower`/`gradeTower`)を通す**=値段も上限も進化の条件も1か所のまま。
  ⚠⚙️は所持の55%までで選ぶ(1基の高い砲で使い切ると枠が埋まらない)。
  ⭐**この面でプレイヤーがやることは運転だけ**になった。轢いて稼ぐほど拠点が育つ。
- 検査に「建物と木が通路の中に立っていない」「拠点が自分でタレットを据える・増やす」を足した。

### 2026-08-02(15) 🏢タレット置き場=建物の屋上 / 街路を曲げて森と建物で埋め尽くす
ユーザー訂正「**自動で建つっていうのはそういうことじゃなくて、配置する位置っていう概念がなくて、
拠点の上にタレットが乗る感じで、設置だけ自動ってこと。何を設置するとかは自分で選ぶ**
と思ってたけど**配置もできるようにしよう。建物の屋上が平面であってそこにタレット置き場を作ろう**」
「今みたいにクロスになってる端から出るんじゃなくて、**この画像みたいに森と建物でマップをかなり埋めて、
明らかな導線の先からどんどんくる感じ**」(参考画像あり)。
- ↩**全自動の設置を取り消した**=何を建てるかまで選べなくなっていた。**何を・どこに、は今までどおり選ぶ**。
- ✅⭐⭐**置き場は「屋根の残った建物の屋上」**=建物に **廃墟(壁だけ・中は瓦礫)** と
  **屋根つき(平らな明るい屋上)** の2種を作り、**屋上が枠**になる。拠点(司令部)の屋上4枠+街路沿いの屋上18枠。
  ⚠**地形を先に作ってから枠を決める**(`bnsMap`→`bnsSlots`)=建物が無いと枠の座標が決まらない。
  ⚠空いている屋上には**細い破線と+**だけ(灰色の四角は描かない)。選ぶと琥珀色で光る。
- ✅⭐⭐**街路を曲げた**=四隅から拠点への直線X字をやめ、**同じ半径・同じ折れ角**で作る曲がった街路4本へ
  (⚠長さが厳密に揃うので `z.d>=PLEN` の判定はそのまま)。向きは**90度刻みにしない**。
- ✅⭐⭐**森と建物で埋め尽くした**=格子を620→430に細かくし、空き地を1割だけに。
  **建物214棟・木3116本**(前は117棟・958本)。木は濃い緑へ(明るい緑は「低木の茂み」に見える)。
  ⚠⚠**密度が足りないと、街路を曲げても「空き地に引いた線」にしか見えない**=この2つはセット。
- ✅通路を 150→**200**(関所 62→**80**)に広げ、設置の猶予を 5→**30秒**に(自分で屋上に建てるため。
  ⚠**探索フェーズが入ったらこの猶予は要らなくなる**)。
- ⚠**探索(①)と設営(②)はまだ未実装**=いま入っているのは③ゾンビラッシュだけ(ユーザー指摘で確認済み)。

### 2026-08-02(16) 🚫道を全部消した / 画面外から歩いてくる
ユーザー「**まず今Xになってる道みたいなの消して。画面外からゾンビが来る感じにして**」。
- ✅**道を1本も描かない**=舗装路→踏み固めた土、と弱めても**拠点から4方向へ伸びる帯**である限り
  何色で塗ってもX字の記号に見えた。通れる所は**建物と森の隙間**だけで語らせる。
  ⚠**赤く光る道の帯(予告)も廃止**=あれも同じ「伸びる帯」だった。
- ✅**画面の外から歩いてくる**=街路の長さ 1700→**3600**(前は画面に映る範囲の内側で湧いていたので、
  何もない所にゾンビが**ぽんと現れていた**)。⚠縦にはみ出すのでマップも 9600x4560→**11000x8000**。
  ⚠歩く時間が延びるので `sp` 1.12→1.75。建物496棟・木6209本。
- ✅**予告は画面の縁の矢印へ**(`drawBnsWarn`)=道で語れなくなったので、
  画面の縁に「⬅ 西から」を出す。⚠画面の座標で描く/ミニマップより先に描く。
- ⚠**ユーザーはまだ「思ってるのと違う」**=ここで細部を積むのをやめて、土台を確かめる段(→[[TODO]])。

### 2026-08-02(17) 🌲森で塗り潰す / 廃墟は小さくまばらに / 🧱地形を焼いて貼る
ユーザー「**まず森が点々としてるからもっと完全に密度を濃くしてほしい。あと建物はでっかい感じで
たくさん立てるのではない**」。
- ✅⭐⭐**森で塗り潰した**=区画ごとに何本か撒くやり方をやめ、**マップ全体を細かい格子(62)で舐めて、
  木の半径(32〜58)が間隔より大きくなるように置く**=枝が重なって**面**になる。木 3116→**19880本**。
  ⚠**撒く方式ではどれだけ数を増やしても点々にしかならない**(塊と塊の間に必ず地面が残る)。
  ⚠空き地は**位置から決まるノイズ**(`bnsNz`)で2割だけ(乱数を順に引くと帯状のムラになる)。
- ✅**廃墟は小さくまばらに**=建物 214→**73棟**、大きさも 58〜140 → **38〜72**へ。
  街路沿いも「基本は森・たまに廃墟」に。⚠屋根つき(=タレット置き場)だけは今までの大きさのまま。
- ✅⭐⭐⭐**地形を焼いて貼るようにした**(`terrTile`/`drawTerrIn`)=木が2万本になった時点で
  1画面に約1万回の弧を打つことになり、**撮影1枚に5分**かかった(実機なら数fps)。
  **地形は動かない**ので升目(1500)ごとに一度だけ焼き、以後は `drawImage` 1回で貼る。
  ⭐これで**森の濃さがいくら上がっても重くならない**(`BNS_TSP` を自由に回せる)。
  ⚠焼く倍率はいまの SC そのもの(貼る時に伸ばさないのでボケない)。
  ⚠⚠**「倍率がぴったり同じでなければ捨てる」にすると地形が虫食いになる**(SCは毎フレーム測り直していて
    わずかに揺れる)=0.4%の遊びを持たせる。⚠**1枚目は全部焼く**+**焼けなかった升はその場で直に描く**
    (描き直しの回数が少ない環境で穴が開くのを防ぐ)。
- 検査に「無防備なら何秒でコアが尽きるか」を出すようにした(いま81秒)。
- 撮影は `bns` の間コアを落とさないようにした(撮影は実時間で1分以上かかるので、
  そのままだとリザルト画面になって盤面が1枚も撮れない)。

### 2026-08-02(18) 🌲森がバスも止める / 街路を広げた
ユーザー「**もうちょい道を広くして、森はバスも通れなくするから**」。
- ✅**森と建物の中へバスが入れない**(`bnsRoadFit`)=走れるのは**街路と拠点の中だけ**。
  ⚠⚠**木や建物1つ1つと当たりを取らない**(木は2万本)=**通路(`bnsCorrW`)の縁で止める**。
  地形はその式から生やしてあるので、絵と当たりが必ず一致する(直す場所も1か所)。
  ⚠**止めるのではなく滑らせる**=壁へ向かう速さだけ殺す。正面から突っ込んでも張り付かない。
  ⚠**拠点の周りの円も走れる**ようにする(でないと拠点で身動きが取れない)。
  ⚠**押し戻す先は縁のぴったり上ではなく0.5内側**=ぴったりだと丸め誤差で毎フレーム判定が揺れる。
- ✅**街路を広げた**=通路 200→**340**(幅680)/関所 80→**200**(幅400)。
  ⭐バスの幅は約280なので、通路では左右に200ずつ余裕があり、**関所ではバスが7割を塞ぐ**
  (「停めて蓋をする」がちょうど成立する)。⚠**バスが通れない関所を作らないこと**。
- 検査に「森の中に置いたバスが街路へ押し戻される/街路と拠点の中では押し戻されない/
  関所がバスの通れる幅である」を足した。

### 2026-08-02(19) 🧟足の速さを戻した
ユーザー「**あとゾンビの足が速い**」。
- ✅`D5` の🚌行 `sp` を **1.75→1.12**(2026-08-02(6)に上げたぶんを戻した)。
  ⚠1.75 は本編のどの難易度(0.95〜1.18)より速く、**ゾンビが走って見えていた**。
- ⭐**学び=街路の長さと足の速さは別の話**。街路を伸ばした時に「歩く時間が延びるから」と速さを上げたが、
  尺を決めているのは**湧く速さ(`BNS_GAP`)と総数**の方で、歩く時間は**最初の1体が届くまで**にしか効かない。
  ⚠速さで尺を調整しようとしないこと。
- 尺は 130→**178秒**(無防備でコアが尽きるまで 82→**103秒**)。⚠検査の番人は 200→260秒へ
  (あれは「決着しない」を捕まえる線であって、尺の good/bad を決める線ではない)。

### 2026-08-02(20) 🚌操作性と速さ / 「変な判定」の元を1本化
ユーザー「**→の道と左下の道に変な判定ある。あとバスの操作性が悪すぎる。そして遅すぎる**」。
- ✅⚠**「変な判定」の温床を潰した**=**地形を置かない範囲**と**バスが走れる範囲**を
  **別々の式で書いていた**(通路は corrW+16 / 拠点まわりは OUT_R+150 / バスは corrW-95 と
  OUT_R+120-95 …と食い違っていた)。⭐**`bnsOpenW` と `bnsClr` の2本だけ**が空き地の形を決め、
  バスはそこから `BUS_CL` だけ内側まで、に統一。**片方だけ直すと必ずズレる**構造をやめた。
- ✅**`BUS_CL` 95→55**=バスの端が木を少し食うくらいまで緩めた(木の手前で見えない壁に当たらない)。
  検査に「バスの端が木の生え際を越えていること(`BUS_R-BUS_CL>=26`)」を足した。
- ✅⭐**当たった所に木の葉を散らす**=「変な判定」と感じる一番の原因は**何に止められたか見えないこと**。
- ✅⭐⭐**操作性の正体は最高速ではなく「曲がらないこと」だった**=倒した向きへ力を足すだけだったので、
  曲がろうとしても前の勢いで真っ直ぐ滑り続けていた。**スティックと直角の速さを落とす(`BUS_GRIP`)**
  を足したら、重さを残したまま素直に曲がるようになった。⚠前後の成分は触らない(触ると重さが消える)。
- ✅**速さ**=最高速 310→**560** / 加速 760→**1400** / 離した時の減速 2.4→3.0。
- 🐞**街路沿いの建物がほとんど建っていなかった**(検査で発覚)=置く位置を `bnsCorrW` から出しているのに、
  弾く側は `bnsOpenW` で見ていたので**自分で自分を弾いていた**。建物 42→**143棟**(屋上の枠も足りていなかった)。

### 2026-08-02(21) 🚧「突っかかる2箇所」の原因=木の無い空き地
ユーザーが実機の写真を2枚。**どちらも木も建物も無い開けた所でバスが止まっていた**。
- 🐞原因は2つとも**自分で空き地を作っていたこと**:
  ①**位置ノイズで2割を空き地にしていた**(森を自然に見せるつもりだった)
  ②**建物の周りを半径141も空けていた**(木が建物に刺さらないようにするつもりだった。
    建物の大きさは38〜72なので、**建物より広い空き地**ができていた)。
  どちらも「**木が1本も無いのに入れない場所**」を作る=これが「変な判定」の正体。
- ✅**①を廃止**(`bnsNz` ごと削除)、**②を建物の大きさ+16に絞った**。
  ⭐掟=**「地面が見えている所は必ず走れる」**。空き地を作りたくなったら、
    その空き地も**走れる範囲(`bnsOpenW`/`bnsClr`)の側に足す**こと。片方だけ空けない。
- ✅⭐**検査で機械的に捕まえるようにした**(`checkRice`⑨)=マップを140刻みで舐めて、
  **「木も建物も無いのに、バスの体(半径140)が届かない点」が1つでもあれば FAIL**。
  ⚠**「その1点が緑か」ではなく「その辺りが森か」で見る**=木と木の隙間まで拾うと本物の空き地と
    区別が付かない(マップの隅の隙間で1回落ちた)。

### 2026-08-02(22) 🏚**入れる廃墟を置いた**(開拓便②の入口)
🚌開拓便は**③ゾンビラッシュだけ**が入っている状態だった。[[TODO]] の順番どおり
**②「入れる廃墟を置く」**に着手。⚠**この回で入れたのは"外側"だけ**=中へ入る仕組み(③)はまだ無い。
- ✅**3種類**を街路沿いに置いた(`ENT_K`/`entPush`/`BENT`)=**⛽ガソリンスタンド / 🏭倉庫 / 🏠民家**。
  各レーンに3棟ずつ狙い、**12棟**(4種ずつ)。⚠**次の段の受け口**として
  `dx,dy`(入口の前に立つ点)・`da`(向き)・`ek`(種類)を1棟ずつ持たせてある。
- ✅**タレット置き場との見分けは3つ**=①大きさ(街路沿いの小屋の2〜3倍)
  ②屋根が金属色(白いキャノピー+赤帯 / 青灰ののこぎり屋根 / 赤錆の切妻)
  ③街路を向いた黒い入口+明るい敷居+踏み固めた土。⚠置き場(`rf`)には**しない**。
- ✅🗺**ミニマップにも出す**(明るい琥珀の四角)=探索の行き先なので地面の廃墟と同じ色にしない。
- ✅撮影オプション `bnsent[=番号]`=入れる廃墟の前へバスごと移して撮る(カメラはバスを追うため)。
- ✅検査 `checkRice`⑪=数/種類の偏り/通路の中に立っていない/建設マスに化けていない/
  入口の点が建物の外で街路側/棟どうしが重ならない/描画で例外が出ない。
- 🐞踏んだ穴は4つ(全部 [[NOTES]] に足した)=①**置く順番**(②の後に置いたら大きすぎて**0棟**になった)
  ②**種類の番号に `li*3`** を使って3で割り切れ、全レーン同じ種類になった
  ③**キャノピーの下に入口を置いて屋根に隠れた**(真上から見た絵) ④**ガススタの屋根を青灰にしたら倉庫と
  見分けが付かなかった**(白+赤帯に変えた)。

### 2026-08-02(23) 🔥**バスの時間を「プロ仕様」に**(楽しさと圧倒的爽快感)
ユーザー「というわけで初期段階のある程度の作りはできたから **バスの時間をプロ仕様にしてほしい。
楽しさと圧倒的爽快感重視**」。
- ⭐⭐**足りなかったのは「いま押す」という山場**だった=走って轢くだけだと、どれだけ数を増やしても
  手応えは平らなまま伸びない。⭐**🔥ニトロ**を入れた=**轢いた数で溜まり、押すと3秒だけ化ける**
  (最高速×1.8 / 加速×1.7 / 轢く力×2.4 / 範囲×1.14 / 跳ね飛ばし×1.8)。
  ⚠**溜まるのは轢いた時だけ・使用中は溜まらない**=撃ちっぱなしにできない。110体で1回(1試合10回前後)。
  ボタンは**スティックの反対側(右下)**・溜まりは下から満ちる帯・満タンで縁が脈打つ。
- ⭐⭐**体そのものが吹き飛ぶ**ようにした=それまでは肉片だけで「何かが散った」止まりだった。
  **黒フチの付いた大きめの塊を1体につき1つ、高く飛ばす**だけで一気に轢殺に見える。
- ⭐**走っている感じ**を3つ=①速さに応じた**流れる線** ②ニトロ中の**縁の橙** ③**まとめて轢いた瞬間の白抜き**
  (⚠1体ごとには出さない)。さらに**土埃**と、**速いほど少し先を見るカメラ**。
- ⭐**音**=`nitro`(噴き出し+110→190Hzの唸りが3秒)。⚠高い音は使っていない(NOTESの禁止事項)。
- ✅リザルトに「🔥 ニトロ n回」。検査 `checkRice`⑫ / 撮影 `bnsnit`。
- ⚠⚠**寄り(`CAM.zm`)は動かさなかった**=SCが変わると焼いた地形を全部捨てて焼き直すので、
  カメラを引くと毎フレーム地形が作り直しになる。**速さは画面の効果だけで出している**。
- ⚠**この面は明確に楽になっている**(ニトロぶん)。数字の詰めは実機のバランス確認とセット。

### 2026-08-02(24) 🛣**バスの面を「次の拠点まで走り抜ける道中」に作り替えた**
ユーザー「やっぱりこの方式やめよう。**あくまで次の場所への移動**ってことでバスで次拠点に移動する感じでいこう。
**道路はほぼ縦の一直線でゾンビががっつりいる感じでゾンビをひき殺す**」
+ 「まず廃墟を探索して物資を取得してバスを強化(耐久力とかスピードとか)して道路を突き進む。
  ゾンビひき殺し&ボスだけインベーダーっぽい感じ」。
- ✅⚠**拠点防衛を丸ごと外した**=中央の拠点・屋上のタレット置き場・4本の道・「一方向ずつ濃く来る」予告
  ・ミニマップを全部やめた。**守る物はバスだけ**。⚠書き戻さないこと。
- ✅**マップ 4400x26000 の縦長**を**下から上へ**走る。道は**ほぼ縦の一直線1本**(少し蛇行)。
  道幅は 1050(関所6か所で520に絞れる)=**画面の半分以上を道が占める**。寄りは .26→**.42**。
- ✅**ゾンビは「バスの1100〜2900px先」に湧く**=走っても密度が落ちない。
  **通り過ぎた敵(1600px後ろ)は消す**=これが無いと盤面の上限が後ろの敵で埋まり、進む先がすかすかになる
  (実走で42秒走って81体しか轢けなかった原因)。上限 560→**900**・湧く間隔 .0175→**.005**。
- ✅**負けるのはバスが潰される時だけ**=コアHPをそのまま**バスの耐久**に流用(HUDの印だけ🚌に差し替え)。
  取り付かれている間だけ削れる(`BUS_ZDPS`)=**走っていればほぼ無傷・止まると死ぬ**。
  ⚠**すれ違って後ろへ抜けた敵は何もしない**(漏れという概念が無い面)。
- ✅**到着=クリア**。HUDは「WAVE」ではなく**あと○m**。⚠**着くまで制圧にしない**
  (盤面が空になっただけで終わると道の途中でクリアになる)。⚠**着くまで湧きを絶やさない**(`bnsTpl`)。
- ✅**道路を描くようにした**=幅は `bnsCorrW` から出すので関所で細くなるのが絵で読める。
  ⚠2026-08-02(6)に道を消したのは「拠点から四方へ伸びる帯」がX字の記号に見えたからで、
  **走る道が1本だけ**ならそれは記号ではなく舞台になる。
- ✅道の両端に**出発した拠点**と**次の拠点(門・旗・到着線)**を建てた。
- ⚠実走の目安=**上へ倒しっぱなしで42秒・455体轢く・受けた傷88(耐久400)**。
- 🐞撮影の罠=**camStep は rAF の中にしか無い**ので、自分で時間を進める撮影では
  カメラが置いていかれて**バスが画面の外**になる(`test_shot` の中で camStep も回すようにした)。

### 2026-08-02(25) 🚌**前を向いたバス / 砲撃なし / 群れをかたまりに / 轢き感**
ユーザー「まずこのゾンビバスはここでは使わないようにしよう。あと**ちゃんと前を向いてる状態のバス**を書いて。
んで**砲撃は無し**。**ゾンビの密度がまばら**になってるから ある程度まとまった個所で今よりもっと密度を増して。
それと**もっと轢いてる感が欲しい**んだけど、SEとかVFXってもういい感じのないかな?」
+「**バスは画面↓に張り付き**にすることによって道路の可視範囲広く使用」。
- ✅**真上から見た、進む向きを向いたバス**(`drawBusTop`)=タイトルのゆるキャラ(`drawBusKun`)は**使わない**
  (横から見た顔つきの絵なので、見下ろす道路の上では寝ている絵にしかならない)。
  ⭐**前に排障器(ラム)**を付けた=「これで轢く道具だ」が一目で分かる。⭐**轢くほど前が血で汚れる**。
- ✅🎯**砲撃を外した**(ボタンも照準の経路も)。
- ✅🎥**バスを画面の下(約8割の位置)に張り付けた**=進む先が広く見える(`BUS_CAM_Y`)。
- ✅**群れをかたまりで湧かせる**=1体ずつ道いっぱいに散らすと、数を増やしても**均一に薄い**ままだった
  (これが「まばら」の正体)。**塊の中心を決めて34〜80体をまとめて置く**(`BNS_CL_*`)。
  実走の轢いた数 455→**634体**。
- ✅**関所の絞りを緩やかにした**(.05→.09/520→620)=急に絞ると画面に絞りの入口と出口が同時に映って
  **八角形の闘技場**に見えていた(撮って気づいた)。
- ✅**轢き感**: ⭐**画面に血が貼り付く**(`bscr`。世界ではなく画面の割合で持つので寄りに左右されない)
  + **まとめ轢きの音 `smash`**(素材から新規に切り出し=鉄の鈍い衝撃+潰れ+破片)。
  ⚠**候補を2つ(`smash`/`smash2`)作って `?sfx=1` に並べた**=聴き比べて選んでもらう。
- 📌**素材の在庫**=🔊効果音は Sonniss のバンドル(商用可)の生WAVが `sfx_src` に18本残っていて、
  そこから切り出せる(今回もそこから作った)。⚠**絵(VFX)は「全部プロシージャル」の約束**なので
  外部素材は使えない=コードで描く。

### 2026-08-02(26) 🔊**轢いた音を作り直した**(「どっと音にしか聞こえない」)
ユーザー「轢いた音というかどっと音にしか聞こえない」。
- ⚠**原因は鉄の衝撃(Metal Thud)を主役にしていたこと**=鉄が前に出ていると必ず「ドッ」になる。
  それは**ぶつかった音**であって**轢いた音**ではなかった。
- ⭐**骨が砕ける(=氷を割る音)+肉が潰れる(=クランチを再生速度0.7〜0.8で濁らせる)**を主役にし、
  **鉄は-13dBの隠し味**まで下げた。1体ずつの `crush` も合成音から素材へ差し替え(0.13秒)。
- ⚠**候補を3案ずつ用意して `?sfx=1` に並べた**=1体ずつ `crush/crush2/crush3`、
  まとめ轢き `smash/smash2/smash3`(②が今までの「どっと音」寄り=比較用に残してある)。

### 2026-08-02(27) 🔊**③を採用**
ユーザー「まぁ③かなぁ」→ **1体ずつ=`crush3`(湿った潰れ) / まとめ轢き=`smash3`(破砕寄り)** を採用。
- ⭐**採用は `SFX_CRUSH`/`SFX_SMASH` の2行が持つ**=選び直しはここだけ書き換えればよい
  (呼び出し側に音のキーを直に書かないこと)。⚠**他の案も鳴らせるまま**残してある(`?sfx=1`)。

### 2026-08-02(28) ⚡**爽快感が無い原因は演出ではなく「速さと大きさ」だった**
ユーザー「悪くないけど やっぱり爽快感がほぼないな **音もエフェクトもなにもかも**」。
- ⚠⚠**演出を足す方向は間違いだった**(ニトロ・肉片・吹き飛ぶ体・速度線・画面の血・専用の音まで足しても
  「ほぼない」と言われた)。⭐**数字で見たら舞台の寸法が間違っていた**:
  - 最高速560×寄り.42=**画面では毎秒125px**=画面の**3分の1**しか進まない。**這っている速さ**だった。
    速度線を何本足しても、この速さでは速く見えない。
  - 敵は米粒で**画面の高さ10px**=**当たった瞬間そのものが見えない**。
    当たった物が見えていない所へ肉片を足しても「何かが弾けた」で終わる。
- ✅**速さ 560→1500 / 加速 1400→3200**(画面では毎秒495px=**画面1.26枚ぶん**)。
- ✅**寄り .42→.62**(敵15px・バス100px=当たった物が見える寸法)。道幅 1050→780・関所 620→430。
  マップ 4400x26000→**3400x42000**(速くなったぶん道を伸ばす)。
- ✅**路肩に標識杭を等間隔で置いた**=**速さは「流れていく目印」でしか伝わらない**。
  のっぺりした路面をどれだけ速く動かしても、目印が無いと止まって見える。
- ✅**まとめて薙いだ瞬間だけ 0.07秒 時間を止める**(`G.hitStopT` を流用)+**車体がつんのめる**。
  ⚠1体ごとのヒットストップは 2026-07-27 に試して捨てている(群れを薙ぐと試合全体がスローになる)。
  **まとまった瞬間だけ**なら効く、が今回の結論。
- 実走=**27秒で到着・463体・受けた傷91**(耐久400)。

### 2026-08-02(29)(30) 🩸**轢いた証拠を路面に残す / 🔊車の音を現実とリンク**
ユーザー「ちょっと早いな でも走ってる感じはあるね **轢いてる感がないやっぱり**」
+「バス(車)の音とかを**現実とリンク**させてほしい。**エンジン音 タイヤの音 ゾンビに当たるときになりそうな音**」。
- ✅**速さを一段だけ落とした**(1500→1250)。⭐「走ってる感じ」は出たので大きく戻さない。
- ⭐⭐**「轢いた感が無い」の正体=路面に何も残っていなかったこと**。
  残っていたのは**血の染みだけ**(画面で数pxの点)なので、何百体轢いても**やった証拠がゼロ**だった。
  ✅**潰れた死体を路面に敷く**ようにした(`bcps`・上限260・薄れさせない)=後ろは死体の絨毯になる。
  ✅血だまりを1.6倍・**血の轍を3倍太く**(細い線だと「タイヤ跡」で終わる)・吹き飛ぶ体を1.4倍で高く。
  ✅**轢いている間の抵抗を体数ぶん効かせた**=厚い所ほど減速する(素通り感が消える)。
  ⚠**バスは画面の下に居る**ので、死体の絨毯は**大半が画面の外**にある。効くのは通り過ぎる一瞬だけ。
- ✅🔊**走行音を入れた**(`busSndOn/Step/Off`)=**鳴らしっぱなしのノードを1組**作って速さで動かす。
  **エンジン**=ノコギリ波2本を少しずらして唸らせローパスで丸める(58→174Hz・アクセルで少し上がる)。
  **タイヤ**=ノイズを帯域で削った「ゴーッ」。⭐**横滑りしているほど明るく**=曲がるときの「キュッ」。
  ⚠**面を出たら必ず止める**(`backTitle`)。⚠作れない環境(検査)では黙って諦める。
- ✅**当たる音に「車体の芯」を薄く足した**(`crush4`=③+鉄 -17dB)。⚠鉄を上げると「どっと音」に逆戻り。

### 2026-08-02(31) 🚫**耐久とダメージ演出を外した**
ユーザー「あと**耐久力とかいらないね ダメージエフェクトもなし**」。
- ✅**この面に負けは無くなった**=バスは削られない/潰れない。**着けば勝ち**。
  被弾の赤い明滅も被弾音も出さない。取り付いて殴る敵(siege)も素通りさせる。
- ✅HUDの**耐久バー**と**「諦める」ボタン**もこの面では出さない(減らない物を見せる意味が無い)。
- ⚠コアHPそのものは**減らさないまま置いてある**(勝敗の仕組みが `core>0` を前提にしているため)。

### 2026-08-02(32) 🔊**轢く音は「素材」ではなく「鳴らし方」が原因だった**
ユーザー「まぁ音は **轢く音 以外**はいいと思う」(=エンジンとタイヤはOK)。
- ⚠⚠**素材を4案作って差し替えても収束しなかった理由**=**同じ音を同じ高さで毎秒13回鳴らしていた**こと。
  どんなに良い衝撃音でも、等間隔・同じ高さで連発すれば**ラチェット(機械)の音**になる。
- ✅**毎回ちがう音にした**=①**種類**を3つから毎回くじ引き(`crush4`/`crush3`/`crush`)
  ②**高さを±24%ばらす**(`SFX_VAR`) ③**音量をばらす**+**速いほど太く**
  ④**鳴らす間隔もずらす**(0.058〜0.108秒。等間隔だと拍が揃って機械に戻る)。
- ⭐**教訓=連発する音は「素材の質」より「ばらつき」で決まる**。次に同種の相談が来たら、
  素材を作り直す前に **SFX_VAR とくじ引きが入っているか**を先に見ること。

### 2026-08-02(33) 🔊**本物のゴア素材に差し替え**(ユーザー「商用OKで無料なら使ってもいいよ」)
- ⚠⚠**限界だと思っていたのは素材を開いていなかっただけ**だった=Sonniss GDC2026 バンドル本体
  (5つのzip・347本)が Downloads にそのままあり、**そのものずばりの素材が入っていた**:
  `GORESplt_Gore Designed Heavy Impact Smash` / `WOODImpt_Hit Blood Spill Splat...Squelch` /
  `FGHTImpt_4 x Punch, Body` / `VEHSkid_Tire Skids on Gravel`。
  ⚠それまでは**氷で骨を、小さな爆発で肉を代用**していた(`sfx_src` に残っていた18本の余り物だけで作っていた)。
- ✅1体ずつ=**本物の血しぶきの潰れ+肉を殴る芯**(`crush5`/`crush6` をくじ引き)。
  ✅まとめ轢き=**本物のゴア・スマッシュ**(`smash4`)。✅**急に曲がるとタイヤが鳴く**(`skid`・1.1秒に1回)。
- ⚠**エンジンとタイヤの走行音は触っていない**(ユーザー「轢く音以外はいい」)。
- ⚠音量は `test_sfx.js` が「飛び抜けて大きい」で落ちたので**測って** `SFX_GAIN` を決めた
  (轢く音は毎秒10回以上鳴るので、揃える先 0.13 よりさらに下げてある)。
- 📌**必要な物だけzipから取り出して `sfx_src` へ**(生WAVはリポジトリに入れない=`.gitignore` 済み)。

### 2026-08-02(34) ✨**粒(VFX)に素材を使えるようにした**
ユーザー「正直音とかエフェクトに限界感じてるんだけど なんかいいのある?」→「**商用OKで無料なら使ってもいいよ**
怪しいサイトとか変なとこじゃない限り」。
- ⭐**Kenney "Particle Pack"(CC0・商用可・クレジット任意)**を入れた。48pxに縮めて7種だけ埋め込み(約25KB)。
  道具=`tool_part.js`(`px_src/kenney/` に素材とライセンス本文。⚠元の大きいPNGはリポジトリに入れない)。
- ⭐**四角と丸をどれだけ重ねても「柔らかい煙」「散る飛沫」にはならない**=そこだけが本当の天井だった。
  ✅**血の飛沫**(鮮血→暗い肉の2色は維持)✅**地面の血だまり**✅**土埃**✅**前照灯の光**
  ✅**まとめ轢きの閃光**(加算)。⚠**キャラ・地形・タワーは今までどおりプロシージャル**。
- ⚠**色は素材のまま使わない**=白い粒に色を被せて使う(`prtTint`。`ctx.filter` は iOS で効かないので
  `source-atop`)。⚠焼いた物は色ごとに使い回す。⚠**画面に入っている物だけ**+出しすぎの安全弁。
- 🐞**踏んだ穴2つ**(どちらも [[NOTES]] に足した):
  ①**同名の関数(`pxImg`/`pxDraw`)が7500行目に既にあり、自分の定義が黙って上書きされた**
    =描画が丸ごと止まった。⚠`test_undef.js` も `node --check` も捕まえない。
  ②**ffmpeg でPNGを縮める時に `-pix_fmt rgba` を付けず、透明が落ちて四角い板**になった。

## 2026-08-02(35) 🔭🚌道中「道路の先が見えない」を直した(縦だけ潰す)
ユーザー「道路の先があまり見えないからもっと見えるようにしたい」→「もう最適に作り替えてもいい」。
- 📐**まず測った**=**走っている時に前方へ見えているのは 897px(最高速1250 で 0.72秒ぶん)**しかなかった。
  レース系の目安は2秒前後なので、体感ではなく**実数として足りていなかった**。
  ⚠しかも**止まっている時(960px/0.77秒)より狭い**=カメラの追従の遅れが先読みを食っていた。
- ⭐**結論=見える時間を増やす手は2つしか無い**(①カメラを前へ出す ②縦の縮尺を下げる)。
  ②は必ず「小さく見える」か「遅く感じる」を払う。**寄り(CAM.zm)を下げるのは②の一種で横まで縮む**
  =却下済みの「バスがちっちゃい」へ戻る。⭐だから**縦だけ潰す**方を選んだ。掟は [[NOTES]]。
- ⭐**`SCY`(縦専用の倍率)を入れた**=world→screen の変換1か所(`setTransform`)に縦の倍率を足しただけ。
  **既定は SCY=SC** なので本編・対戦・冒険は1pxも変わらない。道中だけ **`BNS_SQ=.80`**。
  ⚠回す物(バス・ニトロの炎・血しぶき・ボス/エリート・世界座標の文字)は
  **translate→`scale(1,SC/SCY)`→rotate** の順で打ち消してあるので、**どの向きでも歪まない**。
  ⚠地面に貼る物(影・血だまり・轍)は潰したまま(それが正しい)。⚠焼いた地形タイルも縦だけ縮めて焼く。
- 🎥**カメラを速度で前へ出す**=**画面のどこに座るか**で持つ(止まって `BUS_CAM_P=.72` → 全速 `BUS_CAM_P2=.85`)。
  **横の先読み** `BUS_CAM_X=.14` も足した。⚠固定の `B.vy*.10` はこれに畳んで廃止。
  ⚠⚠**追従の遅れ(`camStep` の1次遅れ)を打ち消さないと、これは丸ごと効かない**(`camLead`)=
  変更前は**止まって0.77秒ぶん見えるのに全速では0.72秒に狭まっていた**。係数は dt から出し、
  さらに位置そのものを範囲で切ってある(撮影のように camStep が多く回る環境で外へ出ないように)。
- 👀**読ませる物を足した**=①路肩の**橙の反射板**+杭を 340→**220px おき** ②中央線の破線を 190/230→**150/165**
  ③**画面上端の霞** ④**前照灯を「丸い光」から「進む向きへ伸びる帯」へ**(速いほど長く・ニトロで1.5倍)
  ⑤速度線を早く・濃く・本数増(13→18/20→26)=**画面の流れが2割ゆっくりになったぶんを取り戻す**。
- ⚙**湧く距離も伸ばした**(`BNS_AHEAD` 1500→**2100** / 4000→**4800**)=可視が伸びたので、
  そのままだと**画面の中で湧く瞬間が見えてしまう**。
- 🔊**ミュートボタンを画面下の中央から右へどけた**=**バスが張り付く場所を丸ごと潰していた**(撮って気づいた)。
  ⚠`#abar` そのものは消していない=**その高さが `fitCanvas` の availH を決めている**ので、
  消すと画面が寄って進む先が逆に狭くなる。
- 📊**結果(実測 852x393)**=**走行中の前方 897px(0.72秒)→ 1435px(1.15秒)=1.6倍**。
  止まっていても 960→1215px。⭐**60/30/20fps のどれでも同じ位置に座る**(遅れの打ち消しが効いている)。
  横(バス・道幅・群れの濃さ)は据え置き。尺と密度も据え置き
  (検査の実走: 32秒で到着・轢いた数 732体→620〜743体=乱数の振れ幅の中)。
- 🔬**寸法を測る道具**=`meas.js` 相当のスタブ実行(test_headless と同じ要領で index.html を読み、
  `bnsBusStep`+`camStep` を自分で回してバスの画面位置と前方の可視距離を出す)。⚠**目分量で詰めない**。

## 2026-08-02(36) 💥吹っ飛びを本物の弾道にした + ゾンビを大きくした
ユーザー「物理演算でゾンビ吹っ飛ぶようにできる?」→「ゾンビを少しでかくするのもあり」→「GO」。
- 🔍**「物理に見えない」の正体は4つ**だった=①高さを持っていない(画面のyを上げて重力で戻すだけの偽物)
  ②初速が乗り物の速さと無関係 ③当たった位置で左右に振り分けていない ④回らない。掟は [[NOTES]]。
- ⭐**高さ z を持つ専用の輪 `bbod`(上限110)へ移した**。初速も高さも**バスの速さから**出し、
  **当たった位置で左右へ振り分け**、**回りながら飛び**、**落ちる影が付き**、
  **着地して2回まで跳ねて転がって止まり**、**落ちた所に血が1枚残る**。
  ⚠**肉片(bchk)と同じ輪にしない**=肉片は1体7個出るので、飛んでいる途中の体が上書きされて消える。
- ⚠**絵は米粒とまったく同じ形・同じ色にした**=最初は四角で描いたら**「板が飛んだ」にしか見えなかった**
  (撮って描き直した)。米粒の rect の式をそのまま使い、回転の軸だけ体の真ん中へ寄せている。
- ⚠**空気抵抗(`BBOD_AIR`)と頭打ちを足した**=無いと**森の奥まで飛んで画面から消える**うえ
  **高く上がりすぎて画面の上へ抜ける**(ニトロ中に撮って気づいた)。
- ⭐**ゾンビを大きくした**(`RICE_S` 1.15→**1.35**=画面で15px→18px)。
  ⚠**当たり判定と跡(肉片・血だまり・路面の死体)は `RICE_K` で自動連動**させた=
  絵だけ大きくすると「当たっていないのに死ぬ」になり、跡だけ小さいとちぐはぐになる。
  📌当たりが17%広がったぶん、実走の轢いた数が 700→800体前後に増えた。
- 🧪**検査を足した**(`checkRice`⑫)=①体が bbod に積まれる ②高さ z と回りを持つ
  ③速く走るほど高く飛ぶ ④着地して跳ねて転がり止まる ⑤bbod が上限を超えない。
- ⬜**生きている敵を飛ばす②は未着手**(いまは死んだ体だけ)。⚠着手はユーザーの指示待ち([[TODO]])。
- 🐞踏んだ穴2つ(どちらも [[NOTES]] に足した): ①test_headless の body はテンプレート文字列なので
  **コメントのバッククォートで SyntaxError**(2度目) ②index.html のコメントで「**」と「/」を続けると
  `*` と `/` が並んで**コメントが途中で閉じる**。

## 2026-08-02(37) 🛣道中に「足りないもの」を一気に入れた
ユーザー「バス時間に足りないもの探して」→ 一覧を出して → 「全部いっちゃおう」。
⚠**①探索だけは着手前の確認が要る決まり**なので手を付けていない(②強化と④ボスは①待ち)。
- 🏁**自己ベスト**(`META.brec`)=タイム/轢いた数/最高連続をステージごとに保存。
  リザルトに「🎉自己ベスト!」か前のベストを併記し、**タイトルの難易度ボタンにもタイムを出す**。
  ⚠走る前に目標が見えていないと記録は効かない(リザルトだけでは次に入る時に忘れている)。
  ⚠`META_RESET` は上げていない(鍵を足しただけ)。`metaResetAll` にも書いた。
- ⭐⭐**難易度カーブ**=進み具合(0→1)で顔ぶれ・硬さ・濃さが開く。
  ⚠それまで道中は**1波きり(`w:1`)**だったので `mw>wave` に弾かれて**実質1種類**、
  エリート(`wave>=3`)もボス(`wave===10/15/20`)も**出ていなかった**。掟は [[NOTES]]。
  ⚠**顔ぶれを丸ごと入れ替えたら轢いた数が 800→280体**に落ちた=**大半は元のまま**にして
  混ざる率だけ上げる形(7%→33%)に直した。硬さの倍率も二重取りになるので 1.6→.5 に落とした。
- 🚗⭐⭐**壊せる障害物**(廃車/ドラム缶/木箱/バリケード・道に約100個)=轢くと壊れて破片が飛び、速さが削れる。
  ⭐**ドラム缶だけ爆発**(半径340)して周りのゾンビを巻き込む=**狙って当てる価値**が生まれ、
  「上に倒すだけ」だった運転に**狙いを付ける理由**ができた。
  ⚠**バスは止めない**(`bnsRoadFit` に入れない)=「地面が見えている所は必ず走れる」の掟を守るため。
  ⚠最初は寸法が小さすぎて**画面で読めない板**にしかならなかった(96→152=バスの半分)。
- 🧟⭐**しがみつき**=倒しきれずに当たり続けた個体の数で最高速が最大42%落ちる。
  🔥**ニトロで一発で振り払える**=ニトロに「脱出」という2つ目の使い道が付いた。画面の縁が緑に締まる。
  ⚠**負けは無いは守っている**(削るのは速さだけ)。
- ⛓**関所の予告**(1700px手前に一度だけ画面へ)/ 🏁**到着で0.55秒スロー**+輪2枚+白抜き。
  ⚠**HUDの札には足さない**=幅が決め打ち(154px)なので項目を足すと実機で溢れる(撮って戻した)。
- 🧪**検査を足した**(`checkRice`⑬⑭)=障害物が置いてある/道からはみ出さない/当たると壊れて減速する/
  ドラム缶が爆発して巻き込む/しがみつきが溜まる・ニトロで消える・離れると戻る。
- 🐞踏んだ穴3つ(全部 [[NOTES]] に足した): ①盤面を空にする検査を前に挿して**後ろの検査が0体で落ちた**
  ②ニトロで0にした値を**同じフレームの後半で溜め直していた** ③`checkDevLoad` の統計の検査(±7%)が
  **無関係な変更でもたまに落ちる**(再実行で通る)。

## 2026-08-02(38) ⏱時間制に作り替えた(道は走り切れない長さ・目標は⚡スコア)
ユーザー「道はめっちゃ長めにして 時間制にしない? それでゾンビを効率よくたくさん倒すのが目標になる」
→ 4つの論点を出して決めてもらった: **①締め切り(負けではない) ②時間切れに「拠点に着いた」の軽いイベント
③効率の中身はこちらで ④尺は1分ちょい**。
- ⏱**制限時間 `BNS_TIME`=75秒**。時間切れで**「次の拠点に着いた」の軽いイベント**(輪2枚+0.55秒スロー+🏁の報せ)。
  ⚠**負けは無いの決定は守っている**=時間切れは失敗ではなく締め切り。
- 🛣**道を 42000→118000 に伸ばした**(道の長さ約116000=最高速でも93秒)。
  ⚠⚠**時間内に絶対に走り切れない長さにする**=端が見えると「あと少しで着く」が嘘になる。
  📐実測: 木57600本 / 建物1383棟 / 障害物451個 / **生成222ms**(マップを読む時の1回だけ)。
- ⚡⭐⭐**スコア=Σ(1体 × 連なりの段の倍率)**(`BNS_CMB_X`=[1/1.25/1.6/2.1/3.0])。
  ⚠**ただの総キル数にしない**=濃い所を走れば誰でも同じ数になり、上手い下手がほぼ出ない。
  ⭐**段を上げて切らさない**ことに値段を付けると「濃い所をどう繋ぐか」が上手さになる。
  ⭐段の札に**倍率も出す**(「地獄のバス ×3.0」)=伸びる理由が見えないと繋ぐ動機にならない。
  ⚠爆発で倒した分も同じ道を通してある(通さないと「爆発させると損」という逆の遊びになる)。
- 📟**HUDを作り替えた**=「あと○m」を捨てて**残り時間とスコアの2つだけ**。バーも残り時間へ。
  ⚠走り切れない道で残りmを出すと**一生終わらない数字**になる。⚠札は幅が決め打ち(154px)なので3つ目は足せない。
- 🏁**自己ベストの中身も入れ替えた**=タイムをやめて**⚡スコア/轢いた数/最高連続**。
  ⚠時間制では誰が走っても同じ秒数で終わるのでタイムは記録にならない。タイトルの🚌ボタンにも⚡を出す。
- 🧪**検査 `checkRice`⑧を作り替えた**=「走れば必ず着く(400秒以内)」→
  **締め切りの前後で終わる / 道の端に着いてしまわない(=道が短すぎない) / スコアが積まれている**。
- 📊参考値(上へ倒しっぱなしの実走)=**75秒 / 2000〜2450体**(ドラム缶をいくつ当てたかで振れる)。

## 2026-08-02(40) 🏚探索場の廃墟に「壁の当たり」を作った(すり抜けを埋めた)
(39)で止めてあった穴の埋め合わせ。**出発地点の広場を探索場に拡げて廃墟9棟を並べた土台**を戻し、
残っていた3つを片付けた。⚠**探索そのもの(主人公で歩く・物資・制限時間)はまだ入っていない**。
- 🧱⭐**壁の当たり `bnsEntFit(x,y,cl)`**。⚠⚠**穴の正体=広場は「何も無い所」として作ってあるので
  中に当たり判定が1つも無い**。そこへ廃墟を建てたので**主人公もバスも壁をすり抜けていた**。
  ⭐**中は空洞・入口側だけ開ける**=壁を**厚さ `ENT_TH`(24)の帯**として持ち、
  入口の面だけ `ENT_DOOR`(34)ぶん切り欠く。⚠外周を丸ごと塞ぐと「入る」が作れない(それはでかい岩)。
  ⚠⚠**乗り物(`cl>=ENT_DOOR`)には丸ごと塞ぐ**=くぐれると屋根の下で画面から消えるし、
  倉庫の中はバスが丸ごと収まる広さなので「壁の帯だけ」では中が素通しになる。
  ⚠**絵と同じ形にした**(`ENT_K[].cb`)=⛽は**売店だけが壁**で、キャノピーの下は素通りできる。
- 🔌**`bnsRoadFit` の広場の分岐から引いた**=バスが使う当たりの入口はここ1本だけ。
- 🐞**入口が全棟とも外を向いていた**=長さ方向を接線(`a+90°`)に取ると法線は**中心を向く**のに
  `ds=-1` を渡していた。**+1 に直した**(検査で捕まえた)。
- 🐞**9棟のうち1棟が毎回建っていなかった**=マップは幅3400しかないのに広場の半径は3000なので、
  **輪の左右の端はマップの外**。⭐**角度の振れ幅を±7°→±45°**にし、**半径を内と外の両方へ**振るようにした。
  ⚠検査が「8棟以上」で見ていたので**ずっと気づけなかった**=`ENT_N` ちょうどに締めた。
- 🧪**検査**: ⑨の「建物が通路の中に立っている」を**道の通路だけを見る `bnsCorrFree`** に分けた
  (探索場にはわざと建てるので、広場まで混ぜると正しい配置が落ちる)。
  ⑪に**壁の当たり6つ**を足した=壁をすり抜けない/中が空洞/入口が開いている/入口の前に立てる/
  **入口の前から中まで歩いて入れる**(点だけ見ても角に引っかかると入れない)/バスは入口をくぐれず中にも居られない。
- 🐞**前からあった「たまに落ちる検査」2つを直した**(⚠★4以上の割合の件とは別):
  ①**吹き飛んだ体の高さ**=上がっているのは0.09秒だけなので「盤面のどれかが vz>0 か」で見ると
  **どのゾンビを引いたかで落ちる**→**その場で1体飛ばして見る**ようにした。
  ②**⚙️の文字が1体ずつ湧いている**=📖図鑑の初登録の文字が混ざっていた→**⚙️の文字だけ数える**ようにした。
  📊5回連続で ALL TESTS DONE。
- 📐並び=出発点の**下側の扇**に9棟(中心から1320〜2400px・道からは1253px以上)。

## 2026-08-02(41) ⛓関所を「抜けた時の一幕」にした(手前の文字の予告は廃止)
ユーザー「**1分だったら関所みたいなところを通過する軽いムービーが欲しい。あとこの先関所 とか文字いらない**」。
- ❌**手前の文字の予告を捨てた**(「この先 関所 — 群れが溜まる」/`BNS_GATE_W`/`BNS_GATE_T`)。**書き戻さないこと**。
  ⭐決定の中身=**予告で身構えさせるのではなく、抜けた瞬間を見せ場にする**。
- ⛓⭐**`bnsGatePass()`**=関所を1つ抜けた瞬間に **0.62秒(`BNS_GP_T`)の一幕**。中身は3つだけ:
  ①**上からの黒帯+下の薄いグラデ+画面全体をわずかに暗く** ②**0.26秒のスロー**(`BNS_GP_SLOW`)
  ③**門柱の左右から土煙**+輪2枚+軽い揺れ。⚠**操作は塞がない**(運転はそのまま効く)。⚠音は付けない。
- ⚠**1試合に4回通る**(75秒で道の7割・関所は6か所)ので、到着の締め(0.55秒スロー)より短くしてある。
- 🐞**撮って直した2つ**: ①**下を「帯」にするとバスが隠れる**(バスは画面の85%に張り付く)=
  下だけ薄いグラデにした ②**上の帯がHUDの黒(高さ約60px)に溶けて何も起きて見えなかった**=
  帯を H*.14 まで厚くし、画面全体の薄い暗転を足した(NOTESの「速さだけでは気づけない」と同じ話)。
- 🧪**検査⑮**=抜けると出る/同じ関所で二度出ない/次の関所ではまた出る/**道の途中に置かれても誤爆しない**
  (`gpN` を最初の1コマで今の位置から決める)。撮る=`node test_shot.js out.png 852 393 "bns+bnsn=12+bnsdrv+bnsgate"`。

## 2026-08-02(41) 📒探索の中身が決まった(ミニゲームを選んで3回)⚠未実装
ユーザー「**中毒性ある軽いミニゲームを数種類用意して、各ミニゲームにそれぞれ多くもらえる物資が
あるからそれによってミニゲーム選んで物資獲得を3回ぐらいやる → バス強化画面**ってことにしよう」。
- ⚠⚠**「主人公で歩き回って廃墟を漁る(vainglory視点の探索)」は捨てた**。**書き戻さないこと**。
- ⭐**(39)〜(40)で作った広場の廃墟9棟がそのまま選択肢になる**=1棟=1ミニゲーム。
  **⛽/🏭/🏠の3種が「多くもらえる物資」を語る**。9棟あって3回しか選べない=毎回どれを選ぶかの判断が出る。
- 明細は [[DESIGN]]。⬜**未決3つ**=ミニゲームの中身 / 物資の種類と名前 / 同じ棟を続けて選べるか。

## 2026-08-02(42) 🏁到着を「拠点の入り口を通過するムービー」にした / 🧱出発点の後ろを壁にした
ユーザー「**ムービーはこういうことじゃなくて、時間が1分ぐらい経ってバスの時間終了したら
拠点の入り口みたいなところ通過するムービーが欲しい**」「**出発地点の後ろにバスで行けないように普通に壁にして**」。
- ❌**(41)で作った「関所を抜けた時の一幕」は外した**(取り違えていた)。**関所では何も出ない**。書き戻さないこと。
  ⭐帯の作り(黒帯+薄い暗転+下だけグラデ)だけ到着のムービーへ持ってきた。
- 🏁⭐⭐**到着のムービー(3秒)**=`bnsArrStart`/`bnsArrStep`。
  ⚠⚠**門まで走らせない**=時間切れは道の7割地点なので、**その場の2100px先に拠点の入り口を出す**
  (2026-08-02(38)の「門まで行かなくても着いたことにしてよい」の決定どおり)。
  中身=**黒帯が入る → 一直線に走る → 門をくぐる(輪2枚+土煙+0.45秒スロー+揺れ)→ 惰性で停まる → リザルト**。
  ⚠**運転はスクリプトで動かす**(物理だと障害物・しがみつきで門に届かないことがある)。
  ⚠**操作の物(スティック・ニトロ・連なりの札)は引っ込める**=出したままだと「黒帯の付いた運転画面」に見える。
  ⭐**門の絵は到着点と同じ物**(`drawBnsGateBld`)=拠点の顔は1つ。
- 🧱⭐**出発点の後ろは壁**(`BNS_WALLY`=出発点の420px後ろ)=**出てきた拠点の門が閉まっている**形。
  ⚠**マップの幅いっぱいに引く**(道幅ぶんだけだと森の縁を回り込んで裏へ出られる)。
  ⚠**絵と当たりを同じ値から出す**(`drawBnsEnds` と `bnsRoadFit`)。
- 🐞**踏んだ穴3つ**(全部 [[NOTES]] に足した):
  ①**カメラの追従が運転の枝の中にあった**=ムービーで飛ばしたら**バスが画面の上端で切れた**
   → `bnsCamFollow` に切り出して両方から呼ぶ。
  ②**湧きの積み直し(`bnsTpl`)がムービー中も回っていた**=門の前で群れが湧き直して
   **盤面が空にならずリザルトが出ない**(実走が75→85秒になって気づいた)。
  ③**掟の検査「地面が見えている所は必ず走れる」が壁の裏で落ちた**=壁が描いてある所は見ないように直した。
- 🧪**検査⑮⑯**=ムービーが始まる/その間 arr が立たない/門が前に出る/通り抜ける/必ず終わる/
  マップの外まで走り抜けない/二度始まらない。壁=裏へ行けない・端から回り込めない・出発点は今までどおり自由。
  撮る=`node test_shot.js out.png 852 393 "bns+bnsn=14+bnsdrv+bnsarr=1.6"`。

## 2026-08-02(43) 🎒走る前の流れを入れた(①ミニゲーム選択 → ②3回遊ぶ → ③乗り込み → ④強化)
(42)で決まった1試合の流れのうち、**入っていなかった①〜④を全部入れた**(⑤運転・⑥到着は既存)。
- ⭐**ユーザー決定4つ**(着手前に聞いた)=
  ①**ミニゲームはまず1種だけ作って手触りを見る**(3種同時に作って全部外すのを避ける)
  ②**物資は ⛽燃料 / 🏭鉄材 / 🏠食料**
  ③**選んだ棟は消えない**(選び直せる)
  ④**選択画面は広場を見下ろして棟を光らせる**(専用のカード画面にはしない)。
- 🎒**状態は `G.bpre` 1つ**(`st`= sel / mini / board / up)。⚠**`gameStep` の頭で丸ごと逸れる**ので、
  この間は**敵も残り時間もスコアも1つも動かない**。⚠**波が始まるのは「準備OK」を押した時**(`bnsPreGo`)。
- ①🏚**選択画面**=広場を見下ろす寄りを**毎回その場で計算**(`bnsSelCam`)。棟に光の輪+札(名前と主物資)。
- ②⛽**給油**(`bnsMg*`)=**動く針を目標の帯で止める**8本勝負。会心5点/成功3点、連続で×1〜×2.4。
  ⚠**回を追うごとに針が速く・帯が狭く**なる。⚠**残り2種(🏭荷下ろし/🏠物色)はまだ無い**=
  **3棟ともこの給油を遊ぶが、もらえる物資は棟で変わる**ので「どこを漁るか」の選択は先に成立している。
- ③🚌**乗り込みのムービー**(2.6秒)=黒帯+「バスに乗り込む」→「エンジン始動」+少し前へ出る。
  ⚠⚠**寄りの切り替え(広場→運転)はこの黒帯の裏で1回だけ**やる(跨ぐと焼いた地形を全部捨てるため)。
- ④🔧**強化画面**=6項目(エンジン/増槽/排障器/タイヤ/乗員/炊き出し)。⚠**この試合かぎり**=
  効き目は**バスの持ち物**(`B.spMx`/`B.dpsX`ほか)に入れる。定数を書き換えると次の試合に残る。
- 🏚**広場の廃墟を9棟→3棟にして出発点の近くへ寄せた**=**選択肢は見えないと選べない**
  (一番近い棟でも1320px先=画面のほぼ外だった)。⚠**左右の翼へ交互に振り分ける**=
  真上は道の通路・真下は出てきた拠点の門で、どちらも置けない。
- 🐞**踏んだ穴4つ**(全部 [[NOTES]] に足した)=①コメントの中の「星2つ+斜線」で**スクリプトごと文法エラー**
  ②CSSで隠す物に**インラインの display:none を書いて、出発後にスティックが戻らなくなった**
  ③画面いっぱいに収める寄りの計算で**一番奥の棟が上の帯の裏に隠れた** ④**札どうしが完全に重なった**。
- 🧪検査=`checkBnsFlow()`(8項目)。撮る=`bns+bnssel` / `bns+bnsmg` / `bns+bnsbrd` / `bns+bnsup`。

## 2026-08-02(44) 🔧バス強化を永続にして、⚔装備の枠を足した(回転ノコほか9種)
ユーザー「**連なりの猶予ってなに?**」→ 文言を全部わかりやすく直した。
ユーザー「**全体的にわかりやすくして、あとこの強化は永続だからそれ込みで数値調整。
それとバスの前面に円で回るのこぎりブレードみたいなやつを←と→につけて蹴散らすやつとか
そういう爽快感がまずバス強化がほしい 別枠のUIで**」。
- 🎒⭐⭐**永続にした**(`META.bres`/`META.bup`/`META.beq`)=**段も装備も余った物資も次の試合へ持ち越す**。
  ⚠**値段を組み直した**=1試合の稼ぎ240前後に対し、**全部揃えるのに20試合前後**(⛽1985/🏭1450/🏠1570)。
  ⚠`METAResetAll` にも書いた/古いセーブの作り直しも入れた/**?dev=1 の底上げも足した**(3種とも99999)。
- ⚔⭐⭐⭐**装備の枠を足した**(強化画面を**2枠**にした=🔧性能6項目 / ⚔装備9種)。
  ⚠⚠**「どれか1つを選ぶ」ではない**(ユーザー明言)=**買い切りでどんどん増える**。安い物から並べてある。
  中身=🔩スパイクローラー / 🪤鉄条網バンパー / 🛡増加装甲 / **⚙回転ノコ** / 💨サイドブラスト /
  🚀追加ブースター / 🔥前方火炎放射 / 🔊サイレン / ⚡電磁バンパー。**9種とも絵が付く**。
- ⚙**回転ノコ**=左右に丸ノコ。⚠⚠**当たりも絵も `EQ_BL_*` の3本から出す**=
  最初は当たりを楕円・絵を小さい円で描いたので**絵の外で当たる**状態になっていた(撮って直した)。
- 🔊**サイレン**=前のゾンビを道の中央へ寄せる。⚠⚠**0まで寄せてはいけない**=
  撮ったら**群れが1本の縦線**になっていた。**残す幅(170)まで px/秒 で寄せる**形に直した。
- 📝**文言を全部わかりやすくした**=「連なりの猶予」→「コンボが切れるまで」/「会心」→「ピッタリ!」/
  「3/8本目」→「あと6本」/「目盛りで止めろ」→「緑の帯でタップ!」ほか。
  ⭐強化画面に**「買った物はずっと残る」**と明記(書かないと使い惜しみされる)。
- 🧪検査=`checkBnsFlow` を8→⑧永続まで拡張(枠の切り替え/買い切り/2度買えない/初期化で消える)。
  撮る=`bns+bnsup`(性能) / `bns+bnseqp`(装備) / `bns+bnseq+bnsdrv`(装備を全部付けて走る)。

## 2026-08-02(45) 🎮ミニゲームを3種とも別の遊びにした(⛽給油 / 🏭荷積み / 🏠物色)
ユーザー「**あと全部同じミニゲームはやめて 違うミニゲームにしよう**」。
- ⚠⚠**3種は「別の腕前」を測る**=⛽**タイミング** / 🏭**目測** / 🏠**視覚探索**。
  同じ形の遊びを3つ並べると、棟を選ぶ理由が「物資」だけになって**選択が薄くなる**。
- 🏭**荷積み**=左右に流れる荷を荷台の真上で落として積む。⚠**ズレたぶんだけ荷が細くなる**=
  **自分の失敗が次の難しさになる**のが背骨。完全に外すと崩れてそこで終わり。8段まで。
  ⭐✨ピッタリの時だけ幅が少し戻る=精度がそのまま点になる。
- 🏠**物色**=お題の物を棚(4×3)から素早く見つけてタップ。**速いと✨**/間違えると時間が減る。
  品物6種は**全部プロシージャル描画**(缶詰/瓶/木箱/薬/パン/電池)。
  ⚠⚠**問題数の上限(9個)を置いた**=時間だけで区切ると上手い人ほど何問でも解けて、
  **3種の実入りが揃わなかった**(検査で30問解けていた)。
- 🖌**枠(見出し・連なり・お知らせ・締め)は3種で共通**にして、真ん中の遊び場だけ差し替える形にした。
  ⚠⚠**遊び場の空いている所は3種で違う**=同じ y に連なりを置いたら🏠は「お題」の上、🏭は荷台の上に
  重なって両方読めなかった(撮って直した)。⭐**🏭🏠はお知らせと連なりを同じ行で入れ替える**。
- 🏚選択画面の札に**遊びの名前**を出すようにした(棟の名前だけだと何をするか読めない)。
- 🧪検査=`checkBnsFlow` が**3棟とも通る**ようになった(種類ごとに上手く遊ぶ)+
  **3種が同じ物になっていないこと**を見る。撮る=`bns+bnsmg=0/1/2`。

## 2026-08-02(46) 📒NOTES.md を6冊に分けた(索引+分冊)
ユーザー「**NOTE md整理しよう**」(きっかけ=「トークン削減できるという投稿を貼ってみた」)。
- ⚠**きっかけの投稿(事業ごとに Rule/Knowledge/Output/Skills を作る形)はここでは採らなかった**=
  ここは**ゲーム1本のリポジトリで「事業」が1つも無い**うえ、md は既に役割で分かれている。
  ⭐**実際に重かったのは NOTES.md(2098行・10万トークン級)の1枚**なので、そこだけを分けた。
- 📒**NOTES.md=索引**(124行)+**6冊**=
  [[NOTES_面]](490)/[[NOTES_絵]](642)/[[NOTES_UI]](265)/[[NOTES_数値]](268)/
  [[NOTES_音]](118)/[[NOTES_道具]](275)。
  ⭐**索引に「触る物 → 開く分冊」の表**を置いた=**触る物に合う1冊だけ開けば済む**。
- ⚠**索引に残したのは「毎回効く物」だけ**=進め方 / セーブと互換 / 設計として決まっていること /
  git と過去の記録(どの分冊にも入らない物)。
- 🔗**他のmdの [[NOTES]] を分冊へ張り替えた**(CLAUDE/TODO/DESIGN/HOME)。
  ⚠**[[NOTES]] のままでも索引に着く**ので、古い書き方が残っていても迷子にはならない。
- ✅**中身は1行も捨てていない**(元の全行が分冊のどれかにあることをスクリプトで突き合わせた)。

## 2026-08-02(47) 🏠物色を「探す」から「覚える」に作り替えた
ユーザー「**物色はちょっと厳しいな ほかのやつがいいかも 絵がわかりづらいから**」。
- ❌**「お題の物を棚から探す」は捨てた**。⚠⚠**小さい絵を見分けさせる遊びは、この画面では成立しない**=
  棚1つが実機で**40px前後**しかないので、缶・薬・電池が同じ縦長の塊に見える。書き戻さないこと。
- ⭐**光った棚を同じ順に押す(記憶)**にした=**絵の見分けがゼロ**になり、場所と順番だけで遊べる。
  棚は**3×2の6マス**(4×3=12マスだと順番が覚えられない)。段が進むごとに1つ長くなる(2→8)。
  **間違いは2回まで**(1回で終わりだと🏭荷積みと同じ緊張の作りになって重い)。
- ⭐これで3種は **⛽タイミング / 🏭目測 / 🏠記憶** と、はっきり別の頭を使う遊びになった。
- 🧪検査は3種とも通したまま(🏠は「見せ終わるまで待ってから順に押す」に書き替え)。撮る=`bns+bnsmg=2`。

## 2026-08-02(48) 🏠記憶の光が出ない不具合を直した / 🏚選択画面を見やすくした / 🎯砲撃の案内を消した
ユーザー「**光が見えなかった しかも初回の1回以降光が何も見えない**」
「**廃墟選ぶ画面もっと見やすくして**」「**バスで運転中に砲撃のメッセージ残ってるから削除して**」。
- 🐞⚠⚠**光が1回しか出なかった原因**=**押した時の光の時計(`litT`)を、見せる光にも使っていた**。
  絵の側が `litT<.30` を見ていたので、**最初の0.3秒を過ぎたら二度と光らなかった**。
  ⭐直し=**見せている間の光は `ph==='show'` の時計だけが持つ**/`litT` は押した時の光にしか使わない。
- 👀**見えるようにした3つ**=①**見せ始めに一拍(0.55秒)置く**(目が棚へ移る前に1つ目が終わっていた)
  ②**光る升を思い切り目立たせる**(中を白く抜く+外へ光の輪+一回り大きく)
  ③**光るたびに音も鳴らす**(見落としの保険)。⭐「あと何個見せる」も出す。
- 🏚⭐**選択画面を作り直した**=**盤面を一段暗く落としてから光らせる**(明るい土の上の細い線は沈む)+
  **丸を塗る→太い輪→外へ広がる輪**の3重ね+**番号の丸**+**札を大きく金枠に**。
  ⚠**札は必ず輪の下**に置き、**並べ替えは棟の位置(py)で**やる=上下が混ざると**線が交差**して逆に読めない。
  ⚠**番号は札より後に描く**(先に描くと札の下に潜る)。
- 🎯**道中で砲撃の案内が出ていた**=この面に砲撃は無い(ボタンもCSSで隠してある)のに
  チャージだけ溜まって「準備完了!」のトーストが出ていた。`m.bus` の時は出さないようにした。

## 2026-08-02(49) 🔩作戦タイムの右上に強化ptも出した
ユーザー「**準備タイムのとき右上に⚙️の総数しか表示されていない。タレットの攻撃力8%UPとかに使う方も表示してくれ**」。
- 🐞**原因**=`ivRender` は最初から ⚙️と🔩 の両方を書いていたのに、
  **0.12秒ごとのHUD更新(`loopBody`)が⚙️だけで上書きしていた**=開いた瞬間だけ正しくて、すぐ消えていた。
- ⭐**同じ枠を書く所を同じ形に揃えた**。教訓は [[NOTES_UI]]。

## 2026-08-02(50) 🚌道中の稼ぎ場を潰した(🧬を配らない + 現実の1日に1回)
ユーザー「**バスの時間を今無限プレイできて研究ポイントを無限獲得できちゃう どうする**」
→「**配らないしできないようにしよう 一日一回とかにできる? 現実の一日に一回まで**」。
- 📐**どれくらい壊れていたか**=`(波×25 + 倒した数×1.4) × 0.9` の共通式をそのまま通していたので、
  **75秒の1走で 2400〜2700🧬**(本編の古参をクリアしても1500🧬・研究所1個目は120🧬)。
- ⭐**直しは2つ重ね**=①**この面では🧬を配らない**(初回クリアの `bnsReward` だけ)
  ②**現実の1日に1回だけ**(`META.bday`・ステージごと)。
  ⚠**印は「入った時点」**(`bnsEnter`)=途中で抜けて入り直す抜け道も同時に塞がる。
  ⚠**周回する動機はもう「永続の物資」が担っている**ので、🧬は本編の難易度を進めた報酬に戻した。
- ⚠**「未解放(🔒)」と「今日は済(✅あと○h)」を書き分けた**=どちらも押せないので、
  同じ見た目だと**まだ開いていないのだと思われる**。押した時にも理由をトーストで出す。
- ⚠🛠**DEVは無制限**(でないと実機で1日1回しか試せない)。⚠端末の時計を変えれば回避できるが追わない。
- 🧪検査=2000体轢いた状態で `awardMeta` を通しても🧬が増えない/入ると印が立つ/同じ日は選べない/
  日が変われば走れる/別のステージは塞がらない。教訓は [[NOTES_面]](同じ掟の3度目)。

## 2026-08-02(51) ⏳「あと何時間何分」を出した / ⏱ミニゲームに制限時間と早得を入れた
ユーザー「**今日はもうプレイできません できるまであと何時間何分‥ みたいなの表示して**」
「**各ミニゲームは制限時間があって早くやればやるほど報酬が増えるようにして**」。
- ⏳**難易度ボタンに「✅今日は済 あと3時間12分」**、押した時も「今日はもうプレイできません。
  次に走れるまで あと 3時間12分」。⚠**タイトルに居る間は20秒ごとに書き直す**(出しっぱなしにしない)。
  ⚠**次の0時まで**であって「走った時刻からの24時間」ではない。
- ⏱**3種とも制限時間30秒**(`BMG_TIME`)+**残り1秒につき+2点の早得**(`BMG_FAST`)。
  ⚠⚠**時計は「自分が操作できる間」だけ動かす**=🏠記憶の「見せている間」を数えると
  **上手い下手と関係なく損をする**(理不尽になる)。
  ⚠**時間切れは失敗ではない**=そこまでの点は残す(この面の「負けは無い」に揃える)。
- 🖌**残り時間の帯を見出しの下に**(3種で同じ場所)+**締めに「⏱早得 +N」の行**を足した
  =上乗せがあったことが分からないと「急ぐ理由」が伝わらない。

## 2026-08-02(52) 🚌回数制限をやめて「2回目からは報酬少なめ」にした
ユーザー「**ちょっと一日一回は少なすぎたな。それにある程度何回かはプレイさせたいから
回数制限はやめて報酬を減らそう。初回報酬は豪華でいいけど2回目以降は報酬少な目とか**」。
- ❌**(50)で入れた「現実の1日に1回」は外した**(1回で撤回)。書き戻さないこと。
  ⚠**回数を絞ると「遊びたいのに遊べない」になる**=短くて気持ちいい面ほどそこが不満になる。
- 💰**歯止めは報酬だけ**=**初回クリア**は今までどおり豪華(🧬600/1100+💎50)、
  **2回目からは ⚡スコア÷40・下限20・上限120🧬**(`BNS_RPT_D/_MIN/_MAX`)。💎は初回だけのまま。
- ⚠⚠**上限がこの仕組みの本体**=量に比例させたまま係数だけ下げても、走る時間を伸ばせば抜けられる。
  📌1走(75秒)で最大120🧬。本編の古参クリア(約10分)が1500🧬なので、**1分あたりで見ると本編より割が悪い**
  =稼ぎ目的で回す理由がない。⭐周回する動機は「永続の物資」が担う。
- 🧪検査=スコアを999999にしても上限を超えない/初回より必ず少ない/2回走っても選べなくならない。

## 2026-08-02(53) 🧬道中の稼ぎを⚙️から研究ptに差し替えた
ユーザー「**バスの時間の時 ゾンビ轢くとスクラップが増えてるよね それなしで研究ポイントにして**」
「**バスの時間の時の上部のUIが歯車になってない?**」。
- ⚠**この面はタワーを1つも建てられない**ので、⚙️スクラップは増えても**使い道がゼロ**だった。
  ⭐**轢いても⚙️は入らない**ようにして、**浮き文字も「+N🧬」**に差し替えた。
- 🖥**上のHUD**=⚙️の枠を **「🧬 20 / 120」**(いま貯まった研究pt / 1走の上限)に。
  **🔩強化ptと🤝部隊枠は消した**=作戦タイムも兵科も無い面なので、**一度も動かない数字**が並ぶだけだった。
- ⚠⚠**画面の数字と実際に入る量を必ず同じにした**=初回クリアだけ0にする作りだったのを直し、
  **初回はこの分に `bnsReward` の豪華な分が上乗せ**される形にした(走っている間ずっと出しておいて
  入るのが0だと嘘になる)。

## 2026-08-02(54) ⚔装備を種別ごとに並べ直した / 🔊サイレンを「整列」から「群がる」に作り替えた
ユーザー「**バスの装備のやつは順番にしよう 種別に分けて**」
「**ゾンビが集まってくるやつは ゾンビが整列しちゃってて違和感あるから うわっとバスに引き寄せられる感じにして**」。
- ⚔**物資ごとに3つずつ・安い順**に並べ直した(⛽3 / 🏭3 / 🏠3)=札は3列なので**1行=1つの物資**になる。
  ⚠**数を揃えないと1行に2種類が混ざる**(4/3/2だった)。⭐🛡増加装甲を🏭→🏠へ移して数を揃えた。
  ⭐🔧性能も同じ考えで**2列**にした(物資ごとに2つなので、やはり1行=1つの物資)。
- 🔊**サイレンは2度目の作り直し**。❌①0まで寄せる=1本の縦線 ❌②幅を残して寄せる=道に沿った**1本の帯**
  (どちらも「整列した」ようにしか見えない)。
  ⭐**寄せる先を「バスが道のどこに居るか」**にして、**個体ごとのばらつきを1回だけ焼き**(`z.sj`)、
  **前後(`d`)も詰める**=**うわっと群がってくる**形になった。
  ⚠ばらつきを毎フレーム引き直すと震えるので、最初の1回だけ焼いて持たせる。

## 2026-08-02(55) ⚔装備を「3系統×4段の段階解放」に作り直した
ユーザー「**順番っていうのは、例えばタイヤにスパイクをつけるのが1段目、2段目はローラー、3段目は…
みたいな感じで段階で解放するってこと。だから段階となりえるように初期の装備増やして**」。
- ⚔**9個(買い切り9種)→12個(3系統×4段)**。**前の段を買うまで次は買えない**=1本道の梯子。
  ⭐**1系統=1つの物資**=札は**列=系統・行=段**で並び、**上から下へ段が上がる**のがそのまま見える。
  - ⛽噴射: 💨排気ブラスト60 → 🚀追加ブースター130 → 🔋二段ニトロ220 → 🔥前方火炎放射340
  - 🏭前まわり: 🛞スパイクタイヤ45 → 🪤鉄条網100 → 🔩スパイクローラー180 → ⚙回転ノコ300
  - 🏠乗員: 🛡内張り装甲50 → 📢拡声器110 → 🔊サイレン200 → ⚡電磁バンパー320
- ⭐**初期の装備を足した**(🛞スパイクタイヤ / 🔋二段ニトロ / 📢拡声器)=**一番下の段は45〜60**で、
  走り出しでも必ず1つは買える。⚠**前の段の効き目は消えない**(増設であって乗り換えではない)。
- ⭐**📢拡声器→🔊サイレンは同じ仕組みの強弱**(近く・弱い → 遠く・強い)=段が上がると効き方も伸びる。
- ⚠**買えない理由を3通り書き分けた**=✅装備ずみ / 値段 / 🔒前の段がまだ。
- 🐞**保存の繋がりが切れる穴**を塞いだ=画面側で `Q.eq` を入れ替えると META と切れて保存されない。
  **保存の直前に必ず繋ぎ直す**ようにした(検査で捕まえた)。

## 2026-08-02(56) 🧬道中の研究ptを 120→5000 に上げた
ユーザー「**120て少なすぎん? 研究ポイント**」→「**5000。これぐらいないと誰もやらん**」。
- 📐**私の見積もりが間違っていた**=「75秒で120」で計算していたが、**1セッションは約3分**
  (ミニゲーム3回60〜90秒+乗り込み+強化+運転75秒+到着)。120では **40🧬/分** で、
  本編の古参クリア(10分1500🧬=**150🧬/分**)より**4倍近く割が悪かった**=遊ぶだけ損だった。
- ⭐**割り算を1にした**=**⚡スコアがそのまま🧬**になる(上のHUDの2つの数字が繋がって読める)。
  1走で **100〜5000🧬**。⚠研究所を全部開けるのに57,040🧬なので**12〜20走ぶん**。
- ⚠**初回クリア報酬も 600/1100 → 3000/5000 へ**=1走ぶん(5000)より軽いと「豪華」にならない。
- ⚠⚠**この面が研究ptの主な稼ぎ場になる**(本編の約10倍/分)=**それを承知の上でのユーザー決定**。

## 2026-08-02(57) 💪強化タブを「中盤〜後半ほど伸びる」形にした
ユーザー「**強化タブの必要経験Pと、ステータスの伸びを中盤〜後半にかけて増やして**」。
- ⚠⚠**それまで伸びは「刻み×段数」の一直線**だった=**19段目も1段目も同じ伸び**なので、
  上の段を積む理由が「値段が高くなるだけ」になっていた(値段だけ指数だった)。
- ⭐**k段目の刻みを (1+0.06×(k-1)) 倍**にした(`LINE_ACC`/`lineAcc`)=**1段目は据え置きのまま**
  20段目は約2.1倍。合計で **1.57倍**。📌タワー威力 x2.00→**x2.57** / 兵科攻撃 x1.80→**x2.26** /
  兵科HP x1.40→**x1.63**。
- 💰**値段の傾きも立てた** x1.2335→**x1.28**(1段目48/35は据え置き)。
  📌20段目 2,588→**5,227** / 1本フル タワー13,465→**23,721**・兵科9,809→**17,296**。
- ⚠⚠**使う所は必ず `lineAcc` を通す**=素の `step*Lv` を書き残すと表と実物が食い違う。
  🐞**検査に「素の刻み×段数」で書いてあった箇所が4つ**あり、全部落ちて初めて気づいた。
  ⭐**「後半の1段が下の1段より大きい」の検査**を足した(一直線に戻ったらすぐ落ちる)。

## 2026-08-02(58) 🚌「バスの日」→「拠点開拓」に改名 / 1日5回まで / 🧬上限3000
ユーザー「**バスの日は名前拠点開拓にしよう。そんで一日5回までにしよう。4/5 3/5みたいな感じで
回数減らしてこう。んで研究ポイントは3000で**」。
- 🚌**名前を「拠点開拓」に**=物語(次の拠点を解放していく)に合わせた。⚠**中の呼び名(bns/道中)はそのまま**。
- ⏳**現実の1日に5回まで**(`BNS_DAY_N`)。⭐**難易度ボタンに残り回数(3/5)**を出す=
  何回遊べるかが押す前に読める。使い切ったら「あと○時間○分」+押した時もトースト。
  ⚠**数えるのは入った時点**(途中で抜けても消費)。⚠ステージごと。⚠🛠DEVは無制限。
  ❌**「1日1回」は入れて外し、無制限も外した**=**3回作り直した**。
  ⚠①1日1回=遊びたいのに遊べない ②無制限=稼ぎ場 → ⭐**5回+1走の上限**で落ち着いた。
- 🧬**1走の上限を 5000→3000**。📌**1日の上限=5回×3000=15,000🧬**で、
  研究所を全部開けるのに57,040🧬なので**4日ぶん**。

## 2026-08-02(59) 📄引き継ぎを md 1枚(HANDOFF.md)にした
ユーザー「**今後の引き継ぎはmdに引き継ぎ内容かいて、そのmd読んでっていえば引き継げるようにしよう**」。
- 📄**[[HANDOFF]] を作った**=**新しいセッションの入口**。中身は
  ①まずやること(読む順) ②これは何/置き場所 ③**直近でやったこと** ④**次にやること** ⑤検査 ⑥この1枚の更新のしかた。
- ⚠**長い引き継ぎ文をチャットに貼るのはやめた**。渡すのは**2行だけ**:
  「作業ディレクトリ: …\DEADTIDE」+「HANDOFF.md を読んで続きから。」
  ⚠⚠**1行目の作業ディレクトリは今までどおり必ず残す**(別のフォルダでセッションが開くのを防ぐため)。
- ⚠**二重管理にしない**=HANDOFF は**入口と直近の差分だけ**。状態の正は [[CLAUDE]]、やることは [[TODO]]。
  **書き換えるのは2節だけ**・**50行前後に保つ**、と本文にも書いた。
- 🔗[[CLAUDE]] の表と「⚡渡し方」、[[HOME]] の見取り図、棚卸しの手順(0番)に組み込んだ。

## 2026-08-02(60) ⚔装備の⑤段目3種 / 🧬は轢いた数そのもの / 🛠バス強化リセット
ユーザー①「1(=装備をもっと増やす)」②「**テストモードでバスの強化もリセットできるようにして
そっちで確認する**」③「**研究Pのゲットできる数はシンプルにゾンビ倒した数だけもらえるようにしよう。
初回だけ３倍分もらえるってことで**」。

**⚔装備を12→15種(3系統×5段)にした**
- ⚠⚠**増やしたのは「段(行)」であって「系統(列)」ではない**=**1系統=1つの通貨**という約束があるので、
  物資が3種のうちは**4列目を作れない**(作ると1列に2つの通貨が混ざって、分けた意味が消える)。
- 🛢**火炎オイル**(⛽⑤ 540)=**900px先に油をまいて燃やす**=**着く前に群れを焼いておく**。
  ⚠⚠**最初は「バスの後ろに落とす」形で作って捨てた**=**バスは画面の下に張り付けてある**ので、
  落とした物は**生まれた瞬間から画面の外**=一度も見られない(撮って初めて気づいた)。
  ⭐🔥前方火炎放射(鼻先310px)との違いは**距離**=仕事が分かれていれば重ねて付ける意味が出る。
- 🪃**ブーメラン刃**(🏭⑤ 480)=⚙回転ノコ(横だけ)の**前方版**。1.45秒で行って戻る2枚。
  ⚠**位置は `eqBoomPt` の1か所**から絵も当たりも出す。⚠**押しのけない**(飛び道具なので)。
  ⚠**への字の3枚刃**にした=丸だと回転ノコと見分けが付かない。
- 🔫**ルーフガン**(🏠⑤ 520)=**前方の一番近い1体を0.26秒ごとに撃つ**。曳光弾1本+銃口の炎+反動。
  ⚠**的はゾンビを舐めるついでに拾う**(別に舐め直さない)。⚠**音は付けない**(毎秒4発=必ず雑音)。
- ⚠**倒した分は3つとも連なり・⚡スコア・🔥ニトロの同じ道を通している**(NOTESの既出の掟)。

**🧬報酬を作り替えた**
- ⭐**毎回=轢いた数そのもの**(1体=1🧬・下限100・**上限3000**は据え置き。`BNS_RPT_K`)。
  ⚠⚠**⚡スコア割をやめた**=スコアには**連なりの段の倍率**が乗るので、**同じ体数でも上手いと3倍**になり、
  「何をすればいくら入るか」が読めなかった。⭐**HUDに出ている生の数字がそのまま報酬**になった。
- ⭐**初回クリアだけ3倍**(`BNS_FIRST_X`)+💎50。❌**決め打ちの額(`BNS_PTS`=3000/5000)は捨てた**=
  1走の実入りと別々に動く数字が2つあると、片方を触るたびに**初回の方が軽い/重い**が起きる(2回直した)。
- 📌実測=1走2300体前後=**ほぼ上限いっぱい**。

**🛠DEVに「バス強化リセット」**
- 🔬研究所の「🛠リセット」の隣に**「🛠バス強化」**(DEVでだけ出る)。`metaResetBus()` が
  **🎒物資・🔧性能の段・⚔装備・初回クリアの印**を戻す(自己ベストと今日の回数は残す)。
- ⚠**中身を空にする(別の物を代入しない)**=強化画面は `META.bres`/`bup`/`beq` を**参照で持つ**ので、
  入れ替えると「画面では増えているのに保存されない」に戻る(NOTESの既出の穴)。

## 2026-08-02(61) ⚔⑤段目を3つとも作り直し / ⏭廃墟あさりスキップ
ユーザー実機判断①「**ブーメランはバス関係ないからやめよｗ**」②「**火炎オイルはなにがどうなってるのか
全くわからなかった**」③「**あとルーフなんとかも弱すぎる**」④「**基本的にバスにつける系のやつがいい**」
⑤「**あとバス自体がでかくなるとか**」⑥「**廃墟あさりスキップ機能は通常版にも欲しい**」。

**⚔⑤段目を3つとも差し替え/作り直し**(⚠**掟は「乗り物に付いている物だけ」**)
- 🚌**大型車体**(⛽⑤ 540・`EQ_BIG`=1.34)=**バスそのものが一回りでかくなる**(ユーザー案)。
  ⭐**主役が大きくなる**のがこの面で一番読みやすい強化=轢ける幅がそのまま広がる。
  ⚠⚠**当たり(`B.rX`)・装備の寸法(ノコ/鎖/炎)・絵の縮尺・影**の全部に `B.bigX` を掛けてある。
  ⚠**地形の当たり(`BUS_CL`)だけは広げない**=広げると森の隙間に入れなくなる。
  ❌**🛢火炎オイルは外した**=路面に置く物はこの寄りでは何をしているか読めない(前へまく形に直しても同じ)。
- ⛓**鉄球チェーン**(🏭⑤ 480)=**左右に吊るした鉄球を遠心力で振り回す**。⚙回転ノコの**もっと外**を薙ぐ。
  ⭐鎖は速いほど伸びる。⚠回る輪を外へずらす(素直に吊るす点の周りだと車体に埋もれる)。
  ❌**🪃ブーメラン刃は外した**=飛んで行って戻る物は**乗り物の改造に見えない**。
- 🔫**ルーフガン**=**線で撃ち抜く**(帯の半幅80・0.2秒ごと・620)に作り直した。
  ⚠**「一番近い1体だけ」では弱すぎた**=画面に数百体居るので**1体ずつ落としても埋もれる**。
  ⭐撃ち抜くと**群れに穴が開くのが見える**=同じ威力でも手応えが変わる。

**⏭廃墟あさりのスキップ**
- 選択画面の上の帯に**「⏭ 漁らずに出発」**。⚠それまではDEV/撮影用の口(`bnsPreSkip`)しか無かった。
- ⭐**飛ばす先は③乗り込みムービー**=③④⑤の流れはそのまま(④まで飛ばすと持ち越した物資を使う機会が消える)。
- ⚠**判定は棟より先に見る**(棟の当たりは広いのでボタンを飲み込む)。⚠物資の行を左へ寄せて重なりを外した。

## 2026-08-02(62) ⚔装備は「車体に固定された物」だけにした
ユーザー実機判断「**鉄球も違うんだよね。明らかに動くやつはなし。いまの丸鋸みたいな動きはいいけど、
自立してるやつは現実的じゃないからなし。あと火炎放射もなし**」。

⭐⭐⭐**掟が決まった=OKなのは3種類だけ**
①**その場で回る物**(丸ノコ・ローラー・タイヤ) ②**ただ付いている物**(バンパー・装甲・ラッパ・コイル・銃座)
③**車体そのものが変わる物**(大型車体・二階建て)。⚠新しく足す時は必ずこの3つのどれかに収まるか確かめる。

❌**外した4つ**(⚠書き戻さないこと)
🔥前方火炎放射 / 🛢火炎オイル / 🪃ブーメラン刃 / ⛓鉄球チェーン。
⚠**2回に分けて怒られている**=最初から上の3種に絞っていれば起きなかった。

⭐**入れ替えた中身**
- ⛽④=🚌**大型車体**(340・×1.26) / ⛽⑤=🚎**二階建て化**(540・×1.52)。
  ⚠**段は積み上がるので倍率は置き換え**(掛け算にしない)。
  ⚠**「連結トレーラー」で作りかけて捨てた**=後ろに繋いだ物は**画面の外**で一度も見えない。
  ⭐**真上から見て大きくなる理由**にすること。
- 🏭⑤=🪚**四連ノコ**(480)=⚙回転ノコを**前後2組・張り出し86→118**にしただけ。
  ⭐**同じ物を増やす**方が、新しい形を足すより段が上がった感じが確実に出る。
  ⚠**張り出しを152で撮ったらバスが4枚の円盤の後ろに隠れた**ので118へ下げた。
  ⚠位置と大きさは `B.blXs`/`B.blY`/`B.blR`(`bupApply` で1回だけ作る)=絵も当たりもここを見る。
- 🏠⑤=🔫ルーフガンはそのまま(2026-08-02(61)に撃ち抜く形へ作り直し済み)。

## 2026-08-02(63) ⚔強化画面のタブを3つに分けた(🔧性能 / 🚌車体 / 🛠艤装)
ユーザー「**そういう 車体自体をいじる ノコとかの装備をつける とかはタブ分けして**」。
- ⚠**それまで15種が1枚に混ざっていた**=車体そのものが変わる物と取り付ける物が同じ列に並んでいて、
  何をしているのか読み分けられなかった。
- ⭐**🚌車体タブ**(5種・2列)=⛽💨排気ブラスト/🚀追加ブースター/🔋二段ニトロ ・ 🏭🚌大型車体/🚎二階建て化。
- ⭐**🛠艤装タブ**(10種・2列×5段)=🏭🛞タイヤ/🪤バンパー/🔩ローラー/⚙回転ノコ/🪚四連ノコ ・
  🏠🛡内張り装甲/📢拡声器/🔊サイレン/⚡電磁バンパー/🔫ルーフガン。
- ⚠**列は今までどおり「1列=1つの物資」**。⭐**タブごとに列の数も段の数も違ってよい**ことにした
  (揃えるために中身を歪めない)。
- ⚠⚠**梯子は「同じタブの同じ物資」の中だけ**で繋ぐ=**表の添字 -1 で前の段を見てはいけない**。
  ⭐表から**自動で列を組み立てる**ようにした(`BEQ_LN`=列の一覧 / `BEQ_AT`=添字→列と段)。
  手で段番号を書くと、表に1行足すたびに書き直すことになって必ず食い違う。
- 🧪撮る=`bnsup`(🔧性能) / `bnseqp`(🚌車体) / `bnsgep`(🛠艤装)。検査もタブごとに回すよう書き直した。

## 2026-08-02(64) 🚌当たりを「絵そのもの」にした / つまみの「艤装」を「装備」に
ユーザー①「**艤装ってなんだ わかりやすい言葉にしてくれ**」
②「**あとちゃんと当たり判定をバスの見た目通りにしてほしいバスの先端。
強化で装備つけたらその装備の見た目通りにも当たり判定あるように**」。

**言葉**
- つまみを **🛠艤装 → ⚔装備** に。⚠**知らない言葉をUIに出さない**=「艤装」は船の言葉で伝わらなかった。
  ⭐**ユーザーが使った言葉をそのまま使う**(「ノコとかの**装備**をつける」)。

**🚌当たりの形**
- ⚠⚠**それまで半径140の丸1つだった**=絵は300×192の箱+前へ突き出た排障器なので、
  **横は絵より39px外で死に / 先端は絵より64px手前までしか当たらなかった**。
- ⭐**`busBoxHit` にした**=**①車体の箱 ②排障器の台形 ③🪤鉄条網の箱(付けている時だけ)**+⚙丸ノコの円。
  ⚠寸法は全部 `BUSD_L`/`BUSD_W`(絵の寸法)から出す。⚠外接の丸で先に弾いてから箱を見る。
- ⚠⚠**向きも絵と同じ `busAng` から出すようにした**=前は当たりが「速度の向き」・絵が「`busAng`」で
  別々だった。丸の頃は向きが関係なかったので気づかなかったが、箱にすると止まった瞬間にずれる。
- ⚠**壊せる障害物(`BOBJ`)も同じ形**にした=丸のままだと横をかすっただけでドラム缶が爆発する。
- 🪤**鉄条網バンパー**=倍率(1.07)をやめて**前だけに箱を足す**形に。⚠倍率だと横まで7%広がって
  「前に張り出す装備」なのに横で死ぬ。⭐**絵も排障器の先へ描き直した**(前は車体の前面に貼った格子で、
  「前に張り出して当たりが広い」と書いてあるのに何も出ていなかった)。
- 📌**1走の轢いた数は 2073→1679体**=**それまで2割は絵の外で殺していた**。⚠難易度の調整と取り違えない。
- 🧪検査に「絵の中では当たる/絵の外では当たらない」を8通り足した(寸法が絵から出ていることも見る)。

## 2026-08-02(65) 実機の写真で3つ直した(文字のはみ出し / 🪤鉄条網 / ミニゲームの稼ぎ)
ユーザー実機①「**まず文字がおかしいから修正**」②「**鉄条網?ってやつは出っ張ってるようには見えるけど
そこに判定はないように感じた。バスの先端は判定あるからおk**」③「**ミニゲームで稼げる額が多い
(普通に人は完璧にクリアできちゃう)からすぐ強化終わる**」。

**①強化の札の文字がはみ出していた**
- ⚠⚠**文字は `dp` 基準・札の高さは画面の割合**という**別々の物差し**で置いていた=
  **縦の短い(横に長い)実機ほど札だけが低くなり、説明が札の外へ出る**。
- ⚠**手元の撮影(dp=2)では2pxしか出ず気づけなかった**(dp=3の実機では十数px出る)。
- ⭐**札1枚に要る高さに対する比(`bnsUpTextK`)で、文字の位置も大きさも全部縮める**ようにした。
  ⚠**比を出す所は関数1つ**=描く側と検査が同じ物を見る。
- 🧪検査に「色々な dp と高さで一番下の行が札に収まるか」の総当たりを足した。

**②🪤鉄条網バンパーに手応えが無かった**
- ⚠**排障器の先46px・幅64px**(車体の101より内側)では、**バスは毎フレーム20px進む**ので
  **そこに居た敵は次のフレームで車体に当たる**=効いた実感がゼロだった。
- ⭐**92px先・幅116px(車体より外)**にした=前も横も明らかに広がる。絵も同じ寸法から描いている。
- 📌**「当たりを広げる装備」は、主役の当たりより外へ出て初めて手で分かる**。

**③ミニゲームの稼ぎを半分にした**
- ⚠**上手い人は毎回ほぼ満点**なので、**満点の額がそのまま1回の稼ぎ**になる。
- ⭐下げたのは3つ=1回の点(`BMG_PT` 5→3)・早得(`BMG_FAST` 2.0→1.0)・おまけ(`BMG_SUB` .35→.25)。
- ⚠⚠**ミニゲームを難しくして調整しない**=難しくすると**下手な人だけが損をする**。
- 📌満点の1試合で**300前後**(前は約590)。全部そろえるのに**23試合前後**(合計約7000)。

**🐞ついでに直した=古いセーブで梯子が途中から始まっていた**
- 段階解放が入る前(2026-08-02(55)より前)は装備を単品で買えたので、
  **③だけ持っていて①②が🔒**という状態のセーブが残っていた(ユーザーの写真で発覚)。
- ⭐`beqFixLadder` で**持っている段より下は全部持っていることにする**(取り上げない)。

## 2026-08-02(66) 🌊2面の開拓便(海沿いのうねる道+高潮) / ⚓4種目のミニゲーム / 🎨UIの作り替え
ユーザー「**2ステージ目の同じ難易度の位置に同じバスのやつ作って。走るとこは直線じゃなくていい感じに。
あと新要素も欲しい。そしてミニゲームももう一種類だけ追加。あと全体的にUIを見直して見やすく
わかりやすくしてほしい。特に文字とかだけ並んでるとこ。新しいUIとかも作っていい**」
+「**UIは本当にあるソシャゲみたいな感じで。雰囲気は崩さず**」。

**🌊2面=海沿いのうねる道**
- 道のうねりを面ごとに分けた(`bnsWobAt`)=2面は**振り幅±510・波長8900px**(約7秒で1回切り返す)。
  ⚠**出発点の手前(`BNS_STR_U`)だけ真っ直ぐに戻す**=広場の廃墟が建たなくなるため。
- **右側が海**(`bnsSeaX`/`bnsSeaAt`)=マップを右へ 1300 広げ、道の中心から 1180 外を水にした。
  砂浜・浅瀬・深い海・白波・**桟橋と沈んだ船**まで焼き込みで描く。⚠海の上には木も建物も置かない。
- ⚠**辺が7本→260本**になったので、`pathPos`/`projPath`/`bnsRoadFit`/`bnsCorrFree` に**索引**を入れた
  (入れないと木を撒くだけで数秒・敵1体ごとに130回の走査になる)。

**🌊高潮(2面だけの新要素)**
- **予告2.6秒 → 厚み700の帯が道を横切る → 引く**を15〜10秒おきに繰り返す。
- バスは**陸側へ押されて最高速が74%**に落ちる。**群れも一緒に陸へさらわれて塊になる**。
- ⭐**ニトロ中は「🌊波乗り」**=押されず・遅くならず・ひき殺す力×1.35。
  ⚠**押す物がニトロ1つだけ**の面なので、新しいボタンではなく**そのボタンに使い道を足した**。
- ⚠**海側を全部水にする形は撮って捨てた**=画面の大半が青くなって盤面が読めない。**帯にした**。

**⚓引き揚げ(4種目・2面だけ)**
- 4棟目 **⚓波止場**(緑青の屋根+ブイ)を足した。もらえる物資は🏭鉄材(`BMG_K[].rs`)。
- 遊び=**沈む前に物資をタップ**(反応)。⚠**🧟が混ざり、押すと失敗**=連打よけがゲームの中にある。
  ⚠**見送りは無罰**(押さないのが正解なので罰を付けると理不尽)。満点は70前後で他の3種と揃えた。

**🎨UI(ソシャゲ風・雰囲気は据え置き)**
- 🏚**廃墟の選択画面**=世界に浮かべていた説明の札をやめ、**下のカード帯**にした
  (4つ並ぶと札も番号も必ず重なるため)。世界には**番号のピンだけ**残し、重なりも外す。
- 🎯**出撃準備**=難易度とステージを「**名前+状態**」の札に。長い説明文は**要点の帯**へ。
  砲撃と英雄はチップにして、**英雄は★の色を枠に出す**。
- 🦸**英雄ステータス**=数字だけの表をやめ、**21人の中での位置が読める棒**にした。
- ⚔**冒険のパーティ**=Lvの棒+力量5本の横棒。頭の数字並びはチップへ。
- 🎒**編成**=見出しの注記を小さく、合計⚙️はチップへ。作戦タイムの札は**数字だけ太く**。
- ⚠**タイトルとステージ2の文章から「死潮」を外した**(物語の決定に合わせた。仕組みの名前は据え置き)。

## 2026-08-02(67) ⚓引き揚げ「🧟は押すな」を絵で分かるようにした
ユーザー実機「**ゾンビをタップしちゃいけないっていう説明がどこにもないからわかりづらい**」。
- ⚠⚠**説明文はあったが、実機では消えていた**=見出しは左寄せ・右の「あと N 個 ⏱」は右寄せで、
  **間が足りるか誰も測っていなかった**。文字が大きい実機ほど右に食われる。
  ⭐**書く前に幅を測って、入らなければ縮める**(`measureText`)ようにした(4種とも)。
- ⭐**押してはいけない物は絵で言う**=🧟に**赤い縁+右上の✕の印**。
  ⚠**✕を顔の上に大きく重ねない**(ただの禁止マークになって何が混ざっているか読めない)。
- ⭐**遊び方の凡例を出しっぱなし**(📦○とる / 🧟✕押すな)。⚠連なりの行は下へ回した。
- ⚠**「取れ」の合図(残り時間の輪)は物資だけ**に付ける。🧟にも付いていたのが誤解の元だった。
- 🧟を押した瞬間は画面が赤く光る。

## 2026-08-02(68) 🎨ゲームの外の4画面をソシャゲの型に作り直した
ユーザー「**UIむしろ見づらい。ゲーム中じゃなくて出撃準備・連れて行く英雄・編成・研究所を改善して。
ソシャゲの画面がどういうUIなのかをしっかり調べて、こんな感じにしますっていう参考を見せてから作業して**」。

**先に調べて、モックを見せてから着手した**(承認後に実装)
- 原神・アークナイツ・FGO・モンスト・Asphalt9 の実画面 + Apple HIG / Material / WCAG を調査。
- 実機と同じ852×393のモック(ゲーム本体の絵をそのまま貼った物)を資料にして承認をもらった。
- 📌**分かったこと**=うまくいっている画面は**情報が少ない**。1画面に「いま何を選んでいるか」と
  「次に押す物」しか無く、中身は**タップした先**に畳んである。

**⚠前回(66)のやり方が逆だった**
- 「読みにくい」と言われた画面に**説明の帯と状態の行を足した**=**横画面(393px)で
  「見出し→説明→帯→一覧」と縦に積むのは調べた中で名指しのアンチパターン**(選択肢が押し出される)。

**入れたもの**
- 🎯**出撃準備**=1画面1目的。難易度は**横に送る大きな札**(名前/波数/状態の3つ)、
  砲撃と英雄は「**持って行く物**」3行(押すと専用の窓)。**出撃ボタンだけを主役**に。説明の帯は削除。
- 🦸**連れて行く英雄**(新しい窓 `#md-hpick`)=**顔の札を横に送る**。★は枠と地の色(`RARE`)。
  下の帯に必殺技とHP/攻撃。⚠**地は暗い色**にすること(薄い地に明色の名前で読めなかった)。
- 🎯**装備砲撃**(新しい窓 `#md-stk`)=絵+名前+効き目。選んだら閉じる。
- 🎖**編成**=枠を10個ぜんぶ表示(空きは破線の＋)・絵を44×24→62×34へ・注意書きを削除。
  ⚠**スクロールするのは一覧だけ**+**入れ替えても位置が飛ばない**(前は開いた瞬間に選択肢がほぼ見えなかった)。
- 🔬**研究所**=段のある項目に**進み具合の棒**(tag の "Lv3/20" から拾うだけ=足す側は無変更)。
- 文字の下限を10.5pxへ / 使わなくなったCSS(`.chipseg` ほか)を掃除。

---

## 2026-08-02(69) 🌊高潮を「罰」から「褒美」へ反転／🚌拠点開拓の並びと報酬／🚦リリースゲート

**なぜ**: (68)から止めていた2つの決定にユーザーの答えが出た(高潮=②に反転 / 道具=リリースゲート1本)。
続けて「拠点開拓は古参の次」「獲得研究P上限は撤廃」の2つの指示。

**入れたもの**
- 🌊**高潮の反転**=❌`BSG_PUSH`(陸側へ押す760)と ❌`BSG_SLOW`(最高速74%)を**削除**。
  ⭐**水を被っている間はずっと🌊波乗り**(ひき殺す力 ×1.35→**×1.6**・ニトロ不要)。
  群れを陸へ寄せる `BSG_ZPUSH` はそのまま=**波が来た→群れが固まる→突っ込んで稼ぐ**。
  予告の文言も「波にさらわれるな」→「**群れが寄る — 突っ込め**」へ(罰が無いのに脅すと嘘になる)。
  ⭐**負けの無い面に引き算だけの仕掛けを置かない**という掟を [[NOTES_面]] に足した。
- 🚌**難易度の並び**=`D_ORD` を `[0,1,BNS_D,2,3,4,5]` → **`[0,1,2,BNS_D,3,4,5]`**(古参の次)。
  ⚠**HTMLの札の並びを直し忘れて画面がちぐはぐになった**(検査は全部グリーンだった)=
  **検査「難易度の並び」を追加**(index.html の `data-v` の並びと `D_ORD` を突き合わせる)。
- 🧬**研究ptの上限・下限を撤廃**=`BNS_RPT_MAX`(3000)と `BNS_RPT_MIN`(100)を削除。**轢いた数がそのまま**。
  HUDの「/3000」も消した。歯止めは**1日5回**だけになった。1走2300前後=5日で研究所を全部開ける見当。
- 🚦**`tool_release.js`(リリースゲート)**=5本を順に流し1本でも落ちたら止める。
  ⭐**落ちない検査(`test_layout`/`test_balance`)は⚠の件数を `release_baseline.json` と比べ、増えた時だけ落とす**
  (いきなり exit 1 にすると元からある⚠4件で毎回赤くなって形骸化する)。★4のゆらぎは自動で1回流し直す。
  index.html の大きさと HEAD比も出す。⚠git hook には入れていない。
- ⚠**踏んだ穴**=`test_headless.js` の本体は**まるごとテンプレート文字列**なので、
  コメントにバッククォートや `${` を書くとスクリプトごと落ちる/正規表現のバックスラッシュは2重に書く。
  ([[NOTES_道具]] に追記)

---

## 2026-08-03(70) 🎯狙撃王ジョルジ(構え・コッキング・必殺の締め)

**なぜ**: ユーザー指摘4件「しゃがんだときぐらつきすぎ/一発ごとにコッキングと薬莢(音もしっかり)/
必殺の最後の攻撃がまた出ない/必殺技は射程無限に」。

**入れたもの**
- 🧍**「スッとしゃがんで狙いすます」**=①しゃがみ切るまでを溜めの35%→**18%**、曲線を smoothstep から
  **ease-out** へ ②`hTop` に**揺れの倍率**の引数を足して、据えたら常時の揺れ(±.042rad)を0に
  ③ケープの揺れも一緒に止める。⭐**ぐらつきの本体は `hTop` の常時の揺れ**だった。
- 🔫**1発ごとのコッキング**=`GEO_FT`(0.26秒)の中で **反動で銃が後ろへ滑る → 銃口が下がる(遊底を起こす)
  → 戻る**。遊底のレバーも足した。⚠**腕は動かさない**(二脚に据えているので銃だけ滑る)。
- 🥉**薬莢は「撃った瞬間」ではなく「遊底を引いた時」(0.10秒後)**に飛ばす+**音を3つ**に
  (撃発 / カチャッ / チャリン)。⭐**素材は1本も足していない**=`sfxPlay` に**高さの引数**を足して
  `thunk` と `coin` を流用(`sfxAtR`)。
- ✨**銃口の閃光を銃口に出した**=共通の閃光は `(26,-33)` の決め打ちで、しゃがんだジョルジだと
  **顔と照準器の上に巨大な橙の丸**が乗っていた。`H_MZOFF` で体側が自分で描く。
- 🎯⚠**必殺『撃墜』の締めを射程無限に**(ユーザー指示)=**これが「最後の攻撃が出ない」の正体**。
  前の9発は盤面のどこでも撃つのに、締めだけ**前方520以内**に限っていたので、
  9発で近場を掃除すると撃つ相手がゼロになり、誰も居ない方へ線が飛んでいた。
  貫通の上限(6体)は残す。前方が空なら盤面のどこでも狙う。
- 🧪**検査を2本足した**=①『撃墜』が射程の外の敵に届くか(⚠**わざと壊した版で落ちることを確認済み**)
  ②`test_shot.js` の `posefire`(撃った直後の5コマを撮る)。

---

## 2026-08-03(71) ジョルジの構えと音／紅蓮の着弾／派生タブのスクロール／道の長さ1.5倍

**入れたもの**
- 🎯**ジョルジが「斜め」に見えた正体**=傾きではなく**1発撃つたびに立ち上がって座り直していた**
  (しゃがみの深さを `u.chg` から出していたが、`u.chg` は撃つたびに0へ戻る)。
  ⭐姿勢用の別の値 `u.aimH` を持たせ、**止まっている間は下げない**。足元の輪と照準線は `u.chg` のまま。
  ⚠銃の角度は実測すると -2.9°(むしろ上)で、疑った所は全部シロだった。
- 🔊**音を作り直した**=`sfxRaw`(高さ決め打ち・遅延・間引きなし)を足して**素材を層として重ねる**形に。
  `geoBig`(要塞砲+重砲+爆発)/ `geoCock`(建設音2枚)/ `geoShell`(命中音を高く小さく2枚)。
  ❌`pierce`(ノイズ合成)と `coin`(⚙️入手のジングル)はやめた。全部 `?sfx=1` に出してある。
- 🔥**紅蓮の大魔導士の着弾ずれ**=通常攻撃のダメージは `H_FX` より前に**即時**で入っていたので、
  **玉が届く前に敵が倒れて**いた。⭐`H_FLY` を足して**飛び道具の英雄だけ当たり判定を飛行時間ぶん遅らせる**
  (紅蓮・軍医)。⚠威力・射程・間隔は1つも触っていない。
- ⭐**気絶の見せ方を★に**=`frzT` だけ立てて `stnT` を入れていない所が3つあった
  (撃墜 / 地響き / 騎槍の突進)=**氷漬けの絵**になっていた。氷は**本物の冷気だけ**。
- 🎖**⭐派生を選ぶタブが下までスクロールできない**のを直した=(68)で親を `overflow:hidden` にした時、
  このタブだけ**流れる箱が無かった**。`.ldscroll` を持たせた。
- 🔬**兵科を解放した時、編成が満杯でも警告を出さない**(ユーザー指示)=良い出来事に⚠を被せない。
- 🚌**道の長さを1.5倍**(BNS_H 118000→174650)=ユーザー「ニトロを使うと時間前にクリアしちゃって
  **ニトロを使わないほうがゾンビを多く倒せる**」。⚠遊びが逆立ちしていた。
  📌**実測した代償**=`startSolo()` が 1.5秒→**2.2秒**(木 57,000→84,784本)。実機はこの3〜5倍。
  重ければノブは `BNS_TSP`(木の間隔)。
- 🧪**検査を2本足した**=①ジョルジが撃つ間ずっとしゃがんだままか ②(70)の締めの射程。

---

## 2026-08-03(72) 🎛音の「調整中」タブ／コッキングの腕のモーション

- 🎛**`?sfx=1` に「調整中」タブを足した**(ユーザー指示)=**候補を並べて記号で選んでもらう枠**。
  先頭に置き、候補が0になったら自動で畳む。⭐**Claudeは音を聴けないので、これが唯一の決め方**。
  📌手順は [[NOTES_音]]。採用は `SFX_GEOBIG` の1行を書き換えるだけ。
- 💥**撃墜の締めの候補を5つ**(A〜E)=A 前のやつ(比較用)/ B 対物ライフル / C 重い単発 /
  D 地面を叩く / E 貫く。⭐**いまはBを採用中**(前の案は「①立ち上がりの鋭い頭が無い」ので
  大砲の爆発に聞こえていた=それが「微妙」の正体)。⚠**実機で聴いて選び直す前提**。
- 🖐**コッキングで腕と手が動く**(ユーザー「腕と手を動かしてコッキングのモーション作れる?」)=
  **引き金側の手だけ**が遊底を追って後ろへ引きながら持ち上がり、肘はその半分だけ動く。
  ⚠前の手(銃身を支える方)は握ったまま=両手を動かすと銃を持っていないように見える。
  ⚠反動では手をほとんど動かさない(反動は肩で受ける物)。

---

## 2026-08-03(73) 手持ちのバンドルを漁って、本物の素材で音を作り直した

**なぜ**: 締めの音が「微妙」→ ユーザー「他にないかな ないならElevenSounds契約して作るか」。
調べたら **Sonniss バンドル(347本)のうち23本しか取り出していなかった**ので、契約の前にそこを漁った。

**分かったこと**
- ⚠**バンドルに銃声は1本も無い**(名前で総当たりして確認)=「銃らしさ」は破裂+雷鳴で組むしかない。
- ⭐**機構音と小さな金属音は本物がある**(骨董の錠前・ブロートーチの蓋・巻尺・バネ線)=
  **コッキングと薬莢は合成をやめて本物にできた**。今回の一番の収穫。
- ⭐他に未使用の当たり=**本物の雷鳴**(鋭い頭+長い自然な尾)・トレーラーのブーム・至近の花火。

**入れたもの**
- 🎛**調整中タブに「★本物」の候補を8つ**足した=コッキング3種 / 薬莢2種 / 締め3種(F・G・H)。
  合成のA〜Eも比べ用に残してある。⭐**いまは F / コッキング1 / 薬莢1 を鳴らしている**。
- 🛠`tool_sfx.js scan` の不具合を直した(`mix` を渡すと落ちていた)＋**素材名を直に渡して調べる形**を追加。
- 📦 index.html は +49KB(音8本ぶん)。

---

## 2026-08-03(74) 音の選択を反映(締めH・コッキング3)／薬莢は作り直して第2陣

- ⭐**ユーザーの選択を実装**=締め`geoBigH`(破裂の頭→金属の胴当たりと残響)/
  コッキング`geoCkC`(巻尺のバネ→骨董の錠前)。
- ❌**薬莢は2案ともボツ**=バネ線と金属やすりでは「跳ねて転がる真鍮」に聞こえなかった。
  ⭐**コイン系の素材に切り替えて4案**(コインの鳴り/コインを弾く/落ちて跳ねる/鍵の跳ね)。
  📌**素材は「材質」ではなく「動き」で選ぶ**という掟を [[NOTES_音]] に足した。
- 🎛**調整中タブを整理**=決まった物とボツは🔫タワーへ移し(★採用中/(ボツ)の印付き)、
  🎛には**いま決めてほしい薬莢4案だけ**を残す。⚠落ちた案は消さない(選び直しが1行)。

---

## 2026-08-03(75) 薬莢=B(コインを弾く)を採用／🎛調整中タブは空になったので畳んだ

- ⭐**薬莢は `geoShD`(コインを1枚弾いた音)を採用**(ユーザー「Bいいね」)。
  これで狙撃王の3つの音(締め`geoBigH` / コッキング`geoCkC` / 薬莢`geoShD`)が全部決着。
- 🎛**調整中タブは候補が0になったので自動で畳んだ**(作った時の仕掛けがそのまま効いた)。
  ボツにした案は🔫タワーのタブに「(ボツ)」印で残してある=選び直しは1行。
- ⚠**踏んだ穴**=タブを畳む判定を**案内文より後**に置いていたので、
  **中身はタワーなのに「いま決めてほしい候補」という案内**が出ていた(撮って気づいた)。

---

## 2026-08-03(85) 引き継ぎの棚卸し／🏋鍛錬所は「設計から作り直す」宿題に

- 🏋**鍛錬所の方針が2つ決まった**(どちらも❌の決定):
  ❌**ドラクエ式の⚔冒険は考え直す**(ユーザー「ドラクエとか言ってたけど考え直そう」)。
  ❌**TDの盤面を使い回す案は却下**(ユーザー「同じとこ使うのはNG」)=
  こちらが勧めた「英雄1人で連続ウェーブを捌く試練」はこの一言で消えた。
  ⭐**白紙から遊びを決めるところ**=決める順は [[TODO]] の先頭。
- ❄**ボスだけインベーダーっぽく**=凍結(ユーザー指示)。
- 📄**棚卸し**=HANDOFF を(69)〜(85)の内容へ書き換え / CLAUDE.md を123→117行へ
  (🚌拠点開拓の明細は [[DESIGN]] と [[NOTES_面]] にあるので要約に畳んだ) /
  HOME に🎯ダミー・★の絞り込み・🎛調整中タブを足した。

## 2026-08-03(86)〜(94) 音の決着・②のギミック・ルピナ・シングルプレイの新動線
- 🏇**騎槍の突きを作り直して決着**=(86)「以外の音はOK」で1本だけやり直し→(87)「風切り→A」→(88)「G Aで」採用。
  ⚠ダメだった正体=**素材のかたまりの頭から切ると当たりが入らない**(前半0.17秒が風切り)。
  `tool_sfx.js` に `o:`(かたまり内のずらし)と `peak`(当たりの位置を測る)を追加。掟は [[NOTES_音]]。
- ⚔**必殺技の音を総点検**(91)「死の宣告の音がダサい。全部見直して」=
  ❌死の宣告と暁の号令が**勝利ジングル(win)**を鳴らしていた。7本を本物の素材で作り直し★仮で採用
  (ultDoom/ultDawn/ultHose/ultTime/ultBolt/ultQuake/ultRush)。漆黒の突撃はmSprGへ。
- 👑**暁の王**=攻撃音5案(dwnA〜E・仮B)/(89)**並びを★4の枠へ移動**(★5だった頃の位置に残っていた)。
- ⚔**連撃**=(88)発数が多いほど強く(cmbBon: 単発x1.00/二連x1.15/三連x1.30)/
  (91)2打目以降は届く幅+50=1体倒したら次の敵へ続きが流れる。
- 🎯**ジョルジ**=しゃがみは「射程に敵が居る間」だけ(居なくなったら立つ。(70)の常時待機を上書き)。
- 🌊**②沈んだ港のTDギミック**(90)(91)=高波→水浸し(敵の足x1.5/⚡電撃x1.5・下から波が押し寄せる)/
  🐟魚3種はたまに潜る/🐢ウミガメは甲羅ごもり(ダメージ2割)/敵HPx1.5・攻撃x1.3/研究Pを難易度別に底上げ。
- 💰**報酬**=難易度の初回クリアで💎(CLR_GEM=5/8/12/18/25/40)。
- 🌈**★5・22人目「跳弾姫ルピナ」**(92・参考絵から)=ゴムボールが敵間を1〜5回跳ねる(等確率)/
  必殺は3発・跳ねるたび+30%・最大10連鎖。体はプロシージャル(入れ子3層のポニーテール)。
- 💎**ガチャ**=(92)虹の文字と✨金ロブは**★5確定**へ(レーザー/どんでん返しは★4+のまま)。
  金の被せをタイトルと同じ濃さに(薄くて「ただの個体」に見えていた)。
- 🖼**アイコン**=(90)絵文字のまま残っていた物を洗い出し→シートE(砲撃5種)/F(✅🔒💀📖)を取り込み。
  ICOに✅🔒💀📖を追加(IC/drawIcoTextで自動置換)。PARTX・toastもゲームの絵に。G〜LはPROMPTS.md。
- 📏**遠距離**=(90)射程x1.2(近接5人除く)/立ち位置=旗の後ろ「射程の半分」(先端が揃う問題を解消)。
- 🏠🗺**シングルプレイの新動線**(93)(94)=タイトル「シングルプレイ」→ホーム(英雄の一枚絵+パネル)→
  エリアマップ(にゃんこ式・15拠点・難易度UI廃止)→いざ出撃。明細は [[DESIGN]]。
  (94)ホームのパネルに中身のプレビュー/マップ右下をカード専用の空き地に。
- 📱**PWAアイコン**を本物のゲームの絵で作り直した(初期の丸顔のまま放置されていた)。

## 2026-08-03(95) 編成まわりのUI総監査(「編成画面とかいろいろ徹底的に調べて本当の意味でいい感じに」)
- 🔬**多面監査→懐疑検証→実装→差分レビューの2段ワークフロー**で、編成/ホーム/マップ/英雄窓/砲撃窓/
  英雄ステータスの指摘28件(high6)を確定→全部直した。
- 🎖**編成・連れて行く**=⭐**派生が絵にも名前にも映るように**(uEff。以前は起動直後=素・1戦後=派生の
  絵と、開くたびに変わっていた)/✅札を**押すと外すトグル**に(disabledの死に札が先頭を埋めていた)/
  「ぜんぶ出すのに」→上の帯へ移して**「⚙️全員の解放に」**(0の時は出さない)/固定枠は🔒→**「固定」**
  +押すと理由をtoast/所持2種だけの時は「研究所で解放すると選べる」の1行/満杯時に低い音。
- 🎖**派生を選ぶ**=**札を幅160px固定+HP/攻を行内の棒**で(数字も残す。4枚目の折り返しと右の大穴を解消)/
  **スクロール位置を保持**(1つ選ぶたび先頭へ飛んでいた=実機報告と同型の生き残り)/🔒は絵の上に重ねて
  名前を切らない/ヒントに**入手経路**(🔒は研究所の兵科タブ)/研究所側の呼称を「進化」→**「派生」**に統一
  +派生を買った時に装着場所をtoastで案内。
- 🏠**ホーム**=**窓を閉じたらホームも書き直す**(renderPickRows末尾+研究所の✕。残高・パネル・一枚絵が
  古いままだった)/出撃ボタンの小文字=**次の拠点名**(静的な「エリア1」だった)/連れて行かない・未所持の
  名札を正しく(未所持は一枚絵を**影絵**に)/続きからに**拠点名**/英雄パネルに顔サムネ/編成パネルの
  サムネの縦潰れを解消(22x26→32x24)。
- 🗺**マップ**=🚌**使い切りの嘘を解消**(灰🔒+「手前の拠点を解放すると開く」→「今日は走り切った—次はあと○○」)/
  🚌クリア済みは「⚡自己ベスト・今日あと○回」(「何度でも遊べる」はTD拠点だけ)/「初回解放で」→**「初回クリアで」**/
  🔒ピンは一回り小さく薄く/出撃ボタン経由の**二重タップ音を解消**/バスをフレームレート非依存+到着時の反転を抑制/
  mc-stの全分岐をIC()に統一。
- 🦸**英雄窓**=**開いた時に選択中の札へ寄せる**(modalがdisplay:noneの間のscrollIntoViewは無効=rAFで1フレーム
  遅らせる)/顔サムネの縦1.7倍の歪みを解消(88x112へ直描き+fitBoxキーで高速化)/hp-footをIC()に(闇商人の⚙️が
  生絵文字だった)/📋くわしくを44pxに+音。
- 🎯**砲撃窓**=「✓装備中」の文字バッジ(色だけで状態を言わない)。
- 📊**英雄ステータス**=**上の情報を固定して一覧だけ流す**(編成と同じ型)+開いた時に選択札へ寄せる+
  スクロール保持/必殺行を金→錆色(紙地で読めなかった)/10.5px未満の字3箇所を引き上げ。
- ⚔**戦闘デッキ**=スマホのサムネ42x36→42x32(4:3。縦伸びを解消)。
- 📷**test_shot**=`ldsel`(派生を選んだ編成)/`hpick`(英雄窓)/`hstk`(砲撃窓)を追加。
- 📌新しい掟: **auto-fillのgridではgrid-column:1/-1が1列に潰れる**(案内は箱の外へ)/
  **display:none中のscrollIntoViewは無効**(rAFで遅らせる)→ NOTES_UI に追記。

## 2026-08-03(96) 画面の全面作り替え(「ホーム画面の配置からなにからなにまで完璧に」)
- ⚠(95)の監査だけでは「ほぼ変わってない」とお叱り=**見た目を大胆に作り替える方針へ**(memoryにも記録)。
- 🏠**ホーム=アークナイツ式に全面改装**=①全画面の情景(#homebg=夕暮れの廃墟スカイライン・沈む太陽・
  残照の帯・バリケード・漂う塵・ビネット。全部プロシージャル) ②英雄は枠から出して情景の上に立たせる
  (後光=レア色のラジアルグラデ+drop-shadow) ③名札は金線の帯 ④機能タイルは**暗いガラス板**
  (紙の枠をapplyFramesから除外)+アイコンの台座 ⑤出撃バナーは文字を大きく+光が周期的に走る(hsheen)。
- 🪟**全部の窓に共通の見出し帯**=`.bphd`を**墨の帯+左に金の背骨**へ(編成/英雄/砲撃/研究所/召集/
  ステータス/鍛錬所/図鑑が一斉に締まる)。カウンタ(.pt)は金色に。
- 🦸**英雄窓の札**=レア度の後光(data-rk+radial)/名前は黒グラデの帯の上/選択中は浮き上がり+金の光。
- 🎖**編成**=帯の札を金グラデ+番号バッジ(左上の墨タグ)に。🎯砲撃の絵は台座に。🗺拠点カードの名前も墨帯に。
- 検査🟢・852x393と780x360で全画面撮影目視。

## 2026-08-03(96b) タイトルと研究所も同じ意匠に
- 🌆**タイトルに夕暮れの情景**=ホームと同じ絵を共通化(`sceneBg(canvas,t)`)して #titlebg に毎フレーム描く。
  ゾンビの行進が情景の手前を歩く形に。ロゴ・メニュー・チュートリアルの段は不変。
- 🔬**研究所**=カテゴリ見出し(.labcat)を窓の見出しと同じ「墨+金の背骨」に/カードに薄い影で立体感。

## 2026-08-03(96c) 編成の中身も暗い札に統一(「編成画面の中とかは?」)
- 🎖**編成の札を英雄窓と同じ「暗い札」に全部統一**=**金の枠+✅=編成に入っている**の言い方で揃えた。
  ①上の帯=暗い札+金枠・番号は金バッジ・固定枠は鋼色の枠 ②下の一覧=暗い札+大きめの絵(96x58)+
  金の✅バッジ ③⭐派生タブも暗い札+選択中は金枠+光。
- 🐞**新しい掟**=スクロールするgridの札に `overflow:hidden` を付けると**行ごと14pxに潰れる**
  (Chromeの仕様。実物のDOMを見て特定)→ NOTES_UI に追記。

## 2026-08-03(97) マップの言葉と作り込み(ユーザー指示3点)
- 🗣**「ナイトメアが解禁された」等をやめて拠点名で言う**=①初クリアのtoast「🏅拠点『○○』を解放!」
  ②リザルトの見出し「🏆『○○』解放!/👑『○○』制覇!!」 ③🌑解禁「最果ての拠点『終末の廃線』への道が開いた」
  ④次の拠点/新エリアが開いた時のtoastを新設 ⑤リザルトの行「拠点を初解放」 ⑥研究所の記録タブも拠点名
  ⑦⚔冒険の門の条件文も拠点名。
- 🎨**ピンを手強さで色分け**(ユーザー指示)=旧鬼軍曹(d3)=青(#5fa8c9)/悪夢(d4)=赤(#d24a32)/
  🌑(d5)=藍(#2c4770)。⭐クリア済みも段の色のまま✅を重ねる(緑に塗ると手強さの色が地図から消える)。
  ★は濃い地(d4以上)では紙色。下に小さな凡例(●手ごわい ●危険 ●極限)。
- 🗺**地図の作り込み**=紙の繊維の点描/海岸線+砂浜の点描/山なみ/枯れ木/沈没船/???の一角に
  未踏のもや+斜線/道は轍の帯+点線の2層/🧭方位/四隅の焼け。全部プロシージャル。
- ⚠canvasの文字色はPANE/INK(表の定数)で=直書きはcheckPaletteが落とす(1回踏んだ)。

## 2026-08-03(98) 盾マーク/召集の帯/ルピナ新必殺/チュートリアルの強調(ユーザー指示4点)
- 🛡**すり抜けを足止めできる兵科に盾マーク**=戦闘デッキ(左上)と🎖編成の一覧(右上)。hook持ち(巨漢/鎖使い/
  長槍兵/装甲車と派生)に自動で付く。
- 💎**召集の演出中は見出しの帯を消す**(ユーザー写真=黒帯が「撃て!」に被っていた)=`#md-gacha.fx` で
  帯を透明化・✕だけ残す(閉じる手は塞がない=掟)。
- 🫧**ルピナの必殺を「バブルガム・ボム」に作り替え**(ユーザー指示)=巨大なガムをふわりと放つ(2.1秒)→
  大破裂(道のり±280=かなり広い・760xW)→**ガムがべたつく**: カラフルな粘つき(3色の粒+白ハイライト+
  足元へ糸)を体に描き、**10秒間 足×0.3+受けるダメージ×2**(=防御力半分)。ノブは GUM_T/GUM_SLOW/
  GUM_DEF/GUM_RNG/GUM_FLY。旧・跳弾3発は廃止(通常攻撃の跳弾は不変)。
- 🔦**チュートリアルの強調を全面強化**(ユーザー「黄色の丸が薄くてだいぶわかりずらい」)=
  ①**スポットライト**(#tutspot=押す所以外を暗く落とす+金枠+光。毎フレーム追従・終了時に必ず消す)
  ②盤面の輪も「まわりを暗く落とす+白フチ+太い金輪」 ③押す所の枠を白フチ+光に ④矢印を44pxに拡大。
- 🎓**新動線の段を追加**(home9=シングルプレイ→ホーム→マップの案内)+古い言い回しを一掃
  (「タイトルの研究所/編成」→ホーム・「ソロ作戦で腕を試せ」→シングルプレイ・中断の案内)。
  ⚠検査の「押せない代表」#bt-solo が home9 の光らせ先と衝突=代表を #bt-opt に変更(test_headless)。
- 🐞**tutEnd が2つあった**(前の定義は後勝ちで一生呼ばれない死にコード)=古い方を削除。

## 2026-08-03(99) 英雄タップで必殺の詳細/開拓便の初回チュートリアル(ユーザー指示)
- 🦸**戦闘中に盤面の英雄をタップ→必殺の詳細**(名前+充填%+説明の札が下中央に6.5秒。押す邪魔はしない)。
  ⚡**必殺ボタンから技名を削除**(途切れて読めなかった=大きな⚡だけに)。**hoverのtitleも廃止**(スマホで一生見えない)。
- 🚌**開拓便の初回チュートリアル**(「開拓便まで行ったらね」)=初めて入った時だけ、走る前に3枚の札を
  タップで送る(ようこそ/スティックと轢き・コンボ・🔥ニトロ/時間制と🎒物資)。`META.bnsTut` で1回きり。
- 🐞test_shot の裸の `/tut/` が `bnstut` に一致して🎓が混入=オプションの正規表現を区切り付きに修正。

## 2026-08-03(100) 英雄ステータスの作り込み/ガチャの目玉タップ/ルピナの弾(ユーザー指示5点)
- 📐**必殺の説明を高さ固定**(ユーザー「1行だったり2行だったりで画面が動く」)=`.hsul` を2行ぶんの
  高さに固定+`text-size-adjust:100%`(iOSの自動拡大止め)。⚠札を押すたびに下の一覧が上下しなくなった。
- 👆**下の英雄札を広げた**(118px→160px・高さ44px以上)=狭くてスクロールしづらかった。
  「未所持」を行頭へ+1行に切って…で省略(実機dp=3で縁に切れていた=ユーザー写真)。
- ▶⭐**必殺技のデモ映像**(ユーザー指示)=姿の枠を押すと**本物の仕組みで**再生
  (newCampの小さな箱庭に英雄1体+倒れないゾンビ7体を置き、`heroUlt`→`campStep` を回す)。
  4.2秒ごとに撃ち直してループ。⚠Gには触らない/音は鳴らさない(isMe=false)。撮影は `hstat+hsdemo`。
- 💎**召集の目玉タップ→ステータス**=正面のキャラを軽くタップ(移動10px未満)でその英雄の画面へ。
  ⚠スワイプ(36px以上=入れ替え)とは別扱い。
- 🎾**ルピナの通常攻撃を「ゴムボール」に**(ユーザー指示「弾もっとでかくして遅く」)=細い線をやめ、
  **大きな玉(半径6.5〜9px)が0.42秒かけて弧を描いて飛ぶ**(粒の寿命をずらして進みを表現)+ツヤ入り。

## 2026-08-03(101) 英雄の顔を画像に(方針変更)+ルピナの歩行の構え
- 🎨⭐⭐**「絵は全部プロシージャル」の縛りを撤去**(ユーザー「その方針削除していい。変な奴以外品質最優先で」)。
  ⭐**顔だけ画像**にできる仕組みを作った=`FACE`(base64)+`hFcImg()`。**体・武器・髪の揺れはプロシージャルのまま**。
  ⚠画像が無ければ今までどおり描く(壊れない)。⚠ヘッドレスには Image が無いのでガード(1回落ちた)。
- 🛠**道具2本**=`tool_face.js`(背景抜き+外周を詰めて256pxへ)/`tool_face_embed.js`(index.htmlへ埋め込み)。
  ⚠**元画像がすでに透過なら何も抜かない**=RGBだけ見て「背景は黒」と決めると**絵の黒フチごと消える**(2回消した)。
  ⚠背景が不透明な絵は**外周からの塗りつぶし**で抜く(内側の黒=目・フチは残る)。
- 🌈**ルピナの顔を差し替え**=見本の比率(顔幅≒肩幅の半分/頭≒全高の37%/首は描かない)に合わせた。
  ⚠**首を描くと顔が浮く**・**小さいと顔だけ別物に見える**(どちらもユーザー指摘で直した)。
- 🎾**歩行中は銃口を上げて構える**(ユーザー指示)=**片手撃ち**なので銃の角度は手前の腕だけ、空いた手は脇で振る。
  ⚠**bHold は溜め中(rc>0)でないと動かない**=歩行の構えは自前で回す(1回踏んだ)。
  ⚠角度は浅く(-0.34)=深いと銃が顔に被る/体を横切る。歩みの波で±0.06揺れる(「固定の型」に見えない)。

## 2026-08-03(102) 顔と体の合わせ方を仕組み化+首の受け/開拓便の文言修正
- 🏚**開拓便チュートリアルの嘘を修正**(ユーザー指摘「ガレージの強化ってのがバスの強化だし、
  物資はミニゲームで稼ぐ」)=①**画面の順番どおり**に並べ替え(ようこそ→廃墟を漁る→バス強化→操作→締め切り)
  ②🎒物資は「走って」ではなく**廃墟漁りで**稼ぐと明記 ③「ガレージ」→画面の見出しと同じ**「🔧バス強化」**。
- 📐⭐⭐**顔と体の合わせ方を仕組み化**(ユーザー指示「見本と見た目で判断だとうまくいかないから仕組化」)=
  `tool_face.js` が**絵から肌の色を拾って接続点を自動で測る**(顔の幅/顎の高さ/顔の中心)→`FACEM` に保存。
  体の側は `FIT`(顔幅は肩幅の0.80倍・顎の高さ・首の幅)だけを持ち、**位置と大きさは式で決まる**。
  ⭐これで絵が変わっても髪型が変わっても、目分量なしで正しく合う。
- 🧣⭐**丸い首の受け**(ユーザーが原因を特定「首が四角いのに体側に首の設置場所がない」)=
  ①角を丸めた肌の首を顎から襟へ通す ②根元を**浅い楕円の襟**が囲む。
  ⚠襟を大きくすると**胸に白い皿**が乗って見える/顎を上げすぎると**ろくろ首**(どちらも撮って直した)。
- 🌈ルピナの**髪の2重を解消**(顔の絵に髪が入っているので、コードのポニーテールは描かない)。
- 🐞`faceImg` のガードは**道具(tool_face_embed.js)側に持つ**=index.html を手で直しても埋め込みで消える(2回踏んだ)。
- 🐞(102b)**背中の黒い塊を解消**(ユーザー指摘)=奥の腕を真下へ畳んだせいで肘から先が細い板になり、
  黒フチだけが残っていた。前方向へ短く畳む形に直した(掟は [[NOTES_絵]])。

## 2026-08-03(103) TD中の英雄=★をやめて頭上にHPバー(ユーザー指示)
- ⚡**盤面(TD中)は英雄の頭上の★を出さない**=代わりに**HPバーを常時**出す(1試合1回の切り札なので
  「あとどれだけ保つか」が常に読める必要がある)。残量で色が変わる(緑→琥珀→赤)。
  ⚠★は図鑑・編成・召集など「見て選ぶ場所」には今までどおり出る。
- 🐞**焼き込みスプライト(bakeU)にも★が入っていた**=`fld:1` を渡して焼く時も出さない。
  ⚠でないと「動いている間は消えて、止まると★が出る」という食い違いになる。
- 🐞**高さは体の拡大率(TSCALE)を掛ける**=★は drawUnit の中(scale済み)で描かれるので、
  外から同じ数字を使うと**顎の辺り**に出た(ユーザー指摘で判明)。

## 2026-08-03(104) 🏋鍛錬所を「普通の強化画面」として開けた(遊びの中身は保留)
- ⭐**ユーザー決定**=「どういうゲームにするかはちゃんと考えたいから一旦未定にしよう。
  普通に強化画面であのリソースで強化できるように一旦しとこう。ヒーローレベルは最大50にしとこう一旦」。
- ⭐**先に「遊びの中身」を詰めて、いったん寝かせた**(全部 [[TODO]] に書いてある)。
  ユーザーの原案=「プレイヤーがゾンビを召喚してヒーローを追い詰めるほど経験値」=**立場の逆転**。
  ⭐こちらが詰めた芯=**HP30%以下の「追い込み中」だった秒数が経験値**/**英雄は自然回復する**ので
  手を緩められない/**倒したら即終了**=1分ずっと綱渡り。⚠**「深さ」で測る案は却下**(一瞬で最大に届く)。
  ユーザー決定=尺60秒 / **📖図鑑で倒した種類だけ召喚できる** / 5体編成は無し。
- 🏋**入っている形**=`TR_OPEN=true`(2026-07-26に閉じてから初めて開けた)/
  **🔧を払って直接 鍛錬Lv+1**(`TR_NEED`/`TR_XP`/`META.hxp` の**経験値の中間層は撤去**=
  あれは⚔冒険RPGの橋渡しでしかなく、冒険をしまった今は無意味だった)/
  **上限Lv50・`TR_PER`=.02(1Lvごと+2%・Lv50で+100%)**/値段 `TR_COST(lv)=3+floor(lv/10)*3`(通しで🔧450)。
  ⚠**行き先はほぼ据え置き**(+80%→+100%)。刻みを1/4にしただけなので難易度は動かしていない。
- 🏠**入口をホームのタイルへ移した**(`hm-train`)=タイトルのボタン(`bt-train`)は**もう出さない**
  (動線がタイトル→ホームになったため。要素だけ残す=アイコン差し替えの表が参照する)。
  ⭐**解放前も🔒で灰色のまま置く**(`.homebt.lk`)=施設の存在を先に見せる。
- ❌**⚔冒険のタブを外した**(お蔵入りのまま。`renderAdv` 以下のコードは残してある)。既定タブは🦸鍛える。
- 🐞**ホームの出撃ボタンの小文字が切れていた**=タイルが3行になって押し潰された。`.homego{flex:0 0 auto}`。
- 🐞**`test_shot.js` の `trhome` が撮れなくなっていた**='trhome'.indexOf('home') が当たるので
  ホームの枝が先に食っていた(2026-08-03(93)にホームを足した時から)。⭐**`home+htrain` を足した**
  (いまの本当の動線=ホームから鍛錬所を開いた絵)。

## 2026-08-03(105) 🦸ヒーローモードの構想を置いた+没の理由の読み違いを訂正
- 🎬**ユーザーのアイデア**(アイデア止まり・着手しない)=🏠ホームに**画面切り替え(ヒーローモード)**ボタン→
  **紙芝居を差し替える演出**で切り替え→**TDとは別のヒーロー専用ゲームを2つ目としてちゃんと作り、
  そちらで英雄を育成する**。全文は [[TODO]] の「🎬アイデア置き場」。
- ⚠⚠**訂正**=🕳洞窟と⚔冒険が没になった理由は**「クオリティが低そうだったから」**(ユーザー明言)。
  ⭐**「別ゲームを丸ごと作ったから死んだ」はこちらの推測で、外れていた**。
  この読み違いを土台に「2本目を作るのは危険」と言いかけた。**構造ではなく品質が門番**。

## 2026-08-03(106) 🎫★5確定チケットの配布 / ホームの戻るを上の帯へ
- 🎫⭐**ログイン配布**(ユーザー指示「俺は消してっていうまで、今からログインした人に
  星5確定チケット配布してくれ 1枚だけ」)=`GIFT={id,tk5,n}` を1つ置いて、起動時に
  `META.gft[id]` が無ければ渡して印を立てる(**1人1回きり**)。受け取った回だけトーストで報せる。
  ⚠⚠**やめる時は `GIFT` を null にする1行だけ**。⚠**`id` は変えない**(変えると重複配布になる)。
- 🎫**★5確定チケット(`META.tk5`)**=💎召集の3つ目の大ボタン(**持っている時だけ出す**)。
  1枚で★5の英雄が1人。⚠**確率(`G_RATE`/`gcPick`)には一切触っていない**=★5から選んで
  通常の `gcApply` に流すだけ。⚠重複も出る(🔧15になる)。
  🏠ホームの「英雄召集」の札にも🎫の枚数を出す。🛠DEVは常に99枚(底上げ)。
- 🐞**召集の右の列がはみ出した**=横画面は高さ393pxしか無いので、3つ目の大ボタンを足すと
  📖図鑑と確率が切れる(撮って判明)。⭐**チケットを持っている間だけ詰める**(`.gcside.tk`)。
- 🏠⭐**ホームの戻るを上の帯の左端へ出した**(ユーザー「下に埋もれてるから場所変えて小さいボタンに」)=
  44pxの小さな暗いタイル。機能タイルはちょうど6つ(3×2)に戻った。
- 🐞**紙の枠(border-image)が戻るボタンにも貼られていた**=`.btn:not(.sm):not(.homebt)` の除外に
  `.homeback` を足した。⚠この枠は `padding` まで上書きするので、貼ると寸法ごと崩れる。

## 2026-08-03(107) 召集の下の空きを直す / 名札のはみ出し / ルピナの件は仕様
- 🐞⭐**💎召集の右の列の下に空きが出て、中身が上に寄っていた**(ユーザー指摘)。
  原因=(106)で3つ目の大ボタンを入れた時、**決め打ちの padding/font で詰めた**こと。
  852x393 では合っていたが、**画面が高いほど余りが増える**(932x430 で下に44pxの空き)。
  ⭐直し=**大ボタンが残りの高さを分け合う**(`.gcbig{flex:1 1 0;min-height:46px}`)。
  ⚠押す所44pxは `min-height` で守る。⚠決め打ちで詰める直し方は捨てた。
  ⭐**getBoundingClientRect で実測して確かめた**(852x393 / 932x430 のどちらもぴったり埋まる)。
- 🐞⭐**名札の必殺の説明が左右にはみ出していた**(跳弾姫ルピナ)。
  ⚠**canvasの文字は折り返しも切り落としもしてくれない**=`measureText` で**収まるまで字を小さくし、
  それでも無理なら末尾を「…」で切る**ようにした。掟は [[NOTES_UI]]。
- 📌**「ガチャにルピナが居ない」は仕様**(直していない)=「いま狙える最上位」は
  **まだ持っていない英雄しか出さない**(`gcStarList`)。持った瞬間に並びから外れる。
  ⚠元の意図=「持っている英雄が出続けると引く理由が画面から消える」。変えるならユーザーの判断待ち。

## 2026-08-03(108) 🏠ホームの下が切れていた(▶続きからが出ている時だけ)
- 🐞⭐⭐**ユーザー写真で判明**=🏠ホームで**タイルの下の段が画面から切れていた**。
  原因=**▶続きから(`bt-home-resume`)が出ていると大ボタンが2つ縦に並ぶ**のに、その状態を一度も撮っていなかった。
  ⚠こちらの3回の撮影は全部「続きから無し」=**同じ画面を3回撮って3回とも見逃した**。
- ⭐直し=①`.homeright{min-height:0}`/`.homegrid{min-height:0}`(flexの子は既定で中身より小さくなれない)
  ②**▶続きからが出ている時だけタイルを詰める**(`.homegrid.cmp`=台座32px・文字12px)
  ③`#bt-home-resume` を出撃より控えめな高さに。⚠**押す所44pxは `min-height` で守る**。
- ⭐**getBoundingClientRect で実測して確かめた**=852x393 と 932x430 の**続きから あり/なし の4通りで
  はみ出し0件・タイルの下端が画面の下端にぴったり**。
- 🛠**撮る口を足した**=`node test_shot.js out.png 852 393 "home+hresume"`(一番きつい形)。
  ⚠⚠**`test_layout.js` はDOMをスタブしている**のでCSSのはみ出しは1件も見つけられない。掟は [[NOTES_UI]]。

## 2026-08-03(109) 実機で下が切れ続けた真犯人(vh)を直す / 👁実測検査を作る / ホームのタイルを横並びに
- 🐞⭐⭐⭐**真犯人=`.homewrap{height:min(94vh,480px)}` の `94vh`**(ユーザー写真2枚で判明)。
  **iOSの `vh` は「ツールバーが隠れている前提の大きい方」**なので**実際に見えている高さより長く**、
  その差ぶん中身が画面の下へ潜っていた。⭐`min(100vh,var(--appH,100vh),486px)`+`max-height:100%` に。
  ⚠⚠**`.modal .panel.bigpanel` は前から正しく書いてあった=`.homewrap` だけ直し忘れ**だった。
  ⚠**PCのブラウザでは絶対に再現しない**(vhと実高さが一致するため)。掟は [[NOTES_UI]]。
- ⭐**上下の遊びを削った**(ユーザー「上にちょっと詰めれない?空いてるから」)=94%で持っていたので
  上下に約24pxの死に地があった。**高さいっぱいに使い、余白は上4/下8だけ**(下はホームインジケータ避け)。
- 🎨⭐**ホームのタイルを横並びに**(ユーザー「無理に2列にしなくていい。横とか良い感じに」)=
  **絵は左・文字は右の2行(名前/中身)**。⚠b と i を `.hlb` でひとまとめにする。
  ⚠編成のサムネは**4体→2体**(横並びだと4体で幅を使い切って名前が読めない)。
- 👁⭐⭐⭐**`test_view.js` を作った**(ユーザー「作って良いよ」)=**本物のヘッドレスChromeでページを開いて
  `getBoundingClientRect` を実測する検査**。14場面 × ✂はみ出し/🔀重なり/👆44px/🔡10.5px。
  ⚠⚠**`test_layout.js` はDOMがスタブ**なのではみ出しを1件も見つけられなかった=今回の3連続の見逃しの理由。
  ⭐**リリースゲートに組み込んだ**(6本目・1.5秒)。⚠基準は `release_baseline.json` の `view_852x393`。
  ⭐**`--window-size` は窓の外枠**(852x393を頼むと836x298)=**先に測って差を足す**ようにした。
  詳しい使い方と落とし穴は [[NOTES_道具]]。
- 📌**この検査が今わかっている元からの分**=はみ出し1(研究所のタブ3px)/重なり1(鍛錬所の足のボタン)/
  44px未満37/10.5px未満72。⚠**増えたらゲートが落ちる**。

## 2026-08-03(110) 召集を固定 / 英雄ステータスに「←戻る」 / 下の一覧を広げる(ユーザー指摘3件)
- 💎**召集の窓を固定した**(「スクロールできないようにちゃんと固定して」)=`#md-gacha .bpbody{overflow:hidden}`。
  ⚠**📖図鑑と確率の面だけは中で流す**(22人ぶんは1画面に収まらない)=`#gc-sub` に `overflow-y:auto`。
  ⭐実測で確認=召集の面 scrollHeight==clientHeight(動かない) / 図鑑の面は動く。
- 🦸⭐**英雄ステータスに「←戻る」を足した**(「ガチャに戻るボタンが無くてホームに戻るしかない」)=
  `openHStat(id,from)` で**どこから開いたかを覚える**(`HSFROM`)。見出しに**行き先を文字で出す**
  (`← 召集` / `← 英雄`)。⚠**直に開いた時は出さない**(行き先が無いのに戻るボタンがあると嘘になる)。
  ⚠**✕(全部閉じてホームへ)とは別物**として両方置いた。3経路とも実測で往復を確認。
- 📋⭐**下の一覧を広げた**(「各ヒーローのレア度見るやつ狭い」)=**一度に見える数 8枚 → 最大15枚**。
  ①**棒4本を2列に畳んだ**(横に空きがあるのに縦へ積んで下を28px圧迫していた。⚠640px以下は1列に戻す)
  ②姿の枠 158→132px ③札の最小幅 160→128px(4列→**5列**) ④名前・余白・説明の詰め。
  ⚠**44pxの掟は守った**(札は134x44)。
- 🐞**`test_view.js` が英雄ステータスで別の窓を測っていた**=`openHStat` は他の窓を閉じないので、
  `.modal.on` の**1枚目**(前の場面の窓)を拾っていた。⭐場面の頭で `MDX()` して直した。
  ⚠**この取り違えのせいで「←戻る」が測られていなかった**=直した結果 44px未満が 37→38 になった。
- 📌**基準を38に上げた**=増えた1件は**新しい「←戻る」(30x28)**で、**既にある✕と同じ形**。
  ⚠モーダルの見出しのボタンは**元から44px未満**(高さ393pxしか無いので見出しを厚くできない)=
  **既知の例外**。⚠**他の場所で44pxを割ったら今までどおり落ちる**。

## 2026-08-03(111) ▶必殺技のデモを「必殺技だけを見せる舞台」に作り直した
- ⚠**元の作りは本番の盤面をそのまま回していた**ので、ユーザー指摘の3つが同時に起きていた=
  ①**ゾンビが歩いて英雄を追い越す** ②**必殺の合間が通常攻撃で埋まる** ③**英雄も歩くので的が射程から
  外れて必殺が空振り**する。⭐**盤面ではなく「舞台」にした**(絵と技の中身は本物のまま=嘘はつかない):
  - **的は歩かない(sp=0)・殴らない(atk=0)・倒れない(dummy)**
  - **英雄はその場から動かない**(毎コマ d を戻す。⚠**px/py には触らない**=踏み込みの動きが死ぬ)
  - **通常攻撃を封じる**(毎コマ cd を先送り)=映るのは必殺技だけ
  - **的は必殺が必ず届く近さに並べる**(44〜244)=近接の必殺でも空振りしない
  - **撃つ条件(hOut/hCg/dead)を毎回そろえてから呼ぶ**=これを怠ると「不発」になる
  - **技が終わったら0.8秒で撃ち直す**(⚠遅れて出る締めの一発は演出が引くまで待つ)
- ▶⭐**見ている間は窓いっぱいで見せる**(ユーザー「ちゃんと必殺技を見れるように」)=数値と一覧を畳む。
  ⚠**canvasの面を箱の実寸で作り直す**(width/height属性とCSSの寸法は別物)=でないと横に伸びる。
  ⚠止めると全部元に戻す(class・canvasの面の両方。片方でも戻し忘れると画面が壊れる)。
- 🎥**寄りと中心を「英雄と的が全部入る」ように計算**=英雄を真ん中に置くと右端の的が枠から切れていた。
- ⭐**22人ぶんを実測で確認**(本物のブラウザで1人1.7秒ずつ回した)=
  **エラー0 / 通常攻撃0 / 英雄が動いたコマ0 / 全員の必殺が発動**(映っている割合は最低50%)。
  ⚠「ゾンビが動いた」が5人で出るのは**押し戻し・引き寄せの必殺そのもの**(消防士/配達人/磁力技師/
  鋼鉄の巨人/暗黒の騎士)=正しい動き。

## 2026-08-03(112) 暗黒の騎士のデモが崩れていた(舞台を道の曲がり角に置いていた)
- 🐞⭐⭐**ユーザー指摘**「暗黒の騎士のやつ、ゾンビ浮いてたり道から外れたり左向いてたりする」。
  ⚠**原因は1つ**=舞台を**道の真ん中(PLEN*.5)に置いていたが、そこは道が折れている所**だった。
  『漆黒の突撃』は**敵を1周で600以上も押し出す**ので、押された敵が**曲がり角の向こう**へ出て
  ①舞台に描いた**まっすぐな道の帯から外れる**(=浮いて見える)
  ②英雄から見て**左右が裏返る**(=左を向く) が同時に起きていた。**実測で(1400,175)対(1100,545)**。
- ⭐直したのは4つ:
  ①**舞台を道のまっすぐな区間へ移した**(`HSD_D0`=260。d=0〜300は y が変わらない)。
   ⭐**hDarkの突撃は d=0 まで駆ける**ので、ここに置くと**駆け抜けが舞台に収まる**。
  ②**道を本物の経路をなぞって描く**(まっすぐな帯の決め打ちをやめた)。
  ③**押し出しを「立ち位置からのずれ幅」で挟む**(⚠一律の床だと6体が1点に積み上がっていた。
   ⚠幅は立ち位置に比例させる=道の端の2体が床で重なるため)。
   ⚠**d を挟んだら描く位置も道の上へ置き直す**(campStepは d から px/py を出すので1コマ浮く)。
  ④**英雄は必ず的の方を向く**(⚠決め打ちにしない=道の向きは場所で変わるので**実際の x を見て**決める)。
- ⭐**22人ぶんを実測で再確認**=**向きが逆0 / 通常攻撃0 / 英雄が動いた0 / 全員の必殺が発動**、
  **必殺が映っている割合の平均74%**。道から外れは20人が0、残る2人(配達人・磁力技師)も
  **1.5秒中2〜3コマだけ**(位置を直に動かす必殺なので同じコマでは追いつけない)=目には見えない。

## 2026-08-03(113) 支援枠=🏥野戦病院だけに / 野戦病院が英雄も「ちゃんと」回復する
- ⭐**ユーザー指示**「支援枠の塔、電波塔いらない。あと物資なんとかも。それと野戦病院はヒーローも回復するように」。
- ❌📡**電波塔**と📦**物資投下所**を外した。⚠⚠**配列(TOWERS)からは消していない**=消すと
  **後ろのグレードアップ専用の番号が2つずれ、中断データ(saveRunは塔を番号で持つ)の廃品工房が別の塔に化ける**。
  ⭐`off:1` の印で**建設リストからも建てる判定からも外す**(戻すなら印を消すだけ)。
- ⭐**支援枠を2→1にした**=建てられる支援施設が🏥野戦病院だけになったので、
  **2つ目の枠は永久に空く**(「同じ施設は1つまで」の決まりがあるため枠だけ増やしても埋まらない)。
  ⚠マスも使うぶんだけ作る(SLOTS 27→26)。⚠**番号は0〜25まで不変**なので中断データは無事。
- 🏥⭐⭐**野戦病院が英雄も「ちゃんと」回復する**。
  ⚠**元から英雄も回復してはいた**(英雄も C.units に居る)が、**毎秒16はHP5400〜7000の英雄には0.3%/秒**で、
  見た目にも手応えにも何も起きていなかった=**「回復していない」というユーザーの指摘は正しい**。
  ⭐英雄だけ**最大HPの割合**にした(`SUP_HERO_P`=1.2%/秒。HP6800なら**毎秒82**=5倍)。
  ⚠**兵科側(毎秒16)は1ミリも変えていない**=難易度を動かさないため。
- 🧪検査も作り替え=**英雄の回復が割合になっているか / 外した2つが建たないか / 支援枠が1つか**を見る。

## 2026-08-03(114) 英雄ステータス=下の一覧を削除 / 数値のはみ出し / 必殺デモが縦になっていた
- ❌**下部の英雄一覧を消した**(ユーザー「この画面の下部のリスト削除で。要らないわ」)。
  ⭐英雄を選び直すのは💎召集と🦸英雄を選ぶ窓の役目=**ここは1人を見る画面**に徹する。
  ⭐空いたぶん**姿の枠を縦いっぱい(横210px)に伸ばした**。⚠**枠の実寸に合わせて canvas の面を作り直す**
  (264x316の決め打ちのままだと姿が潰れる)。⚠★の冠が切れるので余白を10→18に。
- 🐞**「毎秒○○」が棒に重なってはみ出していた**(ユーザー写真=鍛錬Lv5の暗黒の騎士「毎秒1,020」)。
  原因=(110)で棒を**2列に畳んだ**ので数字の欄が狭かった。⭐一覧を消して縦に余裕ができたので
  **1列に戻し、数字の欄を広げた**(鍛錬Lv50の「9,999 毎秒9,999」でも収まる幅)。
- 🐞⭐⭐**必殺技のムービーが縦になっていた**(ユーザー写真)。
  ⚠原因=(112)で「道のまっすぐな区間」を **d=260 と決め打ち**していたこと。
  **道の形はステージごとに違う**うえ、**🚌拠点開拓の道は縦一直線**なので、
  **拠点開拓を遊んだ後に開くと舞台まで縦になっていた**(実測=道が 1700,900→1704,1500)。
  ⭐直し=**その場で道を測って一番まっすぐ横に伸びている所を探す**(`hsdFlatD0`)。
  ⭐**横の区間が1つも無ければ、試合中でない時に限りステージ1の道を読み直してから測る**。
  ⚠実測で再現→修正を確認(拠点開拓の道のままでも英雄と的が同じ高さに並ぶ)。

## 2026-08-03(115) ⚡スコア報酬 / ニトロ / 走行音 / 読み込みの重さ / 車体の大きさ3段
- ⚡⭐⭐**スコアの段位報酬**(ユーザー指示「スコアごとにガチャ石の報酬。1000で10個 3000で30個
  5000で100個 10000で300個。ちゃんと専用のUIも」)=`BNS_SG`。
  ⚠⚠**1段につき1回だけ・拠点ごと**(1日5回走れる面なので、繰り返し配ると石が無限に増える)。
  ⚠**自己ベストではなくその走りのスコアで判定**=ベストで見ると報せが毎回鳴って嘘になる。
  ⭐**専用の窓(`md-bsg`)**=🗺マップの🚌拠点を選ぶと「⚡スコア報酬」ボタンが出る。
  **1行=1つの段**(必要スコア/💎/状態)。✅受け取り済みは金の縁、次の段だけ進み具合の棒。
  リザルトにも**その走りで新しく取った段だけ**出す。
- 🔥**ニトロは押した時だけ消費**(ユーザー指示)=**使っている間も溜まる**ようにした。
  ⚠それまでは**ニトロ中は溜まりが止まっていた**ので「使っている間ずっと消費している」ように見えていた。
  ⚠検査も『使用中は溜まらない』→『使用中も溜まる/押さなければ減らない』へ書き換え(方針が変わったため)。
- 🔊🐞**走行音がクリア後も鳴りっぱなしだった**(ユーザー指摘「もう一度バスやる時もずっと鳴ってる」)。
  ⚠原因=止めていたのが `backTitle` だけで**リザルトは素通り**。走行音は**毎コマ音量を書き換えて鳴らし続ける**
  作りなので、**回るのを止めた瞬間の音量のまま鳴り続ける**。⭐リザルト2か所で止める+
  **毎コマ見張る最後の砦**(🚌の面から出ているのに鳴っていたら止める)を主ループに置いた。
- ⚡⭐⭐**拠点開拓の読み込みが重かった**(ユーザー指摘)=**538ms → 205ms**。
  ⚠原因=**木の候補1つごとに建物を全部見ていた**(8.5万×2049=**約1.7億回**の距離判定)。
  ⭐**建物を升目に入れてから撒く**ようにした=`bnsMap` は **335ms → 37ms(9倍)**。
  ⭐**出来上がる森は1本も変わらない**(84,784本・先頭と末尾の座標まで一致を実測)。
- 🚌⭐**車体の大きさを3段に**(ユーザー指示「3段階のデカさを設けて、最終的に初期の1.5倍に」)=
  **1.17 / 1.33 / 1.50**。⚠**キー(big/trail)は使い回す**=もう買った人の持ち物を消さない。
  ❌**「二階建て化」という名前はやめた**(ユーザー「2階建てって何か意味あるの?」)=
  **真上から見る面なので階数は一度も見えず、何が起きたのか伝わらない名前だった**。
  ⭐**大型車体Ⅰ/Ⅱ/Ⅲ**にして、名前と丸数字で「同じ物が3段で伸びる」と読めるようにした。

## 2026-08-03(116) 💫幸運チケット(ログイン日数の配布)
- ⭐**ユーザー指示**「1日目/3日目/5日目/10日目ログイン時に、タイトル画面で虹犬・金ロブスター・バスが
  出る確率が30秒だけ10倍になるチケットを配ろう。ちゃんと獲得!って感じでタイトル画面でお知らせで表示して。
  そのチケットはタイトル画面の上にUIちょこっと出す感じで。使用はそれをタップして使用しますか? はい いいえ で」。
- 💫**中身**=`LUCK_DAYS`[1,3,5,10] の日に1枚。`LUCK_X`=**30倍** / `LUCK_SP`=**歩く速さ1.5倍** / `LUCK_T`=30秒。
  ⚠**(117)にユーザー指示で 10倍→30倍・歩く速さ1.5倍を追加**(文章も全部そこから作っている)。
  ⭐**1日目=この版を最初に起動した日**(ユーザー「これ実装した瞬間が1日目ね」)=
  `META.lgN` は未設定なら0なので、初回の起動でそのまま1日目になる。
- 🎉**獲得の報せ**=タイトルが出てから「○日目ログイン! 💫幸運チケットを獲得!」+札が4秒光る。
- 🎟**札はタイトルの左上**(持っていない時は出さない)。**タップ→「使いますか?」はい/いいえ**。
  ⭐使っている間は**金色に光って残り秒数**を出す。⚠**二度押ししても減らない**(重ねても意味が無いため)。
  ⚠**タイトルに居る時だけ秒が減る**=効くのはタイトルの行進だけなので、
  別の画面に居る間に切れると「使ったのに何も起きない」になる。
- ⭐**実測で確認**=1/3/5/10日目に配る(12日で4枚)/使うと確率がぴったり10倍
  (虹犬.005→.05 / 金ロブ.0005→.005 / バス.00001→.0001)/二度押しで減らない/
  タイトル以外では減らない/30秒で切れて確率が戻る。
- 🛠DEVは9枚(底上げ)。⚠セーブの全初期化でも消す(消し忘れると初期化した人がずっと貰えない)。

## 2026-08-03(117) 💫幸運チケットを 30倍+歩く速さ1.5倍 に
- ⭐ユーザー指示「ちゃんと10倍にした?もししてたら30倍にして」「あと歩く速度も1.5倍にして」
  「ちゃんと文章も変えてね」。
- ⚡**確率 10倍→30倍**(`LUCK_X`)=虹犬 0.5%→**15%** / 金ロブ 0.05%→**1.5%** / バス 0.001%→**0.03%**。
- 🏃**歩く速さ1.5倍**(`LUCK_SP`)を追加=画面を横切るのが速いぶん**入れ替わりも速い**
  =同じ30秒でより多く抽選が回る。⚠**タップは難しくなる**(そこは腕)。
- 📝**文章は3か所とも数字から作っている**(トースト/札/確認の窓)=**ノブを変えれば文章も自動で変わる**。
  ⚠決め打ちで「10倍」と書いた所は残していない(実測で確認)。

## 2026-08-03(118) 幸運チケット=雑魚は一撃 / 🚌バスは0.1% / 配り直し
- 💥**チケット中は珍しい3種以外がワンタップで倒せる**(ユーザー指示)。
  ⚠**叩く時に見る**(湧く時ではない)=チケットを使う前から歩いている個体にも効く。
  ⚠🌈虹犬・✨金ロブ・🚌バスは**据え置き**=あの3種は「何発も当てて倒す」のが手応えそのもの。
- 🚌**チケット中のバスだけ倍率をやめて 0.1% の決め打ち**(ユーザー「チケット中はバス0.1%にしよう」)。
  ⚠素が0.001%しかないので、**30倍でも0.03%=30秒ではまず出ない**。
- 🔁**配り直した**(ユーザー「さっき配布したやつはノーカンで今からもう一回配布」)=
  ⭐**`LUCK_GEN` を上げると全員が次の起動で1日目からやり直し**になる仕組みにした。
  ⚠**もう貰ったチケットは取り上げない**(手に入れた物を消すのは筋が悪い)=日数だけ0に戻す。
  ⚠**軽い気持ちで上げない**=10日目まで進めた人が振り出しに戻る。
- 📝文章は3か所ともノブから作り直した(トースト/札/確認の窓)。
- ⭐**実測で確認**=チケット中 虹犬15%/金ロブ1.5%/**バス0.1%**/歩く速さ1.5倍/
  雑魚はHP2もHP3も1タップで倒れる/珍しい3種は据え置き/チケット無しでは今までどおり。
