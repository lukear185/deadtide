---
tags: [deadtide, pitfalls]
aliases: [作業ノート, 落とし穴]
---
# DEADTIDE 作業ノート(NOTES.md)

> 見取り図=[[HOME]] ／ ルールと進め方=[[CLAUDE]] ／ 設計の明細=[[DESIGN]]
>
> ⚠**過去に実際にやらかしたこと**と、確認用の道具の使い方。
> ⭐**絵・見た目・演出を触る前と、数値やバランスを触る前に、必ずここを読む**(CLAUDE.mdから移した)。
> 新しい落とし穴を踏んだら**必ずここに1行足す**。

## ⚠作業の時に必ず守ること(過去に実際にやらかしたこと)

> ここは**同じ失敗を繰り返さないための決まり**。新しい落とし穴を踏んだら**必ずここに1行足す**こと。

### 進め方
- ⭐**遊びの土台(ジャンル)が合っているかは、作り込む前に確かめる**。
  🕳洞窟(『ゆうしゃのくせになまいきだ。』式の間接統治ミニゲーム)を**4回作り直して全部捨てた**(第49〜53弾)。
  「なんか違う」が続く時は、**細部を足して埋めようとしない**。
  「そもそもこの遊びはこのゲームに合っているか」を先にユーザーへ聞くこと。
- 大きな仕組みを入れる時ほど、**最初の1回で「これは何のための時間か」を1行で言えるか**を確かめる。
- ⭐**「操作を塞ぐ」仕組みを入れたら、塞いだ先に"閉じないと進まない物"が無いか全部洗う**
  (2026-07-26 第89弾)。チュートリアルで光っている所以外を塞いだら、**閉じるまで待つ物**が3つ引っ掛かった
  (建設メニュー・新登場の紹介モーダル・リザルト)。どれも塞いだ瞬間に**永久に詰む**。
  ⚠**塞ぐ側の実装は「迷ったら通す」**に倒し、**逃げ道(再読み込みで元に戻る等)を必ず1本残す**。
  ⚠**この種の不具合はヘッドレスでは出ない**(イベントを飛ばさないため)。
  **実機のブラウザで1回通しでタップして確かめること**=3つとも実機で押して初めて見つかった。
  言えないなら、それはまだ設計が決まっていない。

### 検査
- ⭐**「動かしてから見る」検査は、見る前に状態を戻すこと**(2026-07-27に実際に踏んだ)。
  冷却塔の検査が **20秒ぶん歩かせたあとの敵**で「再凍結しないこと」を見ていたが、
  その敵は**とっくに射程200の外**に出ていて、何を検査しても素通りで通っていた
  (=仕様を変えても検査が落ちなかった)。位置・HP・クールダウンを戻してから見る。
- ⭐**撮影で「紹介モーダル」に邪魔される時は `INTROQ` と `G.introQ` を空にしてから開く**
  (2026-07-27)。新登場の紹介は `PAUSED=true` で前に出るので、作戦タイムなどを撮ると必ず被る。
  ⚠**ウェーブ開始で積み直される**ので、消すのは `setTimeout` で1.2秒待ってから
  (起動直後に消しても、そのあと始まる1波目でまた出る)。
- ⚠⭐**`test_shot.js` の pose= の並べ撮りは、途中で例外が出ても真っ黒にならず「段が消える」**
  (2026-08-02に踏んだ)。`put()` の中で `var` 宣言した名前を**外の `render()` から参照**したため
  ReferenceError になり、**x6の段だけ出て x3 と実寸の段が丸ごと消えた**(絵の不具合だと思って探し回った)。
  ⭐**段が1つでも欠けていたら、絵ではなく render の例外を疑う**(put の try は drawUnit しか包んでいない)。
- **変更のたびに `node --check` → `node test_undef.js` → `node test_headless.js` を通す**。
  数値(hp/atk等)を触ったら `node test_balance.js`、画面の寸法を触ったら `node test_layout.js`。
- **見た目を直したら必ず `node test_shot.js` で撮って自分の目で見てから報告する**。数値の確認だけで不具合を3回見逃した。
- **一括置換(sed/正規表現)をしたら、その行が実際に通る経路を走らせる**。`node --check` は文法しか見ないので通ってしまう。
  `test_undef.js` がこの種の事故を静的に捕まえるので、置換後は必ず流すこと。
- **rAF(毎フレーム処理)の中の例外は try/catch に飲まれてテストに出ない**。怪しい処理は直接呼んで確かめる。
- **日本語以外の文字が紛れ込む事故**が数回あった(точно/один/강화)。書いたら見直すこと。
- **`const`のTDZは `typeof` でも ReferenceError を投げる**(var や未宣言とは違う)。2026-07-26に実際に踏んだ:
  `devTop()` が下で定義される `ZCAVE` を `typeof ZCAVE!=='undefined'` で守ったつもりが例外になり、
  **スクリプト全体が停止してタイトルから先へ進めなくなった**。「まだ定義されていない const を踏むかもしれない」所は
  typeofガードではなく **try/catch** で受けること。
- **`?dev=1` でしか通らない経路は普段のテストを素通りする**。DEVは`location.search`で決まるので、
  ヘッドレスの実走は常にDEV=false側しか走らない。`test_headless.js` の `checkDevLoad()` が
  **別スコープ(new Function)でDEV=trueの読み込みを1回試す**ようにしてある(上の事故を受けて追加)。

### セーブと互換
- ⭐**0が有効な値の変数に `||` の既定値を使わないこと**(2026-07-26に実際に踏んだ)。
  `const d9=G.pveDiff||2;` と書いていたため、**新兵(=0)でクリアすると古参(=2)として記録**され、
  ①兵長が永久に開かない ②鬼軍曹が勝手に開く という状態になっていた。`==null?既定:値` を使う。
  検査は `checkProgress()` が **awardMeta() を実際に通して**「クリアした難易度がそのまま記録されるか」を見る
  (以前は `scArr(0)[d]=1` と直に書く検査だけだったので、このバグを捕まえられなかった)。
- ⭐**セーブを作り直したい時は `META_RESET` の番号を上げる**(2026-07-26に導入)。
  起動時に1回だけ `metaReset()` が走る。⚠**この番号を安易に上げないこと**(全プレイヤーのデータが消える)。
  消すのは「難易度/ステージの解放」と「研究所で買ったもの+研究pt」だけ。
  💎魔石・英雄・🔧鍛錬素材・📖図鑑・⚔冒険の進捗・鍛錬Lvは**消さない**。検査は `checkMetaReset()`。

### 数値・バランス
- ⭐**進化で引き継ぐ段は「強化代ごと」に見る**(`usMinSt()`。2026-07-27・母艦の🔫弾数)。
  進化先に**素のタワーに無かった強化代**を足すと、塔ごと一律に引き継ぎ段(5)を引く作りでは
  値段が `1.75^-5` になって**只同然**になり、印(◆)の数も負けて壊れる。
  ⚠**その枠を素のタワーが持っていたか**で0か5かを決めること。
- ⭐**「一度に出る数」は総数ではなく間隔で決まる**(2026-07-27・小波)。
  敵を減らさずに軽くしたい時は `pool[].dl`(次の1体までの秒数)に**休みを差し込む**のが一番安い。
  ⚠総数・強さ・顔ぶれを1つも触らないので、バランスの検査は素通りする=**専用の検査が要る**(`checkEarly()`)。
- ⚠**顔ぶれは毎回ランダムに引く**ので、`buildTide` の総HPを1回ぶんで隣の波と比べると普通に前後する。
  検査は**数回の平均**で、しかも**離れた波**(W1<W3<W6)で比べること(実際に1回落ちた)。
- ⭐**序盤の重さは3つで決まる**(2026-07-27に強くした)。`buildTide` の
  ①`warm`(1〜4波の慣らし。**hpと数の両方**に効く。[.6,.72,.84,.94]→**[.9,.96,1,1]**)
  ②`earlyX`(1〜6波だけの体力の底上げ。1.30→1.05へ薄れて7波で1に戻る)
  ③**出る敵の顔ぶれ**(`ZOMBIES[].mw`=何波目から出るか)。
  ⚠**総HPの比で「序盤が軽い」を測ってはいけない**=③のせいで1波目と4波目は必ず数倍開く。
    見るのは**倍率そのもの**(`G.tide.mul`)。検査は `checkEarly()`。
  ⚠中盤・終盤を触りたい時は `midX`(8波〜)/`lateX`(14波〜)の方を見ること。
- ⭐**「上限を伸ばす」時は必ず効き目の式も見る**(2026-07-27・進化後の追加5段)。
  `twRateM=1-.09*lv` のような**引き算の式は、上限を倍にすると0を割り込む**
  (10段で 1-0.9=0.10 → **10倍撃つ**)。`usEff()` を通して**6段目から先は効き目を半分**にした。
  ⚠同じ形の式が 威力/連射/射程/連鎖/凍結回数/昇温/最大倍率/継続ダメージ と8本あるので、**全部通すこと**。
- ⭐**進化先は「元のタワーの言葉づかい」を引き継ぐ**(2026-07-27ユーザー指摘「火炎感があった前の雰囲気に近づけて」)。
  業火放射塔を**二又の平たい板**で描いたら火炎放射に見えず、溶解噴射砲は**葉っぱ**に見えた。
  元の火炎放射塔=**立てた燃料タンク+ラッパ口+種火** / 酸噴射砲=**立てたタンク+送りホース+平たい噴射口+滴る酸**。
  ⭐**その語彙をそのまま使い、数を増やす(タンク2→3本・口を二連に)だけ**で「完成形」に見える。
  ⚠**別の形にしてはいけない**=何の塔か読めなくなる。進化は「同じ塔の上位」であって別物ではない。
- ⭐**爽快感は「時間の伸び縮み」で出す**(2026-07-27ユーザー指示「重くならないやつで」)。
  絵も粒も光源も1つも増やさないので**重さゼロ**。入れたのは3つ:
  ①雑魚を倒すと0.03秒だけ間が空く(`G.kickT`・⚠**0.12秒に1回まで**=`kickCd`。
  毎回掛けると群れを薙いだ時に試合ぜんぶがスローになる) ②倒れた瞬間だけ白く光る
  (死体fxの先頭0.1秒に`lighter`で同じ絵をもう1枚重ねるだけ) ③ウェーブをしのぐと
  **2秒スローモーションの余韻**を置いてから作戦タイムへ(`CLEAR_HOLD`/`CLEAR_SLOW`)。
  ⚠**スローは掛け算にしない**=重なると止まって見えるので `Math.min` で一番遅いものだけ採る。
  ⚠**余韻の残り秒数は実時間で数える**=`dt`はもうCLEAR_SLOW倍で届くので割り戻す。忘れると2秒が約6秒になる。
  ⚠**演出オフ(FXLV=0)では3つとも出さない**。
  ⭐**同日にユーザー実機判断で2回作り直した**。残ったのは①ウェーブ制圧のスローと②撃破の白い光だけ。
  ⚠**雑魚1体ごとのヒットストップは捨てた**=「全く何が変わったのか分からないレベル」。
   .03秒×35%→.05秒×6%(ほぼ完全停止)まで強めても伝わらない。**敵が小さく数も多いので1体ぶんの間は埋もれる**。
   書き戻さないこと。手応えを足すならウェーブ単位に寄せる。
  ⚠ウェーブ制圧のスローは 速さ .35→**.18** + **画面全体を暗く落とす**(fillRect 1枚)。
   **画面の速さだけでは気づけない**のが分かりづらさの正体だった。数値は `CLEAR_HOLD/CLEAR_SLOW`。
  ⚠⚠**専用の音は付けない**(同日「音イランわ」)。「どぅーん」を**4回**作り直した
   (オシレータ3案→ノイズ→大太鼓)が最後まで刺さらず、**スロー+暗転だけで伝わっていた**。
   ⭐**ここから学ぶこと=音を足す前に「画だけで足りていないか」を先に確かめる**。書き戻さないこと。
   (下の音づくりの知見そのもの——ノイズ合成・missing fundamental——は他の音で使えるので残してある)
- ⭐⭐⭐**編成制と解放の作り替え(2026-08-01ユーザー指示)**。設計の要点だけ:
  - **初期に使えるのは 兵科=猟犬・バット / タワー=ライフル台・ショットガン台の2種ずつ**
    (`BASE_U=2` / `BASE_T=2`)。3種目以降は**全部🔬研究所**。
    ⚠**対戦と協力は別枠 `PVP_T`/`PVP_U`(10)**=研究所を持たないモードなので、ここを2にすると遊びが壊れる。
    ⚠**チュートリアルは上限を外す**(`metaTowerCap`/`metaUnitCap` の中)=2種だと🔓解放の段が死ぬ。
  - ⚠⚠**廃品工房はネット砲より前に置く**=TOWERSの並び=研究所で開く順番。工房が後ろだと序盤の経済が死ぬ。
    研究所の1個目=工房=120🧬=初回の出撃で届く額にしてある。
  - **連れて行く兵科は10体まで**(`TEAM_N`)。表 `META.team`(**UBASEのidで持つ**。UNITSは派生に差し替わる)。
    ⭐**戦闘中の解放は「選んだ中の元のコストが安い順」**(`uupCost` が `UNITP[team[uUn]]` を引く)
    =高い兵科ばかり選ぶと解放が進まない。⚠猟犬とバットは**固定枠**(外せない)。
    ⚠**枠の番号 k と兵科の添字 i は別物**=デッキは `DECKI[k]=i` で対応を持つ。
    ⚠選んでいない枠は**安い順に自動で埋める**(編成を触らない人でも今までどおり遊べる)。
  - ⚠**検査で `deployUnit(me,ri(0,me.uUn-1))` と書かない**=team の添字に読み替えないと
    出撃が全部失敗して**落ちずに素通りする**。実走ログの `dep=` が0でないことを必ず見る。
  - ⚠**CPUの「盾役」は `SHD_I`(findIndex)で引く**=直書きの3は斥候を指していて**元から壊れていた**。
  - ⭐**研究所で解放した兵科は、空きがあれば自動で編成に入る**(2026-08-02ユーザー指示)=`teamAutoAdd`。
    ⚠**まだ編成を触っていない人(`META.team==null`)には何もしない**=teamIdx が元から自動で埋めるので、
      ここで配列にすると「触った」扱いになり以後の自動補充が止まる。
    ⚠**満杯(10体)なら入れない**=何を外すかはプレイヤーが決めること(トーストで入れ替えを促す)。
    ⚠**必ず `META.nu` を増やした後に呼ぶ**(teamIdx は持っている兵科しか通さない)。
    ⭐**初めて兵科を解放した時だけ**「🎒編成のはなし」を1回出す(`META.tmTip` / `md-tmtip` / `teamTip`)。
    ⚠印は `metaResetAll` にも書く(書き忘れると初期化した人に案内が出ない)。撮る=`lab+tmtip`。
  - ⚠⚠⭐**`soloMeta()` は G を見るので、タイトルでは必ず false(=対戦扱い)になる**
    (2026-08-01に実機で発覚「巨漢も何も解放してないのに編成に巨漢〜火炎瓶が居る」)。
    🎖編成も🔬研究所も**タイトルからしか開けない**ので、上限が `PVP_U`(10)側に落ちて
    **持っていない10種が並んでいた**。⭐直し=`soloUI()`(=Gが無ければ `MODE==='solo'&&!COOP` で判定)を作り、
    `metaTowerCap`/`metaUnitCap`/`teamIdx` の3か所をそれに差し替えた。
    ⚠**ロビー(MODE='host'/'client')は対戦のまま**にすること。
    ⚠**「持っている物だけ出す」は研究所の派生にも要る**=`ALLVB` の進化カードは
    上級(FVBASE)しか `uvBaseOK` で守っていなかったので、未解放の兵科の進化が並んでいた。
    編成の「⭐派生を選ぶ」タブも同じ(行ごと出さない)。検査は `checkEvo`/`checkTeam`⑤。
- ⭐⭐**敵の量の作り替え(2026-08-01ユーザー指示)**。
  - **序盤を強く**: `warm` を廃止(1固定)、`earlyX` を W1〜2=2.05 → W8で1へ。
    ⚠**W1とW2は同じ値にする**=W1だけ跳ねると W1>W2 の逆転が出る(実際に出した)。
  - **終盤は数を割って1体を厚く**: `cut`(W12から最大1.62)で `n` を割り、**雑魚の mul にだけ**同じ値を掛ける。
    ⚠**ボスに掛けない**=ボスだけ1.6倍になる。総HPはほぼ据え置きで**W20の体数が45→23**。
  - ⚠**軽い新顔がプールに入ると平均が下がって逆転する**=クローラー160→300 / ストーカー190→560 に上げた。
  - ⚠⚠**隣どうしの波を直に比べる検査を書かない**=末尾バイアス(1400HPを最大40%で引く)で±10%振れる。
    **3波の移動平均**で比べること(`checkEarly` にそう入れた)。
  - **砲撃は序盤だけ弱くした**: 直撃 150*(1+.15w) → 105*(1+.24w) / 燃焼20→14 / 初期チャージ .5→.25。
    W20ではほぼ据え置き(−0.5%)、W1で−26%。
- ⭐⭐⭐**🚌ボーナス面「バスの日」を足した時の掟**(2026-08-02)。**土台はいつものソロPvEのまま**
  (敵・タワー・砲撃・当たり判定・研究所がそのまま効く)、差し替えたのは道とルールと波の中身だけ。
  - ⚠⚠**難易度は必ず `D5` の末尾に足す**=真ん中に差し込むと `META.sc` の添字が1つずれて、
    既存プレイヤーの「クリア済み」が別の難易度を指す(`||`の既定値で踏んだのと同じ形の事故)。
    **画面の並びと解放の鎖は `D_ORD` が持つ**。⚠`diffOK` と `awardMeta` の両方を D_ORD で見ること。
  - ⚠⚠**4本のレーンは長さを揃える**(`LANES`)=`z.d>=PLEN` でコア到達を見ている所を
    1つも直さずに済む。揃えないと短いレーンの敵が手前で消える/長いレーンの敵が拠点を通り抜ける。
  - ⚠⚠**道が複数ある面は道も4本とも引く**=1本目だけ引くと残り3本は「地面の上を敵が歩く」ことになり、
    「四方から来る」が絵で読めない(撮って気づいた)。`roadPath` を LANES 対応にする。
  - ⚠⚠**`me.team` を空配列にしない**=作戦タイムや強化カードが「連れて行く兵科」を引いて回るので、
    空にすると undefined を触って **rAF の中で例外**になる。絵は出たまま**中だけ止まる**ので
    「敵が湧かない不具合」に見えて原因を探し回ることになる。出せなくするのは `uUn=0` と CSS で。
  - ⚠**波が始まる前の「🆕新種のゾンビ」を出さない**=ボーナス面は開始と同時に波が始まるので、
    紹介モーダルが `PAUSED=true` で画面を覆うと**地図を一度も見ないまま止まる**。
  - ⚠⚠⭐**撮影では自分で時間を進める**=`node test_shot.js out.png 852 393 "bns+bnsn=9"`。
    **ヘッドレスの仮想時間では rAF がほとんど回らない**ので、放っておくと敵が1体も湧かない絵になる
    (検証場で踏んだのと同じ罠。**2度目**。「敵が湧かない不具合」だと思い込んで長く探した)。
    ⭐切り分けの道具=`dbg` オプション(盤面の状態をトーストで出す/gameStep の例外を拾う)。
  - ⚠**中断させない**(`saveRun` の頭で弾く)=①再開の経路は `loadStage` しか通らずレーンが復元できない
    ②中断→再開で何度も入り直せると報酬の周回になる。
  - ⚠**報酬は初回クリアだけ**(`META.bcl[ステージ]`)。何度でも入れる面で配ると回すだけ増える。
  - ⚠⚠⭐**バスの操作は「押した所へ走る」ではない**(2026-08-02ユーザー「バスの操作とかは全然違う」)。
    最初は🚩集結旗と同じ「タップした所へ行く」で作ったが却下=**あれは指示を出す操作で、
    これは自分で運転する遊び**。⭐**画面左下のバーチャルスティック**で確定。書き戻さないこと。
    ⭐**手触りは「重い車らしく」**=速度を持たせて加速・減速させる(`BUS_ACC`/`BUS_FRIC`)。
    ⚠**スティックの向きへ瞬間移動させない**=倒した向きへ加速し、離しても惰性で流れる。
    曲がる時に外へ膨らむのも同じ理屈で出るので、旋回の専用処理は要らない。
    ⚠スティックは `setPointerCapture` を使う(枠の外まで指を動かしても追従する)+
    **離したら必ず0へ戻す**(pointerleave/cancel も拾わないと壁に貼り付く)+`touch-action:none`。
  - ⭐⭐**群れに見せるのは「数」ではなく「横のずれ」**(2026-08-02ユーザー「もっと密度があってたくさん
    いる感じがいい」)。⚠敵は**道の上の距離しか持っていない**ので、何百体出しても**一列の行列**にしかならない。
    ⭐**`z.off`(道の法線方向へのずれ)を1つ足すだけで塊になる**。⚠**拠点の手前ではずれを絞る**
    (絞らないと拠点の周りをすり抜けて見える)。⚠湧く時に `d` もばらけさせると前後にも散る。
  - ⚠**マップを広げる時は `MAPW`/`MAPH`**(MW/MHは触らない)。⭐**倍率の基準は MW のまま**にしてあるので、
    マップを広げても「画面に見える広さ」は変わらない(面ごとに寄り具合が変わらない)。
  - ⚠⚠**新しい `const` の宣言をファイルの下の方に置かない**=`loadStage` は読み込みの途中でも呼ばれるので、
    下で宣言した const を触ると**初期化前アクセスでスクリプトごと止まる**(`BPROP` で踏んだ。TDZの穴は2度目)。
  - ⚠**廃墟は種から散らす**(`bnsProps`)=手で並べると広い面で密度が保てない。⚠**画面に入っている物だけ描く**。
  - 🗺**ミニマップはカメラが動く面だけ**出す(`drawMiniMap`)。⚠**画面の座標で描く**(世界の変換を戻してから)。
  - ⚠**建設マスは道から95以上離す**。斜めの道(X字)は**拠点に近いほど道と道の隙間が狭い**ので、
    近い所に置けるのは道と直角に近い向きだけ(撮って詰めた)。検査 `checkBonus`。
- ⭐⭐⭐**「敵を桁ごと増やす」時の掟(2026-08-02(2)・米粒ゾンビで分かったこと)**。
  ⚠⚠**重さの正体は描画だけではない**=1体ずつ掛かっている物を**全部数える**こと。
    数えたら8つあった: ①焼いた絵の drawImage ②接地影の楕円 ③HPバー2枚 ④撃破の血の粒9+染み3
    ⑤死体(焼いた絵をもう1枚)⑥「+12⚙️」の文字 ⑦断末魔の音 ⑧**漏れた時の音+赤い明滅+揺れ+粒14**。
    ⭐**絵だけ軽くしても、残り7つがそのまま毎秒何百件になる**。
  - ⭐**まとめて塗る**=数色の小さい**四角(`rect`)**を1本のパスに積み、**色ごとに1回だけ `fill()`**。
    ⚠**`arc`/`ellipse` は積めない**=前の部分パスから線が繋がるので都度 `moveTo` が要る。`rect` は毎回独立。
    ⚠**`globalAlpha` は塗るたびに1つしか持てない**=1件ずつ濃さを変えるとまとめられない。
      薄れは「**時間で小さくする**」か「**2〜3段に分けて塗る**」で表す。
    ⚠**py順の並べ替えは捨てる**=米粒の大きさでは前後が読めないし、並べ替えとパスの分割の方が高くつく。
    ⚠**ミニマップの点も同じ**=`fillRect` を1体ずつ呼ばずに積んで1回で塗る。
  - ⭐**跡が残る演出は「使い回しの輪(リングバッファ)」で持つ**(`bnsRing`)=上限に達したら
    一番古い物を上書きする。何百体轢いても配列は伸びず、毎フレームの `filter` もゴミも出ない。
    ⚠敵の配列も `filter` をやめて**その場で詰める**(毎フレーム新しい配列を作らない)。
  - ⚠**知らせを間引く時は「見せ方」だけにする**=コアの減りや⚙️の額まで一緒に減らさないこと
    (検査でそこを見ている)。
  - ⚠⚠**寄り具合(`CAM.zm`)で割らないと文字も粒も1画素未満になる**=開拓便は .26 まで引いているので、
    本編と同じ数値で描くと**画面では3〜4画素**。⭐**`bnsGz()`(=1/zm)を掛けて「画面での見え方」で書く**。
  - ⚠⚠**1波しか無い面は「最終ウェーブ」の条件に必ず当たる**=**主(siege)**が湧いて、
    米粒と見分けの付かない1体が拠点の手前に居座って**漏れず・死なず・試合が終わらない**
    (実際に検査で「85秒で終わるはずが250秒たっても1体残る」で見つけた)。
    ⭐**体数を増やしたら必ず「波が必ず終わるか」の検査を書く**(`checkRice` の⑧)。
  - ⚠**コアの厚さも一緒に見る**=体数を15倍にしたのにコア50のままだと**3%漏らして陥落**する面になる。
    ⭐開拓便で楽すぎ/きつすぎなら、難易度表(hp/cnt)より先に**`startSolo` の中のコア400**を動かす。
  - ⚠**ミニマップの「いま見ている範囲」は `CAM.x` ではなく `OX/OY` から出す**=カメラはマップの端で
    止まる(`camApply` が切っている)のに、枠だけ中心を追い続けて**ミニマップの外へはみ出す**。
  - ⚠**撮る時はバスを走らせないと轢き応えが1つも写らない**=`bnsdrv`(群れの中を往復する)。
    ⚠中央で回らせても当たらない/片道だけだと**マップの端まで走り去って**群れごと画面から消える
    (どちらも実際に撮って気づいた)。
- ⭐⭐**溜めのDPS補償の総点検(2026-08-01・45体の調査で分かったこと)**。
  ✅**式そのものは正しい**=1周期 `rate+ct` / 1発 `atk*(rate+ct)/rate` → 実効DPS `atk/rate`。
    `CHG_CAP` の頭打ちも `uChgM` が clamp 後の ct を使うので倍率と周期が必ず一致する。
  ⚠⚠**回復役だけ壊れていた**(直した)=回復の枝は `u.chg` を一度も見ずに `u.cd` だけで発動するのに
    `uChgM` の倍率は受け取っていた=**医療兵+21%・工兵+22%の回復量**。`test_balance.js` は
    atk/rate で測るので**この狂いを捕まえられない**。⭐`U.type==='heal'` の時は倍率を掛けない。
  ⚠**残っている小さなずれ(数値は触っていない・共有だけ)**:
    ①フレーム量子化で溜め有りが 60fps で+2.8%〜30fps で+11.3%(間隔が短い兵科ほど効く)
    ②`Math.round` の丸めで81種中41種が ±1.5% ずれる(部隊レベルで得損の向きが変わる)
    ③凍結の減速1.7秒固定 < 絶対零度の周期1.92秒=**毎周期0.22秒(11.5%)減速が切れる**
    ④燃焼2秒 < 炉心の番人の周期2.08秒=3.8%の空白。
  ⚠**`U_MOT` の秒数はそのまま出ない**=`min(表, rate*0.6)` で12種が削られる
    (mgn .5→.096 / hNox 1.2→.54 / hGeo 1.3→1.14 など)。**表を書き換えてもこの12種には反映されない**。
- ⭐**セーブの初期化は `META_RESET` の番号を上げるだけ**(2026-07-27に 1→2 / 2026-08-01に 2→3)。
  ⚠⚠**3からは「難易度と研究所だけ」戻す**(2026-08-01ユーザー選択)=
   💎魔石・引いた英雄・🔧鍛錬素材・📖図鑑・⚔冒険の進捗は**残す**(`metaResetProgress`)。
   3に上げた理由=初期解放を2種に絞ったので `META.nt`/`META.nu`(BASE_T/BASE_U からの相対値)が
   **同じ数字で別のタワー/兵科を指す**ようになったため。中断セーブ(`dt_run`)も `v:1→2` で捨てている。
  ⚠2の時は「全部消す」だった=💎魔石・引いた英雄・🔧鍛錬素材・📖図鑑・⚔冒険の進捗まで消える
  (1では残していた。2026-07-27ユーザー選択「全部消す(完全に最初から)」)。
  ⚠🛠DEVのセーブは別枠(dt_meta_dev)なので影響しない。
  ⚠⚠**新しい META.xxx を足したら `metaResetAll()` にも書く**=書き忘れると「初期化したのに残る」。
  検査 checkMetaReset が消え残りを捕まえる。
- ⚠⚠⭐**高い笛のような音(1000Hz超のsine)は使わない**(2026-07-27「ぴゅぅ～みたいな高めの音は禁止。合ってなさすぎ」)。
  **廃墟とゾンビの世界に高いキラキラ音は合わない**。虹ゾンビのSEを1568〜5200Hzで駆け上げて一発で却下された。
  ⭐**当たり・高揚は「音の高さ」ではなく「和音が厚くなること」で出す**=低い和音を段で開いていく。
  ⚠タイトルBGMの対旋律も1オクターブ下げた(784〜1245Hz → 349〜523Hz)。
  ⚠**sineは細くて笛っぽい**=同じ高さでも triangle にすると馴染む。
  ⚠この判断より前からある音(coin 1350 / heal 1320 など)は指摘されていないのでそのまま。
- ⭐**金属音は「倍音を整数比にしない」**(2026-07-28・黄金のロブスターの打撃音)。
  整数比(1:2:3)は笛・ピアノに聞こえ、**ずらす(1 : 1.51 : 2.31 : 3.41=鐘の比)と金属・鐘**になる。
  ⚠**低い芯を1本足す**(150Hz・0.09秒)=響きだけだと「効いていない」に聞こえる。
  ⚠**短く切る**。伸ばすと上の禁止事項(高いキラキラ音)そのものになる。
  ⚠余韻の鐘は 660〜988Hz を .04 で0.3秒だけ鳴らしている=**ここを伸ばしたら禁止側**。
    もし「高い」と言われたら、まずこの鐘を消すこと(和音の段は残してよい)。
- ⚠⚠⭐**`ctx.filter` は iOS Safari で効かない**(2026-07-27・タイトルの虹ゾンビで踏んだ)。
  `hue-rotate` を掛けても**丸ごと無視されて素の色のまま**出る(エラーも出ないので気づけない)。
  ⭐**どこでも効く色替えのやり方**=別canvasに一度描いて `globalCompositeOperation="source-atop"` で色を被せる。
  source-atop は「すでに描いてある所にだけ乗せる」ので**絵の形どおりに色が乗る**。
  ⚠canvasは1枚だけ作って使い回す。⚠dpr を掛けて焼かないとぼやける。
  ⚠同じ理由で `drawZombie` の `filter=brightness`(被弾の白光り)も**実機では効いていない可能性がある**。
  ⭐**金色は虹と同じやり方では出ない**(2026-07-28・黄金のロブスターで踏んだ)。3つ要る:
  ①**べた塗りで被せない**=金は素の色(茶)と近いので塗り潰すと黒フチも陰影も消えて「金色の塊」になる。
    色ごとに不透明度(`rgba`)を持たせ、**明るい所だけ濃く**乗せる。
  ②**明るい側に振る**=金の暗い側を茶色にすると、タイトルの暗い背景に沈んで「くすんだ茶色」に見える。
  ③**もう1枚 `lighter` で重ねて発光させる**。①②だけでは虹に比べて地味。
  ④⭐⭐**黒フチを描き直す**(2026-07-30ユーザー指摘「ロブスタの姿が見えない」)。①で薄めに乗せても
    source-atop は黒フチにも乗るので輪郭がじわじわ痩せ、**朝(明るい背景)の召集演出では絵が丸ごと
    「金色の靄」になって的が何なのか読めなかった**。夜の背景では見えていたので気づけなかった。
    ⭐直し方=金を乗せたあと `destination-over` で **`silLayer` の太らせた黒シルエットを背面に置く**
    (2度描きの仕組みの流用。絵は1つも描き直さない)。⚠太さは `VEC2_W`(7.0)では太すぎて脚の間が
    埋まる=**`GLD_W`(3.4)**。⚠**明るい背景で撮って確かめる**こと(`+gmorn`)。
- ⚠⚠⭐**スマホのスピーカーは200Hz以下をほとんど鳴らせない**(2026-07-27に実機で踏んだ)。
  「どぅーん」を 26〜95Hz で作ったら**一切聞こえなかった**。重低音が欲しい時は**周波数を下げない**で、
  **倍音の多い波形(sawtooth)を中音域(900→130Hz)でゆっくり下降させる**。それで「低く重い」に聞こえる。
- ⚠⚠⭐**オシレータ(`beep`)は何をどう重ねても必ず「電子音」に聞こえる**(2026-07-27・スローの音で踏んだ)。
  候補を3つ出して全部ダメだった原因がこれ=**波形を変えても倍音がとびとびなので生音にならない**。
  ⭐**爆発・衝撃・地鳴りは `nzHit(f0,f1,dur,vol,q)`**=ノイズをローパスで削る。倍音が連続するので生音に聞こえる。
  低く閉じるほど「ドーン」/高く開くほど「シャー」。⚠ノイズの元は1回だけ作って使い回す(`NZBUF`)。
  ⚠**フィルタのQは1前後**にする=既定の6だと共振が鳴って結局electronicに戻る。
- ⚠⭐**小太鼓と大太鼓を分けるのは「低さ」ではなく ①打ち出しの明るさ ②尾の長さ ③こだまの回数**
  (2026-07-27・「これは小太鼓の音だな〜。いうなれば大太鼓」)。
  小太鼓=明るい打ち出し(760Hz〜)・短い尾(0.75秒)・こだま3回(=ロールに聞こえる)。
  **大太鼓=暗い打面(600Hz・0.09秒) → 長い胴鳴り(300→140Hz・1.35秒) → 遠いこだま1回**。
- ⚠⚠⭐**「聞こえない低音」は倍音で感じさせる**(missing fundamental)。大太鼓の芯である80Hzは
  スマホでは鳴らないが、**その2倍/3倍/4倍(160/240/320Hz)を重ねると耳が勝手に80Hzを作り出す**。
  80Hzそのものを出しても音が消えるだけ。**小さいスピーカーで低音を出す唯一の方法**。
  ⚠**音の good/bad は言葉で詰めても収束しない**。候補を `SFX_LBL` に並べて `?sfx=1` で選んでもらうこと。
  ⚠**新しい合成音は必ず `SFX_LBL` に足す**=`?sfx=1` で鳴らせないと、実機で「聞こえない」と言われた時に
  原因(音量か・帯域か・そもそも呼ばれていないか)を切り分けられない。
- ⚠**色を変えただけでは「爆発」にならない**(2026-07-27・酸爆発で踏んだ)。
  足りなかったのは ①**画面の揺れ**(`C.shake`) ②**弾けた瞬間の白い芯**(`spark`) ③**外へ強く飛ぶ初速**。
  動きが柔らかいままだと、色をどう変えても「霧吹き」に見える。音も同じで、**低音の芯**が無いと弾けて聞こえない。
- ⚠⚠⭐**`test_headless.js` は39行目から末尾まで丸ごとテンプレートリテラル**(`const body=\`…\``)。
  **コメントであってもバッククォートを書いた瞬間に文字列が閉じて壊れる**(2026-07-27に実際に踏んだ)。
  エラーは「Unexpected identifier」で**書いた場所ではなく次のトークンを指す**ので原因が分かりにくい。
  ⭐見つけ方=バッククォートの数が奇数の行を探す。⚠コード引用は 「」 か素の文字で書くこと。
- ⭐⭐**研究所の値段は「効果の強さ」で揃える。段数が違う項目を並べると必ず歪む**
  (2026-07-27「そんなに強くない奴が高すぎたり、レベル1→2の時点で高すぎる」)。
  ⚠正体=**5段しかない永久強化(コアHP+5など)の1段目が189〜486🧬**あり、
  **20段刻みのタワー個別強化(81🧬で威力+5%)より1段が3〜6倍高かった**。段数が違うと1段の重みも違う。
  ⭐直し方=**最終段の値段は1🧬も動かさず、1段目だけ下げて、その分だけ伸び率を上げる**。
  「序盤は軽い / 伸ばし切る重さは今までどおり」が同時に成り立つ。伸び率は `r=(最終/新1段目)^(1/(段数-1))`。
  ⚠数字を触ったら必ず `node test_headless.js`(checkLabSteps / checkLabMul)。
- ⭐**🚩集結旗に詰める距離は兵科ごと**(2026-07-28ユーザー指示)。`uStand(U)=max(0,rng-50)`。
  ⚠**ぴったり射程ぶん下げてはいけない**=敵は前衛の `ENG_GAP`(36)手前で足を止めるので、
  射程ちょうどに立つと**足止めした敵に一発も届かず棒立ち**になる。36+遊び8+余裕=50 引く。
  ⚠当たり判定は世界座標ではなく**経路上の距離(`gap=u.d-z.d`)**で見ている=rng をそのまま引き算してよい。
  ⚠遠距離が下がるぶん**遠距離は生存しやすくなる**(古参PvEが wave9→10 まで延びた)。
- ⚠⚠⭐**「何度でもやり直せるもの」で報酬を配らない**(2026-07-28)。チュートリアルの終盤で💎を3個渡していたが、
  **タイトルの「🎓 あそびかた」から何度でもやり直せる**ので、回すだけ💎が増える状態だった。
  ⭐直し方=`META.tutOk`(最後まで通した印)を見て、**2回目からは配る段ごと外す**。
  ⚠`META.tut` は**始めた時点で立つ**ので「済んだ印」には使えない(再読み込みで自動起動しないための印)。
  ⚠**途中でやめた時(done=false)は tutOk を立てない**=やめた人には次も初回の案内を出す。
  検査は `checkTut()` の末尾(2回目の段数と💎が増えないこと)。
- ⚠⚠⭐**「途中の状態を保存して、進行だけ巻き戻す」を作らない**(2026-07-28ユーザー指摘)。
  中断(`bt-suspend`)は**中断した瞬間の⚙️と塔を保存したうえでウェーブ番号だけ1つ戻していた**ので、
  「敵を倒して稼ぐ→中断→再開」で**同じウェーブを何度でも稼ぎ直せた**(⚙️が無限に増える)。
  ⭐**巻き戻すなら丸ごと戻す**=ウェーブが始まった瞬間の写し(`G.rsnap`)を取り、戦闘中の保存はそれを書く。
  ⚠写しは**JSON文字列で持つ**=中身のまま持つと `us`(強化段)が塔と繋がっていて、
    中断までに強化したぶんが写しにも入ってしまう(参照渡しの罠)。
  ⚠作戦タイム中の中断は今までどおり「その時の状態」でよい=ウェーブをやり直さないので得をしない。
  検査 `checkResumeFarm()`。
- ⚠⭐**閉じられないUIを作らない**(2026-07-27・チュートリアル中に強化ウィンドウが消せなかった)。
  建設・強化ウィンドウの閉じ方は「盤面の何も無い所をタップ」だけだったが、
  **チュートリアル中は光っている所以外を押させない**作りなので、閉じる術が消えていた。
  ⭐**✕ボタンを付けて `TUT_FREE` に入れる**のが正解。⚠「閉じないと詰むUI」は必ず TUT_FREE へ。
  ⚠**それでも足りなかった**(2026-07-28)。✕があっても、**前の段で開いたウィンドウが次の段まで居座る**
  (強化を押すと `actUp`→`openBM` で開き直るため)。次の段では光っている所が別なので邪魔でしかない。
  ⭐**`tutGo` で段が変わるたびに `closeBM()`** を入れた。⚠`build` の段だけは畳まない
  =あの段は「ウィンドウが開いたこと」を条件に入ってくるので、畳むと入った瞬間に消える。
- ⚠⭐**チュートリアルの帯(#tut)と強化ウィンドウは必ず被る**(2026-07-28ユーザー指摘)。
  強化ウィンドウは高さ256pxあり、`openBM` が下の操作バーを避けて置くので**必ず画面の上端に貼り付く**
  (852x393では top≒23)。帯は top:54 なので⚔攻撃・⏩連射がまるごと隠れる。
  ⚠**帯は `pointer-events:none` なので押せてはいる**=「押せない」ではなく「見えないまま押す」不具合。
  ⭐直し方=**帯を下へ回す**(`tutBand()`。ウィンドウが上半分に出ている間だけ `.tl`)。
  ⚠ウィンドウ側を下げる案は不可=帯の下に残る高さは200px弱しかなく、256pxのウィンドウは入らない。
- ⚠**チュートリアルで「押せないボタン」を見せない**(2026-07-28ユーザー指摘「スクラップがなくて支援枠建てれない」)。
  ⚙️は `tutStep` が毎フレーム補充している飾りなので、出し惜しんでも何も得しない。
  段ごとの下限 `sc0` を足した(支援枠の解放は⚙️600なので sc0:700)。⚠**読むだけの段にして逃げない**。
- ⚠⚠⭐**盤面の上に出すパネルにも高さの上限を付ける**(2026-08-01ユーザー指摘
  「タレットの数が増えて、設置の時に下にスクロールすると画面ごと動いてやりづらい」)。
  ⚠`#buildmenu` に上限が無く、**解放が進むほど縦に伸びて画面(393px)を超えていた**。
  中身がスクロールしないので、指で動かすと**代わりにページごと動く**。
  ⭐直し方=**中身だけスクロールさせる箱(`#bm-body`)を1枚挟む**+`overflow-y:auto`
  +**`overscroll-behavior:contain`**(端まで来た時にページへ流れないようにする=症状の本体)。
  高さの上限は `openBM` で毎回計算して入れる(画面の高さ − 余白 − 下の操作バー)。
  ⚠⚠**✕はスクロールする箱の外に置く**=中に入れると一緒に流れて下まで送ると閉じられなくなる。
  ⚠**パネル自身に overflow を付けるのも不可**=枠の外(right:-10px)に置いた✕が切り落とされる。
  ⚠撮る: node test_shot.js out.png 852 393 bmenu
- ⭐**画面は幅より先に「高さ」で詰まる**(2026-07-27・出撃準備画面を2列にした時)。
  横画面のスマホは **852x393**。幅を広げて2列にしても、`max-height:88dvh` のままだと
  **下のボタン列がはみ出して「防衛開始」が画面外に出る**。
  ⚠パネルの高さは `calc(100dvh - ボタン列)` で切り、`@media (max-height:470px)` で余白と字も詰めること。
  ⚠撮って確かめる: `node test_shot.js out.png 852 393 setup`
- ⭐**敵プールは4つある**(2026-07-27に「🌑×ステージ2=深海のナイトメア」を足した)。
  印は `nm` と `st` の**2枚重ね**で ①無/無=廃線 ②無/2=沈んだ港 ③有/無=ナイトメア ④有/2=深海のナイトメア。
  ⚠⚠**`Z.nm` だけ / `Z.st===2` だけで拾うと④が混ざる**。必ず相手側を除く条件を足すこと。
  直す場所は5つあった: buildTideのプール / ボスと最終ボスの選定 / **分裂の子の親探し** / 図鑑の並び / 冒険の敵プール。
  ⚠検査は `checkZPools()`(test_headless)=波・ボス・分裂の子・図鑑をまとめて見る。プールを足したらここに1行。
- ⭐⭐**音量は耳で揃えない。`node test_sfx.js` で52種のRMSを測ってから決める**
  (2026-07-27「タレット置く音とかいろいろ音がでかすぎる。逆にBGM関係は小さい。一回全部見てみて」)。
  ⚠**「うるさい」の正体は平均ではなくバラつき**だった=0.066(火炎)〜0.402(要塞砲)と**6倍**開いていて、
  大きい方だけが耳につく。**下に合わせて全体を0.13へ揃える**(小さい音は上げない)。
  ⚠直すのは `SFX_GAIN` に倍率を足すだけ=**素材を焼き直さない**(tool_sfx.js を回さなくてよい)。
  ⚠**建設音(build)だけ0.10**=タレットを置くたびに鳴る一番手数の多い音なので、揃えても一番うるさく感じる。
  ⚠**ゾンビの声は「ゾンビ以外の最大」を超えられない**(検査①)ので、全体を下げたら `bossKill` も一緒に下げる。
  ⚠**BGMだけ上げても効果音が鳴った瞬間に埋もれる**。3つ同時にやること=
  ①効果音を0.13へ揃える ②`SFXVOL` .85→.72 ③BGM 一律x1.55。
  ⚠検査④(最大 ≤ 平均x1.45)を足したので、飛び抜けた音を新しく入れると落ちる。
- ⭐**BGMは素材を足さず合成する**(2026-07-27・タイトルのホラーBGM `tbgmOn/tbgmOff`)。
  1曲入れるだけで数MBになるので、絵と同じく**その場で作る**=容量ゼロ・ライセンスの心配もなし。
  ⚠**メニューでずっと鳴る音は「間」が命**。音符を詰めると数十秒で耳障りになる(700ms×16歩でスカスカに)。
  ⚠**AudioContextはユーザー操作の後でしか作れない**ので、`show('title')` の時点では鳴らせない。
  `ensureAC()`(最初のタップで走る)から呼び直すこと。⚠`?sfx=1` では鳴らさない(聴き比べの邪魔)。
  ⚠戦闘中のBGM(`bgmOn`)は**ゲーム以外の画面すべてで止める**(以前はタイトルでしか止めていなかった)。
- ⭐**爽快感は「まとまって起きる瞬間」に置く**(2026-07-27に分かったこと)。1体ごとの演出は埋もれる。
  入れたのは2つ:
  ①**爆風で死体が吹き飛ぶ**(`kbZ`→killZ→死体fxの kb/kx/ky)。⚠**必ず dmgZ より先に kbZ を呼ぶ**
   =dmgZ の中で死ぬので、後から書いても死体fxはもう湧いている。⚠縦は半分(見下ろし気味の絵なので)。
   ⚠位置は**毎フレーム積分せず `e.t` だけの式**で出す=fxの更新処理を触らずに済み、重さも増えない。
  ②**🎯砲撃の着弾を思いきり盛る**(白フラッシュ+強い揺れ+短いスロー)。
   ⭐**砲撃はプレイヤーが自分の意思で撃つ数少ない行為**=手応えの本丸。1試合で数回なので盛り放題。
   ⚠**掃射(mgun)だけは合流させない**=3.6秒撃ち続けるので、スローを掛けるとその間ずっと遅くなる。
   ⚠フラッシュは**加算(lighter)**で焚く=ただ白を被せると絵が霞むだけになる。
- ⭐**タワーが「解放順に強くなっているか」は `node test_balance.js` で見る**(2026-07-27に兵科に加えてタワーも見るようにした)。
  実戦DPS = (dmg÷rate) × 同時に当たる敵の数 + 継続ダメージ + 酸爆発。⚠**落ちない道具**=目で見る。
  ⚠この式は TOWERS のコメントの目安値(レールガン174/ガトリング225/プラズマ307/要塞砲482)と一致する。
  ⚠**ネット砲と冷却塔(net)と廃品工房(eco)は対象外**=DPSでは価値を測れない(兵科の壁役と同じ)。
  ⚠**残っている逆転は3件**(2026-08-02に取り直した。以前の「4件」の①は直した):
  ①迫撃砲58<テスラコイル104(射程470=次のスナイパー塔まで唯一の遠射という役割の差)
  ②レーザー塔102≒スナイパー塔103(差-1%=式の誤差。レーザーは焼き切ると102→最大6倍、
    スナイパー塔は `vsB`2.4 と `prio` が式に入っていない)
  ③ドローン基地21(素1機・強化MAXで365。**ユーザー判断「最初弱いのはしゃあない」**=直さない)。
  ⭐**直した①(旧)=爆裂砲台**: 初期解放が2種になった結果「研究所で3個目に解放するのに
    最初から持っているショットガン台49より弱い(34)」になっていた。dmg 38→58(DPS52)。
    ⚠**天井は次の火炎放射塔58**。⚠決まり(最終形態=威力×2.0)で多連爆裂砲も76→116。
    ⚠その煽りで迫撃砲(50)が入口に抜かれるので、同じ回で 85→100(DPS58)に上げた。
- ⭐⭐⭐**出撃枠を「体数」から「重さ」に変えた(2026-08-02ユーザー決定)**。
  ⚠**単調さの正体は「無限に出せること」ではなく「枠が20"体"だったこと」**=枠が有限なら
    そこに安い兵科を入れる理由が無く、**上位兵科だけで埋めるのが常に最適**になっていた。
  - 重さ=`uWt`(=`uTier` 1〜4)。合計 `MAXU`(20)まで。**タイタン級は4枠=同時5体 / 雑魚は1枠=20体**。
    「厚い数体 か 薄い大勢か」の配分が毎回の判断になる。⚠**英雄も4枠**を使う。
  - ⚠**数える所を1つでも落とすと上限を超えられる**。触った所=`deployUnit`/`heroDeploy`/
    英雄の蘇生(hMed)/増援要請(hCmd)/参加側の `actDeploy`。**部隊を push する経路を足したらここも直す**。
  - ⚠**倒れた部隊(`u.dead`)は数えない**=枠がすぐ空くように。
  - ⚠⚠**まだ出していない英雄のぶんは枠を予約する**=予約しないと、部隊で埋め切った時点で
    **英雄を一生出せない**(部隊を下げる操作は無い・作戦タイムは敵が居ないので枠が空かない)。
  - ⚠**参加側(client)の自分の番号は `CS.myJ`**=`meV()`(CS.meN)は `gi` を持っていないので、
    `m.gi||0` と書くと常に0(ホスト)を数えて枠判定が丸ごと狂う。
  - ⚠🧪検証場の英雄ボタンも**重さで枠を空ける**(`units.length>=MAXU` のままだと英雄が出せない)。
  - ⭐**HUDに `#ubox`(🤝 12/20)を出した**=重さ制は見えないと理屈が伝わらない。満杯で赤くなる。
  - 検査=`checkSlotWt`(タイタン5体で止まる/猟犬20体/満杯なら1枠も入らない/戦死した枠は空く)。
- ⭐⭐⭐**波ごとに「物量」と「重厚」が振れる(2026-08-02ユーザー決定)**。
  ⚠狙いは**毎波の最適解を変えること**=それまで20波ぜんぶ「強い兵科で枠を埋める」が答えだった。
  - `tideKof(w)`= **W1〜4とボス波(5の倍数)は振らない / 奇数は物量1.25 / 偶数は重厚0.85**。
    ⚠W12以降は振れ幅を6割に薄める(`cut` の「終盤は数を減らす」を打ち消さないため)。
  - ⚠⚠**ボス波を振ってはいけない**(入れた直後の検証で判明・3つとも実測):
    ①体数が減る=**漏らした時のコア被害が2〜3割減り、ボス波が一番安く諦められる波**になる
    ②`cut` と掛け算になって1体が2.2倍まで厚くなり、W19→W20 で1体HPが2.57倍に跳ねる
    ③W20の体数が設計値(23)を割り込む。
  - ⚠⚠**体数を振ったら報酬(`bt`)も割り戻す**=そのままだと⚙️の実入りが波ごとに±30%動く
    (`zSpec` の第6引数 `bk`)。⚠**総攻撃力は √mul のぶんだけ残る**(物量の波は実質+12%)=
    これは `cut` にも元からある歪みなので、直すなら両方まとめて。
  - ⚠**小波の数は体数でも決める**=波の番号だけで割ると、物量の波で1回に湧く塊が1.25倍に膨らむ
    (重さ対策の仕組みが物量の波でだけ効かない)。
  - ⚠⚠**総HPは変えない**=`n` に掛けた逆数を**雑魚の mul にだけ**掛ける(`cut` と同じ作法・ボスには掛けない)。
    だから難易度カーブ(`earlyX`/`midX`/`lateX`)は1つも動いていない。
  - ⚠**ソロPvEだけ**(対戦/協力は同期の都合で振らせない)。⚠`tideKof` は**Gを見ない純粋な関数**にしてある
    (UIからも検査からも同じ値を引くため)。
  - ⭐**作戦タイムに次の波の性格を1行出す**(`tideKind`)。⚠見せないと「ただの当たり外れ」になる。
    ⚠カードは grid なので `grid-column:1/-1` を付けないとカード1枚ぶんの枠に収まる(撮って気づいた)。
    ⚠撮る=`node test_shot.js out.png 852 393 "iv=14"`(数字=そのウェーブの作戦タイム)。
  - 検査=`checkTideKind`(序盤は振らない/物量と重厚が両方ある/体数と1体の厚みが逆に振れる/案内の文言)。
- ⭐⭐⭐**兵科の戦力カーブのたるみを直した(2026-08-02)**。`test_balance.js` の「ずれ%」が
  斥候-6% → 隊長-14% と**解放が進むほど広がっていた**(端の猟犬・バット・ヴァルキリーだけ0%)。
  - ⭐**直し方=hp と atk を同じ倍率で上げる**。戦力 = dps^0.6 × hp^0.4 なので、
    両方に k を掛けると戦力もちょうど k 倍になる(23兵科・全部ずれ0%)。
    ⚠**atk だけで上げてはいけない**=dps を k^1.67 まで伸ばす必要があり、
      **生の火力が+19〜28%**跳ねる(同率なら+11%)。1発が重くなるぶんオーバーキルも増える。
  - ⚠**回復役(医療兵・工兵)は atk がそのまま回復量**なので**HP寄りに振る**(回復の伸びを+6%に抑えた)。
  - ⚠**基準点(猟犬・バット・ヴァルキリー)の hp/atk は動かせない**=目標曲線がこの3点で決まるため。
    ⭐ヴァルキリーだけ**出撃費 328→260**で費用比を揃えた(cost は式に入らないので基準点を壊さない)。
  - ⚠**burn持ち(火炎瓶・火炎放射兵)は dps に burn×.8 が定数で乗る**ので、atk を同率にしても
    dps は同率にならない。⚠burn を触る時はこの2体を計算し直すこと。
  - ⚠**対戦/協力は先頭10種固定**なので、上がるのは6種だけ(壁2種は据え置き)=対戦は少し壁離れに動く。
  - ⚠部隊の総火力が+11%上がる=**終盤が少し楽になる**。楽すぎたら敵側(`midX`/`lateX`)で戻す。
    ⚠**ここで兵科を再びたるませないこと**。
- ⭐⭐⭐**タレットの進化を2段にした(2026-08-02ユーザー指示)**。実機で「SGタレットを進化させたら鬼軍曹も余裕」
  の原因は**素からいきなり最終形態(ショットガンはDPS 49→152)に跳ぶこと**と、
  **その解放が研究所412🧬で買えたこと**の合わせ技だった。
  - **素 →(中段=新設)→ 最終形態(いままでの進化先)**。⚠**最終形態の数値・id・名前・絵は1つも触っていない**
    =セーブ(`META.tg`)も既存の検査もそのまま生きる。踊り場を1つ挟んだだけ。
  - ⭐**中段は `GRD_MID` から機械的に作る**(id=素のid+`M`)。手で17行書くと `pz`/`drn`/`cont`/`heatN` の
    ような**見えない印を写し忘れて塔が壊れる**ので、`Object.assign({},素)` で丸ごと写してから数値だけ動かす。
  - ⭐**数値は素と最終の相乗平均(√)**。⚠**固定倍率にしてはいけない**=火炎放射塔・レーザー塔は
    最終形態でも間隔を縮めていないので、固定倍率だと中段が最終形態に並ぶ(検査で実際に落ちた)。
  - ⚠**持ち味(multi・chain・aoe・燃焼)は素のまま**=最終形態の「+1」を最後の一歩に残す。
  - ⚠⚠**ドローン基地だけ1段のまま**=機数が `DR_MAX`(5機)で頭打ちなので、中段に置ける
    「伸ばせる強化枠」が作れない(🛩機体数は素で上限・🔫弾数は母艦の特典)。`GRD_MID` に書き戻さないこと。
  - **費用**: ⚙️建て替えは 素×4(→中段) / 素×8(→最終)。🧬研究所は**最終形態だけ2.6倍**(`LAB_TG` の第3引数)。
  - ⚠⚠**中段17種の絵はまだ無い**=`drawTower` が `gmid` を見て**素の絵に読み替えている**(色だけ最終形態のもの)。
    描いたら読み替えと `TW_MZ` の写しを外す。検査は `TODO:` 行で残数を出す(**形が同じでも色が違うので
    「絵が違う」検査は通ってしまう**=通ったからといって絵があるとは限らない)。
- ⭐⭐**🚀開始タブは「段を増やして、1段ごとの効き目も増やす」**(2026-08-02ユーザー指示
  「例で初期スクラップ 40→120→360→720→1200→2000」)。
  - **表は累計**(`SC0_V`/`HP0_V`/`UP0_V`)。⚙️初期スクラップ 6段=+2000(15,966🧬) /
    🏕コアHP 6段=+90(6,035🧬) / 🔩開始の強化pt 6段=+100(4,552🧬)。
  - **枠もの(🤝部隊枠2→4段 / 🧱建設スロット4→6段)は1段=+1のまま段数だけ増やした**(枠は刻めない)。
  - ⚠⚠**効き目・値段・表示・ガチャのはずれ枠を全部この表から引く**=`40`/`5`/`6` を直に書かない。
    値段は「1段の効き目に比例 × 段が上がるほど x1.35」。⚠段を足したら `LAB_MSTEP` の上限も直す。
  - ⚠**はずれ枠(gsc/gup)が最大段に当たったら研究ptへ振り替える**=「+0」を渡さない。
  - ⚠名前は `labClamp`(`labLv` は研究所の「項目→いまの段」で既に使っている)。検査=`checkStart0`。
- ⭐**研究所の値段は2026-07-27に2回上げた**(ユーザー判断「まだ低い」→「少なすぎる」)。
  ①全タブを素の2.7倍(1.5倍→さらに1.8倍) ②**タワー/部隊タブは1段ごとの伸びを x1.13→x1.20**。
  ⭐②は**1段目(81/59)を据え置いて傾きだけ変えた**=最初の数段は軽いまま、上の段だけ重くなる
  (20段目 タワー826→**2,588** / 兵科601→**1,885**)。「レベルが上がるごとに増やして」はこの形にする。
  いまの値: 1本フル= タワー15,123🧬 / 兵科11,014🧬 / 全部取り約65万🧬(1回の出撃で入るのは約2,500〜6,600🧬)。
  ⚠**`checkLabSteps()` の上限(20,000)も一緒に動かすこと**。値段だけ上げると検査が落ちる。
  ⚠🎯砲撃の威力(`LAB_ST0`)は**新種タブ側なので x1.13 のまま**。揃えるなら別途指示を待つ。
- ⭐**「引き継ぐ/0に戻す」は、下がって見える値があるかで決める**(2026-07-27・タレットの進化)。
  廃品工房の建て替えは強化Lvを**0に戻す**が、これは**産出という1つの値しか無い**から成り立っていた
  (次の段の素が前の段のMAXより上)。タレットは 威力/連射/射程 と値が複数あり、
  **射程だけは素×1.12では📡MAX(×1.65)に届かない**=0に戻すと「進化したのに射程が縮んだ」になる。
  → タレットは**強化Lvを引き継ぐ**ことにした。⚠検査も「素の値どうし」で比べること
  (実効値は両方に同じ倍率が乗るので、素の値で比べれば足りる)。
- ⭐**進化先は `base` で元のタワーと研究所の枠を共有する**(`twKey`)。
  分けると「進化した瞬間に研究所で積んだLvが消える」ことになり、進化が罰になる。
- ⚠**`grid=tw` に42種を並べると固まる**(2026-07-27)。素のタワーだけを `grid=tw`、
  進化先だけを `grid=tg` に分けた。⚠1画面に並べる数を増やす時はまず撮ってみること。
- ⭐**「何台も置くと重い」は連射の塔が犯人**(2026-07-27・火炎放射塔とレーザー塔)。
  1秒に7〜10発撃ち、そのたびに 弾・銃口の閃光・曳光線・ダメージ数字・血 を作っていた。
  **継続攻撃(`cont:1`)に変えて、絵は fx ではなく塔の描画(`drawCont`)で引く**ようにしたら丸ごと消えた。
  - ⚠**与える量は変えない**=`dps = T.dmg / T.rate`。⏩連射の強化代が無くなるので `twRateM` は掛けない。
  - ⚠**ダメージ数字も継続だと毎フレーム湧く**。`dmgZ` に quiet(第5引数)を足して、0.35秒ぶんためて1つ出す。
  - ⚠**溜まり(レーザーの昇温)は `tw.chg` に載せる**=レールガンの充電と同じ枠なので対戦の同期にそのまま乗る。
  - ⚠**検査を「fxが出ているか」で書かない**。継続攻撃はfxを出さないので、直した瞬間に落ちる。
    見るのは ①`tw.ct`(狙い先)が入るか ②実際にHPが減るか ③**fxが溜まっていないこと**(=対策そのもの)。
- ⭐**「たまに効かない」の正体は、たいてい"耐性の秒数"と"発射間隔"が近いこと**(2026-07-27・冷却塔)。
  再凍結までの耐性が5.2秒、発射間隔が6秒だったので、⏩連射を買って間隔が縮むほど**空振りが増える**
  =強化するほど体感が悪くなる作りだった。**耐性は廃止し、上限は「回数」で持たせた**
  (回数なら残りが数えられるし、強化(❄冷却回数)の意味もはっきりする)。
- ⭐**同じことを2つの仕組みで止めない**。永久ロック防止が「耐性(時間)」と「回数の上限」で二重になっていた。
  片方で足りるなら片方だけにする=残した方が player に見えるものを選ぶ。
- **敵の攻撃力(`ZOMBIES[].atk`)は `zSpec` で `×√mul` される**=終盤ほど効く。
  2026-07-27に全体1.4倍(ウォーカー14→20 / 原初の獣94→132)。
- **薙ぎ払い(`z.aoe`)は「同時に殴れる部隊の数」**。1体あたりは `atk×0.65`。
  ボス専用だったが2026-07-27に大型5種(brut/tank/nmBoar/sAngl/sTurt)へ広げた。
  ⚠紹介モーダルは `Z.aoe` を見て「薙ぎ払いN体」と自動で出す=タグを足す必要はない。
- **`UNITS` の数値がそのままゲーム内の値**(一括倍率は廃止済み)。強さの調整は hp/atk を直接いじる。
- **壁役(big/shd/gld/tnk/titan)は戦力カーブの対象外**。攻撃力ゼロの壁は戦力式で正しく測れないため、
  `test_balance.js` は壁どうしのHPの並びだけを見る。壁の判定は**IDで固定**する(HPの閾値でやると強化した瞬間に評価が跳ねる)。
- **研究所で解放するタワー10種は、解放順に「実戦DPS」が上がっていること**(2026-07-25ユーザー指示)。
  実戦DPS=そのタワーが現実的に巻き込める数ぶんのDPS(単発1体/複数同時=multi体/範囲4体/ビーム3体/連鎖5体)。
  現行(2026-08-02に `test_balance.js` の出力で取り直した。⚠**古い数字を書き足さない・毎回取り直す**):
  ライフル台29 / ショットガン台49 / 爆裂砲台52 / 火炎放射塔58 / テスラコイル104 / 迫撃砲58 /
  スナイパー塔103 / レーザー塔102 / 酸噴射砲108 / ドローン基地21 / 重砲台89 / 重テスラ168 /
  レールガン174 / ガトリング要塞226 / プラズマ砲307 / 要塞砲482。
  ※前半は役割違いで凸凹しているが、**重テスラ以降は必ず単調増加**にする。
- ⭐**部隊の攻撃範囲は噛みつきの窓(ENG_GAP=36)以上でなければならない**(2026-07-26)。
  ゾンビは gap=36 まで近づけば殴れるが、近接兵科の射程は34〜40しかない。
  素のままだと**猟犬(34)は噛みつかれているのに届かず、旗で足が止まっていて詰めることもできない**
  =一方的に殴られ続ける(ユーザー指摘で発覚)。索敵は Math.max(U.rng,ENG_GAP+2) で見ること。
  	est_headless.js の checkBite() が全近接兵科について検査する。
- **すり抜け敵(noblock)は `hook` を持つ兵科にしか止められない**＝逃げ場が無いので同時足止め枠が広い。
  ⭐**`hook` の値がそのまま「同時に抱えられる数」**(2026-07-27ユーザー指示で鎖使い以外にも増やした)=
  **巨漢2 / 長槍兵4 / 装甲車6 / 鎖使い8**(専門の鎖使いが一番広い)。普通の兵科は `ENG_MAX=3`。
  ⚠ここを普通と同じ3にすると、敵が重なった瞬間に4体目から素通りする。
  ⚠検査(`checkHook`)は **`UNITS.findIndex(u=>u.hook)` で1種だけ見ない**こと=先頭に来た兵科しか見なくなる。
- **属性の相性は2組**: 火炎系タワー×**装甲**(`z.armor`・ステージ1)=`FIRE_VS_ARMOR`1.9倍 /
  電撃系タワー×**鱗の鎧**(`z.scl`・ステージ2)=`ELEC_VS_SCALE`1.9倍。どちらも通常弾は45%しか通らず、貫通は無視する。
  **火炎系の兵科は据え置き**(兵科は戦力カーブで管理しているため)。
- **敵プールは「リストの最後の個体」が終盤に多く選ばれる**(buildTide: W4以降、最大40%の確率で `zp[zp.length-1]` を引く)。
  つまり**そのプールの一番最後に置いた敵が、終盤の総HPをほぼ決める**。プールに敵を足す時は必ず末尾の重さを揃えること。
  ※2026-07-25: ステージ2にレイス/重装兵の相当種が無く、末尾が影サメ(HP195)だったためW20総HPがステージ1の6割しか
  無かった。亡霊エイ・巨大ウミガメを足して揃えた(悪夢W20: ①383k ②373k ≒ 同等)。
- **状態異常の持ち時間はゾンビ側に持つ**: `z.slowT`(減速×.55)/`z.frzT`(完全停止)/`z.frzCd`(再凍結までの耐性)/`z.burnT`。
  タワー定義の`slow:`は**使われていない死んだ値**(減速率は一律.55)。効き目を変えるなら`campStep`の`slow`の行を直す。
- **敵の総量の目安(悪夢W20の総HP)**: ステージ1≒383k / ステージ2≒373k / 🌑ナイトメア≒719k(=悪夢の1.9倍)。
  最終ボス単体HP: 屍山の王・深淵の母(悪夢)=102k / 原初の獣(ナイトメア)=140k。

### 画面・レイアウト
- ⭐⚠**同じ id の枝を2つ書かない**(2026-07-27に実際に踏んだ)。`drawTower` は長い if/else の連なりなので、
  **先に書いた方だけが当たり、あとから直した方は一生描かれない**。
  無人機母艦を「飾りの機体3機」で描いた古い枝が残っていて、**本物の5機とは別に3機浮いて見えていた**。
  ⚠検査(`checkTwLook`)は**形が違うか**しか見ないので、重複した枝は捕まえられない。
    絵を描き直す時は `grep -n "tid==='<id>'"` で**枝が1つだけか**を確かめること。
- ⭐⚠**`innerHTML=''` で作り直す所へDOMを移したら、必ず先に退避させる**(2026-07-27に実際に踏んだ)。
  「→全員に分散」を `#minis` の中へ入れたが、`buildMinis` が毎回 `innerHTML=''` で中身を消すため、
  **2回目の出撃でボタンごと消えて**、以後 `updHUD` の `tc.style` で毎フレーム例外=
  画面下に赤いエラー帯が出続けた。**1回目は動くので撮影では見つからない**(ユーザーが実機で発見)。
  ⚠**2回目でしか出ない不具合は `node test_shot.js out.png 852 393 re2` で撮る**
  (一度タイトルへ戻ってもう一度出撃してから撮る)。
  ⚠あわせて「無くても落ちない」ガードも入れること(DOMを移す作りにしたら必ず null を想定する)。
- ⭐**「そこから出そうな所」から撃たせる**(2026-07-27ユーザー指摘)。
  弾も電撃も砲弾も**塔の中心(sx,sy)から**出していたので、テスラのように
  **てっぺんに明らかな発射口がある塔**で違和感が出ていた。`TW_MZ` に塔ごとの位置を持たせた。
  ⚠**絵の中の座標(局所)に `TW_SC` を掛けて世界座標にする**=塔は1.3倍で描いてある。
  ⚠直す所は多い(銃口の閃光/曳光線/ビーム/電撃の起点/砲弾/継続攻撃の帯)。**1つでも残すと中心から出る**。
- ⭐**盤面(canvas)に置くUIを世界座標で決め打ちしない**(2026-07-27ユーザー指摘・ボスのHPバー)。
  対戦はマップが1割小さい(`SC`が違う)ので、**同じ世界座標でも画面ではその分だけ上に来る**。
  ボスのHPバーが 死潮ゲージ と重なっていたのがこれ。**画面の上から何CSSpxかで置いて世界座標へ直す**:
  `world_y=(cssY*DPR-OY)/SC`。⚠**SC/OX/OY はデバイスpx**(`cv.width=w*DPR`)なので DPR を掛けること。
  ⚠**名前は baseline の上に出る**ので、HUDの下端(約28px)+文字の高さ ぶん余裕を取る(ソロ46px/対戦62px)。
- ⚠**画面の右に大きな空きを作らない**(2026-07-27ユーザー指摘)。対戦の「→全員に分散」を
  `position:absolute;bottom:76px` で画面の下に貼っていたので、相手の小窓との間が80pxほど空いていた。
  `#minis`(縦のflex)の最後の子に入れて流し込むと、相手が1人でも2人でも自動で詰まる。
- ⚠**canvasに描く名前を決め打ちにしない**。ボスのHPバーが常に「ジャガーノート」と出ていて、
  屍山の王・深淵の母・原初の獣・触腕の主でも同じ名前だった(`ZOMBIES[bs.zi].n` から引く)。
- ⭐**気絶は凍結と別の見せ方にする**(2026-08-02ユーザー指示「凍ってるエフェクト一本だから安っぽい」)。
  ⭐**止める仕組みは `z.frzT` のまま / 見せ方だけ `z.stnT`**(頭上を★3つが円を描いて回る)。
  ⚠`stnT` が立っている間は**❄の輪も氷漬けも出さない**(両方出ると何をされたのか読めない)。
  ⚠★は**奥に回った時は小さく暗く**する=平面に並べると回っているように見えない。
  ⚠いま★になるのは 一斉罠・引力場・時間停止・雷嵐 の4つ。**氷は本物の冷気だけ**(冷凍兵・絶対零度・冷却塔・凍結爆弾)。
- ⭐**狙撃の英雄は敵が居なくても構えたまま待つ**(2026-08-02ユーザー指示)。
  ⚠溜め(`u.chg`)は敵が居なくても満タンまで溜まっているので、**見せる条件(`u.ct`)だけを緩める**。
  ⚠⚠**英雄だけ**にすること=兵科のスナイパーまで常時ポーズにすると焼いたコマを使えない体が増えて
    「1か所に固めると重い」が再発する(盤面に英雄は1人だけなので英雄なら安全)。
- ⚠⚠⭐**遅らせておいた演出(`later`)は作戦タイムでも進める**(2026-08-02ユーザー指摘
  「必殺技の最後の一発を撃たなくなってる」)。⚠`campStep` は**ウェーブ中しか回らない**ので、
  狙撃王ジョルジの『撃墜』(締めの貫通弾が押してから3.9秒後)のように**その技で波を終わらせると
  最後の1発だけが宙に浮く**(次の波が始まった瞬間に遅れて出る)。⭐`stepIdle` で待ち行列と演出だけ進める。
  ⚠**敵と部隊は動かさない**(作戦タイムの意味を壊さない)。⚠長い必殺技を足す時はこの制約を思い出すこと。
- ⚠⚠⭐**構えの傾き(TL)は頭だけでなく武器も戻す**(2026-08-02ユーザー指摘「ジョルジがまだ↓向いてる」)。
  頭を `TLC` で水平に戻していたが**銃には傾きが残っていた**ので、銃口だけ下を向いた狙撃手になっていた。
  ⚠**肩を軸に**戻すこと。⚠腕と銃が2か所に分かれて描かれている場合は**両方**戻す(片方だけだとねじれる)。
- ⚠⚠⭐**`H_FX`(英雄の固有攻撃)は「着弾の瞬間」に呼ばれる。投げる絵をここに書かない**
  (2026-08-02にユーザー実機で発覚「爆破屋の攻撃がエフェクトと着弾のタイミングで合ってない。2回爆弾投げてる?」)。
  ⭐飛ばすのは**呼び出し側の `tossFly`** で、その飛行時間ぶん `later()` で遅らせた先が `H_FX`。
  爆破屋の `H_FX` は**そこでもう1個 `toss` を積んでいた**ので、
  **爆弾が2個飛び、爆発だけがさらに0.34秒遅れて**出ていた(当たり判定は1個目の着弾で起きる)。
  ⚠**投擲型(`U_MOT` が 'toss')の英雄はいま爆破屋だけ**。増やす時も H_FX は着弾の絵だけにする。
  ⚠あわせて**爆発は「投げた瞬間の狙い先」で出す**=`H_FX` に渡す tg を固定点に差し替えた
  (当たり判定 `hit9` と同じ点。渡さないと敵について行って「当たっていない所で爆発」に見える)。
  検査=`checkHeroFx` で**実際に1発撃たせて**「飛んだ物1個 / 爆発と命中が同じコマ」を見る。
- ⭐**色と書体は「表」で決める。直に書かない**(2026-07-26 第75弾)。同じ役目の色が少しずつ違う値で散らばり、
  334色まで増えていた。**CSSの `:root`** と **JSの「見た目の決まりごと」ブロック**(ICONS-ENDの直後)に
  **同じ値の表**を置いてある。片方だけ直すと**HTMLとcanvasで色がずれる**ので必ず両方直すこと。
  - 役割で選ぶ(見た目が近いからという理由で選ばない): 基調=`INK`〜`INK4`/`VOID`・`PANE`/`PAPER`/`PAPER2`/`PAPER3`/`DIM`/`DIM2`、
    差し色=`RUST`/`RUST2`/`AMBER`/`GOLD`/`GOLD2`/`GLOW`/`TOXIC`/`LIFE`/`LIFE2`/`TECH`/`TECH2`/`FROST`/`STEEL`/`HEROC`/`DANGER`/`DANGER2`/`GONE`、
    レア度=`RARE[0..4]`(CSSは `--r1`〜`--r5` と `--r2bg`〜`--r5bg`/`--dud`/`--dudbg`)
  - **canvasの文字は必ず `'900 '+px+'px '+FF`**。以前は canvas だけ `sans-serif` で、パネル(丸ゴシック)と別書体だった
  - ⚠残っている直書きは**キャラ/ゾンビ/タワー/背景の絵の色**。ここは絵を作り直す時(⑤⑥)まで触らない
  - 検査は `test_headless.js` の **`checkPalette()`**=書体の通し漏れ・CSSとJSの値のずれ・
    文字色とHTML本文の直書きを静的に見る。**色や書体を足す時はこの表に1行足す**
- **CSSの順番の罠**: `@media (pointer:coarse)` のスマホ用指定は、**対象の定義より後ろ**に書かないと効かない(同じ強さの指定は後ろが勝つ)。
  デッキ・操作バー系は `#deck .dk:disabled` の直後の第2ブロックへ書く。上のブロックにはHUD系だけ。
- **操作バーの幅にもう余裕が無い**(実測 中身730 vs 使える幅734)。**兵科を増やす時は必ず幅を再計算**する。しないと横スクロールになる。
  2026-07-25の実測(852×393・スマホCSS): 🚩52+🎯52+デッキ453(8枚)+🦸52+🔊38+🏳諦める95+すき間25=**767 / 使える幅836**。
  🦸英雄ボタンは**対戦の🌊押すが消えるソロにしか出さない**ことで増分を吸収してある。これ以上ボタンを足す余地は無い。
- **`el.style.display=''` はCSSの`display:none`に戻る**。CSSで初期非表示にした要素を出す時は`'inline-block'`等を明示すること
  (🦸英雄ボタンで踏んだ)。
- 地面の裁ち落とし: 時間帯の色・夜の陰・ビネットは**マップ矩形ではなく画面の可視範囲(VL/VT/VR/VB)**に掛ける。
  マップ矩形に掛けると四角い縁が出て「盤が浮いて」見える。
- **建設マスを動かす時の除外条件**: 道路の中心線から95 / 拠点150 / 協力の出撃テント(BRANCH)120 /
  コアHPバーの位置(x<270・y385〜535) / **マス同士72**(枠64pxが重ならない距離) / 画面内(x75〜1560・y205〜730。工房枠だけ上端の制限なし)。
  ※以前はマス同士110だった。**タップ判定を「範囲内の一番近いマス」に直した**(先着順だと近づけた時に奥のマスが押せなかった)ので
  くっつけて置けるようになった。**自動配置スクリプトの方は見た目のゆとりのため110のままにしておくとよい**。
- **建設マスの数は全ステージ25で固定**(通常22+工房3+支援2)。ECO_BASE=22/SUP_BASE=25が動くと保存や配列の形が壊れる。
  工房枠とBRANCHは全ステージ共通の座標。
- ⭐**初期から使えるマスは `SLOT0`(2026-08-02に7→8)**。中身は各ステージの `order` の**先頭 SLOT0 個**。
  ⚠2026-08-02にユーザーが実機の画面で指定して、ステージ1の**マス21([1229,349]=侵入路の内側)を8番目へ繰り上げた**。
  ⚠**既存の7か所は取り上げていない**(=8つ目として足した。ステージ2も自動で1枠増える)。
  ⚠`order` は「⚙️で買う順」と「CPUの建設優先順(`AI_ORDER`)」も兼ねる=並べ替えると両方動く。
  ⚠チュートリアルの本文に「7ヶ所」と書かない(`checkTut` のNG語に入れてある)。
- **🛠 マスの位置を決めるなら実機の編集モードが一番速い**: 公開URLに **`?edit=1`** を付けて起動すると、
  建設マスを指でドラッグして動かせる(番号と座標が出て、条件違反は赤枠+理由つき)。
  **📋出力**ボタンで `slots:[...]` / `ECO_SLOTS=[...]` / `` が出るので、それを貼れば確定。
  ➕マス/🗑削除もあるが、**数を変えたら必ず報告すること**(ECO_BASEがずれる)。`?edit=1`が無ければ一切動かない。
- **マスを一から置き直す時は自動配置スクリプトを使う**(手で並べると条件を1つ落とす)。角のマス→残りを道に近い順に詰める、
  という手順で全条件(道95/コア150/工房135/施設135/BRANCH120/HPバー帯/マス同士110/画面内)を機械的に満たせる。
- 研究所の項目は**値段順に並べ替えない**。買うと値段が上がって順番が入れ替わり、押した項目が別の場所へ飛ぶ。
- **⚠横画面のスマホは高さが約393pxしか無い**。ここが全レイアウトの制約。見出し+説明文+情報帯を縦に積むと
  **本題(買える項目/ボタン)が1つも見えない**画面になる。2026-07-25に研究所・編成・ガチャ・鍛錬所・タイトルを作り直した。
  - **一覧が長いモーダルは `.bigpanel` の型を使う**: `.bphd`(見出し・固定) + `.tabs`(タブ・固定) + `.bpbody`(ここだけスクロール)
    + 必要なら `.bpfoot`(決定ボタン・固定)。項目は `.lgrid`/`.lc` のカードにする(アイコン/名前/小ラベル/説明3行/価格ボタン)。
    説明文をカードの外に長く書かない。カテゴリはタブで切り替える=1画面に1カテゴリだけ出す。
  - ⚠**`.bpbody` に `max-height` を持つ古い指定が残っているとパネルの下に死に空間ができる**(`#lab-items`で実際に踏んだ)。
  - **タイトル画面は `@media (max-height:560px)` でロゴとメニューを左右2カラムに切り替える**。縦積みのままだと
    ロゴが画面外へ出てボタンの下2つが切れる。
- **アイコン画像の入れ方は3通り**(2026-07-26)。素材は `ui_src/` → `tool_slice.js` → `tool_embed.js` で index.html へ埋め込む。
  - `IC(html文字列)` … 文字列中の**資源5種の絵文字**(⚙️🔩🧬💎🔧)を`<img>`へ一括置換。**innerHTMLに入れる時だけ**使う
  - `icoImg(名前,絵文字)` … 名前で1枚ぶんの`<img>`。素材が無ければ絵文字に落ちる
  - `applyIcons()` … 静的なボタン/見出しの絵文字を起動時に差し替え。⚠**updLabBtnの後に呼ぶ**(先だと数字側の💎を食う)
    ⚠**同じ画面の同じ資源は必ず同じ絵にする**(2026-07-26ユーザー指摘)。💎召集は見出しだけ画像で
    ボタン(単発/10連)と説明文が絵文字のまま残り、**1画面に2種類の💎**が並んでいた。
    ⚠ の置換は **u«·¦ét@C͂ è܂¹ñ で最初の1つだけ**。同じ絵文字が複数ある要素(あそびかたの本文など)は
    直しきれないので、そこは絵文字のままにしてある(文章の中の絵文字は画像にしない方が読みやすい)。
  - **canvasの中は`drawIcoText(c,文字列,x,y,サイズ)`**。canvasはHTMLを描けないので、文字を絵文字で切って
    「文字→画像→文字」と順に並べている。素材が無い環境(ヘッドレス)では黙って絵文字のまま描く
- ⭐**専用枠は「他のどの枠とも違う見た目」にする**。しかも⭐**未解放(🔒)の状態こそ区別が要る**
  (2026-07-26ユーザー指摘=「分かりづらいと言ったのは**解放前**の見た目が普通のタワー枠の解放前と同じだから」)。
  🔒未解放: 支援枠=**青い破線枠+🏗+「🔒支援」** / 工房枠=**琥珀の破線枠+🏭+「🔒工房」** / 普通の枠=小さく薄い🔒だけ。
  解放後: 支援枠=青い二重枠+「支援施設」 / 工房枠=茶枠+「廃品工房」。
  🏗支援枠=**青い二重枠+`支援施設`のラベル** / 🏭工房枠=茶枠+`廃品工房`のラベル。
  ⚠アイコン(🏗/🏭)だけでは足りない。**枠の形・色・名前の3つ**でようやく一目で分かる。
- ⭐**ボタンを押した音は「1本のリスナー」でまとめる**(2026-07-26ユーザー指示)。
  ボタンごとに `sfx.tap()` を書くと必ずつけ忘れる(実際タイトル・研究所・編成・召集が全部無音だった)。
  `UI_TAP_SEL` に当たる要素を `pointerdown` の capture で拾って鳴らす。
  **tap=進む(高い) / back=閉じる・戻る(低い)** の2つだけ。⚠すでに音があるもの(デッキの出撃・建設メニューの品目・小窓)は
  **セレクタに入れない**=二重に鳴る。音は素材ではなく `beep` の合成なのでファイルサイズは増えない。
- ⭐**操作の説明は🎓チュートリアル(`tutSteps`)に一本化してある**(2026-07-26 第86弾ユーザー指示
  「案内いらんな。チュートリアルを最初に用意して、そこで全部説明すればOK」)。
  **WAVE1のトースト案内(`waveTips`)と📖あそびかたのモーダルは両方とも廃止した**。書き戻さないこと。
  - **実際に操作させる形**(ユーザー選択)。段ごとに「光らせる所」と「進む条件」を持ち、
    **その操作を実際にした時だけ進む**(時間で勝手に進めない)。読むだけの段は▶で進む
  - ⭐**いきなり戦場を始めない**(2026-07-26ユーザー指示)。**タイトルで「🎓あそびかた」を光らせる段**から始まる。
    はじめての人には起動0.6秒後にこの段が出る(難易度を1つでもクリアしていれば出さない /
    `META.tut` で二度目は出さない / `?sfx=1`・`?edit=1` では出さない)。ボタンから開いた時はそのまま戦場へ。
  - ⭐**終わりに💎3個を渡し、ガチャを1回引くまでがチュートリアル**(2026-07-26ユーザー指示)。
    ⚠**💎はその段に入った時点で渡す**=途中で抜けても手元に残る。
    ⚠🛠DEVでは💎が補充されるので「減ったか」だけでは引いたと判定できない=演出(`GC`)が始まったかも見る。
  - ⚠**段は画面をまたぐ**(`scr:'title'`/`'game'`)。`tutGo()` が自動で行き来する。
    `TUT.mv` が立っている間は「チュートリアル自身が画面を移している最中」なので `backTitle` で畳まない。
    ⚠**毎フレームの `tutStep` は `gameActive` で絞らない**(タイトル側の段があるため)。
  - ⚠**タイトル側の段では帯を下に出す**(`#tut.tl`)。上だと「ソロ作戦」のボタンに被る。
  - ⚠**帯の文字は `IC()` を通す**。`textContent` のままだと💎が絵文字(青)、ボタンは画像(紫)で
    **1画面に2種類の💎**が並ぶ(過去に指摘された事故と同じ形)。
  - **本編の記録を汚さない**: `G.tut=1` を立てて `awardMeta`/`saveRun` を素通りさせる。新兵・ステージ1に固定
  - ⚠**盤面の印は「ずっと出す」**。輪(`addFx`)を打ち直す作りにしたら、消えている間に
    どのマスか分からなくなった。`tutMark()` が毎フレーム 太い輪+下向き矢印 を描く
  - ⚠**光らせるマスは一度決めたら動かさない**(`TUT.si`)。毎回選び直すと、建てた瞬間に
    「空いている枠」が別のマスに移り、印が飛ぶうえ「建てたか」の判定が別のマスを見て**永久に進めない**
  - ⚠**光らせるマスは画面の下寄りから選ぶ**。チュートリアルの帯が画面の上に出るので、
    一番上の枠を選ぶと**印が文で隠れて「光っているマス」が見えない**
  - ⚠**1波目の前には作戦タイムのパネルが出ない**(2波目以降だけ)。そこで「✅配置完了!」を
    押させようとすると、見えないボタンを待つうえに時間も止めていて**永久に進めなくなる**
  - ⭐**光っている所以外は押せない**(2026-07-26 第89弾ユーザー指示「ここを選べって案内はあるけど
    ほかのところをタップできちゃう。強制的にやらせて」)。`tutPass()` が `pointerdown/pointerup/click` を
    **capture で握り潰す**(押した音のリスナーより**先に登録**して `stopImmediatePropagation`
    =押せていないのに手応えだけ鳴るのを防ぐ)。押せない所を押すと帯が左右に揺れる(`tutNope()`)。
    段ごとの許可は3つ=**hi**(光らせる所)/**al**(その段だけ許すセレクタ)/**sl**(建設マス。
    **1=光っているマスだけ・2=どのマスでも**)。
    - ⚠**詰みの逃げ道を必ず用意する**(全部実機で踏んだ): ①**建てる段は `sl:1`**
      =建設メニューを閉じられた時に開き直せないと永久に詰む(閉じている間は印も出し直す)
      ②**戦っている段(fight/clear)は `al:'#deck,#flagbtn,#strikebtn'`+`sl:2`**
      =手を縛ると押せないまま拠点を削られる ③**`TUT_FREE`(=`#md-intro`)と `PAUSED` 中は全部通す**
      =新登場の紹介モーダルはOKを押すまで `PAUSED` なので、塞ぐと止まったまま動かない
      ④**リザルト(`SCR==='result'`)は全部通す** ⑤**`tutBegin` で `META.tut=1`**
      =万一詰んでも再読み込みで自動起動しない(最後の逃げ道)
  - ⭐**「ここだ」の矢印を添える**(2026-07-26 第90弾ユーザー指示「矢印とか"ここだよ〜"みたいな表示が欲しい。
    左右とかに反復しながら案内印」)。**枠を光らせるだけでは気づかれない**。`#tutptr`=金色の矢印を
    光らせている所のすぐ外側に置き、**指している方向へ往復**させる(`tutPtrPos()` が置き場所と向きを決める)。
    - ⚠**置き場所は「空いている方」ではなく「隣に何が並んでいるか」で決める**:
      **下端の操作バー(デッキ・🚩・🎯)は真上から**指す(横から指すと隣のボタンに重なる)/
      上端のHUDは真下から / それ以外は横から(縦が393pxしかないので上下に置くと被る)。
      画面からはみ出す時は反対側へ回す
  - ⭐**工房枠・支援枠を教える段は帯を下へ回す**(`low:1` → `#tut.tl`。2026-07-27)。
    ⚠この2つは**画面のいちばん上**にあるので、帯が上のままだと**輪も矢印も帯の下に隠れる**
    (撮って気づいた=「光っているマス」の説明なのに何も見えない)。
    決め打ちのマスを光らせるのは `hi:{k:'slotI',i:マス番号}`(`tutSlot()` は普通のマスしか返さない)。
    そのマスだけ押せるようにするため `tutSlotOK()` は **3** を返す。
  - ⭐**説明のために英雄を1人つけておく**(`tutBattle` で `hUi` を直に入れる)。
    ⚠持っていなくても付ける=🦸ボタンが出ないと英雄の説明ができない。`G.tut` が立っているので記録は汚れない。
    - ⚠**盤面のマス/拠点の段では出さない**(`tutMark()` の輪と矢印があるので二重にしない)。
      **PAUSED中も引っ込める**(紹介モーダルの上に浮いて残る)。`pointer-events:none` は必須
    - ⚠**位置を決める所はDOMを触らない**(`tutPtrPos(r,W,H,S,G)`)=寸法を渡すだけで検査できる
  - 検査は `test_headless.js` の **`checkTut()`**=①古い言い回しが無いか ②説明の抜けが無いか
    ③操作させる段が4つ以上あるか ④戦績を汚さないか ⑤**進めなくなる段が無いか**(実際にウェーブが始まるか)。
    縛りの方は **`checkTutLock()`**=偽の要素(closestだけ持つ)で段ごとに「押せる/押せない」を見る。
    撮影は `node test_shot.js out.png 852 393 tut=3`(数字はその段まで進めてから撮る)。
    **`+tutbm`** を足すと塔を建てて強化ウィンドウも開く(帯と被っていないかを見る用)。
    ⚠⚠**ヘッドレスは撮る直前に resize を飛ばす**(`--screenshot` が窓を作り直す)。
      index.html は resize で `closeBM()` するので、**開いた窓は撮影の瞬間に必ず閉じられる**。
      `setInterval` で開き直しても間に合わない=**resize のリスナーを足して開き直す**こと。
      (窓が写らない絵を3回撮って、ツールでなくゲーム側を疑ってしまった)
  - ⭐**「あとで見る条件」は待ってから見る**。読み込んだ瞬間に条件を見て、あとで待つ形にすると
    待っている間に条件が変わっても効かない(撮影ツールが自動起動を止めても始まってしまい、
    タイトルの撮影が全部チュートリアルの絵になった)
- ⭐**開いている画面は「開いた瞬間の値」で固まらせない**(2026-07-26ユーザー指摘)。
  建設メニューは開いた時点の⚙️で押せる/押せないが決まっていたため、
  **開いている最中に敵を倒して足りたのに、閉じて開き直すまで強化できなかった**。
  `refreshBM()` が0.12秒ごとに押せる状態だけを付け直す。⚠**中身(innerHTML)は作り直さない**
  =押している最中に作り直すとタップを取りこぼす。価格は各ボタンの `data-p` に持たせてある。
  **新しくボタンを足す時は `data-p` を付けること**(付け忘れると、そのボタンだけ固まったままになる)。
- **未解放の建設マスは小さく薄く描く**(2026-07-26)。27マス中20マス前後が未解放なので、
  全部くっきり描くと盤面が錠前だらけになり「今どこに建てられるか」が読めない。
  ⚠タップ判定は座標からの距離なので、見た目を小さくしても押しやすさは変わらない。編集モード(`?edit=1`)では従来どおり全部出す。
- **画面中央上に何かを置いてはいけない**。上のHUD(#topbar)は左上から横に455px伸びており、
  中央寄せの帯を置くと**コアHP・⚙️・🔩・⏸ を丸ごと覆い隠す**(2026-07-25まで死潮ゲージが実際にそうなっていた)。
  さらに画面右上(x420以降)は道路と侵入口の帯(y13〜)なので置けない。**空いているのは上のHUDの真下・左寄せの1行だけ**
  (x0〜420・y33〜50。ここも建設マスの上端y41を数px掠める=これ以上は詰められない)。
- ⭐**タワーは「砲身の太さ・本数・砲口の形」で分け、特徴パーツは基部(回らない側)に置く**
  (2026-07-26 第91弾)。細い棒を1本乗せるだけだと、実機35pxでは**どれも同じ塔**に見える。
  砲身の共通パーツは `twGun`(筒)/`twMuz`(砲口 brake・flare・cone・ring・sight)、
  特徴パーツは `twFins`(放熱フィン)/`twDrum`(弾倉)/`twBelt`(弾帯)/`twTank`(タンク)。
  - ⚠**特徴パーツを砲身側に置かない**。砲身は狙う向きで回るので、向き次第で見分けが付かなくなる
  - ⚠**フィンは基部に食い込ませる**。離すと宙に浮いた棒に見える(撮って気づいた)
  - ⚠**粒を並べない**。レーザー塔にレンズを3つ並べたら実機で芋虫に見えた=先端に1枚だけにした
- ⭐**兵科32種は共通部品を直すと一度に全部良くなる**(2026-07-27 第100弾)。
  ゾンビは1種ずつ枝を持っていたので個別作業だったが、**兵科は `uBody`(胴)と脚のコードを共有**している。
  - `uBody`: 角丸長方形をやめ、**肩を落として腰でくびれさせた形**+**帯**(胴の塊を割る)
  - 脚: 5x10の四角2つ+履き物なし → **根元が太く先が細いズボン+軍靴**
  - ⭐**腕(`uArm`/`uShoulder`)を足した**。それまで**腕が1本も無く、武器だけが体の横に浮いていた**
    (76pxのプレビューで見て気づいた)。奥の腕は胴より先に描いて根元を隠し、手前の腕は胴の後・武器の前
  - ⚠**ブロックコメントの中に「アスタリスク2つ+スラッシュ」を書くとコメントが早く閉じる**。
    強調の `**` の直後にスラッシュを置くと起きる。症状は
    「どう見ても正しいコメント行で Invalid regular expression: missing /」(2026-07-27に2回踏んだ)
  - ⚠`grid=u` の撮影は環境によって固まる(32種×2倍率+`bakeUThumb`のgetImageData)。
    その時は `intro=u` と `u=<id>` の実戦で見る
- ⭐**兵科は「体型・かぶり物・背負い物」で分ける**(2026-07-26 第91弾・`U_LOOK`)。
  **武器だけを変えるのは効かない**(腕の先の小さな棒は25pxで消える)。頭と背中は輪郭に出るので効く。
  - 体型=`uBody`(細身/普通/ずんぐり/のっぽ/前傾。上を明るく下を暗く1段) /
    かぶり物=`uHead`(25種) / 背負い物=`uBack`(タンク・弾倉・矢筒・工具・マント等)
  - ⚠**同じかぶり物を5種以上に付けない**(`checkULook` が落ちる)
  - ⚠角や耳は**横に太く**。細く上へ伸ばすと兎に見えた。細い輪を重ねると石鹸の泡に見えた
- ⭐**英雄18人は `H_LOOK` で1人ずつ**(2026-07-26 第91弾)。かぶり物・武器・背中の物が全員違う
  (`checkHeroLook` が重複を落とす)。**光の色はレア度の色**(`RARE`)にしてある=
  全員を同じ琥珀色で光らせると、せっかく分けた色が光に飲まれて全部金色に見えた。
- ⭐**プレビューは `fitDraw()` で枠いっぱいに描く**(2026-07-26 第91弾ユーザー指示
  「全容が見える大きさに」)。決め打ちの倍率だと**大きい物ははみ出し、小さい物は豆粒**になる。
  やり方=一度下絵に描いて絵のある範囲を測り、**枠いっぱいになる倍率でもう一度描き直す**
  (焼いた絵を引き伸ばすとぼやけるので、測るのは下絵・本番はベクタで描き直し)。
  使っている所=🔬研究所のカード(タワー/兵科/派生)・🎖編成(`ldPrev`)・新登場の紹介(`#itr-cv`)。
  派生キャラを描く時は `drawUnitAs(c,i,def)`(⚠UNITSを必ず元に戻す)。
  - **背景の上に重ねる時は `fitDrawIn(c,fn,x,y,w,h,pad,key)`**(canvasの一部分を枠に見立てる版)。
    ⚠**測るのは重い**(200×200の全画素)ので、毎フレーム描く所は必ず `key` を渡して覚えさせる
  - ⭐**足元の光や影を敷くなら `fitRect()` で「絵が実際に占める矩形」を先に測る**(2026-07-27に踏んだ)。
    枠の中で絵は上下に寄るので、決め打ちの座標に光を置くと**足元から離れて水たまりのように見える**。
    ⚠絵には★や影も含まれるので、**キャラの見た目の大きさ=枠の高さではない**
- ❌**「形そのものをコードで描き直す」も捨てた(2026-07-27 第94弾・ユーザー判定=「ゴミ」)**。
  ウォーカー1体で試した内容: 真円/角丸長方形/丸い端のカプセルをやめ、**手足を「根元が太く先が細い帯」**
  (`limb`/`limbPath` を追加)、**胴+頭を1本のベジエ輪郭**(首で細くする)、腰を胸より細く、裾を欠かす、
  腕を片方前・片方後ろに垂らす。**まるごと元に戻した**(コードも消してある)。
  - 途中で踏んだ形の失敗(記録): 首を細くしないと頭と胴が1つの卵=鳥になる / 裾がいちばん広いと洋梨になる /
    腕を左右対称に出すと1枚の翼に潰れる / 手前の腕を胸の中央から生やすと黒フチが襷に見える
  - ⭐**ここまでで3つ試して3つ捨てた**(①ドット絵 ②輪郭の2度描き `?v2=1` ③形の描き直し)。
    **⚠見た目の作り直しは、これ以上コードの描画をいじる方向では詰まっている**。
    次に手を出す時は方向自体をユーザーと決めてから着手すること(勝手に4つ目を試さない)。
- 🧪**「〇と□を組み合わせた感」を消す2度描き(2026-07-27・`?v2=1` の時だけ)**。
  ユーザーの問い「ほんとにあの〇とか四角の素材を組み合わせてる感ってなくせるの?」への試作。
  ⭐**原因は部品ごとに黒フチが閉じていること**。胴・頭・腕・脚を1個ずつフチ付きで描いて重ねているので、
  生き物1体ではなく「図形が5個乗っている」に見える。
  - 直し方=**同じ絵を2回描く**。①太らせた黒だけ(部品が融合して輪郭が外周に1本になる)
    ②塗り+**内側だけの細い線**。⭐**キャラの描画コードには一切触らない**=
    canvasの手前に層(`silLayer`/`flatLayer`=Proxy)をかぶせて振る舞いだけ変えるので、
    ゾンビ36+兵科32+英雄11+タワー20を1個ずつ描き直さずに済む。焼き込み時しか通らないので重くない
  - ⚠**縁の光は失敗した**(`VEC2_R=0`で切ってある)。明色を太らせて重ねると黒の輪郭を塗りつぶし、
    **黒フチが1px以下に痩せて全体が白っぽい塊**になった。出すなら黒より十分細く・薄くすること
  - ⚠**内側の線を全部捨てると胸当てや肩当てが胴に溶けて塊になる**。捨てずに
    「その部品の塗りを暗くした色で・半分の太さ」で引く=外周は黒1本、内側は色の段差として残る
  - 撮る: `node test_shot.js out.png 852 393 "z=walk,arm,brut+v2on+w4000"` / 大きく見るなら `intro=z+v2on`
- 🧪**ドット絵の試作(2026-07-27 第92弾・ウォーカー1体だけ)**。ユーザーの問い
  「ハリボテじゃなくちゃんとした見た目にできないか」に対する実験。**既定は切ってある**
  (`PX_ON`)。**公開URLに `?px=1`** を付けた時だけ有効=1体だけ画風が違う状態で公開しないため。
  - 仕組み: `PX_Z` に1文字=1画素の絵を置き、`PX_PAL` で色を引く。`pxDraw()` が
    `imageSmoothingEnabled=false` で描く。⚠**焼き込み(bakeZ)を通さない**(焼くとドットがずれてボケる)
  - ⭐**向きは3つ持つ(横向き/正面/背面)**(2026-07-27ユーザー指示)。⚠**道の約半分は縦の区間**
    (①廃線ハイウェイ47.8% / ②沈んだ港30.3%)。横向きの絵だけだと、そこを歩く間ずっと真横を向いたまま
    上下に滑る=**「歩いている」ではなく「絵が動いている」**に見える(ユーザーが気づいた)。
    ⚠道は全部直角なので**3枚で足りる**(斜めの区間はゼロ)。左向きは横向きの反転。
    - 絵は**1体=1回の生成で3行×4コマ**にまとめる。⚠**生成をまたぐと同一性が保てない**ので、
      横向き・正面・背面を別々に頼むと**向きが変わるたびに顔が変わる**
    - データ: `PX_Z[id]` の直下が横向き、`.fr`=正面 `.bk`=背面。⚠**幅(w)と基準点(ax)は向きごとに違う**
      (横向きは腕を伸ばすぶん広い)。持っていない種類は横向きに落ちる
    - 選ぶのは `pxVw(角度)`。⚠**画面のyは下が正**なので `sin>0` が「手前に来る=正面」。
      ⚠**正面と背面は左右反転してはいけない**(頭の傷が裏返る)=`pxDraw` で外側の `dir` を打ち消している
    - ⚠生成AIは**頼んでいない4行目を足すことがある**(ウォーカーで実際に出た)。
      `node tool_px.js <png> <id> --map=0,1,3` でどの帯を使うか選び直せる
  - ⚠**共通パレットは32色で満枠**(`CHARS`の上限)。24色目までがウォーカー由来の緑・黄・青、
    残り8色が鋼3・赤2・紫2・桃1。⚠鋼を足す前は**アーマードの胸当てと兜が黄緑に化けて消えた**。
    色を足したい時はどれかを捨てること
  - ⚠**頭を大きく(全高の4割)・目を大きく**。写実的な等身で描くと実寸ではただの棒になる(1回目に踏んだ)
  - ⚠**`lh`(盤面での高さ)をコード描画に合わせる**。合わせないと他のキャラと大きさが揃わない
  - 分かったこと: **画風は全部そろえないと駄目**(ドット絵のゾンビの隣にベクタのゾンビが居ると別ゲームに見える)。
    やるなら**カテゴリ単位で全部**。タワーは砲身が回るので、基部だけドット絵+砲身はコード描画の合わせ技になる
  - 撮る: `node test_shot.js out.png 900 700 pxcmp`(コード描画と並べて実寸/3倍/6倍)/
    `node test_shot.js out.png 852 393 "pxon+z=walk,walk"`(実戦)。検査は `checkPixel()`
- ⭐**見た目を直す時は `grid=` で並べて撮る**(2026-07-26 第91弾):
  `node test_shot.js out.png 1400 800 grid=tw|u|hero|z`。**左が実寸・右が3倍**で並ぶ。
  ⚠**実寸で見分けが付くか**が本番。3倍だけ見て満足すると実機で潰れる。
  紹介モーダルは `intro=t|u|z` で撮れる。検査は `checkULook`/`checkHeroLook`/`checkTwLook`/`checkPreview`。
- ⭐**キャラの表示を大きくした(2026-07-27 第95弾ユーザー指示「でかくしよ」)**。
  ⚠それまで**ウォーカーは画面で22.7px**しかなく(実測・SC=0.392)、**この画素数が「絵が安っぽい」の本当の原因**だった
  (見た目の作り直しを3回捨てたのは、22pxで差の出ないものを直そうとしていたから)。
  - 仕組み=`CH_UP`(1.5)と`CH_P`(.72)。`chUp(sc)=CH_UP*sc^CH_P` を **ZOMBIES[].sc と TSCALE** に掛ける。
    ⚠**一律倍率にしない**。ボスは元から111pxあるので1.5倍だと画面(高さ298px)の半分を覆う。
    **小さいものほど効く累乗**にしてある=雑魚×1.5 / 屍熊×1.29 / ボス×1.15
  - タワーは `TW_SC`(1.3)。⚠**建設マスの枠とタップ判定は変えていない**(枠を広げると72px間隔で接触する)
  - 結果(画面px): ウォーカー22.7→**34** / ブルート29→40 / 重装兵32→43 / 屍熊42→53 /
    ジャガーノート58→71 / 屍山の王111→123 / 深淵の母は幅137→146
  - ⚠**Z.sc と TSCALE は見た目にしか使われていない**(当たり判定・射程・戦力・ENG_GAPには入らない)=
    バランスは動かない。焼き箱(`bakeSet`)も scale に比例するので絵は切れない
  - ⚠⭐**演出の「py からの高さ」を同じだけ持ち上げること**(62か所に `*CH_UP`)。
    掛け忘れると**銃口の閃光・血・ダメージ数字・HPバーが体の中にめり込む**。
    ゾンビの印(❄👑HPバー)は元から `*ZOMBIES[].sc` を掛ける作りだったので触っていない。
    ⚠持ち上げは**一律 CH_UP**(個体ごとの sc ではない)=元のコードも一律だったので、
      個体ごとにすると今までの見え方が変わる
- ⭐**キャラの絵は「色の区分け」で決まる(2026-07-27 第96弾・ウォーカーで実施)**。
  ⚠それまで**頭も服も脚も `Z.c` 一色+明暗1段**だった=形をどう凝っても「緑の塊」にしか見えない。
  第94弾(形だけ描き直し=不採用)との違いはここ。**34pxあれば4色に分けられる**:
  **肌(`Z.c`) / シャツ / ズボン / 靴**。これだけで「服を着た人」に読める。
  - ⚠**肌を出す所を作る**(頭・片腕の袖から先)。全部服だと着ぐるみに見える
  - ⚠**割り付けが一番効く**: 全高55のうち **頭13 / 胴17 / 脚24**。
    頭を3割まで大きくすると人形、2割まで小さくすると顔が読めない
  - ⚠**頭は肩より狭く**(頭12幅 vs 肩19幅)。同じ幅だと全身のうち頭だけが目に入る
  - ⚠**頭に明暗の段を入れない**(`zFill` の第4引数をあごの下に置いて効かなくする)。
    顔の途中で切ると上半分が別の球に見えて**頭に瘤が乗った絵**になる
  - ⚠**両腕を前に出す**のがいちばん「ゾンビ」に読める。ただし**奥の腕は短く・低く**
    (長さと高さが揃うと1本の太い腕に潰れる)
  - ⚠**血は横に広く付ける**。縦長にするとネクタイに見える(2回踏んだ)
  - ⚠**髪を側頭部まで回すと頭巾/ヘルメットに見える**。前髪だけか、禿げにするのが安全
  - ⭐**「ハリボテ感」の正体は次の4つ**(2026-07-27ユーザー指摘でブルート/ジャガーノート/ストーカーを直した):
    ①**同じ部品を等間隔に並べる**(棘3本・鋲・肩パッド左右対称)=**長さ・傾き・大きさを不揃いに**する
    ②**直線の台形**=段ボールに見える。胸を外へ張らせ腰でくびれさせ**肩の上を丸める**
    ③**一直線の帯の腕**=板に見える。**肘で折る**
    ④**棘や角を胴の上から描く**=底辺の黒フチが体を横切って「櫛を乗せた」ように見える。
      **胴より先に描いて根元を体に埋める**(手足と同じ扱い)
  - ⭐⭐**兵科32種の骨格を2026-07-31にバットへ揃えた**(共通部品なので**1回直すと32種に効く**)。
    ⚠それまでの割り付けは **頭17 / 胴23 / 脚10**(全高50)=頭でっかちで脚が黒い塊だった。
    ⭐**いまの割り付け=頭12.6 / 胴16 / 脚18.8**(全高46。バットは12.6/13.4/20.5)。
    直した5か所(ぜんぶ共通部品):
    ①**脚**=腿→膝→すね→靴の4段にし、**基準をほぼ0にして振りだけで前後**させた(1周期に2歩)。
    ②**胴(`uBody`)の丈**を24→16に詰めた。⚠`uShoulder` の表も**必ず同時に直す**。
    ③**頭(`uHead`)をあごを軸に0.74倍**。⚠かぶり物30種を1つずつ縮めるのではなく丸ごと縮める。
      ⚠**縮めると黒フチも細くなる**ので `lineWidth` を割り戻す。⚠★の印も下げる(頭の上に浮く)。
    ④**背負い物(`uBack`)も肩を軸に0.82倍**=胴を詰めたぶん、素のままだと腰より下へ垂れる。
    ⑤**胴の模様を「暗い方」から「明るい方」へ**=襟/負い革/帯を全部 col より暗く塗っていたので、
      胴を詰めた瞬間に**暗い模様が胴の大半を覆って兵科の色が消えた**。負い革は**外した**。
    ⚠**`shade()` は16進の文字列しか受け取れない**。`shade(shade(c,x),y)` と重ねると
      `rgb(...)` を parseInt して NaN → **真っ黒**になる(2026-07-31に踏んだ)。
    ⚠銃(`uGun`)・靴(`UW.sho`)は**黒フチとほぼ同色**だったので明るい側へ寄せた。
  - ⭐**骨格を縮めたら武器も一緒に縮める**(2026-07-31)。体だけ小さくすると
    長槍・迫撃・チェーンソー・盾が**体より大きくて主役を食う**。
    ⭐32種ぶん寸法を書き換えず、**武器の分岐をまとめて胸(0,-28)を軸に0.86倍**すればよい(`WK`)。
  - ⭐**腕の付け根は胴の前ぶちに置き、肩の丸み(pad)は付けない**(2026-07-31)。
    胴の内側から生やすと二の腕の黒フチが胸を横切って**胴が上下に割れ**、
    padを足すと**胸のど真ん中に黒い輪**が出る(両方やって両方駄目だった=ゾンビで踏んだのと同じ)。
  - ⭐**見た目を変えるなら、その兵科の「挙動」と噛み合わせる**(2026-07-31・ヴァルキリー)。
    ユーザー要望「光の剣をもった女剣士に」に対し、**射程340・範囲130の `blast` 型**のままだと
    「なぜ遠くの敵が爆ぜるのか」が読めない。⭐**攻撃の見せ方を『飛ぶ光の斬撃』に変えて辻褄を合わせた**
    (`U_MOT` に **`slash` 型**を新設 / `wave` fx=進行方向に開いた三日月 / 着弾は爆発ではなく斬撃2本)。
    ⚠**数値(射程・範囲・威力・間隔)は1つも触っていない**=見せ方だけ差し替えるのが安全。
    ⚠**丸い弾で飛ばすと魔法弾に見える**。⚠刃を武器の色1色にすると**翼と同化して4枚目の翼**に見えた
      =**芯を白く太く**して金はふちだけに残す。検査は `checkFx2` のヴァルキリーの段。
    ⚠34pxで女性に見せる手は**細身の体型と髪だけ**。⚠髪を大きく広げると翼に見える(縮めた)。
  - ⭐⭐**兵科ごとの差は「構え・手の位置・装い」で作る**(2026-08-01・残り25種に適用)。
    ⚠それまで兵科ごとに違うのは **体型(b)/かぶり物(h)/背負い物(k)** の3つだけで、
      **首から下は32種すべて同じ立ち姿・同じ服・同じ手の位置**だった。
      武器だけが手の届かない所に浮いていて、実機では「色違いの兵隊+小さな棒」に見える。
    ⭐足したのは3つ(どれも**輪郭に出る**=25pxで効く)。表は `U_PZ`:
    ①**構え**(`uLegs`)= ls歩幅 / lw脚の開き / lh腰の落とし / tl上体の前傾。
      ⚠⚠**`lh`と`tl`は腰から上をまとめて translate/rotate すること**=脚だけ動かすと腰と胴が離れて絵が割れる。
      ⚠★の印と派生の装いもその中に入れる(外に出すと頭だけ下がって印が浮く)。
    ②**手の位置**(`uArmTo`)= 肩と手の座標を渡して肘で折る。
      ⭐⭐**手の座標は「武器の座標」で書く**=武器は胸(0,-28)を軸に0.86倍で描くので、
        同じ数字が書けるように手も同じ変換(`wkp`)を通す。通さないと必ずずれる。
      ⭐⭐**握った手は武器の上にもう一度描く**=武器は腕より後に描くので、
        そのままだと手が武器の裏に消えて「浮いた武器」に戻る。⚠盾のように面で持つ物は出さない(`hh:0`)。
    ③**装い**(`uCos`)=vest/plate/coat/apron/apronL/bando/harn/heat/parka/tech/work/scarf/tabard/ghil/pelt。
      ⭐**胴からはみ出す物(コート・前掛け・陣羽織・毛皮)が一番効く**=輪郭がまるごと変わる。
      ⚠**胴の中に入れる物は3割まで**=広く覆うと兵科の色が消えて茶色い塊になる(毛皮で踏んだ)。
      ⚠**装いを持つ兵科では `uBody` のたすきを描かない**(`nosash`)=たすき+装い+帯+襟で模様だらけになる。
      ⚠外套(cloak)を `U.c` で塗らない=隊長は白っぽいので背中に**白い帆**が立って見えた。
  - ⭐⭐⭐**溜め(クールタイム)は体の動きで見せる**(2026-08-01ユーザー指示)。
    ⚠それまでは足元の輪と型ごとの光(drawUChg)が主役で、体は「歩行を止めて足を開く」だけだった。
    ⭐**表 `CHG_LEAN` + `uChgUp(o)`**=攻撃の型ごとに**上体を腰から傾ける**。投擲は後ろへ大きくひねり(-.34)、
      狙撃・回転・炉熱は前へかがむ(+)。⚠`drawUnit` が `o.mk` に型を載せて各体へ渡す。
    ⚠**fx側(drawUChg)の足元の輪は薄くした**(alpha .10〜.36 → .04〜.14)=主役を体に移すため。
    ⚠英雄は `drawHeroBody` の手前でまとめて傾けている。
  - ⭐⭐**ヴァルキリーの連撃**(2026-08-01ユーザー指示「しっかり剣も振って。モーションを複数つくって連撃っぽく」)。
    ⭐**1周期を1本の進行度 `o.vsw`**(`uValkP`)=振り抜き(0→.20)→戻し(.20→.60)→振りかぶり(.60→.86)→
      **振り出し(.86→1・加速)**。⚠p=1が「当たる瞬間」(バットと同じ約束)。
    ⚠⚠⚠**振りを「画面の平面で1周させる」と、剣でも必ず縦振り(風車)に見える**
      (2026-08-01ユーザー指摘「バットの時もいったけど縦振りだけになるのなんなの?」=**バットと同じ穴を剣で踏んだ**)。
      ⭐**得物を振る時は必ずバットの射影を使うこと**=上から見た角度 TH で持ち、
        画面x=cos(TH) / 画面y=sin(TH)*TZ、画面上の角度は ±TILT に収め、奥/手前は「短く・太く」で表す。
      ⭐**振りの種類は「面の傾き(TZ)と面そのものの角度(PLANE)」で分ける**=同じ仕組みのまま見た目が変わる。
    ⭐**斬り方を3種**(`o.vcb`=`u.cmb` を1発ごとに+1)=
      ①水平の薙ぎ(TZ.16/PLANE0) ②斜めの袈裟(TZ.34/PLANE-.52) ③**突き**(回さない=腕ごと前へ出す)。
    ⚠**腕と剣は一体で肩を軸に回す**+**上体の捻り**も一緒に動かす。⚠掃いた跡はシルエット層では描かない。
    ⚠数値は rate 1.8→1.0 / atk 351→200 / 溜め .55→.3 で**実効DPSは据え置き**(195→200)。
    ⚠⚠**握りは腰の横・胸より下**(2026-08-01ユーザー指摘「首から剣生えちゃってる」)。
      胸の高さで握らせると鍔が肩〜首に来て**首から剣が生えて見える**。肩から腕を下ろして腰の横で握る。
      ⚠**腕の付け根は胴の前ぶちに**=内側から生やすと二の腕が胸当てを覆って「胸の飾り」になる。
    ⚠音は専用の `lslash`(ノイズの「シャッ」+金属比の和音+低い芯)。**高いキラキラ音の禁止は守ること**。
    ⚠⚠**「光感」は色を明るくしても出ない**(2026-08-01ユーザー指摘「斬撃も全然光感ない」)。
      通常合成で黄色い帯を1本引くと**紙のリボン**にしかならない。
      ⭐**加算(lighter)で3枚重ねる**=①外の広いにじみ(薄い金) ②中の光(白金) ③**細い白い芯**。
      ⚠**芯だけ細く**=太い面を加算すると明るい地面と混ざって白い塊になる(爆発で踏んだ穴)。
      ⭐飛ぶ斬撃(`wave`)には**後ろへ薄くなる残光**を2枚足すと「飛んでいる光」に見える。
    ⚠撮る: `node test_shot.js out.png 1100 700 "pose=u:valk+posesw"`(5コマを振りの段階に割り当てる)。
  - ⭐⭐⭐**兵科32種を全部「専用の体」にした(2026-08-01ユーザー指示「各キャラ専用の体と見た目にして」)**。
    ⭐仕組み=**表 `UBODY[id]`** に1体ずつ描画関数を登録する。**登録した id は共通の体を一切通らない**
      (`drawUnit` の頭で分岐して return。★の印と後始末は呼び出し側でやる)。
    ⭐1体ずつ書くと2000行を超えるので、**描くための道具**を5つ用意して各体は30〜50行に収めた:
      `bLeg`(腿→すね→靴) / `bArm`(肩→肘→手) / `bTorso`(肩を丸め腰で絞る胴) /
      `bNeck`(首) / `bFace`(卵+鼻の影+目+口)。⚠**ここに兵科ごとの分岐を書かない**(道具に徹する)。
    ⚠**腰を落とす兵科(狙撃・迫撃・ミニガン・重装盾ほか)は `LH` を脚と上体の両方に足す**
      =片方だけだと腰と胴が離れて絵が割れる。
  - ⚠⚠⭐**「腰を下げる」だけでは絶対に"しゃがんだ"に見えない**(2026-08-01ユーザー指摘
    「ジョルジの攻撃モーション、全然しゃがんでない」)。`bLeg` は**膝の高さが固定**なので、
    腰を下げても**腿が縮むだけ**=絵が丸ごと下へ平行移動したようにしか見えない。
    ⭐**しゃがみに要るのは3つ**(1つでも欠けると読めない):
      ①**膝を折った脚の形**=新しい道具 **`bLegP(c,腰x,腰y,膝x,膝y,足首x,足首y,ズボン,靴,明るさ,幅,つま先の向き)`**
        で**関節の位置を直に指定する**(`bLeg` は"まっすぐ立つ脚"専用のまま。⚠こちらにも分岐を書かない)。
      ②**裾を持ち上げる**=長いコート・マントをそのままにすると裾が地面にめり込み、
        **折った脚を丸ごと隠す**ので何も伝わらない。溜め(rc)に比例して裾の下端を上げる。
      ③腰の落としを深くする(ジョルジは 6.4→**9**)。
    ⚠⚠⭐**姿勢の変化は溜めにそのまま比例させない**(2026-08-01ユーザー指摘
      「しゃがんでる最中に撃ってる感じになってる」)。rc に比例させると**撃つ瞬間にちょうど沈み切る**
      =ずっと動き続けている狙撃手に見える。⭐**溜めの最初の35%で姿勢を完成させ、あとは静止**
      (`kn=smoothstep(clamp(rc/.35,0,1))`)。⭐**分け方=体の姿勢(脚・腰・傾き・裾・二脚)は kn /
      狙いの詰め(銃の振り上げ・照準の光)は rc**。「据えてから狙う」の順になる。
    ⚠**つま先の向きは ±1 で切り替えず、`1-2*kn` のように0を通して連続で反転させる**
      =片膝立ちは後ろ足のつま先が後ろを向くので、途中でパッと反転するとカクつく。
      `bLegP` の靴は |向き| が0でも潰れない形にしてある。
    ⚠撮って確かめる: `node test_shot.js out.png 1200 740 "pose=u:hGeo+posechg=1"`(0 / 0.5 / 1 の3枚を見る。
      ⭐**0で以前と同じ絵になること**を必ず確認する=`bLeg` を `bLegP` に置き換えた影響が出ていないか)。
    ⚠**背負い物・武器も各体の中で描く**(uBack/武器の分岐は通らない)。
      ⚠古い武器の分岐は残っているが**もう当たらない**=直しても何も変わらないので注意。
    ⚠**★の印は頭飾りより上へ**。氷の冠・アンテナ・前立てを足したら頭に食い込んだ(-57→-64)。
    ⚠描く順は **脚 → 背負い物 → 奥の腕 → 胴 → 装い → 武器 → 手前の腕 → 首 → 頭**。
      武器を手前の腕より先に描くと、手が武器の裏に消えて「浮いた武器」に戻る。
  - ⭐⭐**「斧に見えない」の正体は刃ではなく柄**(2026-08-01ユーザー指摘)。
    刃をどれだけ描き直しても、**柄が胴と腕の裏に隠れていると「シャベル/うちわ」**にしか読めない。
    ⭐直し方=**柄を体の外へ長く斜めに出す**(肩口→腰の前)+**頭の先まで柄を突き出させる**
    =「柄に頭が嵌まっている」と読める。刃は**片側にだけ大きく**張り出させ、反対側は小さな顎に。
    ⚠上下対称に開くと**メガホン**、上下2枚に割ると**蝶**になる(両方とも同日に踏んだ)。
  - ⚠⚠**兜と髭(または口布)で顔を挟むと、顔が「帯」になる**(2026-08-01・斧使いと盾持ちで踏んだ)。
    上から兜、下から髭が寄ると素肌が縦3px程度しか残らず、**暗い塊に目が1つ乗っただけ**に見える。
    ⭐**素肌が縦7以上残る位置**に兜と髭を置くこと。
  - ⭐⭐⭐**得物を実際に振る仕組み(`bSwing`/`uSwP`/`U_SWG`)**(2026-08-01ユーザー指示
    「終焉の騎士の攻撃でしっかり鎌を振ってほしい。ほかも武器があるのに振ってなかったり
     飾りになってるやつはちゃんと機能させて」)。
    ⚠**`bHold` は溜めている間しか動かない**=①溜め0の兵科は武器が一度も動かない
      ②溜めがあるキャラも「振りかぶったまま撃って、次のフレームで構えへ戻る」だけで**振り抜きが無い**。
      これが「飾りに見える」の正体だった(**総点検の時点で武器が動いていたのは実質3種だけ**)。
    ⭐**足し方は2手**: ①`U_SWG` に id を足す ②その体の `bHold(c,rc,…,角度,dx,dy)` を
      `bSwing(c,o,…,振りかぶり角,振り抜き角,dx,dy)` に替える(第2引数が rc→o)。
      ⚠**奥の腕と手前の腕+武器の両方に同じ g を掛ける**(両手持ちの掟)。
    ⚠**溜め0の兵科は「振りかぶり角」に0を渡す**=`bSwing` は振り出しをその角度から始めるので、
      溜めで到達しない角度を書くと1フレームで武器が飛ぶ。
    ⚠⚠**振っている間は焼いたコマ(bakeU)が使えず直描きになる**=だから
      **窓は硬直の前半 `SWG_W`(.45)だけ**+`U_SWG` に載せた id だけ。全員に付けると
      「1か所に固めると重い」が再発する。⚠**銃は入れない**(振る物ではない)。
    ⚠**共通の反動(drawUnitの `o.fireT` の平行移動)と喧嘩する**=そのままだと
      「振り抜いているのに体が後ろへ9px下がる」。振りの時は**前へ踏み込む**側に差し替えてある。
    ⚠撮る: `node test_shot.js out.png 1400 800 "pose=u:<id>+posesw"`(5コマが
      振りかぶり→振り出し→振り抜き→戻りの順に並ぶ)。
  - ⚠⚠⭐**「役割が同じ2体」は必ず輪郭ごと分ける**(2026-08-01ユーザー指摘
    「タイタンと重装兵(重装盾)が見た目一緒すぎるからちゃんと変えて」)。
    ⚠**専用の体を持たせても同じ語彙で描いたら同じ絵になる**=どちらも
    「明るい鋼の全身鎧+四角い面頬+腕の四角い大盾」で、実寸では**盾の飾りの色しか違わなかった**。
    ⭐直し方=**持ち物を取り替えるところまでやる**。タイタンは
    ①**肩と兜のてっぺんを城の胸壁(凹凸)**に ②**盾をやめて巨大な鉄拳** ③**背中の軍旗**
    ④**暗い鉄+青銅**(重装盾は明るい鋼+金)。⭐**輪郭が変わる①②③のどれかは必ず入れる**
    =色だけ変えても実寸では見分けが付かない。
    ⚠背中に高い物(旗・アンテナ)を足す時は**中央から外す**=★の印(y=-64)と喧嘩する。
    ⚠**暗くしすぎない**(一度やらかした)=黒フチに食われて黒い塊になる。
      格子や溝は**明るい側**で引くこと(暗い線は黒フチに溶ける)。
  - ⚠**1体を1色系でまとめない**(2026-08-01・斧使いで踏んだ)。毛皮・上着・ズボン・靴を全部茶色に
    したら、形をどれだけ凝っても茶色い塊だった。⭐**毛皮=灰みの明るい茶 / 上着=兵科の色 /
    ズボン=冷たい灰 / 靴=暗い革**のように**色相もずらして4段**に分ける。
  - ⚠⚠**かぶり物のつばを目の高さに置かない**(2026-08-01)。helm/hard/bowl/face のつばが
    目(y+.2)の上に乗っていて、**32種のうち半分が「目隠しをした兵」**に見えていた。
    帽体の下ぶち(y-5前後)まで上げる。⚠面頬(face)は逆に**あご側**へ下げる。
  - ⚠**細い部品は黒フチに食われて黒い棒になる**の再発(2026-08-01):
    斥候の拳銃は**スライドの高さが4しかなく**、実機ではただの黒い棒だった(**高さ6以上**にする)。
    銃の素の色 `#3a3128` も黒フチに近すぎたので `#544736` へ上げた。
    ⭐**銃には握り(グリップ)を付ける**=無いと手を置く所が銃の裏しか無い。
  - ⚠**共通の脚はバットと同じ明るさ(1.34/1.06/1.45)にする**(2026-08-01)。
    素の 1.08/.86/1.0 では腰から下が1つの黒い塊だった。胴の下半分も .76→.84 に上げた。
  - ⚠⚠⭐**★4の光に `shadowBlur` を使わない**(2026-07-31ユーザー指摘「色がぜんぶ濁っている」)。
    影は**描く図形1つ1つに掛かる**ので、体・腕・武器の重なった所が光で埋まって塗り分けが飛ぶ。
    ⭐**背面に薄い輪(楕円3枚)を置く**のが正解。⚠シルエットの層では描かない(真っ黒な塊になる)。
  - ⭐⭐**2026-07-30にウォーカーとバットで分かった「もう一段上げる」5つ**(大写しで撮って実測した)。
    ⚠どれも第96〜102弾の掟を守ったうえで**まだ残っていた**もの。他のキャラを直す時もここを見る:
    ①⭐**基準のずれと振りを同符号で足さない**。`leg(-3.4-sw*1.7)` と `leg(3.2+sw*1.7)` は
      sw>0 で12開き sw<0 で1.2しか開かない=**1周期に1歩**しか出ず、半周期は脚が重なって
      「同じ脚が前後しているだけ」に見える。**基準をほぼ0にして振りだけで前後させる**。
    ②⭐**上体を脚と位相をずらして傾ける**(`Math.sin(t*8+ph+1.15)*.05` を胴から上に掛ける)。
      これが無いと脚だけ開閉して上体が板のまま平行移動し、**台車に乗って滑って見える**。
    ③⚠**腕は肘で本当に折る**。肩→肘と肘→手の傾きが同じだと、太い黒フチで区切られた帯が3枚
      並ぶだけで「スパナ/芋虫」になる。**折れ角15°以上**。前腕は二の腕と**黒フチ2本ぶん以上重ねる**。
    ④⚠**腕の付け根は胴の輪郭に重ねる**。胴の内側から生やすと根元の縦の黒フチが胸を上から下まで
      横切って胴が2〜3枚に割れる(逆に肩へ楕円を足すと「肩章」になる。両方やって両方駄目だった)。
    ⑤⚠**円に stroke を掛けない**(釘・鋲)。半径1.5の円に黒フチ2.4を掛けると芯が0.3しか残らず、
      外径2.75の**黒い円盤**になる。塗りだけにするか半径を2.6以上にする。
  - ⭐⭐**攻撃モーションは「本物の動作」を調べてから作る**(2026-07-30・バットで確立)。
    ⚠ユーザー指示「そういうモーションはしっかり忠実に再現してほしい」→ 1回目に出したのは
      **「振り下ろし」=斧や剣の振り方**で、ユーザーに「野球のバットの振り方ってこれ?」と返された。
    ⭐**野球のスイングと振り下ろしの違いは3つ**: ①スイング面がほぼ**水平**(体の周りを回る)
      ②フォロースルーでバットが**背中側へ巻き付く** ③構えはバットを**立てて肩の後ろ**(手は耳の高さ)。
      さらに**腰が先に回って腕が遅れる**(下半身リード)。
    ⚠⚠**回転させるだけでは「縦振り」にしかならない**(2026-07-30ユーザー指摘
      「いまって縦振りか横振りかどっちっていう認識?」)。どれだけ軌道を凝っても、
      画面の平面で回している限り**風車のように縦に回る棒**にしか見えない。
    ⚠⚠**画面の角度を手で並べたまま短縮だけ足しても駄目**(2回目の失敗)。角度を1周させている限り
      「めっちゃ縦振り」のまま。⭐⭐**得物は「上から見た角度」で持ち、画面上の見え方はそこから
      計算する**のが正解:
      ```
      TH = 上から見た角度(0=正面/右=攻撃する向き、+=手前へ回る、-=奥)
      画面のx = cos(TH) / 画面のy = sin(TH) * TZ   ← TZ=スイング面の傾き(0.34≒20°)
      画面上の長さ = √(x²+y²)  /  画面上の角度 = atan2(y,x)
      ```
      **TZ が小さいほど横振りに見える**(TZ=1 が完全な縦回転=「めっちゃ縦振り」の正体)。
      あわせて太さ(手前ほど太い)と上下のずれ(奥は上・手前は下)も `sin(TH)` から出す。
      ⚠`lineWidth` は補正しない(補正すると細くなった側だけ黒フチが残って歪む)。
      ⚠**構えとテイクバックは円に乗せない**(`lay`=0)=本物もそこだけはバットを立てている。
        寝かせるのは振り出しの最初だけ。
      ⚠**角度の補間は±πを跨がせない**=立てた時の角度は +2π 側で持つ(でないと戻る所で逆回転する)。
    ⚠⚠⚠**射影を正しくしても駄目だった**(3回目・2026-07-30ユーザー「全然横振りに見えない。根本的に変えて」)。
      ⭐⭐**本当の原因は構えだった**=構えとテイクバック(**1周期の約6割**)で**バットを立てて**いたので、
        そこから水平へ振ると目には「縦 → 横」= **振り下ろし**にしか映らない。
        **立った絵が半分以上ある限り、振りの射影を何度直しても縦振りに見える**。
      ⭐**4回目の直し方(これで通った)**:
        ①**1周期のどこでもバットを立てない**。構えも「後ろへ寝かせて担ぐ」にした
          (本物の野球は構えで立てるが、**34pxでは写実より読めることを採る**)。
        ②**画面上の角度を水平から ±17°(TILT)に必ず収める**=`atan2` の生の値を使わない。
          生のままだと奥/手前を向く一瞬に**バットが真下を向いて**風車に見える。
          奥/手前は角度ではなく「**短く・太く**」だけで表す。
        ③**握り(手)も体の周りを水平に動かす**=握りが1点に止まるとバットだけ回って風車に戻る。
        ④**掃いた跡を平たい楕円の弧で置く**=「水平の円を横から見た形」そのものなので一枚で伝わる。
          ⚠**シルエットの層では描かない**(`o.sil`。silLayer は globalAlpha を捨てるので真っ黒な塊になる)。
        ⑤**手前を通る間だけ胴より前に描く**(しきい値 sn>0.55)。⚠0にすると構えで胸を横切る板になる。
        ⑥**長さを0まで潰さない**(`.34+.66*len`)=潰し切ると奥を通る区間でバットが胴に隠れて「消えた」に見える。
    ⭐**担ぎ+滑り台**(同日ユーザー指示「最初の持ち方を担いでる感じに、そこから斜めに滑り台みたいな軌道で」)。
      `sh`(0〜1)= 肩に担いでいる度合い。`SH_A`(0.72≒41°)ぶん後ろ斜め上へ起こし、握りも肩へ上げる。
      ⚠⚠**担ぐのは構えとテイクバックだけ**。振り出したら `sh` を**前半で0まで落とし切る**
        (`clamp(1-q/.46)` を smoothstep)=肩から斜めに滑り落ちて水平の輪に乗る=滑り台。
      ⚠**振りの後半まで `sh` を残すと、また「立てた所から振り下ろす」絵に戻る**。
      ⚠`SH_A` を上げるほど立てた構えに近づく=縦振りへ戻る。
      ⚠**掃いた跡は `sh<.35` の間だけ出す**=跡は水平の輪の形なので、斜めに滑っている最中に出すと食い違う。
    ⭐あわせて**横から見ても伝わる要素**を足す=ほぼ1回転する軌道 /
      巻き付くフォロースルー / 腰が先に回る。
    ⚠他の得物(斧・槍・チェーンソー)も、**その道具の本当の振り方を先に確かめてから**作ること。
    - ⚠⚠⭐**攻撃モーションは「交戦している時」だけ動かす**(2026-07-30ユーザー指摘
      「出した途端に攻撃モーションに入る」「敵を倒したあと敵がいないのに振る」)。
      ⭐**原因=`u.cd`(硬直)だけで振らせていたこと**。cd は
      ①**出撃した瞬間**にも入っている(`cd:rnd(0,.3)` のばらけ) ②**最後の1体を倒した後**も残って減り続ける。
      ⭐**直し方=`u.eng`(いま狙う相手が居るか)と `u.fired`(一度でも攻撃したか)の両方を見る**。
      ⚠**振り抜きの区間(p<.34)だけは交戦が切れても続ける**=倒した瞬間に構えへ飛ぶとカクつく。
      ⚠⚠**他のキャラに攻撃モーションを付ける時も必ず同じようにすること**。検査は `checkAtkMotion()`。
    - ⭐**攻撃の全周期を1本の進行度 p で描く**。`p = 1 - u.cd/U.rate`(0=いま振った直後 → 1=次に振る直前)。
      ⚠溜め(`u.chg`)は使えない兵科がある=`U_MOT` の「即時」枠は溜め0。**そこに溜めを足してはいけない**
        (1発が重くなって撃つ回数が減り、数の多い相手に押される。ユーザーが前に指摘した現象)。
        **見た目だけで振りかぶらせる**のが正解。
    - ⚠⚠**振り出しは周期の「終わり」に置く**。当たり判定が出るのは p が 1→0 に戻る瞬間なので、
      振り出しを頭に置くと**当たってから振る**ことになる(1回そう作って撮って気づいた)。
    - ⚠⚠**p=1.0 で得物が「前方水平」**になるように組む。振り切った姿(真下)を p=1.0 に置くと、
      当たる瞬間にはもう振り終わっていて手応えが消える。振り切りは p=0〜0.14(当たった直後)。
    - ⭐**腕と得物は一体で回す**(`肩を軸に rotate` してから得物を描く)。手首だけ回すと腕が付いてこない。
      ⭐あわせて**上体の捻り**と**手前の脚の踏み込み**も動かす。これが無いと上半身だけ回って足が地面に貼る。
    - ⚠**各段の継ぎ目で値が一致する**ように組む(飛ぶと絵がワープする)。⚠共通の反動(`o.fireT` の
      後ろへの蹴り)は**打ち消す**=スイングと二重になって体が跳ぶ。
    - ⚠**焼いたコマ(`drawSpr`)では振れない**ので、振っている間だけ直に描く分岐が要る(`uBatP`)。
      ⚠構えの区間は0を返して焼いたコマに任せる=毎フレーム直に描く時間を減らす。
      ⚠**焼いたコマは進行度0の絵と一致していること**(ずれると構えに戻る瞬間にカクつく)。
    - 撮る: `node test_shot.js out.png 1200 740 "pose=u:bat+posesw"`(5コマを振りの段階に割り当てる)
    - ⭐⭐**大きいゆるキャラは「顔の面」を側面より広く取る**(2026-08-01・🚌ゾンビバスで踏んだ)。
    最初、側面(横146)に対して前面を35しか取らなかったので、**目2つと口が重なって顔に見えなかった**。
    ⭐直し方=**3/4に振って前面を70まで広げる**(側面106 : 前面70)。目・口の形は1つも変えていない。
    ⚠**タイトルの背景は暗い茶色**=屋根の荷物を茶系の暗い色で置いたら**丸ごと背景に沈んで消えた**
      (「描いたのに出ない」と原因を探しかけた)。明るい側に振ること。
    ⚠**行進canvasは高さ120pxしかない**=大きい個体は上下が切れる。
      **その1体が居る間だけ canvas を高くする**(`BUS_TALL`)。⚠**素の高さは決め打ちにせずCSSから測る**
      (中の寸法とCSSの見た目がずれると絵が縦に潰れる=`.pvc` で踏んだのと同じ話)。
    ⚠**絵が大きいと当たり判定も広げる**=普通と同じ34pxのままだと「押しているのに減らない」に見える。
    ⚠**8発も要る相手は、残りHPが絵で減っていくようにする**(窓の手が2発ごとに1本消える+
      叩くたびに窓のゾンビが1匹こぼれ落ちる)。数字が見えないので、減っていることが絵で分からないと詰まる。
  - ⭐⭐**溜めの間は「装備そのもの」を動かす**(2026-08-01ユーザー指示)。道具は
    **`bHold(c,rc,肩x,肩y,角度,ずれx,ずれy)`**=**手前の腕と装備を一体で肩を軸に回す**
    (ヴァルキリーの剣で確立した掟をそのまま表に落とした)。⚠**画面のyは下が正=負が振り上げ**。
    ⚠**両手で持つ装備は奥の腕にも同じ g を掛ける**。奥の腕は胴より先に描くので**付け根のずれは胴が覆う**
      =多少動かしても見えない。ただし付け根が胴からはみ出す角度まで回さないこと。
    ⚠⚠**長い物・大きい物を大きく回すと、得物が頭を横切って何を持っているか読めなくなる**
      (火炎瓶で2回・手榴弾と爆弾で1回ずつ踏んだ)。**片手の小物だけ大きく(0.4〜0.6rad)回し、
      長い物(杖・槍・軍旗・大鎌・放射器)は『持ち上げ+わずかな傾き』に留める**。
    ⭐**回さずに読ませる手もある**=火炎瓶は**炎を大きくする**/工兵は**火口を強く燃やす**/
      スナイパーは**二脚が開いて照準が光る**/迫撃兵は**砲身が起き上がって砲弾が落ちる**/
      ミニガンは**銃身が回る**(手前ほど明るく太い4本)。**動かす所は武器の可動部を選ぶ**のが安全。
    ⚠英雄は表 `H_CHG`(得物ごと)。⚠**★の印は動かさない**(頭の上に浮く)。
    ⚠撮る: `node test_shot.js out.png 1200 740 "pose=u:<id>+posechg=0.95"`
  - ⭐⭐⭐**必殺技の詠唱モーション(2026-08-02ユーザー「のけぞるだけだと安っぽい」)**。
    ⚠それまでの前置きは **光(drawUltWind)+「下がる」+通常の溜めの構え**だけで、しかも
      **押した瞬間に完成形の姿へ飛ぶ**=21人がほぼ同じ絵だった。
    ⭐**型ごと(U_MOT の9種)に1つずつ作る**=表 `U_ULM`。1つ書けば同じ型の英雄すべてに効く。
      ⚠即時(now)は英雄7人と一番多いので、**得物を振る人(U_SWG)と退がる人(H_ULW_BACK)で枝を分けた**
        (`nowS`/`nowB`)=表を3つに割るだけで7人が同じ動きにならずに済む。
    ⭐**3つで1組。どれか欠けると安っぽさが戻る**:
      ①**体の動き**(主役)=傾き(ln)・上下(ri)・踏み込み(fw)・潰し伸び(sx/sy)
      ②**型ごとの光**(脇役)=詠唱の進みで `drawUChg` を呼ぶ。⚠⚠**これが一番効いた**=
        詠唱中は `u.chg` を0に伏せてある(通常攻撃をさせないため)ので、それまで
        **足元の魔法陣・照準線・陽炎・雷雲・★4以上6人の専用の溜めが1つも出ていなかった**。
        `drawUChg(c,u,t,lay,k0)` に第5引数を足して外から溜まり具合を渡せるようにしただけで直る。
      ③**溜め終わりの一拍(タメ)**=`ulKof` は **p=.82 で完成し、残り18%は姿勢を止めて震えるだけ**。
        止めが無いと出だしが軽い。⚠**7割で構え・3割で詰める**配分(前半に寄せると押した瞬間に固まって見える)。
    ⭐**発射(`o.ulr`)では姿勢が反対側へ抜ける**=溜めた向きの逆へ打ち出して戻る。
      ⚠**1フレームで反対の姿へ飛ばさない**(押した直後の12%で立ち上げる)=ワープに見える。
    ⭐⭐**得物は各英雄の `bHold`/`bSwing` に上乗せして動かす**(21人ぶんポーズを書き直さないため)。
      口は2つ: ①`bRc` の上限を詠唱中だけ `RC_UL`(1.34)まで開ける
      ②**箱 `UL_HA`/`UL_HX`/`UL_HY`**(腕ごと余分に回す角と肩のずれ)を `bHold`/`bSwing` が足す。
      ⚠**引数ではなく箱にした理由**=bHold は50か所から呼ばれていて呼び出し側が `o` を持っていない。
      ⚠⚠**`bUlt` が返す後始末で必ず0へ戻す**(戻し忘れると次のキャラの腕まで曲がる)。検査で見ている。
    ⚠⚠⭐**体を縦に伸ばして「掲げる」を出そうとしない**(2026-08-02ユーザー「詠唱中身体が縦に
      伸びるのはだいぶ微妙。巫女とか魔導士のやつ」)。潰し伸び(sx<1/sy>1)+浮き(ri)で伸び上がらせたら
      **体そのものが細長く歪んで気持ち悪い**。⭐**掲げるのは腕と得物(hy)だけにして、体は反らす(ln)で受ける**
      =形は歪まないまま上を向いた姿になる。⚠**足も地面から離さない**(ri=0)。
    ⚠⚠⭐**狙撃(snipe)だけは体を動かさない**(2026-08-02ユーザー指示「ジョルジは構えてるとき
      体は動かさないように」)。狙撃の英雄3人は `hold9` で**敵が居ない間もずっと構えたまま待つ**ので、
      詠唱で振ると「据えた狙撃手が急にもぞもぞ動く」になる。⭐印は `still:1`=
      **bUlt を通さず通常の溜めの傾き(uChgUp)に任せる**+**決めポーズの14%の膨らみも掛けない**。
      ⭐⭐**あわせて `r0`(詠唱の始まりの rc)が要る**=詠唱中は `u.chg` を0に伏せてあるので、
      0から立ち上げると**押した瞬間に構えを解いて立ち上がり、また構え直す**。狙撃は `r0:1`。
    ⚠⚠⭐**縦に構えている得物(杖・槍)を大きく回してはいけない**(撮って気づいた)=
      肩を軸に -.5rad 回すと**杖の先が後ろへ倒れて頭の横まで落ちる**(掲げるの逆)。
      ⭐**縦の得物は「回さずに持ち上げる」**(hy を大きく・ha はほぼ0)+**体ごと伸び上がる**で見せる。
    ⚠`walk` の判定は `o.chg>0` だけでは足りない=詠唱は chg が0から立ち上がるので**出だしの1コマだけ歩く**。
    ⚠検査 `checkUltMot()`=①型が21人ぶんある ②進みが戻らず.82で完成しそこから止まる
      ③**変換を記録する偽の画布で姿勢が実際に変わることを測る**(表の値だけ見ても動いた証拠にならない)
      ④UL_HA が0へ戻っている。
  - ⭐⭐⭐**英雄18人も全部「専用の体」にした(2026-08-01)**。表 `HBODY[id]`(兵科の `UBODY` と同じ仕組み)。
    ⚠それまで英雄は**全員が同じ胴・同じ脚・同じ腕**で、違うのは かぶり物/背負い物/武器/色 だけ=
      兵科で捨てた「実機では色しか違わない絵」そのものだった。
    ⚠⚠**割り付けが古いままだった**=胴26/脚17(頭-48)。第95弾以前の頭でっかちと同じ形。
      ⭐**`HSK`=脚20.6/胴16/頭-47.2**(全高≒53)に揃えた=兵科より一回り大きいが比率は同じ。
    ⚠**ズボンを明るく**しないと英雄も「腰から下が1つの黒い塊」になる(兵科と同じ手当て)。
    ⚠⚠**フード・頭巾は大きくすると「一つ目の黒い塊」、ローブは広げると「紫の柱」になる**
      (闇商人と預言者で同時に踏んだ)。**頭巾を小さくし、口元の肌と光る目を出す**と直る。
    ⚠**★4の光に shadowBlur を使わない**(掟)。英雄側に残っていたので外した=背面の輪は drawUnit が出す。
    ⚠`H_LOOK` は絵には使わず**台帳**として残してある(検査 checkHeroLook が18人ぶんの重複を見張る)。
    ⭐⭐**★5は「色の格」で決まる**(2026-08-01ユーザー指摘「暁の王がダサすぎる」)。★5なのに
      **上着がくすんだ茶**で18人中いちばん地味だった。⭐**白+金にして差し色を1色**入れると一気に格が出る。
    ⚠⚠**翼は「尖らせると蝙蝠」「平たい葉を並べても羽根にならない」**=前縁を1本の弧で**後ろへ跳ね上げ**、
      後縁を**丸い羽根の先**で欠く。⚠**頭の後ろに楕円を置くと光輪ではなく「つば広の帽子」に見える**
      =王冠の**上へ浮かせる**(★の位置も上げて重ならないようにすること)。
    ⚠**細くて長い竿(軍旗・杖)は頭上に消えて読めない**=短く太くして体の横に置く。
    ⚠**マントを長くすると下半身の主役になって脚が消える**=膝上で切る。
- ⚠**ゾンビの `ZW.pa`(#4a3d2e)と `ZW.sho`(#2b2419)は黒フチ `INK`(#241f19)とほぼ同じ明るさ**。
    奥の脚を .56倍すると塗りと黒フチの差が2しかなく、大写しでも脚が1本しか無いように見える。
    ウォーカーだけ `#6a5945`/`#453a2a` をローカルで使って直した(⚠`ZW` 自体は他の12種に効くので触っていない)。
  - ⚠**黒フチを絵の大きさに比例させて太くするのは駄目**(2.6→3.9で試した)。
    34pxではフチが図形を食い、頭がヘルメット・腕が黒い棒になる。**2.6のままが正しい**
  - **色は `ZW` の表から引く**(`sh`シャツ/`sh2`/`rag`ぼろ布/`pa`ズボン/`sho`靴/`bl`血)。
    ⚠**体ごとに服の色を変えると盤面が散らかる**ので服は3系統だけ。肌は `Z.c` のまま
  - **ステージ1の13種に適用した**(2026-07-27): ウォーカー(全面描き直し)/ランナー/アーマード/
    ブローター/スクリーマー/クローラー/ブルート/ストーカー。
    ⚠**獣(ハウンド)・塊(スライム)・幽霊(レイス)・全身鉄(重装兵/ジャガーノート)は触っていない**
    =服を着ていないか元から2色以上あるため。**服を着せると設定が壊れる**
- ⭐**爆発を見本の画像に寄せて描き直した(2026-07-27 第106弾)**。
  ⭐**1回目(第105弾)は「べた塗り+黒フチの多角形」にして「オレンジの石」になり、ユーザー判定=前の方がマシ**。
    ⚠**爆発は物体ではなく光**。キャラの原則(黒フチ+べた塗り)をそのまま持ち込むと物になる。
  - ⭐⚠**色が茶色く沈む本当の原因は `lite()` の半径だった**。画面には時間帯の陰が掛かっていて、
    `lite` はそこに穴を開ける仕組み。**半径が固定値で火球とずれていた**ため、
    どれだけ彩度の高い橙を置いても陰の下で泥色になっていた。
    ⭐**`lite` の半径は火球の大きさから計算する**。これを直した瞬間に色が出た。
  - ⚠**加算合成(`lighter`)を主役にしない**。見本は黒背景だから映えるが、この game の地面は明るい砂色なので
    加算すると「砂色+橙」で白っぽい山吹色に飛ぶ。**通常合成で狙った色をそのまま置く**。
  - ⭐見本から取った形は4つ: ①**放射状の細い筋がたくさん**(輪ではなく筋が主役)
    ②**中心は暗い**(煙が手前=火は輪になる) ③外へ 暗赤→橙→黄 の段階 ④外にうっすら煙・黒フチ無し。
  - ⚠**太くて白い筋を少数**にすると太陽/星のマークに見える。**細い筋を多く、色はほぼ火の色、白は数本だけ**。
  - ⭐**ユーザーに見本画像を出してもらうのが一番安い**。1回目の失敗は実装ではなく**狙いの設定**を外したから。
    絵の好みは推測できないので、迷ったら「画像をください」か「3〜4案を並べて選んでもらう」。
- ⭐**🦸英雄11人を仕上げた(2026-07-27 第104弾)**。兵科と**まったく同じ5つ**が効いた
  (脚の色分け / 肘のある腕 / 目と口 / 襟と負い革 / 首)。**共通部品をそのまま流用**した:
  `uBody(c,'',色,[x,y,幅,高さ,r])`(第4引数で箱を上書きできるようにした)・`uArm`・`UW`・`zEye`。
  - ⭐⚠**`c2`(胴の色)を暗く置きすぎない**。胴は下半分をさらに1段暗くする作りなので、
    元が `#2c333a` のような暗色だと**全員が黒いシルエット**になり、描き分けた形が全部消える。
    11人ぶんの `c2` を1段明るい値に置き直した。
- ⭐**🌑ナイトメアの獣8種を描き直した(2026-07-27 第103弾)**。ここが一番古い絵だった。
  - ⚠**「群れ」と名付けたものは群れにする**(巨鼠)。1匹だけ描くと石鹸に尻尾が付いて見える。
    大きさ・高さ・位相を変えて3匹並べると、部品の繰り返しが**意味を持つ**。
  - ⭐**蜘蛛は膝で折る**。等間隔の直線8本では櫛にしかならない。
    ⚠**脚の付け根は全部 頭胸部に置く**=腹から生やすと脚が腹を縦に横切って**檻**に見える(2回踏んだ)。
    ⚠膝は体の**外側**へ張り出させる(真上に置くと縦の棒になる)。
  - ⭐**蝙蝠の翼は指の骨で割り、後縁を扇形に欠く**。平たい三角2枚だと紙飛行機。
  - ⚠**巨大なボスを Z.c 一色で塗らない**(原初の獣)。脚を暗く・頭と仔獣を明るくして部位に段を付ける。
- ⭐**タワーは細部だけ直した(2026-07-27 第104弾)**。第91弾の作りが効いていて、ここは崩れていなかった。
  - ⚠**音響砲**(⚠2026-07-27に削除したタワー): 平たい台形に縦線3本だと**扇子**に見えた。
  - ⚠**要塞砲**: 同じ太さの筒を3本並べただけだと**手袋の指** → 砲口に制退器を付ける。
- ⭐**兵科32種を仕上げた(2026-07-27 第102弾・ユーザー指示「一旦完璧といえるレベルまで」)**。
  第100弾で胴・脚・腕を入れたあと、**実機で見ると まだ「色の違う棒人間」**だった。効いたのは次の5つ:
  - ⭐**ズボンと靴の色が両方とも暗く、脚が1つの黒い塊だった**(旧 `#4a4034`/`#2b2419`)。
    共通の表 **`UW`**(`pa`ズボン/`sho`靴/`glv`手袋/`blt`帯/`bkl`留め金)を作り、**ズボンを明るく**した。
    ⚠これがいちばん効いた。形を凝るより先に**明暗の段**を作ること(ゾンビの `ZW` と同じ話)。
  - ⭐**腕に肘が無かった**。同じ太さの帯を3つ横に並べていたので「体の横に板が生えている」に見えた。
    **肩(丸み)→二の腕(太)→肘で下に折る→前腕(細)→手(別色の角ばった塊)** にした。
  - ⭐**顔が黒い点1つだった**。ゾンビと同じ `zEye`(白目+瞳)+小さい口にした。
    ⚠口は**小さく薄く**。太く暗いと口髭か傷に見える。
  - ⭐**胴が1色+帯1本だった**。**襟**と**斜めの負い革**と**帯の留め金**を足すと兵隊に読める。
    ⚠これ以上足すと25pxでは模様が潰れてただの汚れになる。
  - ⭐**武器が「線1本」か「角丸長方形1つ」だった**。⚠線で描いた武器は黒フチに食われて**ただの黒い棒**になる
    (局所座標で黒フチは2.4ぶん=**3より細い部品は塗りが残らない**。海の生き物のハサミと同じ話)。
    共通部品 **`uGun`**(台尻→機関部→銃身→弾倉)と **`uNoz`**(本体→握り→先へ広がる筒先)を作り、
    近接武器も**厚みのある帯+刃**にした。
  - ⚠**持ち物を体から離して浮かせない**。手榴弾・爆薬を頭の横に丸で浮かべていたので風船に見えた
    =**手の位置(x≒16,y≒-30)に持たせる**。
  - ⚠**帽子のつばを長く伸ばさない**。顔から棒が突き出た絵になる(野球帽と制帽で踏んだ)。
  - ⚠**同じ形の翼を平行に重ねても1枚に潰れる**(ヴァルキリー)=**向きの違う翼**にする。
  - ⚠**金の帯を胴の上端に横一文字で引かない**(メカスーツ)。操縦席の頭が帯で切られて棒に見える。
- ⭐**ステージ2(海の生き物13種+深淵の母)を描き直した(2026-07-27 第101弾)**。
  ⚠陸のゾンビと違って**服を着ていない**ので `ZW` が使えない。海は
  **背(`Z.c`)/腹(`ZS.bel`)/ひれ(`ZS.fin`)/関節(`ZS.jnt`)/腐肉(`ZS.rot`)** の5つで分ける。
  - ⭐⚠**黒フチは局所座標で約1.7の幅がある**(`lineWidth=2.6/s`・s≒1.5)。
    つまり**太さ3未満の部品は塗りが1つも残らず、まるごと黒い棘になる**。
    カニ/ロブスターのハサミの指を細い三角で描いて実際にそうなった=**指も脚も太さ4以上**にすること。
  - ⭐⚠**部品を `scale()` で縮めて描く時、`lineWidth` を 1/倍率 で補正してはいけない**。
    補正すると画面上のフチの太さが一定になり、**小さい方だけが黒に食われて塊になる**
    (奥のハサミ・群れの小さい魚で踏んだ)。補正しなければフチも一緒に細るので比率が保たれる。
  - ⭐⚠**「向き」の無い形は必ずUFO(または傘)に見える**。亡霊エイを「左右に翼を広げた座布団+
    真下に尾」で描いていたため、翼端を尖らせても尾を直しても**UFOのまま**だった。
    **真上から見た凧の形にして、他の敵と同じく右を向かせる**(吻が前・翼端が後ろ・尾は後ろへ引く)と
    一発で「進んでいる生き物」に読めた。⭐**直らない時は細部ではなく向きを疑う**。
  - ⚠**尾を太いまま先まで引いて大きな棘を付けると矢印になる**(エイで踏んだ)。先へ細らせ、棘は根元寄りに小さく。
  - ⚠**体の真下に赤い塊(腐肉)を置くと舌を出しているように見える**(フグとヒトデで2回踏んだ)。脇腹か断面に置く。
  - ⚠**放射対称のもの(ヒトデ)は腕の長さを崩し、1本を千切れた短い腕にする**。
    ⚠ただし根元を太くしすぎると腕どうしが繋がって**1つの塊**になる(1回踏んだ)。
  - ⭐**巻貝は「大小の玉を渦に沿って重ねる」**と、部品の大きさが自然に不揃いになり等間隔の問題が起きない。
  - ⭐**甲羅は六角の甲板を敷き詰める**。直線を等間隔に引くと方眼紙に見える。境の線は黒ではなく甲羅を暗くした色。
  - ⭐**魚は「背を暗く腹を明るく」**。`zFill` は下を暗くするので魚には使えない=下半分を切り抜いて明るい色を重ねる。
  - ⚠**ひれを大きくすると手裏剣/蝙蝠に見える**(イワシで踏んだ)。ひれは体より小さく・明るく。
- ⭐**キャラの絵は「1種ごとに専用の枝」を持たせる**(2026-07-26 第82弾・ゾンビで実施)。
  ⚠**大きさは第95弾で1.5倍にした(ゾンビ34px・タワー45px)。以下の記述の22px/35pxは変更前の数字**。効くのは **①輪郭の形 ②明暗 ③色** の3つだけで、
  細かい模様は何を描いても消える。共通の胴に真偽値(big/hel など)を足して差を出す作りは、
  **実機では色しか違わない絵**になる(ステージ1の13種のうち10種が実際にそうだった)。
  - 陰影は **`zFill()`**(上を明るく下を暗く**1段だけ**)、目は **`zEye()`** を通す。
    タワー側の `twBase` と同じ理屈=平たい図形が塊に見える。**2段以上入れても22pxでは分からない**
  - 検査は `test_headless.js` の **`checkZLook()`**=①全ゾンビが `Z.id==='…'` の枝を持つか
    ②drawZombie が積んだ**図形の並び**に重複が無いか。⚠**色も数値も見ない**
    (`Z.sc` と歩幅がゾンビごとに違うので、数値を入れると必ず通る検査になる)。**ゾンビを足す時は枝も足す**
  - ⚠形の落とし穴(全部実際に踏んだ): **胴を横に寝かせると四つ足の生き物に見える**(二足なら胴は斜めに立てる)/
    **爪や武器を頭と同じ高さに置くと「開いた嘴」に読めて鳥になる** / **腕を2本並べると1本の突起に潰れる**
    (胴の前と後ろに分けて明暗を変える)/ **同じ印を全種に入れるとその印が一番目立って見分けを邪魔する**
    (海の13種に同じ白い骨を3本引いていた=`rotMark` を薄く小さくした)
  - ⚠**撮る→直す→また撮るを繰り返す**。数値だけ見ていると気づけない。道具は scratchpad の `mk_z.js`
    (index.htmlを読み込んで別canvasに実機と同じ大きさで並べて撮る。`mk_tw.js` のゾンビ版)
- **ゾンビのスプライトは箱に焼かれる**(bakeSet)。描ける局所座標は**|x|≦54・y≧-105**まで。
  はみ出した部分は無言で切り落とされる(ボスの顎が消えて気づいた)。大きいボスを描く時は必ずこの範囲に収めること。
- **`drawUnit`/`drawZombie`を小さく描く時のスケールは「欲しい高さ ÷ (素の絵の高さ50 × 内部倍率)」**(2026-07-26)。
  ⚠`drawUnit`は中で`TSCALE`倍、`drawZombie`は中で`Z.sc`倍している。これを割り忘れると
  スケールが16倍などになり、画面いっぱいに巨大な絵が出る(洞窟の英雄で踏んだ。ヘッドレスでは気づけない)。
- ⭐**タワーは「威力が違うだけ」にしない=撃ち方(絵)と発射音を必ず分ける**(2026-07-26 第66弾ユーザー指示)。
  同じ絵・同じ音を使い回していたものを全部分けた: 冷却塔(氷が張る/専用の音) / ドローン基地(機体が飛び出す) /
  要塞砲(極太の貫通弾/閂→巨砲→破片) / プラズマ砲(プラズマ球とエネルギーの炸裂) /
  ショットガン台(散弾) / レーザー塔(細いビーム) / ガトリング(薬莢の滝) / 爆裂砲台(軽い発射音・小玉) /
  **レールガン(きゅいーんと充填してから超電磁砲。`RAIL_CHG`秒前から`tw.chg`が0→1で溜まり、ビームは`RAIL_BEAM`秒残る)**。
- ⭐**ドローン基地の2機は「状態を持たない」**(2026-07-26 第68弾)。位置は `droneOff(k,枠番号,時刻)` だけで決まる。
  こうしておくと**描画と発射で必ず同じ場所**になり、**スナップショットに載らない対戦の相手盤面でも同じに見える**。
  ⚠ここに速度や慣性を持たせると、対戦で相手側だけドローンが消える。
- ⭐**レールガンは「線上に一番多く乗る向き」を自動で狙う**(2026-07-26ユーザー指示)。貫通するので狙うべきは
  敵1体ではなく**並びの向き**。同数なら合計HPが多い並びを選ぶ。⚠候補×敵の総当たりなので、
  **0.1秒ごと(と撃つ直前)だけ引き直す**+向きの候補は24本まで。毎フレーム全部やると重い。
  検査は `test_headless.js` の **`checkTwFx()`**=絵の種類と `TW_SFX` の重複を見る。**新しいタワーを足す時もここに1行足す**。
  ⚠**数値(威力・射程・間隔・体数)には触っていない**。変えてよいのは絵と音だけ。
- ⭐**パネルの上に演出のcanvasを重ねる時は、見出し(✕ボタン)だけ上に出す**(2026-07-26 第71弾)。
  全面を覆うと**途中で閉じられなくなる**。`#md-gacha .bphd{position:relative;z-index:7}` の形。
- ⭐**canvasの寸法はモーダルを開いた瞬間には確定していない**(文字や画像の読み込みで動く)。
  1回測って持つと**円が楕円に潰れる**。毎フレーム `clientWidth/Height` と見比べて、変わっていたら測り直すこと。
- ⭐**canvasは「中の寸法(width/height属性)」と「CSSの見た目の寸法」を必ず合わせる**(2026-07-27に踏んだ)。
  食い違うとブラウザが引き伸ばすので、**絵が横に伸びて縦に潰れる**。研究所のカード(`.pvc`)が
  「CSSは 幅いっぱい×52px、中身は120×104」で、派生タブのキャラが全部つぶれて見えていた
  (ユーザー指摘)。⚠**`fitDraw` は中の寸法に綺麗に収めるので、潰れていたら原因は必ずこの食い違いの方**。
  直し方=差し込んだ後に `clientWidth/clientHeight`(×DPR)を測って `cvv.width/height` に入れてから描く。
  ⚠**開いた直後は clientWidth が0のことがある**ので、`requestAnimationFrame` で**次のフレームにもう一度**描く。
  ⚠他の canvas(デッキ104×78→56×42・建設メニュー92×80→46×40・図鑑128→64)は縦横比が合っているので無事だった。
  **canvasを足す時は必ず属性とCSSの縦横比を突き合わせること**。
- ⚠⚠⭐**連射する兵科の演出は「1発ぶん」で決めてはいけない**(2026-08-01ユーザー指摘
  「短機関銃兵の攻撃時のフラッシュがキャラが見えなくなるレベル」)。1秒に3〜6発撃つので、
  **1発ずつは小さくても絶えず重なって体が光に埋まる**。⭐**間引く(2発に1回)+小さく+薄く**が正解。
  ⚠銃口の閃光(mzl)に **al**(濃さの上限)と **core**(白い芯の大きさ)を足してある=連射の型だけ下げられる。
  ⚠同じ理屈は**タワーの連射(火炎放射・レーザー)で既に踏んでいる**(そちらは fx をやめて塔の描画に寄せた)。
- ⭐**閃光は「白を先に敷いてから色を重ねる」**。逆にすると全部混ざってセピア色の日章旗になり、弾けた感じが出ない。
- ⭐**絵文字は `str[0]` や `/^\S/` で切ってはいけない**(2文字ぶんのコード単位でできている)。`Array.from(str)[0]` を使う。
- ⭐**書き込んだファイルに見えない制御文字が紛れることがある**(2026-07-26に再発)。
  症状は「**どう見ても正しいコメント行で `SyntaxError: Unexpected identifier`**」のような不可解な構文エラー。
  疑ったら `grep -P '[\x00-\x08\x0b\x0c\x0e-\x1f]' <file>` で走査する(過去に `\b`=0x08 と `\f`=0x0C が混入した)。
  ⚠**その行を書き直せば直る**。原因を探して時間を溶かさないこと。
- ⭐**DOMを組み立てる処理は「中身を作る関数」と「差し込む関数」に分ける**(2026-07-26 第73弾)。
  ヘッドレスのDOMは**差し込んだ要素を数えられない**ので、DOM側だけの検査は
  **0個でも通ってしまう=何も見ていないのにOKと出る**。`gcResRows()` のように
  **DOMを触らないデータの関数**を用意して、そちらを検査すること。
- ⭐**演出の仕組み(第64〜65弾で整えた・新しい絵を足す時はここに乗る)**:
  `addFx(C,種類,x,y,文字,{...})` で1つ足す。持ち時間は `FX_LIFE` の表、**1つずつ変えたい時は ex に `lf`**。
  **`d0` は「その秒数だけ待ってから始まる」**(描画の頭で `e.t<e.d0` なら飛ばす。中で使う経過時間は `e.t-e.d0`)。
  **`later(C,秒,関数)`** は着弾に合わせて演出を遅らせる待ち行列(campStepで進む)。
  ⚠`later` に**ダメージ計算を入れないこと**。当たり判定は今までどおり即時のままにして、見た目だけ並べ替える。
- ⭐**新しい演出の絵も「太い黒フチ+べた塗り」に合わせる**(2026-07-26)。氷の破片を白い線だけで描いたら、
  地面に刺さっているのではなく**宙に浮いた三角**に見えた。輪郭に `INK` を入れ、足元に色の付いた地面(凍結の白/焦げ跡)を敷くと収まる。
- ⭐**寿命が0.2秒しかない演出(炎など)は、透明度を線形に落とすと出た瞬間しか見えない**。
  `1-k*k` のように**終わり際で一気に消す**カーブにする。
- ⭐**火・煙は「丸を並べる」と炎に見えない**(2026-07-26)。根本が太く先が細い舌の形にすること。
  逆に**重ねすぎると画面が一面の光になる**(撮影で砲撃を連打したら画面が真っ白になった)。
- **canvasの画面は「窓」の意匠を1つ決めて全部そこに揃える**(2026-07-26 第55弾)。
  素の `fillRect`+`strokeRect` を並べると一気に安っぽくなる(実際にユーザーから指摘が出た)。
  冒険では `rgWin`(影+本体+外枠+内側の細線+四隅の鋲)に統一してある。
- **キャラの枠だけ出して姿を出さない画面は安っぽく見える**(2026-07-26)。戦闘は一番長く見る画面なので、
  ①キャラの姿 ②行動した側が一歩前に出る ③ダメージ数字が飛ぶ ④斬撃/爆発/回復の絵 — この4つは最低限入れる。
- **NPCは主人公と同じ大きさで描く**。小さくすると人に見えない(冒険の町で最初トカゲに見えた)。
- **見下ろしマップは「床を明るく・壁をほぼ黒く」描く**(2026-07-26)。明暗が近いと**暗い方が地面に見えて**
  どこが道か読めない(冒険のフィールドで実際にそうなった)。壁にはわずかにムラを乗せて「岩」に見せること。
- **canvasに並べる枠の幅は、固定値ではなく「入れ物の幅」から割り出す**(2026-07-26)。
  `Math.min(132,w*.155)` のような書き方だと、列数×幅がパネルからはみ出す(門のエリア一覧で6列目が出た)。
- **マップが画面より小さい時は中央に寄せる**。拠点の町(21マス=546px)をそのまま左寄せにしたら右半分が真っ黒になった。
- **マス目のゲームを作るなら「1マスの定員」と「同時に殴れる数」を必ず決める**(2026-07-26)。
  廃止した洞窟で決め忘れ、ゾンビ18体が1マスに重なって英雄を4秒で溶かした。TD側の`ENG_MAX`と同じ理屈。
- `test_undef.js` の `GLOBALS` に **TypedArray系(Uint8Array/Float64Array等)** を足してある。

### テストを書く時
- ⭐**`test_headless.js` の body はテンプレート文字列(バックティック)**。中のコメントに
  バックティックを書くと**文字列が途中で閉じて「どう見ても正しいコメント行で SyntaxError」**になる(2026-07-26に踏んだ)。
  コード片を引用したい時は「」で囲むか記号なしで書く。ファイル先頭にも注意書きを置いてある。
  ⚠**同じ理由でバックスラッシュも食われる**(2026-07-26 第84弾に踏んだ)。body の中に置く検査では
  **正規表現も `\n` のエスケープも使わない**(`Invalid or unexpected token` で落ちる)。
  行で分けたいなら `String.fromCharCode(10)`、パターンは `indexOf` で書く。
- ⭐**撮影ツールの「絵が重なって描かれる」に注意**(2026-07-27に踏んだ)。`pxcmp` の3段(実寸/3倍/6倍)を
  **窓の高さの割合**で置いていたため、6倍の絵(130px超)に3倍の絵が重なって描かれ、
  **「頭に瘤が乗った変な絵」**に見えた。それを絵の不具合だと思って**4回も直しに行った**。
  ⚠**並べて撮る道具は「絵の高さから下から積み上げて」置くこと**。割合で置くと絵の大きさを変えた途端に壊れる。
  ⚠**「絵が思っていたものと違う」時は、直しに行く前に道具の座標計算を疑う**(下の教訓と同じ形)。
- ⭐**「直したのに直っていない」と言われたら、まず"確認に使った経路"を疑う**(2026-07-26 第84弾)。
  同じ機能に入口が2つあると、片方だけ直して**確認画面/撮影ツールが嘘をつく**。
  実際に2回踏んだ: ①`?sfx=1` の確認画面だけ音量の表を通っていなかった
  ②`test_shot.js` の `t=` が効いていなかった(別のものを撮って満足していた)。
  **確認用の画面・ツールは本番とまったく同じ経路を通すこと**。通らないなら確認になっていない。
- ⭐**検査どうしがMETAを汚し合う**(2026-07-26)。前の検査が `META.hsel` を残していたせいで、
  次の検査で「英雄を連れていない状態」が作れず FAIL した。**前提にする値は検査の中で明示的に設定する**こと。
- `test_headless.js` が**建てる枠は AI_ORDER の先頭(初期解放ぶん)から選ぶ**。解放されていない枠に建てても無視される。
- 紹介モーダルはOK待ちで止まるので、`frames()` の中で `PAUSED && INTROQ` なら `introNext()` で自動送りする。
- ⭐**中断/再開は `checkResume()` が検査する**(2026-07-26)。`test_headless.js` に **localStorage のスタブ**を入れたので
  往復を自動で見られる。⚠それまで「保存系は検証されない」ままにしていたため、
  **`supN` の保存漏れ(再開すると建ててあった支援施設が消える)を長く見逃していた**。
  `newCamp()` に持ち物を足したら、**`saveRun`/`resumeRun` の両方**と `checkResume()` の期待値に必ず1行足すこと。
- ⭐**対戦のスナップショットは「送っている値」だけが相手に見える**(2026-07-26)。`tws` は
  `[ti, jam, own, us.d, us.r, us.g, us.c, us.f, chg, ang, spent]` の配列。
  ⚠`us.f`(冷却回数)・`chg`(レールガンの充填)・`ang`(砲身の向き)・`spent`(売却額)が**抜けていた**
  =クライアント側で強化Lvが0に見え、砲身が全部同じ向きで、売却額の表示が嘘になっていた。
  **タワーに状態を足したらここに足すか、「状態を持たない」形にする**(ドローンの `droneOff` が後者の例)。
- ⭐**`test_shot.js` の `t=` は数字も含むIDを渡せる**(2026-07-26に修正)。
  以前は `/t=([A-Za-z]+)/` だったため **`t=scrap2` が `scrap` として解釈**され、
  別のタワーを撮っているのに気づけなかった。また `t=` は `z=` と併用しないと**丸ごと無視されていた**(これも修正済み)。
  ⚠**撮れた絵が「思っていたものと違う」時は、まずオプションが本当に効いているかを疑う**。
- **ヘッドレスのNode環境には localStorage が無い**ので…と長く書いてあったが、上のとおりスタブを入れて検査できるようにした。
- **敵/部隊の画面座標(px/py)は campStep の中でしか入らない**。`mkZ`で並べただけの敵は px=py=0 なので、
  **距離で効く処理(範囲攻撃・必殺技)は空振りする**。テストでは並べたあと `campStep(me,.001,w)` を1回挟むこと。
- **画面のボタンからしか呼ばれない処理は普段のテストを素通りする**(必殺技で踏んだ)。`checkHero()`のように直接呼ぶ検査を足す。
- ⚠⚠⭐**経路の距離 `d` は「0が湧き口・PLENがコア」**(2026-08-01に実際に踏んだ)。
  つまり**敵の居る前方は `d` が減る向き**で、`u.d - k*歩幅` が正しい。
  ⚠**漆黒の突撃と紅蓮の柱が `q0+k*(PLEN-q0)/n` と書いてあり、敵の居ないコア側へ走っていた**。
  ⭐**なぜ気づけなかったか=1歩目(k=0)は自分の足元なので必ず当たり、検査の「何の効果も無い」を
    すり抜ける**。⚠**道に沿って何歩か進む技を書いたら、最後の1歩が敵側に届くかを必ず見ること**。
- ⚠**`later()` で遅らせた処理は `campStep` が回す**。検査で待つ時、**campStep を回すと敵も歩いてしまい**
  「効果が出たのか敵が動いただけか」を見分けられなくなる。⭐**待ち行列(`C.dly`)だけを直に進める**こと
  (campStep の中は try/catch なので**例外も握り潰される**=直に回した方が不具合が出る)。
  必殺技の前置きモーション(2026-08-01)で `checkHero()` をこの形にした。
- ヘッドレスChromeでは `(pointer:coarse)` が効かずPC寸法で描画される。`test_shot.js` はスマホ用CSSを強制適用して回避している。
- **一瞬しか出ない演出(電撃の連鎖など)はスクショに写らないことがある**。ヘッドレスの仮想時間は
  `setInterval` を仕掛けると進みが鈍り、撮影時に演出が消えている。写らない時は、
  ①`w` の値を変えて数回撮る ②`--dump-dom` で `C.fx` の中身(種類・太さ)を吐かせて数字で確かめる、の順で。
  ③最終確認は実機。**「写らない=壊れている」と早合点しない**(2026-07-25に重テスラでこれをやって時間を溶かした)。
- `test_shot.js` の `t=` は**建てる瞬間だけ `unlocked` を上げて戻す**。上げっぱなしにすると画面が止まる。
- ⭐**`test_shot.js` のオプション名に「w+数字」を入れてはいけない**(2026-07-26)。
  時間指定が `/w(\d+)/` の**最初の一致**を拾うので、`gtw2` のような名前を作ると
  **仮想時間が2msになって画面が何も出ない**(タイトルのまま撮れて、原因が分からず時間を溶かした)。
- ⭐**一瞬の演出は「実戦を撮る」より「並べて止めて撮る」方が速い**(2026-07-26)。`fxdemo` は新しい演出を
  1つずつ画面に並べ、**寿命の途中の姿に固定**して撮る。実戦の撮影(`u=`/`stk=`)で当たりを待つより確実。
- ⭐**ヘッドレスの `setInterval` は間隔が長いとほとんど回らない**(2026-07-26)。撮影用の仕込みを
  1300msにしたら砲撃が1発も撃てなかった。**90ms程度の短い間隔で回し、条件で間引く**こと。
  同じ理由で `--virtual-time-budget` を大きく(16000等)すると固まったように見えることがある(9000前後が無難)。
- ⭐**撮影用に敵を並べる時、`zombies[0]` を掴んではいけない**(2026-07-26)。倒したり漏れたりで入れ替わるので
  **足したばかりで px=0 の個体**を掴むことがあり、距離で効く処理が永久に空振りする。
  `px` が入っている個体を探して使うこと(上の「画面座標はcampStepでしか入らない」と同じ罠の別の顔)。

### 設計として決まっていること(蒸し返さない)
- **部隊の段階進化(Lv3/5/10で姿が変わる)は廃止済み**。部隊レベルは数値強化だけ(+10%/Lv)。
- **協力/対戦では編成(ロードアウト)は無効**。素の兵科・通常弾で統一する。
- 協力/対戦は**必ずステージ1**に戻す(地形の食い違いを防ぐため)。


## 🔊 音の確認(`?sfx=1`)
公開URLに **`?sfx=1`** を付けると、**効果音53種を一覧でタップして鳴らせる画面**が開く。
⚠**いちいちゲームを進めないと音が聴けない**のは確認が回らないので作った(2026-07-26ユーザー指摘)。
- タブは 🔫タワー / 🧟ゾンビ / 🖥UI・経済 / 🏁節目 の4つ。「▶このタブを順に鳴らす」で一気に確認できる
- 音量スライダーつき。🛠DEVモードならタイトルにも「🔊 音の確認」ボタンが出る
- 差し替えは `tool_sfx.js` のレシピ1行(素材・切り出し位置・長さ・音量・ピッチ)。`af:` を書けばEQも掛かる
- ⭐**音量の微調整は素材を焼き直さず `SFX_GAIN` の表でやる**(2026-07-26 第83弾)。
  キー→倍率の表で、`tool_sfx.js` を回さずに効く。**1.0が素材そのまま**。
  ⚠合成音(`SFXSYN`)側の値も同じ倍率で直すこと=素材が無い環境で釣り合いが崩れる。
  ⚠⭐**音を鳴らす入口は2つある**(`sfx.xxx()` と、確認画面の `sfxTry()`)。
  **両方とも必ず `SFX_GAIN` を通すこと**(2026-07-26 第84弾に踏んだ)。
  `sfxTry()` だけ素の音量で鳴らしていたため、**下げる直しを入れても確認画面では下がって聴こえず**、
  「ゲームを進めずに音を確かめる」というこの画面の存在意義が丸ごと無効になっていた。
  検査は `test_headless.js` の **`checkSfxGain()`**(`sfxPlay(` を呼ぶ行が全部 `SFX_GAIN` を渡しているか。
  表のキーが実在するか=打ち間違えると**黙って効かない**。1.0超が無いか)。
  ⚠**長く鳴る音は、RMSが同じでも大きく感じる**。ボスの唸り(1.1秒)は他(0.2〜0.6秒)と
  同じRMSでは釣り合わなかった。音量を揃える時は秒数も見ること。
  ⚠**BGMは `beep()` を直に呼ぶので音量スライダー(`SFXVOL`)も `SFX_GAIN` も通らない**。
  `bgmOn()` の中の数字がそのまま音量(通常/ボス/ナイトメアの3種ぶんある)。
- ⭐**「音量を下げて」と言われたら、まず測る。一律に下げない**(2026-07-26ユーザー指示で実施)。
  ゾンビの声を下げた時に53種のRMSを測ったら、本当の問題は「全部大きい」ではなく
  **①ボス登場の咆哮 `growl` だけが53種で一番大きかった**(0.441・平均0.150の3倍)
  **②同じ役割の変種どうしが揃っていなかった**(`growl` 0.441 と `growl2` 0.191=2.3倍差)の2つだった。
  ⭐**くじで引く音(`SFX_SET`)は必ず音量を揃える**。揃っていないと
  「同じ場面なのに鳴るたびに大きさが変わる」という原因の分かりにくい違和感になる。
  一律に下げていたら、元から小さい声が消えるだけで突出は残っていた。
  測る道具は scratchpad の `meas.js`、検査は **`test_sfx.js`**(`SFX_GAIN` を掛けたあとのRMSで
  ①ゾンビの声が非ゾンビの最大を超えていないか ②うめきが他の平均より小さいか ③変種どうしが離れていないか)。
  **声を足したら必ず測ってから `SFX_GAIN` に1行足す**
- ⭐**「重い音」を作るなら 150〜300Hz を上げる。120Hz以下ではない**(2026-07-26 重テスラで踏んだ)。
  **スマホのスピーカーは120Hz以下をほぼ鳴らせない**ので、そこに置いた低音は聴こえないまま音量だけ食う。
  超低域は `highpass` で削り、空いた分を150〜300Hzに回すと**同じ音量でずっと重く聴こえる**。
  ⚠耳で確かめられないので、帯域ごとの音量と前半/中盤/後半の残り方を**数字で見て**判断すること
- ⚠**生WAVは作ったあと消してある**。作り直す時は Downloads の5つのzipから**必要なファイルだけ**
  取り出して `sfx_src/` に置き、`node tool_sfx.js build sfx_src <キー>,<キー>` で**そのキーだけ**焼く
  (素材が無い他のキーを巻き込まない。`sfx_out/` の既存mp3はそのまま残るので `embed` で全部入る)
- 撮影は `node test_shot.js out.png 852 393 sfx`


## 🛠 実機テスト用のDEVモード(`?dev=1`)
公開URLに **`?dev=1`** を付けて起動すると、実機での動作確認がすぐできる状態になる
(例: `https://lukear185.github.io/deadtide/?dev=1`)。
- 🧬研究pt・💎魔石・🔧鍛錬素材が**実質無限**(使ったそばから補充=`devTop()`。`saveMeta()`が通るたびに戻る)
- ゲーム中の⚙️スクラップ・🔩強化ptも99,999を切ったら補充(HUDの桁が溢れないよう99,999止まり)
- **全ステージ・全難易度・全英雄が最初から選べる**(`diffOK`/`stageOK`/`renderHeroSeg`)
- ⭐**タワー20種・兵科32種・建設マス全部(通常22+工房3+支援2)・砲撃5種が最初から使える**
  (2026-07-26ユーザー指示=毎回⚙️を貯めて解放していては実機確認が回らないため)。
  ⚠仕込みは3か所ある。**片方だけ直すと元に戻る**ので必ずセットで見ること:
  ①`metaTowerCap()`/`metaUnitCap()` が DEV では全種類を返す
  ②`newCamp()` の `unlocked`/`uUn`/`ecoN`/`supN`/`slk` の初期値
  ③`startSolo()` の「開始時ボーナス」(`me.uUn=` と `me.slk=`)と `resumeRun()` の復元 — ここで巻き戻さない
  ⚠研究所側の「新しい種類」(`META.nt`/`META.nu`)と砲撃(`META.st`)は**起動の最後**で開けている。
    `devTop()`(METAを読んだ直後)でやると `T_PLAY` などのconstがTDZで落ちる。
  検査は `test_headless.js` の `checkDevLoad()`。撮影は `node test_shot.js out.png 852 393 dev`。
- **セーブは別枠**(`dt_meta_dev` / `dt_run_dev`)。⚠普通に遊ぶ時のデータは汚れないので、
  ここで買った研究所の解放や引いた英雄は**通常モードには持ち越されない**
- タイトル右上の版数が `v2.0 β 🛠DEV` になる+ゲーム開始時にトーストが出る(付け忘れ/外し忘れ防止)
- ⭐**✨黄金のロブスターが50%で出る**(通常は0.1%)。`lbRate()` が DEV を見て切り替える。
  0.1%のままでは実機で見て確かめられないため(2026-07-28ユーザー指示)


## ❌Blenderで焼くのを試した時の記録(2026-07-27 第99弾・不採用)
CLAUDE.md には結論だけ置いてある。手順と落とし穴はこちら。
- 焼き方: `blender --background --python bl_make.py -- <出力先>`(スクリプトは scratchpad に残した)。
  トゥーンシェーダ(Diffuse→ShaderToRGB→ColorRamp定数2段→Emission)+Freestyleの黒フチ+
  正射影・film_transparent。色はゲームと同じ `ZW` の値を sRGB→リニアに変換して渡す。
- ⚠**カメラはヨー45度に置く**。正面寄りに置くと「画面の右=キャラの前方」になり、
  **左右の脚が前後に重なって1本に見える**。
- ⚠`primitive_cube_add(size=1)` は1辺1なので `o.scale=寸法`。半分にすると全部隙間だらけになる。
- ⚠腕や脚は**関節から角度と長さで置く**。中心座標を手で書くと必ず宙に浮く。
- ⚠実機で要る解像度は128px前後(34〜71px×DPR2)。1枚12〜16KBで、ゾンビ36種でも約1.9MB。
  512pxで焼いて「数十MB」と見積もるのは間違い。

---

## 2026-07-30(第105弾)に踏んだこと

### ⚠⚠**1つのオブジェクトに同じキーを2度書かない**(対戦の2人目が壊れた)
対戦のスナップショットで **砲撃強化Lv と スロット解放の文字列がどちらも `sk`** だった。
後から書いた方が勝つので、クライアントの「砲撃強化 Lv」に `"0100010…"` が入って画面に出ていた
(ユーザー報告「2人目の準備タイムの画面がおかしい、0100010みたいになってた」)。
⭐**キーを足す時は同じオブジェクトの中を目で全部なめる**。`node --check` も `test_undef.js` も捕まえない。

### ⚠⚠**「もう効かない強化」を売らない**(レーザー塔の🔥昇温速度)
`20*(1-.13*段)` の**引き算**だったので、進化後(最大10段)は下限3に張り付いて
**8段目から先が1秒も縮まない死んだ強化**だった。⭐**掛け算にする**と必ず縮む。
⚠**丸めない**(`Math.round` を挟むと同じ値が並んで、やはり死んだ段ができる)。

### ⚠⚠**canvasの線の太さは世界座標**(SC≈0.39倍で縮む)
盤面の描画は `SC` で縮めて描くので、**`lineWidth=2` は実機で1px以下**になって消える。
攻撃の溜めの輪を2.2で引いたら**撮っても何も見えなかった**。盤面に引く線は **5〜9** を目安にする。
粒(`arc`の半径)も同じ=1.6pxは見えない。**2.5〜3倍**にすること。

### ✅**向きは `pathFace`/`uFace` で決める**(2026-08-01に直した)
以前は `u.dr`/`z.dr` を**その場の経路の接線だけ**(`Math.cos(角度)>=-.05`)で決めていた。
⚠⚠**道が真縦の所では cos がほぼ0になり、分岐がいつも同じ側(左)を選ぶ**ので、
**敵が右上にいるのに部隊が全員左を向いて撃っていた**(2026-08-01ユーザー指摘
「攻撃するときに左向いて攻撃するの全キャラやめて。しっかり敵の方見よう」)。
⭐直し方=**少し先の道の点(90→200→380)まで見に行って左右を決める**(`pathFace`)。
 部隊はさらに**狙っている敵の横位置(`u.tgx`)を最優先**にする(`uFace`)。
⚠**真上・真下の敵では敵の位置を使わない**(横の差が6px以下)=左右にちらつく。
⚠**相手の盤面(`clientCamp`)にも同じ決め方を入れること**=片方だけ直すと対戦で向きが食い違う。
⚠**演出の左右は今までどおり狙っている座標から出してよい**(`drawUChg` の `fx9`)。
⚠⚠**先を見に行く段は「道の一番長い直線」より長くすること**(同日にもう一度踏んだ
 =ユーザー指摘「一番長い通路だけ左向いちゃう」)。ステージ1の右端は**505続く真縦の道**で、
 90/200/380 では最後まで縦のまま=左右が決まらず古い分岐(左)に落ちていた。いまは 700/1200 まで見る。
⭐**確かめ方=経路を25刻みで舐めて `pathFace` の答えを出し、少し手前(d-40)の x と食い違う点を数える**
 (scratchpad に小さなnodeスクリプトを書けば数秒。ステージ1もステージ2も食い違い0を確認した)。

### ⚠**乱数が絡む演出を検査する時は「例外の枝」を先に切り分ける**
💎召集に**どんでん返し**(★4以上でわざと一番しょぼい見せ方をする)を足したら、
「★5ならレーザー」を見ていた検査が**数回に1回だけ落ちる**ようになった。
⭐乱数の枝を足したら、**その枝を先に `continue` で抜く**形に検査を書き換える。

### ⚠**git stash / checkout で改行コードが CRLF に化ける**
`core.autocrlf` が効いているので、stash を挟むと作業中の index.html が CRLF になる。
**複数行のアンカー置換が全部「見つからない」で落ちる**。落ちたら改行コードを疑い、LFに戻してから作業する。

### ⚠⚠**「DPSが同じ」は「強さが同じ」ではない**(攻撃の溜めで踏んだ)
溜めを入れて1発の威力で戻せば**紙の上のDPSは同じ**になるが、実戦では弱くなる。理由は2つ:
①**オーバーキル**=雑魚1体に必要以上の威力が乗ってこぼれる ②**撃つ回数が減る**=同時に相手できる敵の数が減る。
ミニガン(間隔0.16秒)に0.5秒の溜めを付けたら1発4.1倍・回数1/4の別物になり、
ユーザーから「数が多いときに押されやすくなってる」と報告が来た。
⭐**溜めは「その兵科の攻撃間隔」に比例させて頭打ちにする**(`CHG_CAP=0.6`)。速い兵科ほど溜めは短く。
### ⚠**「持続DPSを戻した」だけでは接敵の瞬間が戻らない**
溜めがあると**最初の1発が必ず遅れる**ので、波がぶつかった瞬間だけDPSが落ちて押し込まれる。
⭐**歩いている間に溜めておく**(絵は出さない)と、ぶつかった瞬間に撃てて元の手応えに戻る。

### ⚠**「珍しいもの」を足したら、同じ回に 🛠DEV の底上げも足す**(2026-07-30ユーザー指示)
「見て欲しいやつの確率は上げて欲しい。テストするときは毎回そうして」。
★4以上は素で約1.1%しかないので、どんでん返しも黄金のロブスターも**実機で一度も見られないまま公開**になる。
⭐底上げの一覧は下の「🛠 実機テストのための底上げ」。
検査 `checkDevLoad()` が見張っているので、消すと落ちる。

### ⚠⚠**`git checkout -- .` は「戻したいファイル」以外も巻き添えにする**(2026-07-30に実際に踏んだ)
Obsidianでフォルダを作ってファイルが移動された時、`git checkout -- .` で戻したら
**同じ回に書いていた未コミットの変更(index.html / test_headless.js)まで消えた**。
⭐**戻す前に `git stash` するか、`git checkout -- <戻したいパスだけ>` にすること**。
⚠git管理外(.gitignore)のフォルダは checkout では戻らない=**手で mv して戻す**(px_src / sfx_src / ui_src の画像)。


---

## 🛠 確認用の道具(2026-07-30に CLAUDE.md から移した)

> ⚠CLAUDE.md は毎回まるごと読まれるので、**撮る時にしか要らない物はここに置く**。

### 撮影(test_shot.js)のオプション一覧
必要な時に流すもの:
```
node test_balance.js             # 兵科の強さが解放順に上がっているか(数値を触ったら必ず)
node test_layout.js              # マス/道路/死潮ゲージがHUDや操作バーと重なっていないか(寸法を触ったら必ず)
node test_shot.js out.png 852 393        # 実画面のスクショ(スマホ用CSSを強制適用)
node test_shot.js out.png 852 393 st2    # ステージ2を撮る / pc=PC表示 / w30000=時間を進める
node test_shot.js out.png 852 393 vs     # 対戦(空き枠はCPU)を撮る
node test_shot.js out.png 852 393 "nmboss+z=fBeast,nmHorr"   # ナイトメアで、指定した敵だけ経路に並べて撮る
node test_shot.js out.png 852 393 "st2+nmboss+noitr+z=dWhal,fLevi" # ⚠🌑深海のナイトメアは **st2 も付ける**
                                                #   (nmboss だけだとステージ1のまま=獣プールになる)
                                                # ⭐noitr =「新種のゾンビが現れる!」の紹介モーダルを出さない
                                                #   ⚠新しい敵の絵を撮る時は必ず付ける(画面を覆って邪魔になる)
node test_shot.js out.png 852 393 u=grn          # その兵科を3体出して撃たせ続ける(攻撃の演出用。+die=倒れるまで放置)
node test_shot.js out.png 852 393 stk=napalm     # その砲撃の着弾を撮る(air/mgun/carpet/frost/napalm)
node test_shot.js out.png 852 393 fxdemo         # 新しい演出を1つずつ並べて、寿命の途中で止めて撮る
node test_shot.js out.png 852 393 dev            # 🛠DEVモード(全開放)の画面を撮る
node test_shot.js out.png 1400 800 grid=tw       # 素のタワーを並べる(grid=tg で進化先だけ)
node test_shot.js out.png 852 393 t=fort2        # 進化先のタワーを実戦で撮る(素を建てて自動で進化させる)
node test_shot.js out.png 852 393 iv             # 作戦タイム(準備画面)の強化カード
node test_shot.js out.png 852 393 "iv=14"        # ⭐そのウェーブの作戦タイム(次の波の性格の行を見る。W5以降)
node test_shot.js out.png 852 393 bmenu          # 🔨建設メニュー(タワーを全部解放した状態=一番縦に長い)
node test_shot.js out.png 852 393 re2            # 一度タイトルへ戻ってから2回目の出撃(2回目でしか出ない不具合用)
node test_shot.js out.png 852 393 "tut=3+tutbm"  # 🎓チュートリアル(数字=進める段数。tutbm=塔を建てて強化ウィンドウも開く)
node test_shot.js out.png 852 393 hero=all        # 英雄18人を経路に並べて撮る(見た目の確認)
node test_shot.js out.png 852 393 hero=hNox       # その英雄を出撃させて撮る(+nodep=出撃前のボタンだけ見る)
node test_shot.js out.png 852 393 "title+opt"    # ⚙オプション(音量バー+演出の強さ)
node test_shot.js out.png 852 393 "title+lob"    # ✨黄金のロブスター(0.05%・5発・💎50) / rbz=🌈虹のゾンビ
node test_shot.js out.png 852 393 "title+bus"    # 🚌ゾンビバス(0.001%・8発・💎250)。+busn=2 で残りHPを指定
                                                #   ⚠バスが居る間だけ行進canvasが高くなる=上が切れていないか見る
node test_shot.js out.png 852 393 title          # タイトル画面 / lab lab=twup lab=unup 記録は lab=rec
node test_shot.js out.png 852 393 "lab+tmtip"    # 🎒編成のはなし(初めて兵科を解放した時に1回だけ出る案内)
node test_shot.js out.png 852 393 load           # 🎖編成
node test_shot.js out.png 852 393 "gacha5+gbang" # 💎召集(gacha=撃て! / +gfly=弾が飛ぶ / +gbang=木っ端みじん+示唆 /
                                                #   +gcard=結果カード / +gres=結果ウィンドウ / gacha5=★5 / +gt0/1/2=タレットの段)
node test_shot.js out.png 852 393 "gacha+gopen"   # 💎召集画面(引く前・目玉の英雄)。+gdex=📖図鑑 / +grate=確率
node test_shot.js out.png 852 393 "gacha+gopen+gowned" # 目玉が下のレア度に降りた時の絵(★5まで持っている状態)
node test_shot.js out.png 852 393 trhome         # 🏋鍛錬所(既定=⚔冒険タブ。trzoo=図鑑 / trhero=🔧鍛える)
node test_shot.js out.png 852 393 rpg            # ⚔冒険の拠点の町 / rpgf=エリア / rpgb=戦闘 / rpgg=門 / rpgs=つよさ
node test_shot.js out.png 852 393 "u=snp+uchg=0.8"  # ⭐溜めの途中で止めて撮る(攻撃モーション用)
node test_shot.js out.png 852 393 hstat          # 🦸英雄ステータス(hs=hNox でその1人を選んだ状態)
node test_shot.js out.png 852 393 "gacha5+glob+gaim"   # ✨黄金のロブスターが的(★4以上で1/3)
node test_shot.js out.png 852 393 "gacha5+gtwist+ggold" # どんでん返し(跡から金色の個体が現れる所)
node test_shot.js out.png 852 393 "gacha5+gtwist+gtwgib" # どんでん返しの手前(まず普通に爆散する所)
node test_shot.js out.png 852 393 "gacha5+gmorn+gfc=0"  # 朝の背景 / gnight=夜 / gfc=0白 1赤 2虹
node test_shot.js out.png 852 393 "gacha+gopen+gspin=1" # 召集画面(何人目を正面にするか)
node test_shot.js out.png 852 393 "arena=bat:walk+arn=16" # ⭐🧪検証場(一本道)。arn=進める秒数
                                                #   ⭐**+arnh** で上の帯を🦸英雄側にして撮る(2026-08-01)
                                                #   ⚠帯は左端の🧟/🦸で中身を切り替える=英雄を敵46種の
                                                #     後ろに並べると右へスクロールしないと届かない(実際に踏んだ)
                                                #   ⭐検証場では英雄の必殺チャージが常に満タン=前置きを何度でも見比べられる
  # ⚠検証場は**押した時だけ出る**(2026-07-30に自動湧きを廃止)=撮影側が味方3体と敵を出している(`tstPut9`)
                                                #   ⚠**ヘッドレスの仮想時間では rAF がほとんど回らない**ので、
                                                #     撮る前に `tstStep` を直に回して時間を進めている。
                                                #     これをしないと「湧かない・動かない」に見えて原因を探し回る(実際に踏んだ)
node test_shot.js out.png 852 393 "bns+bnsn=9"    # ⭐🚌ボーナス面「バスの日」(st2 で2面目)。
                                                #   ⚠⚠**bnsn=何秒ぶん自分で進めるか**を必ず付ける
                                                #     (仮想時間では rAF が回らず、敵が1体も湧かない絵になる)
                                                #   ⭐dbg = 盤面の状態をトーストで出す(中が止まっている時の切り分け)
node test_shot.js out.png 852 393 "hero=hNox+ult=0.9" # ⭐**必殺技の詠唱を盤面ごと撮る**(2026-08-02)。
                                                #   数字=詠唱の進み(0〜1)。0.9=溜め終わりの一拍
                                                #   ⚠**足元の紋章・照準線・★4以上の専用の溜めは盤面でしか出ない**
                                                #     (pose= の並べ撮りでは体の動きしか見えない)
node test_shot.js out.png 1900 900 "pose=u:hNox+poseult" # ⭐**詠唱モーションを5コマ並べる**(2026-08-02)。
                                                #   押した直後→構え→詰め→**一拍のタメ**→発射の抜け
                                                #   ⚠★5は絵が大きいので窓を 1900x900 くらいにする(x6の段が切れる)
node test_shot.js out.png 1200 740 "pose=z:walk"  # ⭐⭐**1体を大写しにして絵を磨く**(2026-07-30)。
                                                #   pose=u:bat で兵科。posechg=0.8 で溜めの姿。
                                                #   上=x6(暗い地面)/中=x3(明るい地面)/下=実寸x1(紙)を1枚に並べる
                                                #   ⚠**明るい地面でも確かめる**(金色は夜では見えて朝で消えた)
                                                #   ⚠オプション名に t= u= z= w数字 を含めない(他の指定に食われる)
node tool_slice.js ui_src/sheet-a.png --list     # 1枚のシート画像からアイコンを自動で切り出す(まず数と並びを確認)
node tool_slice.js ui_src/sheet-a.png ic-scrap,ic-up,ic-res,ic-gem,ic-mat   # 名前を付けて書き出す
```
合格基準: 試合が over=true / シェア合計100±2 / FAILが出ない。
※ ヘッドレスはDOM/Canvasをスタブしているため見た目は検証できない。**見た目は`test_shot.js`で撮って目視**し、最終確認は永井さんが実機で行う(画面下部に赤いエラー帯が出る仕組みあり=window.onerror)。

### 🛠 実機テストのための底上げ(DEVモード) ※2026-07-30ユーザー指示
⭐**「見て欲しいやつの確率は上げて欲しい。テストするときは毎回そうして」**。
⚠⚠**新しく「珍しいもの」を足したら、必ず同じ回に `?dev=1` の底上げも足すこと**。
素の確率のままでは実機で一度も見られず、**確かめずに公開する**ことになる。

| もの | 本番 | 🛠DEV |
|---|---|---|
| 💎召集の★4以上 | 1.1% | **約31%**(`RK_EACH_DEV`) |
| 召集の演出の内訳 | ✨金20/どんでん返し35/通常45 | **均等(1/3ずつ)**(`GC_P_LOB`/`GC_P_TW`) |
| タイトルの✨黄金のロブスター | 0.1% | **50%**(`lbRate()`) |
| タイトルの🌈虹のゾンビ | 1% | **50%**(`rbRate()`) |
| タイトルの🚌ゾンビバス | 0.001% | **50%**(`busRate()`。実際は3種を均等33%ずつ) |
| タワー/兵科/砲撃/マス/進化 | 解放制 | **最初から全部**(`devTop()` ほか) |

⚠**2026-08-01から DEV でも「連れて行くのは10体」**(編成制)。持っているのは30種だが、
デッキに並ぶのは編成で選んだ10体だけ。**別の兵科を試したい時は🎖編成で選び直す**。
⚠DEVで全30体をデッキに出す作りに戻さないこと=編成画面が「30/10」と壊れて出る(実際に踏んだ)。

⚠**普通に遊ぶ側の確率は1ミリも変えない**。DEVはセーブも別枠(`dt_meta_dev`)。
⚠確率の画面(💎召集→確率)にはDEVの時だけ「本番はこの数字ではない」と出る。
⭐検査 `checkDevLoad()` がこの底上げを見張っている(消すと落ちる)。

### 🎨 見た目で効くと分かっている3つ(2026-07-27に判明)
1. **大きさ**(第95弾): ゾンビ22.7px→**34px**。`CH_UP`(1.5)と`CH_P`(.72)で、小さいものほど効く累乗。
   ⚠これが「絵が安っぽい」の本当の原因だった。**演出の高さも同じだけ持ち上げる**(`*CH_UP`)。
2. **色の区分け**(第96弾): 肌 / シャツ / ズボン / 靴 の4色に分ける。1色+明暗1段では何をしても塊に見える。
3. **ハリボテ感の正体**(第97弾): ①同じ部品を等間隔・左右対称に並べる ②直線の台形 ③一直線の腕
   ④棘を胴の上から描く ——この4つ。
